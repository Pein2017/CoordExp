# Task 5 failure-mode matrix — bounded CPU correction

| invariant / boundary | executable owner | minimal counterexample | closing evidence | admitted counters / stop rule |
| --- | --- | --- | --- | --- |
| Every non-model forward input is non-trainable before acquisition | `HFSharedSurfaceSession.pre_acquisition_graph_input_attestation` via `ExistingOwnersProductionBackend.pre_acquisition_admission` | injected pixel/input tensor with `requires_grad=True`; injected sentinel attempts `sample_group` | content-addressed typed input-attestation receipt; sentinel requires all model tensors on CPU, receives no live session, and is bracketed by unchanged model/session action snapshots | successful sentinel count is separate from K16; attempted sample/replay/model action rejects before admission |
| Every replay graph leaf is an exact registered trainable Parameter of the same model object | shared graph-owner attribution helper called by `HFSharedSurfaceSession.replay_group` and `CudaHFVerticalAdapter` | same-model graph (positive); foreign Parameter placed after more than eight unregistered input leaves | graph-owner receipt records all-leaf disposition/full count before retaining eight value-free details and an explicit truncation flag | strict reject; never filter foreign/unknown leaves or let the detail bound change disposition |
| Replay tensor and graph-owner receipt remain one content-addressed object pair | `CudaHFVerticalAdapter._validate_task2_lineage` and replay-source revalidation | stale/rebuilt graph, detached/no-grad tensor, wrong model object, receipt/tensor swap, or graph-evidence journal failure | expected-versus-observed receipt hashes plus bounded foreign-leaf details; publication failure is a secondary note on the original typed adapter error | reject before backward; zero optimizer steps; never replace the primary graph failure |
| CUDA identity resolves only under the frozen world-one/current-index/visibility contract | `revalidate_human13_adamw_runtime_ownership` | explicit index conflict, multi-device trainables, unavailable current index, or ambiguous visibility | `Human13CudaLogicalDeviceIdentity` hash with raw accelerator, parameter, current-index, process, and visibility fields, or typed `Human13LiveModelError` | unchanged; reject before K16 |
| A physical loader is counted only after handle publication | `ProductionOneImageServices.open_training` / `open_audit` | checkpoint or placement fails before handle publication | append-only `ActionAttemptReceipt` and failed phase with exception hash and bounded redacted output | `model_loads=0`, `gpu_allocations=0`; no close claim; preserve primary failure |
| A handle is not exposed before its phase is durable | `ProductionOneImageServices.open_training` / `open_audit` | journal write fails after backend returns a handle | primary phase error plus best-effort close note; no caller-visible handle | admitted counter remains zero; close unpublished handle exactly once |
| Evaluator attempts are distinct from admitted audit opens | `ProductionOneImageServices.source_audit` / `proposal_audit` | evaluator/loader raises during source or proposal audit | failed evaluator attempt with exception/provenance; existing session remains separately closable | admitted open count unchanged; rollback/close by lifecycle |
| Terminal action lineage exactly matches the phase ledger | `ProductionOneImageServices.persist_terminal` | service attempt ledger differs from terminal action list | terminal publication rejects before exclusive write | unchanged; retain phase evidence and primary error |
| Historical schemas are immutable | explicit receipt/terminal schema dispatch | legacy v1 terminal lacks attempt or graph-owner fields | original v1 payload and content hash round-trip without inferred fields or rewriting; new graph receipts use their own schema | historical counters never inferred; immutable reload only |

The matrix is a production-admission correction record, not algorithm evidence.
No GPU/model/K16/retry/update/checkpoint action is authorized by these rows.
