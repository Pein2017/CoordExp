# User-provided GPT-5.6 Standard API price snapshot (2026-08-06).
# These rates are an estimation input, not an invoice export.
# The screenshot lists cache-write as a separate billing dimension, so this
# file keeps cache_write_is_in_input=false. Reasoning is treated as output.

[[rates]]
provider = "openai"
model = "gpt-5.6-sol"
currency = "USD"
input_per_million = 5.0
cached_input_per_million = 0.50
cache_write_per_million = 6.25
output_per_million = 30.0
cache_write_is_in_input = false
reasoning_is_in_output = true
source = "User-provided screenshot: GPT-5.6 API token price (Standard API), 2026-08-06"

[[rates]]
provider = "openai"
model = "gpt-5.6-terra"
currency = "USD"
input_per_million = 2.0
cached_input_per_million = 0.20
cache_write_per_million = 2.50
output_per_million = 12.0
cache_write_is_in_input = false
reasoning_is_in_output = true
source = "User-provided screenshot: GPT-5.6 API token price (Standard API), 2026-08-06"

[[rates]]
provider = "openai"
model = "gpt-5.6-luna"
currency = "USD"
input_per_million = 0.20
cached_input_per_million = 0.02
cache_write_per_million = 0.25
output_per_million = 1.20
cache_write_is_in_input = false
reasoning_is_in_output = true
source = "User-provided screenshot: GPT-5.6 API token price (Standard API), 2026-08-06"
