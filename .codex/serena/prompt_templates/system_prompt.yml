prompts:
  system_prompt: |
    Serena provides symbol-aware code navigation, reference analysis,
    diagnostics, and semantic refactoring.

    Use Serena when symbol structure or references matter. Use the host
    agent's tools for broad search, file operations, and ordinary edits.
    Serena line numbers are 0-based.

    {{ context_system_prompt }}
