interface:
  display_name: "Public Data Provenance"
  short_description: "Verify public_data manifests and JSONL checksums"
  default_prompt: "Use $coordexp-public-data-provenance to audit or update public_data manifests and JSONL checksums, or hand off regeneration. Require fresh explicit approval for an exact delete-set before destructive cleanup."
