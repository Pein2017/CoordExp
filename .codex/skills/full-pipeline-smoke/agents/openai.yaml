interface:
  display_name: "Full Pipeline Smoke"
  short_description: "Production-like multi-stage CoordExp smoke"
  default_prompt: "Use $full-pipeline-smoke to validate a change through a production-like data, train-or-rollout, checkpoint, inference, evaluation, and artifact smoke."
