interface:
  display_name: "Audit Review"
  short_description: "Audit a change, run, claim, or fixed diff"
  default_prompt: "Use $audit-review to return a read-only evidence-backed decision on this change, run, claim, or fixed diff."
