interface:
  display_name: "Audit Review"
  short_description: "Audit contracts or review a fixed diff"
  default_prompt: "Use $audit-review for a read-only CoordExp audit or fixed-point diff/code review, separate engineering from intent judgments, and compare research-meaning changes with originating intent before assigning fix."
