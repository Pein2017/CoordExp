interface:
  display_name: "Swift Detection Visualization"
  short_description: "Render current scored GT/pred review images"
  default_prompt: "Use $detection-gt-vs-pred-visualization to render current CoordExp-Swift raw-plus-scored artifacts with scripts/visualize_detection.py or src.vis. Preserve row and GT identity, pixel-space handling, one PNG per sample, manifests, and the canonical green/yellow/red/purple semantics."
