interface:
  display_name: "Detection Visualization"
  short_description: "Canonical detection GT/pred visualization"
  default_prompt: "Use $detection-gt-vs-pred-visualization to render or modify CoordExp detection GT/pred visualizations through the shared canonical pipeline. Preserve raw/scored/guarded/proxy artifact identity, use resolved config/root-image provenance when present, render each sample as its own strict 1x2 GT-left/Pred-right PNG, and avoid contact sheets, packed multi-sample images, bespoke renderer-local schema, or denormalization logic."
