interface:
  display_name: "BaiduDisk Union Sync"
  short_description: "Append-only Baidu Netdisk union sync"
  default_prompt: "Use $baidudisk-union-sync when syncing large artifact trees across machines through Baidu Netdisk. Preserve append-only union semantics: add missing files, detect conflicts, never delete, and never overwrite automatically."
