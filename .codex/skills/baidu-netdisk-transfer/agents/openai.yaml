interface:
  display_name: "Baidu Netdisk Transfer"
  short_description: "BaiduPCS-Go transfer and union sync"
  default_prompt: "Use $baidu-netdisk-transfer for one-off BaiduPCS-Go transfer or append-only union sync. Default to non-overwrite behavior and require fresh explicit approval for any overwrite or deletion."
