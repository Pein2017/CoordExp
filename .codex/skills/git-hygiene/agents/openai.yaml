interface:
  display_name: "Git Hygiene"
  short_description: "Exact worktrees and scoped, token-safe git"
  default_prompt: "Use $git-hygiene to bind this task to the exact worktree and safely stage, commit, synchronize, publish, or retire its scoped changes."
