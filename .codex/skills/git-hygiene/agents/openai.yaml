interface:
  display_name: "Git Hygiene"
  short_description: "Scoped commits, sync, and token-safe git"
  default_prompt: "Use $git-hygiene to stage, commit, split dirty work, synchronize, or push while preserving unrelated edits and keeping credentials out of history and output."
