interface:
  display_name: "RTK Token Saver"
  short_description: "Use RTK for noisy shell output, not exact stdout"
  default_prompt: "Use $rtk-token-saver for noisy shell work such as broad search, docs reads, git output, tests, logs, and discovery. CoordExp Codex shells initialize the `ms` conda environment by default, so use plain `python`/`pytest`; bypass RTK for exact stdout, machine-readable output, delicate quoting, tiny commands, or Python symbol reasoning that should use Serena."

policy:
  allow_implicit_invocation: true
