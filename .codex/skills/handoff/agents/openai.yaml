interface:
  display_name: "Handoff"
  short_description: "Continuation or cross-machine state package"
  default_prompt: "Use $handoff when the user asks for a handoff, continuation prompt, another-agent brief, compact session summary, or cross-machine execution note. Return inline unless the user gives a path or asks for a durable handoff file."
