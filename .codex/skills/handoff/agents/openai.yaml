interface:
  display_name: "Handoff"
  short_description: "Continuation or cross-machine state package"
  default_prompt: "Use $handoff to produce a compact continuation brief. Return it inline unless a stable output path is explicitly provided or agreed; never use a temporary path for a durable or cross-machine handoff."
