interface:
  display_name: "Handoff"
  short_description: "Continuation or cross-machine state package"
  default_prompt: "Use $handoff for a continuation or transfer. Return it inline by default, treat it as a one-shot route to owning state rather than current authority, and use a repo-local handoff only for an actual transfer or user-named path."
