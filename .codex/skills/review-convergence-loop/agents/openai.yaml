interface:
  display_name: "Manual Review Convergence"
  short_description: "Manually run an important review loop"
  default_prompt: "Use $review-convergence-loop to run this decision-important review gate to a bounded stop state."

policy:
  allow_implicit_invocation: false
