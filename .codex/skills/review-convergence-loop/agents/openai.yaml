interface:
  display_name: "Review Convergence Loop"
  short_description: "Review, decide, revise, and stop"
  default_prompt: "Use $review-convergence-loop to iterate a CoordExp task through work, independent review lanes, a fix/narrow/drop/probe decision turn, bounded revision, and an explicit stop state."
