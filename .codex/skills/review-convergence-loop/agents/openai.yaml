interface:
  display_name: "Review Convergence Loop"
  short_description: "Explicit review-revision convergence loop"
  default_prompt: "Use $review-convergence-loop when I explicitly ask to iterate a CoordExp task through work, review or audit lanes, revision, and an approval or hold gate."
