interface:
  display_name: "Workflow Self-Distillation"
  short_description: "Mine recent work for reusable workflows"
  default_prompt: "Use $workflow-self-distillation to mine recent work for recurring workflows, check provenance and existing coverage, then recommend the smallest durable asset or a deliberate skip."
