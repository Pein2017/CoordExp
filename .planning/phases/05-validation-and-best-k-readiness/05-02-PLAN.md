---
phase: 05-validation-and-best-k-readiness
plan: 02
type: execute
wave: 1
depends_on:
  - "05-01"
files_modified:
  - configs/stage2_two_channel/smoke/b_majority_coco1024_pseudo_positive_4steps.yaml
  - temp/
autonomous: true
requirements:
  - VAL-02
user_setup: []
must_haves:
  truths:
    - The enabled pseudo-positive smoke must run through the normal `src.sft` entrypoint.
    - The smoke must exercise `1 + (K-1)` rollout execution, support-rate accounting, and coord-only pseudo-positive supervision without introducing a second teacher-forced forward.
  artifacts:
    - Runtime logs or temp artifacts proving the smoke reached the pseudo-positive path.
  key_links:
    - Use the authored smoke profile rather than ad hoc CLI overrides.
---

<objective>
Run and verify the first real pseudo-positive smoke execution on live training infrastructure.

Purpose: move from code/spec correctness to runtime evidence.
Output: smoke logs, any minimal artifact captures under `temp/`, and a concise execution summary.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@openspec/changes/study-channel-b-pseudopositive-promotion/tasks.md
@configs/stage2_two_channel/smoke/b_majority_coco1024_pseudo_positive_4steps.yaml
@docs/training/STAGE2_RUNBOOK.md
@docs/training/METRICS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute the pseudo-positive smoke profile through `src.sft`</name>
  <files>configs/stage2_two_channel/smoke/b_majority_coco1024_pseudo_positive_4steps.yaml</files>
  <action>Run the authored smoke profile with the repo-standard entrypoint. Capture whether the environment has the required rollout backend/server/GPU prerequisites. If runtime infra is missing, fail fast and record the exact blocker rather than silently substituting a different backend.</action>
  <verify>PYTHONPATH=. conda run -n ms python -m src.sft --config configs/stage2_two_channel/smoke/b_majority_coco1024_pseudo_positive_4steps.yaml</verify>
  <acceptance_criteria>
    - The run reaches the enabled pseudo-positive path or records a concrete infrastructure blocker.
    - No ad hoc CLI override changes the authored pseudo-positive contract.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Inspect runtime outputs for one-forward and coord-only behavior</name>
  <files>temp/</files>
  <action>From the smoke logs/metrics, confirm `stage2/raw_rollouts`, pseudo-positive counters, and dead-anchor suppression behavior are present and consistent with the one-forward contract. Save any concise notes or extracted evidence under `temp/` if useful.</action>
  <verify>rg -n "stage2/raw_rollouts|pseudo_positive|loss_dead_anchor_suppression|rollout/explorer" temp output tb || true</verify>
  <acceptance_criteria>
    - Evidence exists for arbitrary-`K` rollout execution.
    - Pseudo-positive metrics are visible.
    - No evidence suggests a second teacher-forced forward was introduced.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The smoke was attempted through the authored profile.
- [ ] Either runtime evidence exists or a concrete infrastructure blocker is recorded.
- [ ] Any generated scratch evidence lives under `temp/`.
</verification>

<output>
After completion, create `.planning/phases/05-validation-and-best-k-readiness/05-02-SUMMARY.md`
</output>
