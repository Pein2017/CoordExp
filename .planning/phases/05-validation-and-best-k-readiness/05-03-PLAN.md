---
phase: 05-validation-and-best-k-readiness
plan: 03
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - temp/
autonomous: true
requirements:
  - VAL-03
  - VAL-04
user_setup: []
must_haves:
  truths:
    - Best-`K` readiness requires at least one enabled comparison across different `num_rollouts` values.
    - Enabled `K=2` must remain a no-promotion control in runtime evidence, not just in unit tests.
  artifacts:
    - Comparison notes or extracted metrics under `temp/`
---

<objective>
Prepare and verify the first best-`K`-ready comparison workflow.

Purpose: prove the authored `K` knob is usable for the next ablation stage.
Output: comparison evidence for at least two enabled `num_rollouts` settings and the enabled `K=2` no-promotion control.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@openspec/changes/study-channel-b-pseudopositive-promotion/tasks.md
@configs/stage2_two_channel/prod/ab_mixed_coco1024_bmajority_channel_b_pseudo_positive.yaml
@configs/stage2_two_channel/smoke/b_majority_coco1024_pseudo_positive_4steps.yaml
@docs/training/METRICS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compare at least two enabled rollout counts using the same pseudo-positive surface</name>
  <files>temp/</files>
  <action>Run or materialize two enabled configs that differ only in `stage2_ab.channel_b.triage_posterior.num_rollouts`. Capture the comparable support-rate and selection metrics for the two settings.</action>
  <verify>rg -n "pseudo_positive_(selected|candidate|support_rate)|recovered_ground_truth_rate" temp output tb || true</verify>
  <acceptance_criteria>
    - Two enabled `num_rollouts` settings are compared with the same pseudo-positive knobs.
    - The comparison uses the documented rate-based observability surface.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Confirm enabled `K=2` behaves as a no-promotion control in runtime evidence</name>
  <files>temp/</files>
  <action>Run or materialize an enabled `K=2` control and verify it does not produce pseudo-positive winners while keeping the enabled path legal.</action>
  <verify>rg -n "pseudo_positive_selected_count|num_rollouts" temp output tb || true</verify>
  <acceptance_criteria>
    - Enabled `K=2` is exercised through the same pseudo-positive surface.
    - Runtime evidence shows no pseudo-positive promotion under the `support_count >= 2` floor.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] At least two enabled `num_rollouts` settings were compared or a concrete runtime blocker was recorded.
- [ ] Enabled `K=2` no-promotion control was verified in runtime evidence.
- [ ] Results are stored or summarized under `temp/`.
</verification>

<output>
After completion, create `.planning/phases/05-validation-and-best-k-readiness/05-03-SUMMARY.md`
</output>
