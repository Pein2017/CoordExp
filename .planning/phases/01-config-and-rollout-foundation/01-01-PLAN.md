---
phase: 01-config-and-rollout-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/schema.py
  - tests/test_stage2_ab_config_contract.py
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
user_setup: []
must_haves:
  truths:
    - Stage-2 Channel-B accepts only versionless pseudo-positive knobs under `stage2_ab.channel_b.pseudo_positive.*`.
    - Disabled pseudo-positive configs preserve legacy `K=2` behavior and enabled configs fail fast on invalid pseudo-positive keys or weights.
  artifacts:
    - src/config/schema.py contains typed pseudo-positive config parsing and cross-field validation.
    - tests/test_stage2_ab_config_contract.py contains config-contract coverage for the new pseudo-positive surface.
  key_links:
    - `Stage2ABChannelBConfig` validates `pseudo_positive.enabled`, `pseudo_positive.coord_weight`, and `triage_posterior.num_rollouts` together.
---

<objective>
Create the typed Channel-B pseudo-positive schema surface and pin the config invariants before any runtime changes land.

Purpose: Make the new mode legal to author in YAML while keeping legacy disabled-path behavior stable.
Output: Schema changes plus config-contract regression tests.
</objective>

<execution_context>
@/data/CoordExp/.codex_config/pein/get-shit-done/workflows/execute-plan.md
@/data/CoordExp/.codex_config/pein/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@openspec/changes/study-channel-b-pseudopositive-promotion/specs/stage2-ab-training/spec.md
@openspec/changes/study-channel-b-pseudopositive-promotion/specs/channel-b-lightweight-pseudopositive-v1/spec.md
@src/config/schema.py
@tests/test_stage2_ab_config_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed pseudo-positive config classes and versionless key validation</name>
  <files>src/config/schema.py</files>
  <read_first>openspec/changes/study-channel-b-pseudopositive-promotion/specs/stage2-ab-training/spec.md, openspec/changes/study-channel-b-pseudopositive-promotion/specs/channel-b-lightweight-pseudopositive-v1/spec.md</read_first>
  <action>Add a typed `pseudo_positive` config section under `Stage2ABChannelBConfig` with strict unknown-key rejection and no accepted `v*` aliases. Keep the surface YAML-first. Parse `enabled` and `coord_weight`, and make the default authored pseudo-positive path legal with `triage_posterior.num_rollouts = 4` while preserving the legacy disabled-path default of `2`.</action>
  <verify>rg -n "pseudo_positive|coord_weight|num_rollouts" src/config/schema.py</verify>
  <acceptance_criteria>
    - `src/config/schema.py` defines a typed pseudo-positive config holder and threads it through `Stage2ABChannelBConfig`.
    - Config parsing rejects versioned pseudo-positive aliases and unknown nested keys.
    - Cross-field validation distinguishes disabled compatibility behavior from enabled arbitrary-`K` behavior.
  </acceptance_criteria>
  <done>Schema code matches the authored OpenSpec contract and exposes a stable typed surface for later phases.</done>
</task>

<task type="auto">
  <name>Task 2: Add config-contract tests for pseudo-positive schema invariants</name>
  <files>tests/test_stage2_ab_config_contract.py</files>
  <read_first>src/config/schema.py, openspec/changes/study-channel-b-pseudopositive-promotion/tasks.md</read_first>
  <action>Add focused tests covering: enabled pseudo-positive parsing, strict unknown-key rejection, versioned alias rejection, invalid `coord_weight` bounds, enabled `num_rollouts >= 2`, and disabled-path preservation of legacy `K=2` semantics.</action>
  <verify>conda run -n ms python -m pytest -q tests/test_stage2_ab_config_contract.py</verify>
  <acceptance_criteria>
    - New tests fail before the schema change and pass after it.
    - Disabled pseudo-positive configs still parse with the legacy `K=2` contract.
    - Enabled pseudo-positive configs with malformed keys or invalid weights fail deterministically.
  </acceptance_criteria>
  <done>Config-contract tests prove the new schema is strict, versionless, and compatibility-safe.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `conda run -n ms python -m pytest -q tests/test_stage2_ab_config_contract.py`
- [ ] `python - <<'PY'` style spot checks are unnecessary because pytest covers the config invariants directly.
- [ ] The schema surface matches the OpenSpec knobs exactly: `stage2_ab.channel_b.pseudo_positive.enabled` and `stage2_ab.channel_b.pseudo_positive.coord_weight`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No legacy config behavior regresses when pseudo-positive is disabled
- Enabled pseudo-positive configs can author the default `K=4` rollout profile without introducing new CLI flags
  </success_criteria>

<output>
After completion, create `.planning/phases/01-config-and-rollout-foundation/01-01-SUMMARY.md`
</output>
