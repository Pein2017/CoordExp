---
phase: 01-config-and-rollout-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/trainers/stage2_two_channel.py
  - tests/test_stage2_ab_training.py
autonomous: true
requirements:
  - ROLL-03
  - ROLL-04
user_setup: []
must_haves:
  truths:
    - Enabled pseudo-positive mode drops malformed anchor samples instead of using empty-prefix fallback.
    - Enabled pseudo-positive mode aborts the optimizer step on malformed or missing explorer preparation so support-rate denominators never silently shrink.
  artifacts:
    - src/trainers/stage2_two_channel.py contains explicit enabled-path anchor-drop and explorer-abort logic.
    - tests/test_stage2_ab_training.py covers anchor-drop and explorer-abort behavior.
  key_links:
    - Failure handling branches consult the enabled pseudo-positive contract and preserve disabled-path fallback compatibility.
---

<objective>
Finalize Phase 1 by hardening failure behavior for the enabled pseudo-positive runtime without breaking the disabled compatibility path.

Purpose: Ensure the runtime produces honest support denominators and never silently degrades the enabled pseudo-positive contract.
Output: Failure-handling runtime changes and regression coverage.
</objective>

<execution_context>
@/data/CoordExp/.codex_config/pein/get-shit-done/workflows/execute-plan.md
@/data/CoordExp/.codex_config/pein/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-config-and-rollout-foundation/01-01-SUMMARY.md
@.planning/phases/01-config-and-rollout-foundation/01-02-SUMMARY.md
@openspec/changes/study-channel-b-pseudopositive-promotion/specs/stage2-ab-training/spec.md
@src/trainers/stage2_two_channel.py
@tests/test_stage2_ab_training.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add enabled-path anchor-drop and explorer-abort failure handling</name>
  <files>src/trainers/stage2_two_channel.py</files>
  <read_first>openspec/changes/study-channel-b-pseudopositive-promotion/specs/stage2-ab-training/spec.md, src/trainers/stage2_two_channel.py</read_first>
  <action>Patch the Channel-B rollout preparation path so that, when pseudo-positive mode is enabled, malformed anchor preparation drops that sample from Channel-B training for the current step and malformed or missing explorer preparation aborts the current optimizer step. Keep the existing disabled-path fallback semantics untouched.</action>
  <verify>rg -n "fallback|abort|anchor|explorer" src/trainers/stage2_two_channel.py</verify>
  <acceptance_criteria>
    - Enabled anchor failure does not route through the legacy empty-prefix fallback.
    - Enabled explorer failure does not silently continue with fewer explorers.
    - Disabled pseudo-positive mode still uses the legacy fallback behavior.
  </acceptance_criteria>
  <done>Failure handling matches the enabled pseudo-positive contract exactly and stays compatibility-safe when disabled.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests for enabled failure semantics and disabled compatibility</name>
  <files>tests/test_stage2_ab_training.py</files>
  <read_first>src/trainers/stage2_two_channel.py, openspec/changes/study-channel-b-pseudopositive-promotion/tasks.md</read_first>
  <action>Add trainer tests proving: enabled malformed anchor prep drops the sample, enabled malformed explorer prep aborts the step, and disabled mode preserves current fallback behavior. Keep the tests narrow and deterministic.</action>
  <verify>conda run -n ms python -m pytest -q tests/test_stage2_ab_training.py -k 'pseudo_positive or fallback or malformed'</verify>
  <acceptance_criteria>
    - Tests explicitly distinguish enabled failure behavior from disabled compatibility behavior.
    - Failure paths do not silently change the effective explorer denominator in enabled mode.
  </acceptance_criteria>
  <done>Phase 1 finishes with deterministic failure semantics backed by regression tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `conda run -n ms python -m pytest -q tests/test_stage2_ab_training.py -k 'pseudo_positive or fallback or malformed'`
- [ ] The enabled-path runtime no longer uses the legacy empty-prefix fallback for malformed anchors.
- [ ] Explorer preparation failures are surfaced as step-aborting errors rather than silent partial evidence.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Enabled pseudo-positive runtime has deterministic failure semantics
- Disabled legacy runtime behavior remains regression-safe
  </success_criteria>

<output>
After completion, create `.planning/phases/01-config-and-rollout-foundation/01-03-SUMMARY.md`
</output>
