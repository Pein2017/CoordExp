---
phase: 01-config-and-rollout-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/trainers/stage2_two_channel.py
  - src/trainers/stage2_two_channel/rollout_views.py
  - tests/test_stage2_ab_training.py
autonomous: true
requirements:
  - ROLL-01
  - ROLL-02
  - ROLL-05
user_setup: []
must_haves:
  truths:
    - Enabled pseudo-positive mode schedules one deterministic anchor and `num_rollouts - 1` explorer rollouts with deterministic explorer identity.
    - Explorer rollouts can return zero accepted-clean objects without triggering fallback behavior.
  artifacts:
    - src/trainers/stage2_two_channel.py generalizes singular explorer scheduling to arbitrary explorer lists.
    - tests/test_stage2_ab_training.py covers enabled default `K=4` scheduling and zero-object explorer behavior.
  key_links:
    - Explorer identity is derived from the configured rollout count and stable explorer ordinal, not from ad hoc loop state.
---

<objective>
Generalize the Channel-B rollout evidence path from one explorer to arbitrary `K-1` explorers while keeping the anchor deterministic and the disabled path intact.

Purpose: Make the runtime capable of the default `K=4` pseudo-positive profile before support-rate triage is added.
Output: Multi-explorer scheduling/runtime scaffolding and trainer tests.
</objective>

<execution_context>
@/data/CoordExp/.codex_config/pein/get-shit-done/workflows/execute-plan.md
@/data/CoordExp/.codex_config/pein/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-config-and-rollout-foundation/01-01-SUMMARY.md
@openspec/changes/study-channel-b-pseudopositive-promotion/specs/stage2-ab-training/spec.md
@src/trainers/stage2_two_channel.py
@src/trainers/stage2_two_channel/rollout_views.py
@tests/test_stage2_ab_training.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace singular explorer scheduling with deterministic explorer lists</name>
  <files>src/trainers/stage2_two_channel.py</files>
  <read_first>openspec/changes/study-channel-b-pseudopositive-promotion/specs/stage2-ab-training/spec.md, src/trainers/stage2_two_channel.py</read_first>
  <action>Generalize the current anchor-plus-one-explorer scheduling logic to `1` anchor plus `num_rollouts - 1` explorers. Keep one deterministic anchor decode and reuse the authored explorer decode profile for every explorer, varying only deterministic explorer identity derived from the explorer ordinal and base seed. Preserve legacy disabled behavior when pseudo-positive mode is off.</action>
  <verify>rg -n "explorer|num_rollouts|rollout" src/trainers/stage2_two_channel.py</verify>
  <acceptance_criteria>
    - The runtime no longer assumes a single explorer view in the enabled path.
    - The default enabled profile naturally schedules `K=4` as one anchor plus three explorers.
    - Explorer identity is deterministic and stable across runs for the same seed/config pair.
  </acceptance_criteria>
  <done>Channel-B runtime can gather arbitrary-`K` evidence without changing the one-forward supervision architecture.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve valid zero-support explorer behavior</name>
  <files>src/trainers/stage2_two_channel/rollout_views.py, tests/test_stage2_ab_training.py</files>
  <read_first>src/trainers/stage2_two_channel/rollout_views.py, src/trainers/stage2_two_channel.py</read_first>
  <action>Audit and patch explorer view preparation so zero accepted-clean explorer output remains a valid explorer result instead of being treated as failure or fallback. Add a trainer regression that exercises enabled pseudo-positive scheduling with at least one zero-object explorer view.</action>
  <verify>conda run -n ms python -m pytest -q tests/test_stage2_ab_training.py -k 'pseudo_positive or explorer or rollout'</verify>
  <acceptance_criteria>
    - Zero-object explorer views survive preparation and contribute zero support.
    - Enabled pseudo-positive runtime tests cover default `K=4` scheduling.
    - Disabled-path behavior remains unchanged.
  </acceptance_criteria>
  <done>Explorer lists are runtime-stable and zero-support explorers are treated as valid evidence carriers.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `conda run -n ms python -m pytest -q tests/test_stage2_ab_training.py -k 'pseudo_positive or explorer or rollout'`
- [ ] Enabled pseudo-positive scheduling can be inspected in logs/objects without singular-explorer assumptions.
- [ ] Disabled-path trainer tests continue to pass for the legacy `K=2` flow.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Enabled pseudo-positive mode schedules arbitrary-`K` explorers deterministically
- Zero-object explorers are valid zero-support evidence rather than runtime failures
  </success_criteria>

<output>
After completion, create `.planning/phases/01-config-and-rollout-foundation/01-02-SUMMARY.md`
</output>
