# Current Project Memory

Last verified for the dense-enumeration live route: 2026-08-04.

## Authority boundary

This memory is continuity, not research authority. The owning executed result
is:

`research/investigations/qwen3-vl-dense-enumeration/experiments/2026-08-04-sorted-crossing-matched-length-neutral-row-insertion-control/results.md`

The frozen protocol is the sibling `unit.md`; the program route is owned by
`research/investigations/qwen3-vl-dense-enumeration/compass.md`. Handoffs,
reviews, agent output, and memory notes remain provenance only.

## Current state: neutral-row insertion control complete

The matched-length neutral-row score control over the frozen crossing-boundary
evidence is complete and independently verified. Its immutable owning artifact
is:

`/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-08-04-sorted-crossing-matched-length-neutral-row-insertion-control/20260804T093553Z/`

The governing outcome remains one `list all objects` prompt, one free
autoregressive completion, and unique trusted physical-owner coverage.
Category-conditioned likelihood, forced continuation, and owner-centred banks
are diagnostics, not the final task.

## Durable evidence update

The CPU plan admits `9` voting owners and `12` specificity owners and ledgers
`5` structurally infeasible owners. The immutable runtime captures all `54`
paired requests with zero quarantine. Input/token identity, runtime replay,
all same-run clean-C controls, all benign references, and representative
cached-versus-uncached parity pass.

The frozen route is `neutral_row_not_neutral_inconclusive`: the selected
already-covered control row is material for `8/12` C-nonmaterial specificity
owners, failing the absolute specificity threshold of four. This blocks both
the content-specific and generic-insertion routes.

Independent post-hoc review finds that all seven N-nonmaterial owners have a
`rows_back_distance` no greater than three, while thirteen of fourteen
N-material owners jump back at least five native rows. The sole exception is
`gt:4134:29`; Spearman association with the relative coordinate delta is
`-0.7511`. This makes sorted-route regression the strongest remaining
alternative but not a causal result. A lower-regression candidate exists for
only `5/12` specificity and `5/9` voting owners, so no sibling capture is
admitted under the frozen predicates.

## Current judgment

False negatives and downstream unmatched rows remain heterogeneous. Tested
support, selection/routing, identity/extent ambiguity, incomplete annotation,
and no-support-under-the-tested-interface are separate states. The current
evidence strengthens path-sensitive sorted traversal as a mechanism class: a
backward duplicate insertion can damage a later exact row, but the unit cannot
separate duplication, route regression, row identity, position, or recency.
It does not select a training objective, selector head, slot module, explicit
ledger, visual-tower change, or inference policy.

## Next continuation point

The repeated-covered-row insertion line is closed. Do not add another neutral
row, relax the frozen predicates, or duplicate the already completed
`114`-owner reachability-prevalence census.

If the user authorizes a successor, the next proposal should return to the
deployment target: recover a skipped owner `C` while measuring whether later
suffix owners are retained. It should compare exact self-prefix contexts before
and after the skip and report unique-owner gained, retained, and lost outcomes.
The choice between a score-only causal contrast and a small training screen is
not yet frozen.

The next model-facing panel remains materialized and CPU-verified as a separate
prospective thirteen-image input. It retains the frozen twelve byte-for-byte
and adds image `2299` from the training-side
`/data/CoordExp/public_data/coco/rescale_32_1024_bbox_len12000/val.coord.jsonl`
authority. The full panel has 392 owners: 346 legacy plus 46 in image `2299`
(38 persons and 8 ties). All 13 image references are hash-verified. Do not
retroactively mix it into any completed twelve-image denominator, and report
legacy-12 versus image `2299` separately before any future pooled summary.

No new GPU capture, training, optimizer update, prompt change, architecture
branch, or free rollout is admitted at this frontier.

## Minimum reading path

1. `research/investigations/qwen3-vl-dense-enumeration/experiments/2026-08-04-sorted-crossing-matched-length-neutral-row-insertion-control/results.md`
2. `research/investigations/qwen3-vl-dense-enumeration/experiments/2026-08-04-sorted-crossing-matched-length-neutral-row-insertion-control/review.md`
3. `research/investigations/qwen3-vl-dense-enumeration/experiments/2026-08-04-sorted-crossing-matched-length-neutral-row-insertion-control/unit.md`
4. `research/investigations/qwen3-vl-dense-enumeration/experiments/2026-08-04-sorted-crossing-owner-row-geometric-relation-stratification/results.md`
5. `research/investigations/qwen3-vl-dense-enumeration/experiments/2026-08-03-sorted-crossing-boundary-owner-release-realization/results.md`
6. `research/investigations/qwen3-vl-dense-enumeration/compass.md`

The result owns the evidence and claim boundary. The unit owns the executed
contract. The compass owns the program route. This file only tells the next
agent where the frontier moved.
