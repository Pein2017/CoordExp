# Primary-predicate design-scope fork

The admission census remains mechanically authoritative, but its stop boundary
is narrower than the earlier project-memory wording implied. Its primary
predicate requires one exact final-owner-set class with at least two distinct
token serializations starting from different first owners, and requires that
class to be the higher side of at least one universally safe,
first-owner-preserving strict owner-set inclusion edge. Eight of 2,004
Source-eligible images pass this exact composite predicate. Seventy-six images
have at least one admissible strict edge, and 709 have at least two eligible
candidates.

Two independent advanced-model reviews reconstructed the originating side-chat,
comprehensive handoff, census contract, analyzer, and result. Both concluded
that `8 < 256` decisively stops the exact all-positive natural-alias pairwise
screen, but does not stop every possible 256-image owner-set training design.
The exact numerical composite predicate first became explicit in the census
unit. A strict inclusion edge or another real nonzero set-level contrast is
needed for images claimed to train set expansion under the frozen pairwise
loss. Natural same-set, different-first-owner aliases are a conservative guard
for a strong natural order-independence claim, not a universal prerequisite for
all owner-set training. They can be replaced only by a different explicit
design, such as validated order augmentation, a listwise objective, or a mixed
signal/control curriculum with correspondingly narrower claims.

The Stage Zero artifact is now clean and independently approved after the user
removed audit-created bytecode caches. It exactly partitions all 1,622 censored
nonpassers into 1,106 possible and 516 impossible cases. The selector and blind
review/finalization machinery are also frozen and independently approved. No
entropy, sample, image adjudication, inference, graphics-processing-unit work,
or training has occurred. There is no mechanical sample-selection blocker.

The remaining fork owns research meaning. Continuing the current salvage unit
would estimate whether the 1,106 possible cases can supply 248 more admissions
for the exact strict design. If the intended 256-image screen instead permits a
mixed cohort, that 248-admission estimand is the wrong next question and the
salvage unit must be narrowed or retired before entropy. A plausible mixed
design would expose the 76 edge-positive images as the true set-expansion
signal stratum, treat the eight exact-primary images as the strongest natural
order-alias subset, and add explicit preservation, background, and matched-dose
control strata without claiming that all 256 images carry the same signal.

The user stated that 256 was understood as an arbitrary training-set size and
asked to stop after this round for discussion. Therefore pause before entropy
and discuss the exact strict design versus a mixed signal/control design. Do
not resume the current 248-admission sample until that design choice is made.

Primary evidence:

- `handoff/from-side-chat.md`
- `handoff/2026-07-23-qwen3-vl-dense-enumeration-comprehensive-research-flow.md`
- `research/investigations/qwen3-vl-dense-enumeration/experiments/2026-07-23-trajectory-owner-set-admission-census/unit.md`
- `research/investigations/qwen3-vl-dense-enumeration/experiments/2026-07-23-trajectory-owner-set-admission-census/results.md`
- `research/investigations/qwen3-vl-dense-enumeration/experiments/2026-07-23-trajectory-owner-set-adjudication-salvage-gate/stage-zero-independent-audit-v2.json`

