## 1. Freeze authority and inventory

- [x] 1.1 Record the exact local evidence roots, counts, worktree ownership boundaries, and Notion page/database IDs in `receipt.md` without copying raw content.
- [x] 1.2 Validate this OpenSpec and confirm that only this change directory is locally modified by this task.

## 2. Pilot current-state pages

- [x] 2.1 Refactor the CoordExp home in place into a concise start-here/current-frontier/decisions/methods/legacy router and correct the `research/`, `progress/`, OpenSpec, and Notion authority wording.
- [x] 2.2 Rewrite Global Research OS as a shared reading map, remove the E0–E5 ordinal ladder and mandatory V→S→A→R→T→E lifecycle authority, and retain evidence surface/status/scope/source as plain descriptors.
- [x] 2.3 Update the serialization page with the exact closed-wrapper/no-row-separator/terminal-suffix contract and the current y-then-x versus future x-then-y boundary.
- [x] 2.4 Condense the Image2299 page into established, closed/rejected, unresolved, not-claimed, and sources; set the current frontier to HOLD with no scientifically authorized continuation.
- [x] 2.5 Refetch all four pilot pages and record distinctive verification markers before continuing.

## 3. Reconcile stale and abandoned records

- [x] 3.1 Mark the compact-sequence Research Unit as historical/superseded for the default route while preserving bounded historical val200 and implementation evidence.
- [x] 3.2 Convert the compact Pilot into a historical redirect and mark its unexecuted claims stale/untested rather than completed.
- [x] 3.3 Reconcile the blank or stale Methods records with current canonical paths and reversible historical/archive status.
- [x] 3.4 Slim Experiments & Evidence into a legacy router and add historical boundaries to its preserved child pages without deleting or moving their URLs.

## 4. Consolidate current local knowledge

- [x] 4.1 Add bounded current summaries and canonical pointers for unique worktree-owned topics: Image2299, all-HF retirement, Human13, OwnerBridge, ordering, coverage, geometry, training/inference, and research infrastructure.
- [x] 4.2 Summarize session history only through metadata, indexed decisions, and decision-bearing messages; explicitly state that raw JSONL was not imported.
- [x] 4.3 Link active `research/` interpretation and legacy `progress/` provenance to their current Notion owners without copying large artifacts or duplicating records.
- [x] 4.4 Connect related Research Units, Methods, Claims, Decisions, track pages, and legacy sources using existing page links and concise source sections.

## 5. Verify and close

- [x] 5.1 Refetch every touched page and record page ID, intended outcome, verification marker, and result in `receipt.md`.
- [x] 5.2 Check that no Notion page was deleted, no raw session/artifact dump was imported, current production behavior was not changed, and repository edits remain confined to this OpenSpec change.
- [x] 5.3 Run `openspec validate consolidate-coordexp-notion-research-os --strict` and reconcile all task checkboxes with literal verified completion.
