## 1. Freeze the successor evidence contract

- [x] 1.1 Add the canonical successor-sidecar records and builder over the sealed Human-13 manifest, with exact optional A4@1/@2 alignment and typed exclusion receipts.
- [x] 1.2 Add CPU tests for complete duplicate-row recovery, covered/uncovered owner state, alias normalization inputs, coordinate sites, `G` watch sites, deterministic hashing, and fail-closed identity/span behavior.

## 2. Implement the pure objective math

- [x] 2.1 Add TDD coverage and pure fp32 helpers for same-description bbox contrast, cross-description row contrast, four-coordinate fallback unlikelihood, and rectangle-valid set-valued argmax margins.
- [x] 2.2 Add TDD coverage and a world-size-one accumulated-gradient projection helper with exact pre/post dot, norm, coefficient, finite, and zero-watch receipts.

## 3. Extend Human-13 materialization and dry-run

- [x] 3.1 Materialize successor candidate, duplicate-row, rectangle, replay, and `G` watch segments with exact causal positions and owner/image denominators while reusing existing no-padding/A4 streaming paths.
- [x] 3.2 Add R1/R2 resolved configs, exposure `{1,2}` scheduling, immutable output roots, and CPU-only dry-run/launcher tests with no model or GPU actions.
- [x] 3.3 Run focused and adjacent CPU suites, Ruff/format/compile checks, strict OpenSpec validation, and a residue check; resolve all P0/P1 contract findings before model execution.

## 4. Retire the real-entry risk

- [x] 4.1 Run the authorized image-14038 R1 production-shaped vertical slice through real encode/pack/forward/backward/AdamW/checkpoint write/read/HF fp32-SDPA batch-one greedy/analyzer.
- [x] 4.2 Inspect the bound receipt, losses, gradients, pack counters, memory, checkpoint identity, decoded output, and analyzer row; stop on any declared gate failure.

## 5. Execute the bounded two-arm benchmark

- [ ] 5.1 Launch fresh-Source R1 and R2 world-size-one jobs on two GPUs to cumulative exposures one and two, preserving independent optimizer and artifact roots.
- [ ] 5.2 Evaluate R1@1, R1@2, R2@1, and R2@2 under the exact HF clean-greedy surface using up to four GPUs, then run the canonical Human-13 analyzer.

## 6. Close the research record

- [ ] 6.1 Publish immutable artifact hashes and the full per-image/pooled Pareto table against historical A4@2, including projection and performance receipts.
- [ ] 6.2 Update the owning research unit/results with supported, ruled-out, unresolved, claim-boundary, and next-decision statements; do not extend dose or scope.
- [ ] 6.3 Run final targeted verification, strict OpenSpec validation, standards review, intent-contract review, and project-continuity check before marking the goal complete.
