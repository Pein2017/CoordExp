## 1. Qualification identity and supervision

- [x] 1.1 Reproduce semantic source drift retaining old admission on baseline, extend source coverage and add local-import coverage guard; verify changed semantic source rejects old receipts through admission and existing qualification tests pass.
- [x] 1.2 Add configurable finite positive child deadline and bounded GPU census; verify RED/GREEN timeout behavior, real owned child/descendant cleanup, invalid input rejection, CLI forwarding and no admitted publication on failure.

## 2. Renderer fixture

- [x] 2.1 Reproduce the golden failure, compare structured actual/expected output, update only stale fingerprints and pass the renderer suite.

## 3. Integration acceptance

- [x] 3.1 Document timeout/default/override and source identity invalidation; inspect final diff, run affected inference/templates CPU suites and strict OpenSpec validation, and record exact evidence plus delegation outcomes in acceptance.md and task-owned routing receipts.
