## 1. Contract Decisions and Feasibility Gate

- [x] 1.1 Record the approved V1 decisions: reject empty members while retaining Drafts, same-machine loopback only, one immutable all-or-nothing same-split batch with one active batch per split, durable enqueue distinct from terminal Commit success, nonblocking annotation/navigation, complete last-terminal ordinary JSONL, and newer-Draft preservation.
- [x] 1.2 Add immutable fixtures for both selected source paths, row/box counts, representative row schema, source hashes, image root, official COCO-80 sparse ID registry, and the pinned Label Studio revision.
- [x] 1.3 Add failing exhaustive/golden/property tests for unchanged-bin Label Studio float round trips, edited outward quantization, parser canvas semantics, discrete crop/letterbox inversion, clipping, and strict non-degenerate final geometry.
- [ ] 1.4 Pin and bootstrap accepted Node/Yarn/Python Label Studio frontend/backend runtimes, record build receipts, and execute fake-backend spikes proving source seeding, current-user authoritative Draft capture, nonblocking batch status, CAS-safe terminal handling, and direct ROI append as one undo action with result-meta save/reload.
- [ ] 1.5 Execute progressive 1k/10k/full-train Label Studio import/open/Next/Draft/restart latency/RSS probes; separately measure durable batch enqueue and background 1/10/N-row full-train completion, amortized time per changed row, file passes, RSS, and every queue/transaction recovery cut without modifying sources. A blocked-worker probe must prove enqueue, later Draft save, editing, and navigation do not wait for JSONL publication.
  - 2026-07-15 redesign evidence: the superseded one-row synchronous full-train Commit took 12.145s because it performed three full-file passes. See `research/investigations/label-studio-coco-refinement-commit-gate/`.
  - 2026-07-15 async-core evidence: 1k/10k ten-member enqueue took 2.749/3.105ms and background publication took 0.155/1.117s; one pre-final-hash full run took 2.538ms enqueue and 12.935s background. Final-hash receipt states, source/image identity, 91 store tests, and 261 package tests are attested under `outputs/label_studio_coco_refinement/async-batch-v1/`. Browser/HTTP navigation and final-hash full-size execution remain open, so this task stays unchecked.
- [ ] 1.6 Gate Wave 1 on strict OpenSpec validation and separate standards/intent audits with no unresolved P0/P1 findings; return to the user if either spike fails or requires a contract change.

## 2. Project, Category, and Working-Data Core

- [x] 2.1 Add parent-owned module/config boundaries for `Coco80Registry`, `RefinementProjectAdapter`, `WorkingDatasetStore`, durable batch queue/worker/status contracts, `WorkingCoordMaterializer`, manifests, same-origin service contracts, and the dedicated ignored one-instance/two-split runtime layout.
- [ ] 2.2 Implement/test canonical English-name to official sparse-ID validation (forbidding the contiguous evaluator map), hidden stable region-key identity, preservation of positive IDs, deterministic batch-wide non-reused negative allocation, pre-candidate durable reservation with same-key reuse across failed/corrected batches, tombstones, idempotent batch retry, and stable-key-only identity merge into newer Drafts.
- [x] 2.3 Implement/test direct norm1000-to-percentage import, exact unchanged-bin reuse, tolerance-aware edited quantization, clipping/strict geometry, accepted fields, working image-locator rebasing/managed links, and stable `(y1,x1)` ordering with prior-rank tie preservation.
- [x] 2.4 Implement durable same-split batch enqueue plus one worker per split: immutable `batch_id + payload_hash`, deterministic unique members, one active batch, per-row freshness, all-or-nothing validation/allocation, last-published input hash/line-count attestation during one-pass copy/substitute/hash, fsync/atomic working-file replacement/directory-fsync under a shared reader barrier, authoritative transaction status, and repaired manifest/queue projections. Preserve a narrow single-member wrapper only if tests/callers require it.
  - 2026-07-16 closure evidence: the service-owned per-split workers and authenticated Draft-capture/status HTTP boundary are registered by the explicit production runtime; 677 parent tests and 152 canonical-environment Django tests cover the current implementation. Full browser/performance execution remains tracked by 1.5, 3.6, 6.2, and 6.5.
- [ ] 2.5 Implement/test startup reconciliation for lost enqueue response and every queue/reservation/candidate/prepared/file/directory/manifest/journal-terminal/queue-terminal/response cut, orphan-candidate cleanup, sequential-batch preservation, untouched-input drift, projection disagreement, batch-wide ID-allocation recovery and same-key corrected retry, repeated recovery, and no partial/double application. At controlled pauses before/after working-file rename, directory fsync, journal-terminal projection, and queue-terminal projection, concurrently attest that a supported reader, status query, and second enqueue observe only the old generation, the new generation, or `Reconciling`/blocked—never cross-authority claims.
  - In-process fault injection, dual pre-opened store instances, legacy compatibility, torn tails, and queue/journal projection repair are covered by the 91-test store suite. An independently killed OS worker/power-loss run remains open, so this task stays unchecked.
- [x] 2.6 Implement idempotent one-instance train/validation bootstrap with stable `(split,image_id)` tasks, exactly one editable imported annotation per task, disabled alternate/native submit paths, exact image-root document setting, project-bound split storage records, managed links, manifest drift checks, and an authenticated current-user authoritative-Draft catalog/capture seam that copies full immutable payloads before `202`.
  - 2026-07-16 execution evidence: the loopback production runtime reused the pre-existing Label Studio projects, built both working stores through exact managed links, registered the authenticated bridge, and served both split APIs. Full counts and hashes are recorded in `research/investigations/label-studio-coco-refinement-runtime/`.
- [ ] 2.7 Implement the explicit atomic norm-to-current-coord materializer bound to the exact store `committed_generation_guard`; reject arbitrary/private locks and unresolved legacy/batch publication. Verify core behavior with source hashes, norm validation, real loader smoke including a negative ID, full-size latency rerun, adjacent-family residue checks, and independent standards/intent audits before Wave 3.
  - The exact-store guard, unresolved-publication behavior, schema-v3 bootstrap image authority, production resolver/facade, and durable operator receipt are implemented. The bounded real-fixture probe passes with negative IDs through the current training path; the full-size latency rerun and fresh independent audits keep this task unchecked.

## 3. Bbox Editing and Commit UX

- [x] 3.1 Generate the bbox-only fixed COCO-80 configuration, suppress the native rotation field/handle when disabled, and enforce parent-side zero rotation while excluding free-form classes, aliases, masks, polygons, and crowd controls.
- [x] 3.2 Add deterministic exact/prefix/substring/spelling-tolerant canonical-name search with keyboard focus, traversal, selection, cancellation, and tests that never persist aliases, translations, or synonyms.
- [ ] 3.3 Add per-task semantic-hash `Committed`/`Draft` state plus separate `Queued`/`Running`/`Reconciling`/`Succeeded`/`Failed` batch overlays, pending/member/generation cues, `Draft ahead of active/committed batch`, enqueue/terminal error banners, and exact-token CAS rebase versus stable-key identity-only merge.
- [ ] 3.4 Add/test one in-app navigation coordinator that force/awaits only active Draft save for row click, shortcuts, Previous/Next, Back, route/popstate, and close, then continues without waiting for batch work; add a project pending-Draft reminder/Commit action, explicit discard/reset, save/enqueue/terminal failure handling, and browser-native warning only for unsaved in-memory edits.
- [x] 3.5 Add overlay-only show-all, dim-non-selected, hide-non-selected, restore, native per-region visibility, focused-deletion recovery, and insertion visibility transitions without semantic dirty changes.
- [ ] 3.6 Run targeted frontend/store/browser probes for create/move/resize/relabel/delete, zero-rotation rejection, empty-Draft policy, multi-task save/capture, blocked-worker navigation/editing, enqueue/terminal loss, exact/newer-Draft terminal handling, dense focus, and all-or-nothing multi-row Commit; close audits before Wave 4.

## 4. Inference Profile and ROI Mapping Service

- [x] 4.1 Add strict profiles with content-addressed base-weight/adapter/checkpoint/embedding-delta/tokenizer/model-config/processor artifacts plus complete resolved config/prompt-policy/parser/adapter/transform/Transformers/processor-kwargs fingerprints, same-path drift rejection, processor-derived factor, axis/total-pixel bounds/defaults, deadline, one active profile per project, and credential-safe receipts.
- [x] 4.2 Implement a pure immutable ROI transform owning float-to-half-open crop, half-up realized dimensions, `scale_x/scale_y`, Pillow bicubic resize, black centered odd-padding rule, no-resize canvas identity, ordered inverse mapping, image clipping, and strict final norm1000 quantization.
- [ ] 4.3 Add authenticated/CSRF-protected same-origin proxy and allowlisted single-flight service keyed by project/task epoch/authoritative annotation revision/profile/generation, with loopback backends, no arbitrary reads, cooperative deadline cancellation, target abandonment, and no direct annotation mutation.
- [x] 4.4 Add a new resident ROI adapter over runtime assembly plus current prompt/parser/no-resize image/backend components without calling or modifying offline `src/inference/pipeline.py::run`; add tracked-source/AST residue tests against historical `src.infer.*` imports.
- [x] 4.5 Validate canonical COCO-80 outputs, implement the exact parser/mapping outcome table, retain raw/per-result replay data, and return one target-bound direct-insertion payload without NMS/replacement/merge/per-box acceptance.
- [ ] 4.6 Run unit/property tests for full fingerprints, same-origin security, single-flight/deadline/post-cancel reuse, target epoch, discrete transform edges/padding, every parser outcome, raw receipt replay, and official sparse categories; close audits before Wave 5.

## 5. AI Region and Inference Review UX

- [x] 5.1 Add one temporary AI Region overlay with replace-on-redraw behavior plus independent width/height controls defaulting to `1024 x 1024` and enforcing the active profile's processor-derived factor and axis/total-pixel bounds.
- [x] 5.2 Add compact profile selection and request UI for running/accepted/partial/zero/all-rejected/response-failure/runtime-failure/abandoned states; lock navigation/ROI/profile/resolution while running and implement the exact clear-versus-retain table.
- [x] 5.3 Insert valid responses into the frozen authoritative annotation as one undo action, persist request/region result metadata through Draft reload, revalidate target epoch, mark dirty without a prediction-copy step, and visibly abandon detached responses.
- [x] 5.4 Implement `visual_policy_v1` across all uncommitted inference regions: 12-bin-expanded neighborhood, stable-key greedy colors, palette-exhaustion badges, same-class IoU>=0.5 advisory conflicts, insertion focus transition, and exact-hash-gated class-color restoration/metadata stripping after terminal batch success while newer Draft presentation survives.
  - 2026-07-16 closure evidence: parent and browser now share the exact eight-color palette, least-used-neighbor exhaustion rule, and fixed cross-language golden cases for 24/25-bin adjacency, ten-region clique badges, and IoU `0.5`; targeted parent/browser tests pass.
- [ ] 5.5 Execute fake-backend browser E2E for rectangular resolutions, lock/redraw/sequential ROI behavior, every outcome, target mismatch/unload abandonment, atomic undo, direct edit/delete, Draft reload colors/provenance, overlaps, retry, nonblocking batch navigation, and inference-assisted multi-task Commit with a newer post-enqueue Draft.
- [ ] 5.6 Execute one accepted real-profile ROI smoke through the current prompt/parser/runtime and attest the input canvas, transform receipt, mapped boxes, direct edit, batch/member journal link, terminal committed working row, and newer-Draft preservation before closing standards/intent audits.

## 6. Full-Project Validation and Operator Handoff

- [x] 6.1 Bootstrap disposable full train and validation projects in one Label Studio instance under the ignored runtime root and verify idempotence, exact task/annotation counts, split namespaces/storage, managed shared-image reuse, and manifest drift rejection.
  - 2026-07-16 execution evidence: the real ignored SQLite instance contains 117,266 train and 4,952 validation tasks with exactly one authoritative annotation each, zero predictions, separate split storage, and two links to the exact shared image root; the explicit runtime completed exact reuse attestation before serving.
- [ ] 6.2 Exercise representative human-only and inference-assisted multi-row batches, continue editing captured and unrelated tasks while the worker is paused, restart/reconcile every queue/transaction phase, reject stale member rows and conflicting batch IDs, and validate prior/new complete JSONL generations while proving all source JSONL/image hashes are unchanged. Include the publication-barrier concurrency probe across rename/fsync/journal/queue projection boundaries for reader, status, and new-batch admission consistency.
- [x] 6.3 Materialize a bounded `working.coord.jsonl` through the new explicit materializer and load it through the current training data path to prove IDs, sparse COCO mapping, tied ordering, geometry, image resolution, and metadata compatibility.
  - 2026-07-16 evidence: `scripts/probes/label_studio_coco_refinement/materialize.py` committed a representative real-image row with tied new regions, materialized positive and negative IDs through the real bounded resolver, and passed both `src.data.iter_raw_examples` and the current training pipeline. The persistent receipt records hashes, sparse category IDs, order, geometry, image resolution, and metadata under the operator-selected ignored output root.
- [x] 6.4 Add a concise operator runbook for dependencies, launch/shutdown, project/profile configuration, Draft accumulation versus batch enqueue/terminal Commit, active/failed/reconciling recovery, output lag/locations, validation, known V1 exclusions, and safe runtime-only rollback.
- [ ] 6.5 Run targeted backend/frontend tests, browser E2E, real-profile smoke, separate durable-enqueue/background full-size batch performance gates, strict OpenSpec validation, source-residue checks, and final independent standards plus intent-contract audits with no unresolved P0/P1 findings.
- [ ] 6.6 Conduct user acceptance on a small train and validation slice and wait for explicit approval before treating the full projects as the active COCO refinement workflow or archiving this change.
