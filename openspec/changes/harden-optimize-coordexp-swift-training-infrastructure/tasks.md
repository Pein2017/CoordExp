> **2026-08-11 production-path disposition.** Complete Wave 7, then Wave 8,
> then Wave 9. Wave 3 through Wave 5 performance experiments are intentionally
> dropped. Waves 3 through 5 are closed without promotion; Wave 4 task 5.7
> cleanup and verification are complete. Checked disposition tasks record
> default retention, not an
> unexecuted performance result. Wave 6 remains
> explicitly pending for later design/research, retains
> `source_order_next_fit`, and does not block this Wave 7 -> Wave 8 -> Wave 9
> production path.

## 1. Wave 0 - Stabilize Authority And Freeze Measurement

- [x] 1.1 Confirm the exact implementation worktree, HEAD, dirty-state ownership, active GPU/process state, and existing artifact/cache roots before any apply action.
- [x] 1.2 Sync `streamline-coordexp-swift-base-infrastructure` into stable specs, pass all strict OpenSpec validation, and archive it as `2026-08-06-streamline-coordexp-swift-base-infrastructure` without losing its 29/29 artifacts or receipts.
- [x] 1.3 Record the committed stable-baseline and spec identities, inventory the current code/test owners for cache identity, Qwen attention, packing, protected losses, evaluation hydration, provider modes, checkpoints, and run artifacts, and identify the interface-level failing test or executed probe for every behavior-changing slice.
- [x] 1.4 Record ms-swift's exact selected reference-checkout identity and its explicit non-runtime role, plus the exact imported Transformers, FlashAttention, Torch, Accelerate, PEFT, and tokenizers source/binary identities, without changing the environment or dependency set.
- [x] 1.5 Create a change-local measurement contract that freezes workloads, pack streams, seeds, numerical tolerances, warm-up exclusion, paired arm ordering, wall-clock scope, resource ceilings, practical noise bands, repetition cap, GPU cost gate, and stop rules before candidate runs.
- [x] 1.6 Add failing artifact tests for clean/dirty repository provenance, dependency identity, unavailable identity, non-secret collection, phase status, resource high-water marks, and additive policy fields.
- [x] 1.7 Implement compact executed-code/dependency provenance and comparable phase/resource receipts without storing patches, credentials, or unrestricted environment dumps.
- [x] 1.8 Capture a compatibility baseline for cache preparation/admission, time to model load, time to first optimizer step, steady-state step wall clock, per-rank input build/wait, CPU RSS/I/O, GPU memory, pack utilization, eval hydration, and checkpoint publication using temporary output/cache roots.
- [x] 1.9 Gate Wave 0 with targeted artifact/config tests, receipt-schema validation, `openspec validate ... --strict`, residue and diff checks, and separate standards-compliance and intent/contract audit verdicts; do not continue with unresolved P0/P1 findings.

## 2. Wave 1 - Complete Cache Identity And Pre-Model Admission

- [x] 2.1 Add failing cache mutation tests for same-path tokenizer asset edits, processor asset edits, newly observed model-front-end assets, dataset-referenced image-byte edits, model/config asset edits, special/control-token ID changes, full realized vocabulary-group membership changes, model-logits precision and FA2-proof fields serialized into each micro-step, every unique source in an independently hard-coded complete renderer/parser/data-geometry/image-loader/pack/supervision/MRoPE/micro-step-constructor/schema/serializer owner inventory, unknown determinant owners, and retired cache versions; prove realized-vocabulary, image, discovered-file, and model-weight-index identities remain bounded.
- [x] 2.2 Implement a versioned cache-determinant registry with named owners, content identities, reasons, schema versions, and a stable aggregate fingerprint; bind and execute the production micro-step constructor and schema independently of the registry-derived mutation matrix, and replace superseded hand-maintained fingerprint knowledge at every caller.
- [x] 2.3 Publish and publicly admit v3 only under the caller-selected canonical `<cache-root>/coordexp-swift-pack-cache-v3/<64-hex-fingerprint>` target; require every public reader and writer to bind that selected root, reject symlinked cache-path components, authenticate and restricted-decode each chunk from one bounded stable byte snapshot, re-resolve every determinant after staged validation and immediately before no-replace installation, treat a valid target as a read-only hit, and reject determinant drift, noncanonical copied caches, unreadable asset subtrees, unsupported no-replace platforms, unknown, old, incomplete, mismatched, corrupt, or colliding targets without exposing a partial target or mutating, replacing, or deleting any existing cache directory.
- [x] 2.4 Add a model-free startup preflight that requires launcher `RANK` and `WORLD_SIZE` together for distributed launches, initializes one pre-Accelerator rank-zero artifact owner through a bounded CPU control plane, resolves tokenizer/processor identities, realized vocabulary groups, expected train/eval fingerprints and manifests, hydrates the current rank's required training payloads, and fully validates every eval payload before model/adaptor/optimizer/Accelerate setup, then rank-converges exact Accelerator identity equality before completing `cache_admission` or beginning model work. Retained rank-selective eval hydration remains owned by Wave 4.
- [x] 2.5 Add direct and distributed failure tests proving partial/malformed launcher identity and every missing, retired, unpublished, corrupt, or immutable-collision required cache exits before the model loader and finalizes the one shared failure artifact at `cache_admission`; a post-writer ownership-handshake failure must also finalize rather than orphan that artifact. Missing or retired targets emit the exact expected v3 target and preparation argv; an occupied invalid v3 target instead emits a stable collision code, exact target/version/fingerprint, bounded validation category, and `automatic_recovery: unavailable` without presenting the same-root preparation command as recovery. Rank reports carry only typed, length-bounded allowlisted diagnostic fields.
- [x] 2.6 Run bounded direct and multi-rank temporary-cache probes proving identical admission semantics, convergence of a rank-local required-payload failure, successful temporary-Gloo teardown followed by matching Accelerator identity, convergence of a rank-one-only Accelerator identity mismatch before model work, rank-selective required-training-payload validation, exactly-one no-replace publication for concurrent same-fingerprint writers, immutable cache hits, and no shared production-cache write.
- [x] 2.7 Gate Wave 1 with the complete cache/config/training test subset, mutation-test receipt, fail-before-model receipt, `openspec validate ... --strict`, residue and diff checks, and separate standards-compliance and intent/contract audit verdicts; do not continue with unresolved P0/P1 findings.

## 3. Wave 2 - Prove Every Qwen Text Layer Uses The Accepted FA Path

- [x] 3.1 Add failing tests showing that one matching FA varlen call, a missing layer, a duplicate layer, a mismatched boundary vector, a non-FA backend, or a vision-attention call cannot satisfy the text-layer proof.
- [x] 3.2 Implement typed attention-proof events that identify model topology, Qwen text layer, backend implementation, cumulative boundaries, maximum sequence length, dtype/device, and separately classified vision or unrelated calls.
- [x] 3.3 Replace the single-call proof with exact expected-versus-observed text-layer set/count admission and actionable failure diagnostics; keep proof mode bounded and disabled after admission in ordinary steady-state steps.
- [x] 3.4 Amend and independently audit the Wave 2 v3 contract before implementation: bind the exact immutable v2 workload/model/config/sample identities, new v3 plan/receipt schemas, one-shot eight-GPU-hour boundary, fixed `5e-3/5e-3` FP32-computed BF16-derived comparison, fixed `2.5e-3` same-packed repeat gate under plan-bound probe-only `FLASH_ATTENTION_DETERMINISTIC=1`, model-free structural 196/196/196/1 inventory plus pre-GPU exact 589-row runtime attestation, production streaming separate backward cadence, and no retry/sample switch/tolerance change/v4.
- [x] 3.5 Implement the v3 harness and validators: two identical packed arms that each clear gradients once before one forward/backward, a two-forward/two-immediate-backward separate arm with one initial gradient clear, the production BF16-autocast then graph-connected FP32 logits/loss route plus detached FP32 receipt observations, model-free structural inventory declaration followed by exact installed-name/shape/dtype attestation in the pre-GPU marker, coverage independent of parity, both packed-versus-separate comparisons, the repeat measurability gate, and rich passed/failed/unmeasurable receipts; keep historical v2 receipts readable without recomputing a legacy comparator or permitting retroactive acceptance.
- [x] 3.6 Add CPU-only execution and mutation tests proving exact inventory/name/shape/dtype coverage, stale packed gradients are cleared exactly once per arm, two observed streaming backwards in order, rejection of a retained summed graph, rejection of storage-dtype tolerance selection, rejection when only one packed repetition passes, complete failure evidence, immutable publication, pre/post-marker kill behavior and second-invocation rejection, and exact plan/receipt/workload binding.
- [x] 3.7 Add and execute a boundary-only negative control whose supervised logits or total loss leaves the frozen BF16 band; gradient-only detection MUST NOT satisfy the negative-control gate.
- [x] 3.8 Freeze and independently audit one exact v3 plan, command, absent receipt and attempt-marker targets, dependency/model/source identities, resource preflight, and one-shot consumption boundary, then execute at most one authorized real Qwen GPU probe and preserve its marker and terminal receipt without blind retry.
- [x] 3.9 Gate Wave 2 with targeted Qwen/packing tests, the real all-layer proof, bit-exact supervised-logit and accepted loss/denominator parity, same-packed repeat measurability, the retained packed-versus-streaming gradient diagnostic, negative-control and resource receipts, no-fallback inspection, strict OpenSpec validation, residue/diff checks, and separate standards-compliance and intent/contract audits. Apply the 2026-08-10 user decision that cross-shape BF16 backward equality is diagnostic rather than a release gate; preserve the failed v3 receipt without retry, rescoring, or threshold change, scope the accepted claim to forward/loss/FA2 semantics, and do not release Wave 3 with any other unresolved P0/P1.

## 4. Wave 3 - Remove Zero-Weight Autograd Work Without Changing Diagnostics

- [x] 4.1 Add failing protected-loss tests that compare zero-weight raw diagnostics with the current FP32 reference from the exact same content-bound frozen logits, targets, semantic atoms, denominators, and weights, reject a second-forward reference, and compare total loss plus every trainable gradient with a base-only objective.
- [x] 4.2 Implement the zero-weight token-type diagnostic from detached inputs or under `torch.no_grad()` while leaving every nonzero-weight differentiable path and normalization rule unchanged.
- [x] 4.3 Remove superseded zero-multiplication graph construction and caller assumptions; assert that the emitted zero-weight diagnostic has no autograd dependency.
- [x] 4.4 Apply the 2026-08-11 lead disposition to drop the production GPU performance comparison. Retain the detached/no-grad correctness implementation and its numerical/graph tests, but make no memory, timing, efficiency, or promotion claim and do not consume the prepared v4 marker.
- [x] 4.5 Close Wave 3 as `no_performance_promotion/default_semantics_retained`, preserving immutable failed historical attempts and the CPU launch-contract audit while requiring no new GPU performance receipt.

## 5. Wave 4 - Hydrate Only Required Eval State And Expose Startup Cost

- [x] 5.1 Add failing eval-cache tests for canonical ordinal indexing, disjoint rank assignment, exact cover, corrupt required shards, corrupt shards assigned to another rank, empty/fewer-pack fallback, and no partial global metric.
- [x] 5.2 Add a manifest-backed eval payload index and rank-selective loader that preserves the accepted modulo rank assignment, validates all manifest declarations, and hydrates only indexed steps referenced by the current rank; treat chunk skips as workload-dependent rather than guaranteed.
- [x] 5.3 Preserve a full-hydration compatibility reference for tests only and remove any production fallback that could silently hide a selective-loader failure.
- [x] 5.4 Add row-exact parity tests comparing selective and full hydration for pack IDs, canonical ordinals, predictions, loss components, aggregate denominators, checkpoint selector inputs, and metrics.
- [x] 5.5 Extend phase receipts to separate config/provenance resolution, cache identity resolution, publication admission, rank hydration, model load, first optimizer step, and steady-state; capture per-rank RSS/I/O and global maxima.
- [x] 5.6 Apply the 2026-08-11 lead disposition to skip the matched startup/resource experiment. Retain the current correctness-tested rank-selective eval hydration behavior, but make no RSS, I/O, startup, timing, or efficiency promotion claim.
- [x] 5.7 Close Wave 4 as `no_performance_promotion/current_default_retained` after removing or fail-closing incomplete performance-controller surfaces and re-running the targeted cache/eval/pipeline, strict OpenSpec, residue, diff, and independent contract gates.

## 6. Wave 5 - Decide The Production Input Provider From Eight-Rank Evidence

- [x] 6.1 Make legacy fused device-direct, current synchronous CPU-build-then-transfer, and depth-one bounded-overlap provider modes explicit strict-config values while retaining the current synchronous provider as the compatibility default and recording the resolved mode in every run receipt.
- [x] 6.2 Add exact provider-equivalence tests for pack IDs, tensor values and metadata, device-transfer ownership, loss components, optimizer steps, exception/cancellation propagation, end-of-stream behavior, and artifact/checkpoint inputs.
- [x] 6.3 Add boundedness tests proving one-item lookahead, declared host-memory ceilings, no cached GPU vision/hidden/trainable states, and clean worker termination on normal completion and failure.
- [x] 6.4 Apply the 2026-08-11 lead disposition to drop the eight-rank provider performance matrix without consuming a fresh marker; shared-load correctness/plumbing evidence is explicitly non-promotional.
- [x] 6.5 Preserve the immutable resource-stop and CPU contract packets as bounded historical evidence only; do not synthesize a provider-performance decision packet from them.
- [x] 6.6 Retain `synchronous` as the production default, leave alternatives explicit/experimental, and record `no_performance_promotion` rather than inferring a winner from unmatched or shared-load observations.
- [x] 6.7 Close Wave 5 on the targeted provider/runtime CPU suite, strict schema/static checks, and independent launch-contract audit; no eight-rank performance receipt is required by the revised disposition and no provider default changes.

## 7. Wave 6 - Compare Source-Order, Windowed Binpacking, And Online Packing

- [x] 7.1 Define a versioned `PackPlan` receipt/interface containing atomic image ordinals, intra-image sorted-row identity, encoded lengths, pack membership, within-pack image order, utilization, rejected examples, tie-breaker, seed, algorithm version, worker count disposition, and replay cursor.
- [x] 7.2 Retain `source_order_next_fit` as the compatibility implementation and add plan round-trip/replay tests that preserve fixed object/row order inside every image under the sorted policy, supervision mapping, segment isolation, and cache identity.
- [x] 7.3 Implement a deterministic fixed-window binpacking planner inspired by the pinned ms-swift offline comparator without importing its dataset/template ownership; bind every semantic parameter into strict config and cache identity.
- [x] 7.4 Implement a deterministic finite-lookahead online planner inspired by the pinned ms-swift iterable comparator with explicit memory/lookahead bounds, exact once-only input coverage, pending-window/cursor serialization, and explicit non-resumable rejection if replay cannot be proved.
- [x] 7.5 Run CPU-only planner comparisons over existing encoded-length evidence for utilization, pack count, tail waste, planning wall clock, peak RSS, cross-image order displacement, co-presentation changes, deterministic each-once coverage, exact intra-image row-order preservation, worker-count equality, and replay/resume behavior; do not train or write production caches.
- [x] 7.6 Produce a research-meaning packet that treats cross-image reorder/co-presentation as authorized scope, protects sorted object/row order inside every image, separates utilization evidence from optimization-dynamics risk, and identifies the smallest matched training comparison for surviving policies.
- [ ] 7.7 Run the smallest matched training comparison for every surviving changed-order policy with identical images, intra-image rows, tokens, optimizer budget, evaluation, and artifact semantics; reject any candidate with correctness, quality, reproducibility, boundedness, or end-to-end wall-clock regression.
- [ ] 7.8 Apply the lead-owned decision rule: a candidate may become a production policy only after the frozen semantic, quality, and practical end-to-end wall-clock gates pass; planner utilization alone is insufficient.
- [ ] 7.9 Apply that evidence-backed disposition and gate Wave 6 with packing/cache/config/training tests, planner and training receipts, `openspec validate ... --strict`, residue and diff checks, and separate standards-compliance and intent/contract audit verdicts; do not continue with unresolved P0/P1 findings.
- [x] 7.10 Record Wave 6 as `pending_future_design`: keep the authenticated CPU research packet and experimental policies, remove the unexecuted matched-training controller, retain `source_order_next_fit` as the production default, and make Wave 6 nonblocking for the current Wave 7 -> Wave 8 -> Wave 9 path without marking tasks 7.7-7.9 complete.

## 8. Wave 7 - Add Exact Same-World-Size Training Resume

- [x] 8.1 Implement the delegated first-version contract in strict config and tests: opt-in, same-world-size, optimizer-step-boundary saves, every trainable surface plus optimizer/scheduler/scaler/RNG/cursor state, frozen-base content binding without weight duplication, atomic `training_state/` publication, and append-only run lineage.
- [x] 8.2 Measure checkpoint size, publication latency, and cadence cost; prove the inference payload remains independently loadable and the first contract performs no automatic pruning of committed exact-state checkpoints.
- [x] 8.3 Add failing tests that reject inference-minimal, incomplete, corrupt, uncommitted, wrong-world-size, wrong-cache, wrong-policy, wrong-topology, wrong-trainable-set, and unsupported dependency checkpoints before any mutable state restore.
- [x] 8.4 Implement a versioned exact-state manifest and atomic distributed publication at optimizer-step boundaries, including every rank-owned RNG component and the exact data/pack cursor; retain inference checkpoint behavior unchanged.
- [x] 8.5 Implement fail-closed resume admission and restore, with explicit field-level mismatch diagnostics and no fallback to restart or model-only loading.
- [x] 8.6 Implement additive continuation run segments with parent run, checkpoint identity, and monotonic continuation index; remove any superseded caller assumption that appends ambiguously into a completed run.
- [x] 8.7 Prove an uninterrupted run against an interrupted-and-resumed run at the supported world size, comparing next pack IDs, accumulation/optimizer positions, RNG-derived behavior, learning rate, losses, gradients or parameter updates, checkpoint selection, and final artifacts within frozen tolerances.
- [ ] 8.8 Prove incomplete-rank publication and interruption failure behavior, exact checkpoint readback, no parent overwrite, and explicit rejection of unsupported mid-accumulation save.
- [ ] 8.9 Gate Wave 7 with checkpoint/runtime/artifact/config tests, interruption receipts, storage/publication measurement, `openspec validate ... --strict`, residue and diff checks, and separate standards-compliance and intent/contract audit verdicts; do not continue with unresolved P0/P1 findings.

## 9. Wave 8 - Freeze The Upstream And Attention-Backend Baseline

- [x] 9.1 Record the user decision that this change keeps the installed Transformers, FlashAttention, Torch, CUDA, Accelerate, PEFT, tokenizers, and FA2 route unchanged; record their exact imported source/binary identities.
- [x] 9.2 Add strict-config and provenance tests that reject an unknown or unapproved backend, expose drift in the actual imported runtime dependency/source/binary set before cache preparation or model setup, persist every supported semantic override source, and keep reference-only ms-swift drift provenance-visible rather than making it a training admission dependency.
- [x] 9.3 Re-run cache admission, real packed forward/backward parity, all-layer attention proof, protected-loss, and exact-resume compatibility against the pinned baseline after the Wave 7 gate and retained production-default compatibility owners converge. Wave 4 task 5.7 is a Wave 9/10.1 cleanup precondition, not a Wave 8 precondition; Wave 6 tasks 7.7-7.9 are explicitly deferred and nonblocking.
- [x] 9.4 Record ms-swift and Transformers implementation reference points for future work without installing, selecting, benchmarking, or promoting an upgraded stack in this change.
- [ ] 9.5 Gate Wave 8 with the pinned compatibility matrix, dependency/environment residue checks, `openspec validate ... --strict`, and separate standards-compliance and intent/contract audit verdicts; do not continue with unresolved P0/P1 findings.

## 10. Wave 9 - Converge The Approved Production Infrastructure

- [ ] 10.1 Confirm every accepted cache determinant and production policy owner has settled, Waves 3 and 5 are closed without promotion, Wave 4 task 5.7 has completed its no-promotion cleanup gate, Wave 6 remains isolated as pending future research with `source_order_next_fit` unchanged, all decision-bearing Wave 7 and Wave 8 gates are green, and no production-path P0/P1 finding remains.
- [ ] 10.2 Produce the production-transition packet with the exact new cache version/fingerprint, build command, source identities, expected CPU/I/O/time/storage cost, immutable destination, rollback route, and GPU launch budget.
- [x] 10.3 Record the 2026-08-06 user authorization for one production cache-version materialization window and the final multi-GPU convergence run after all preconditions pass; re-open the gate only if the actual scope or cost exceeds the frozen envelope, and never mutate or delete an old production cache.
- [ ] 10.4 Materialize and validate the new train/eval production caches once, atomically, after all accepted owners settle; record content, manifest, rank-selective load, and admission receipts.
- [x] 10.5 Run the full authorized eight-rank production-like train path through preflight, model load, all-layer FA proof, steady optimizer steps, sharded evaluation, inference checkpoint publication, and the enabled Wave 7 exact-resume interruption/continuation.
- [ ] 10.6 Compare the converged result with the Wave 0 reference for protected input/pack identities, losses, gradients or parameter updates, metrics, checkpoint semantics, and artifact lineage. Report shared-load timing/resources descriptively only and do not reopen or backfill the dropped Wave 3-5 or pending Wave 6 performance questions.
- [ ] 10.7 Update canonical operator docs and config examples for only the accepted behaviors, delete superseded shims/debug selectors/caller knowledge introduced or replaced by this change, and keep rejected comparators clearly experimental or remove them.
- [ ] 10.8 Run the complete relevant unit/integration suite, strict config matrix, cache mutation suite, real parity probes, resume probes, full-pipeline smoke, `openspec validate harden-optimize-coordexp-swift-training-infrastructure --strict`, `git diff --check`, and task-created residue checks.
- [ ] 10.9 Obtain separate final standards-compliance and intent/contract audits over code, configs, tests, specs, receipts, docs, protected semantics, promotion decisions, and claim boundaries; resolve every P0/P1 before presenting the change as archive-ready.
- [ ] 10.10 Present the final evidence/decision ledger to the user, including accepted and rejected waves, unchanged defaults, production-cache transition, remaining limitations, exact-resume scope, upstream disposition, and safe rollback; archive only through the separate approved OpenSpec archive workflow.
