## 1. Freeze Interface And Red Tests

- [x] 1.1 Add interface-level failing tests for every typed binding kind,
  deterministic directory inventory, destination-relative resolution, absolute
  executable identity, strict-value rejection before root creation, and full
  live revalidation drift.
- [x] 1.2 Add failing lifecycle tests for reserved absent roots, exact two-stage
  order, durable CPU evidence across vertical failure, exact-identity
  continuation, required producer/validator/output closure, and idempotent late
  final admission publication.
- [x] 1.3 Record the two read-only consumer source/test identities and the
  pre-change active-worktree status; classify the existing archived
  source-binding fixture path failure without altering its scientific inputs.

## 2. Typed Binding Owner

- [x] 2.1 Implement immutable regular-file, directory-tree,
  resolved-data-file, absolute-executable, strict-value, and reserved-path
  request/value types in the stable artifact owner.
- [x] 2.2 Implement strict in-memory capture before root creation, deterministic
  full relative-path tree inventory, canonical manifest fingerprinting, and
  typed fail-closed errors for every missing, wrong-kind, symlink, live-object,
  and duplicate-name path.
- [x] 2.3 Implement complete live manifest revalidation and exclusive canonical
  binding-manifest publication without adding a cache, short-digest identity,
  PATH lookup, Git authority, or scientific schema.

## 3. Journal-Backed Admission Lifecycle

- [x] 3.1 Implement admission creation/opening over an unchanged nested
  `ExecutionEvidenceJournal`, with immutable plan/context identity, exactly
  `cpu_preflight` then `vertical_smoke`, and reserved paths validated only at
  the pre-work boundary.
- [x] 3.2 Implement fixed CPU and vertical mechanics-envelope validation,
  producer/validator binding resolution, output-file capture and revalidation,
  stage-order enforcement, independently durable stage append, and no automatic
  retry or continuation.
- [x] 3.3 Implement terminal inspection and idempotent write-once
  `mechanically_admitted` projection that survives late publication failure and
  excludes scientific validity and launch authority.
- [x] 3.4 Export the smallest public admission surface and add residue tests
  proving journal schema version 1, existing execution-context absence behavior,
  and science vocabulary ownership remain unchanged.

## 4. Two Consumer Adapters

- [x] 4.1 Add a natural-boundary support admission example that translates its
  current sealed-plan/source/runtime/model identities, a CPU legacy-materializer
  plus unchanged-merger fixture, and a bounded durable terminal plus
  mechanics-only terminal validator into the shared interface without changing
  scheduling or receipt semantics.
- [x] 4.2 Add a K10-H20 crossover admission example that translates its current
  planner/sealer CPU preflight, runtime identity, finalizer fixture, and evidence
  validator without copying owner, endpoint, qualification, or tau semantics
  into stable infrastructure.
- [x] 4.3 Replace the stale test-only reference to the pre-archive
  `source-bindings-v3.json` location with its accepted archived provenance path,
  then prove both adapters remain read-only against the active sibling and
  sealed roots.

## 5. CPU Production-Path Gate

- [x] 5.1 Run focused artifact/admission tests covering present, absent, drift,
  corrupt, unsupported-value, partial-stage, terminal, and idempotent
  finalization paths, including fresh-process continuation.
- [x] 5.2 Run the current support adapter/worker/source-binding regressions and
  the sibling crossover production-path CPU suite; preserve separate consumer
  validators and record exact pass/failure receipts.
- [x] 5.3 Run relevant inference execution-context, artifact, worker, and merge
  regressions plus Ruff, compileall, `git diff --check`, and strict OpenSpec
  validation; close all deterministic failures before GPU use.

## 6. Bounded Single-GPU Vertical Gate

- [x] 6.1 Freeze exact implementation, consumer, adapter, model, config,
  runtime, device, and fresh output-root identities; verify GPU ownership and
  select one explicit physical device with child logical `cuda:0`.
- [x] 6.2 Execute one bounded existing support worker path, require at least one
  independently durable work item and current bounded terminal plus
  mechanics-validator closure, then publish the vertical stage and final
  admission receipt in a fresh change-owned root without claiming legacy
  merger closure.
- [x] 6.3 Preserve a mechanics-only GPU receipt and verify model/GPU cleanup,
  active-unit non-mutation, sealed-root immutability, and absence of scientific
  claims or bulk-launch authorization.

## 7. Fixed-Tree Verification And Audit

- [x] 7.1 Run the OpenSpec completeness/correctness/coherence verification and
  create a self-excluding fixed-tree manifest over implementation, adapters,
  tests, CPU/GPU receipts, and planning artifacts.
- [x] 7.2 Run one independent fixed-tree engineering review and one
  intent-contract audit over the same identity; accept or reject each P0/P1
  finding and allow at most one bounded implementation correction before full
  revalidation.
- [x] 7.3 Record the final audit verdict, verified commands, residual risks,
  routing disposition, and explicit no-merge/no-push/no-archive state for user
  handoff.
