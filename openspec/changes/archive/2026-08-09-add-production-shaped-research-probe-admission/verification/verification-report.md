# Verification report

## Accepted fixed point

- The shared admission owner provides typed pre-root binding capture, complete
  live revalidation, reserved-output admission, exactly ordered durable
  `cpu_preflight` and `vertical_smoke` stages, and an idempotent mechanics-only
  terminal receipt.
- The natural-boundary support adapter closes a connected CPU
  `materialize_legacy_shard_receipts` to unchanged-merger fixture and a bounded
  production worker vertical path. The K10-H20 crossover adapter closes its
  current CPU preflight and finalizer/validator compatibility only; it has no
  vertical stage and is intentionally not mechanically admitted.
- Infrastructure/admission/support tests passed with `104 passed`; relevant
  inference artifact/context/worker/merge regressions passed with `172 passed`;
  the sibling crossover CPU suite passed with `192 passed`; and the unchanged
  sibling support consumer suite passed with `10 passed`.
- Ruff, compileall, `git diff --check`, strict OpenSpec validation, and the
  connected full-plan support materializer-to-merger test passed.

## Durable CPU and GPU evidence

- The support CPU receipt binds three successful commands: current consumer
  validator `10 passed`, connected model-free finalizer `1 passed`, and the same
  invocation's unchanged downstream merger `1 passed`. Its raw SHA-256 is
  `a358d3102c0b38713a70bc790b1e6a1ff594468dbf327577811b36e7f96d3f62`.
- The crossover CPU receipt binds its real source preflight through the current
  sealer deep validator, its model-free finalizer fixture, and its unchanged
  evidence validator, each with one successful command. Its raw SHA-256 is
  `aaa4eb1ac4c5ad2ba3be2d8a8ffe7141f3cfd187984cce0affaef81d5076c501`.
- The single-GPU gate used physical GPU `0` and child logical `cuda:0`. It ran
  exactly one context (`ctx:0a055737dcb38ca92bb8bce5`), recorded `17`
  scalar-equivalent forwards, durably published one journal record, returned
  code `0` without a signal or retry, materialized a bounded terminal, and
  published a mechanically admitted support receipt. The mechanics receipt raw
  SHA-256 is
  `31ca56dede41811c668c9ba9facb35fa93231483cbb9f728391e6d7bda4b8bc2`.
- GPU processes were absent after completion. The active sibling HEAD and dirty
  status hashes were identical before and after the gate; no sealed research
  root was mutated.

## Independent audits

- A Sol/xhigh fixed-tree engineering audit returned `PASS` with no P0, P1, or
  P2 findings. It independently verified all `39/39` preaudit entries, canonical
  artifact JSON, live admission state, focused tests, strict OpenSpec, and the
  intended support-versus-crossover terminal distinction.
- A Terra/high intent-contract audit returned `PASS` with no P0/P1 finding. Its
  one P2 metadata finding was that the preaudit manifest's algorithm label
  conflated per-file identity with manifest-content hashing. The final manifest
  separates per-file SHA-256, entry-set SHA-256, and self-excluding manifest
  content SHA-256.
- Routing evidence remains lane-specific: Luna/max was effective for bounded
  OpenSpec and receipt auditing and found actionable strict-value/stale-receipt
  defects; Sol/xhigh was valuable for silent verifier-identity and final
  engineering invariants; Terra/high owned lifecycle and intent-boundary review.
  This does not support one global model default.

## Claim boundary and handoff state

The evidence establishes production-shaped admission mechanics and compatibility
for the named consumers. It does not establish a model mechanism, support
prevalence, K10-H20 causal validity, legacy-merger closure for the one-context
GPU smoke, scientific interpretation, automatic retry/continuation, or bulk
launch authority. The crossover path remains CPU-only.

The worktree is intentionally uncommitted. No push, archive, merge,
cherry-pick, or modification of the active `research-probes` unit was performed.
