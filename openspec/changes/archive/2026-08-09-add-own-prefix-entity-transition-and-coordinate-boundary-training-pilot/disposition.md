# Disposition

Status: `archive_with_historical_exceptions`

The user selected archival on 2026-08-09. Tasks `0.1` and `1.1` remain
intentionally unchecked:

- `0.1` required a shared immutable fixture before implementation worktrees
  were forked. That sequencing requirement cannot be satisfied retroactively.
- `1.1` was conditional on that missing fixture. Its substantive contract
  coverage exists in the implemented test surface, but marking the original
  conditional task complete would rewrite the historical process record.

The reusable own-prefix calibration and supervision-loss contracts remain
implemented, so their delta specs are synchronized into the main specs during
archive. This disposition does not promote the closed research treatments,
convert the two process exceptions into positive evidence, or authorize new
training.
