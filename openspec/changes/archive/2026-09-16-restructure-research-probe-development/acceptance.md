# Slice acceptance

Implementation and authorized retirement are complete. Acceptance is bounded to the checks below; it does not certify untested model profiles or new scientific results.

## Aligned scoring and unused scaling diagnostic

Lead inspected the seven-file change and the actual runtime neutral-accumulation guard. Aligned token scores preserve device/gradients and expose no reduction. Packed CE uses the same operation. Removed the definitions/exports/two dedicated tests of the unconsumed scaling diagnostic; no runtime guard was removed.

- Before change: packed full/compact gradient characterization and normalization/runtime checks,44passed.
- Worker final: `python -m pytest -q tests/losses tests/runtime/test_train_runtime.py`,111passed.
- Lead replay: `python -m pytest -q tests/losses/test_token_scores.py tests/losses/test_context_and_terms.py tests/runtime/test_train_runtime.py::test_accelerator_accumulation_must_be_neutral`,26passed.
- Standards: scoped diff, meaningful caller/gradient checks, and source/API residue checked.
- Intent: fewer caller assumptions and removal of an unused receipt with the real scaling guard retained. Lead-accepted for tasks5.1/5.2.

## Assignment and independent row-cross reducer

Extracted the existing cardinality-first, quantized-IoU matcher and pixel IoU without changing the greedy visualization contract. The reducer now uses same-checkout parsing/matching and an explicit historical-code root; seven old manifest bindings were verified from preserved bytes and never imported.

- Worker targeted suite:27passed, including original matcher characterization.
- Original and migrated saved-input reduction agree on every historical result field:4 qualification cases,16 complete outputs. The added `execution` field distinguishes current reducer identity.
- Lead executed the documented CLI into `preservation-20260909/row-cross-lead-replay`; exact equality after removing only `execution` was asserted.
- Worker independent-copy run forbade all original-worktree file opens and had no scripts directory; same result. This is offline qualification-subset acceptance, not final32-case or model evidence.
- Standards: public owner, original counterexample, no import fallback and exact saved-input consumption verified. Intent: shared assignment plus usable package without sibling code. Lead-accepted for tasks3.1-3.4.

## Maintained direction packages and research profile entry

The four maintained direction packages are independently importable and have no
`scripts.research` imports. Lead package verification passed 87 tests in
package-specific invocations: DORA 16, Human13 26, logit-lens 23, and
Source/Rweak row-cross 22. The package test roots are run separately because
their local `tests.*` module names collide when collected as one combined
pytest invocation.

Human13 and logit-lens preserved independent receipts are under
`/data/CoordExp/outputs/research/restructure-research-probe-development/
preservation-20260909/` (`human13-independent.json` and
`logit-lens-independent.json`). Their package entries compare against the
original effective-config snapshots, including `debug.smoke=False`, and use
the shared V1 resolution/value/fingerprint code without production-only leaf
authoring rules. DORA's independent CPU package verification and the
Source/Rweak saved-input receipts were separately replayed by lead. No model
or data semantics were changed by package organization.

## Explicit parameter and attachment inputs

Existing config entry points now use the same attachment/group core as direct research callers. Named parameter selection preserves identity/order without changing gradient flags; state-key normalization is the unchanged transformation exposed publicly.

Worker pre-change122passed/2failed and post-change126passed/the same2failed. The two source-gate cases reference absent historical owner-commit-binding paths; their checks were not relaxed. Lead inspected all three core diffs and replayed adapter setup/reload, parameter groups and special-embedding checks, excluding those exact pre-existing failures:85passed. Direction consumers use direct public attachments/selection; simple native AdamW remains valid. Standards and intent both accepted for the changed inputs; no claim that the external source-gate fixture failures are repaired.

## Native Qwen and strict HF adapter

Lead inspected native ownership/imports and exact replay/generation contracts and replayed the focused native/generation/capture/HF suite:53passed. Real RED evidence included inference-mode positions failing backward and three public HF post-EOS/error-family regressions that passed unchanged HEAD, failed the intermediate adapter and pass the final adapter. Strict trace retains padded emitted steps; ordinary continuation stays trimmed and may omit traces. No global context or mandatory image hashes were added.

The broader baseline145passed/38failed was reproduced on untouched HEAD. The obsolete test_backend_trace module targeted already removed APIs (0passed/34failed); four old owner_bridge configs were invalid under the existing strict schema and had no current caller beyond mutual inheritance. Those five historical surfaces were removed after coverage comparison. Current surviving config/HF/native suite:129passed. Archived refs preserve their original sources; no removed backend API was reintroduced.

The bounded real-entry Source smoke then completed through
`probes/source_rweak_row_cross/run.py` using the preserved Source checkpoint,
DoRA adapter, selected-embedding delta, native Qwen execution, disk parsing,
and matching. Receipt:
`preservation-20260909/source-native-smoke-67d305d57/receipt.json`.
The frozen case `coco2017_val_000000322574` generated exactly 21 tokens,
terminated at `im_end`, performed 11 model forwards and one image forward,
and completed in the reported 11.11-second wall bound. The receipt records
`git_revision=67d305d57`, `git_dirty=true`, the manifest digest, checkpoint
identities, and no diagonal token mismatches. This closes the bounded
model-semantic smoke for the retained entry; it is one source case and does
not establish scientific performance or population validity.

## Row-cross native entry (CPU acceptance)

The prepare entry reconstructs all32 frozen cases byte-for-byte. Source/Rweak preflights and cached real Qwen processor checks reproduced the four qualification prompts/grids/RGB identities without weights. Actual CLI through a tiny Transformers model, native generation, disk parsing and matching passed; lead replayed the full package suite with saved-input bindings:22passed. The preserved real Source smoke above now supplies the one-case model-path evidence. Native execution remains reported separately from historical bindings, and no receipt-only HF session is constructed.

## Integrated and post-retirement acceptance

Default suite: `python -m pytest -q` completed with **1,432 passed, 14 skipped, 4 failed** (84.72s). Failures are the existing active-profile baseline set mismatch, missing step-917 embedding-delta benchmark directory, and two absent `owner-commit-binding` source-gate checks. Their assertions were not weakened; the active-profile test/config surface and benchmark readiness test are unchanged from the preserved base. Raw failure evidence is `integrated-pytest.log` in the preservation root. An isolated extraction of the original archived base reproduced exactly the same four failed test names (5.83s; `original-four-failures.log`). This is not an all-green full-suite claim.

After deleting all eight source worktrees, the full four-package suite with frozen row-cross manifest/code bindings passed **87 tests**. Independent Human13 and logit-lens copies then passed **26** and **23** tests plus their actual CPU preflights and exact saved-reader comparisons. Both lacked a `scripts` directory and forbade original provider opens. This directly checks retirement did not leave hidden executable dependencies. Logs are `post-retirement-{probes,human13,logit-lens}.log`.

Knowledge intake: 109 changed/new Markdown files (excluding the explicitly historical compass snapshot) have no missing local link targets. The current compass, overview and navigation distinguish N32's absent null result, CPU-only feedback, selected-panel row-cross, and the separate operational benchmark. Human13 panels retain their respective 65/123/392 owners at each reported IoU threshold. No new scientific acceptance is inferred.

The raw/prepared optimizer boundary has a standalone real CPU Accelerate/AdamW caller test (`tests/runtime/test_real_optimizer_handle.py`), one update passed. Core packed/runtime/loss tests remain in the default suite. No extra training abstraction or eight-rank acceptance is claimed.

## Historical closure and final disposition

Final contraction removes **118 historical producers and 110 dedicated tests** (228 files), all recoverable at the original research-base archive ref. **54 scripts remain** for the existing optional admission/evidence and test consumers, coverage and graph checks. The first retained-suite collection exposed mixed test dependencies; exact original bytes of 18 producers and one dynamically loaded test were restored. No test assertion was relaxed. The [exact disposition](retired-files.md) documents this retained obligation.

The corrected `tests/research tests/analysis` run finished with **1,088 passed, 55 skipped, 30 failed, 90 errors** (74.62s), with no collection/import failures. Lead inspected error lines: all remaining failures originate in missing dated external evidence under the 2026-08-01/05/06/07 output roots, including expected-error assertions intercepted earlier by those missing paths. These are not source-closure acceptance and are not reported green. Full evidence: `retained-research-pytest-after-closure.log`; compact path summary: `retained-research-pytest-missing-artifacts.log`.

Final checks: source refs/removed branches match all eight saved commits; six saved output symlinks remain accessible; research graph passes (6 decisions, 6 edges); change and both synchronized main specifications pass strict validation. Out-of-scope shared configuration deletions are left unstaged. All changes are local; nothing was pushed.

Standards conformance: public interfaces have caller-level falsification, optional strict contracts retain their failures, exact historical bytes/identities remain recoverable, and retirement has direct post-removal consumer checks. User-intent conformance: one protected research base, four substantive packages, reduced default historical surface, question-oriented knowledge intake and no added generic runtime or layer-2 delegation. Engineering change accepted; scientific and full-model limits remain explicit above.
