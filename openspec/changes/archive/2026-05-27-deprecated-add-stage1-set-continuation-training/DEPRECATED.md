# Deprecated

This incomplete change is archived as historical Stage-1 set-continuation
context.

Reason: remaining tasks are broader train-forward/runtime optimization and smoke
work, not required for the unified generation/decoding closeout.

Do not finish this change in place. Open a new narrower change if Stage-1
set-continuation runtime work becomes active again.
