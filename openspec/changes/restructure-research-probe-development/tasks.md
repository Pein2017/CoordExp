## 1. Preservation and implementation entry — lead

- [x] 1.1 Record the user's stop and implementation authorization, current scoped refs/status and relevant live holders; verify self-rollout is at the committed clean tip and exclude unrelated/shared configuration changes from the write set.
- [x] 1.2 Preserve effective source/config/test closures with explicit paths and recoverable versions, using fresh clean source tips and committed self-rollout/COCO dependencies; verify sampled recovery includes content that was absent from historical HEADs.
- [ ] 1.3 Preserve necessary ignored/local outputs separately from Git, prioritizing DORA's real `outputs/`; verify cited evidence can be opened without the retiring directory and record original/new locators.
- [x] 1.4 Reconcile exact overlap with other open changes and establish exclusive worker paths from design section 8; verify no unfinished scientific route or unselected historical producer is marked implemented by this change.

## 2. Knowledge consolidation — K, parallel with code after preservation

- [ ] 2.1 Select original records across all seven directions by purpose and ancestry/blob identity; verify the selected-file list excludes producer code and separates the self-rollout agent benchmark from science.
- [ ] 2.2 Return unique results/protocols and update experiment navigation; verify all directions have source-linked dispositions and partial N32, CPU-only feedback and selected-panel row-cross evidence retain their limits.
- [ ] 2.3 Rewrite compass and reconcile overview/index around current questions, evidence limits and discriminators; verify every substantive conclusion links its source and decisions change only with supported route changes.
- [ ] 2.4 Validate selected links and necessary artifact locators; verify obsolete current summaries/routes are removed while original facts and conflicting scopes remain recoverable.

## 3. Assignment and first offline package — E

- [x] 3.1 Freeze current global assignment behavior using the existing cardinality-first counterexample plus tie/quantization/category/threshold cases; verify the oracle distinguishes the retained greedy visualization algorithm.
- [x] 3.2 Move global assignment to `src/eval/assignment.py` and shared pixel IoU to the geometry owner, updating retained callers; verify current coverage fixture metrics and visualization tests preserve their separate semantics.
- [x] 3.3 Migrate committed Source/Rweak reduction into `probes/source_rweak_row_cross/reduce.py` with native parser/assignment and local scientific attribution; verify its existing direct-replacement/global-reassignment counterexamples and saved-input reduction match before removing COCO imports; resolve original manifest code-path/hash bindings from preserved sources without rewriting old receipts.
- [x] 3.4 Establish package discovery and one documented offline command with the lead-owned Git/pytest integration; verify it in an independent checkout without sibling code and run targeted tests, residue checks and strict OpenSpec validation. Record this as offline acceptance, not model acceptance.

## 4. Native model operations — M

- [x] 4.1 Characterize exact-history preparation and position behavior in HF, DORA and row-cross callers; add a tiny-model forward/backward sensitivity check that catches causal shifts and inference-mode tensor leakage before extracting implementation.
- [x] 4.2 Implement cohesive Qwen-native input/history/forward operations beneath the strict HF adapter; verify literal IDs, image grids, stale-cache behavior, gradients and existing HF exact-history/position tests, then delete replaced native-input/position implementations.
- [x] 4.3 Move row-cross exact continuation to public native generation, including heterogeneous padding/budgets, terminal-action no-forward behavior and disabled traces; verify tiny-model request/result parity and the existing committed row-cross tests before removing private HF and COCO runner dependencies.
- [x] 4.4 Migrate seeded rollout while preserving DORA's raw-softmax/top-k/config differences and fixed-seed/fixed-batch behavior; verify resolved policy and raw/policy score separation without changing deterministic scored-inference contracts.
- [x] 4.5 Extract named Qwen capture/cleanup used by logit-lens and Human13; verify pre-injection clone sensitivity, exception cleanup and repeated calls while leaving intervention formulas local.
- [ ] 4.6 Exercise the migrated real package preflight/evidence consumer and relevant HF tests; define and run any necessary bounded model smoke before claiming model-semantic parity, with explicit inputs and resource bounds. Do not substitute helper tests for that claim or launch a scientific rerun.

## 5. Learning and parameter foundations — L and P, disjoint from M

- [x] 5.1 Add aligned differentiable token scoring below packed `LossContext`, adapt matching packed CE use and retain explicit caller reductions; verify score/gradient parity, full/compact causal positions and DORA CE/RLOO versus coordinate/full-action formulas.
- [x] 5.2 Remove unused `BackendScalingReceipt`, its validator/private helper and dedicated exports/tests after replaying scoped reachability; verify the actual runtime extra-accumulation rejection, planned-step normalization and runtime sequencing remain intact.
- [x] 5.3 Expose adapter parameter selection at the existing owner and accept explicit optimizer groups without unused embedding settings; verify order, uniqueness, exact trainable coverage and Human13 raw-AdamW/prepared-wrapper CPU acceptance.
- [x] 5.4 Refine adapter/embedding attachment inputs so native research does not build whole inference/train configs merely to load a model; verify existing attachment validation, selected parameter identity and persisted optimizer/adapter continuation checks. P owns these files exclusively and hands signatures to M.
- [ ] 5.5 Retain concrete packed training/runtime/calibration behavior and move only selected Human13 arm composition to its direction owner; verify existing streamed-gradient/strict-crossing tests and no new universal training/context layer appears.

- [x] 5.6 Share direction-local inference-profile resolution without changing effective configuration or debug flags; verify the copied-profile failure first, equal fingerprint/config values after the fix, inherited profiles and preserved production/unknown-field rejection.

## 6. Complete four maintained direction packages — D, after required M/L/P interfaces

- [ ] 6.1 Migrate DORA Source256 preparation/training and distinct self owner-outcome objective consumer into `probes/dora_owner_learning`; verify explicit profiles, DDP reduction/no-sync behavior and no sibling-script imports, preserving old producer identities historically.
- [ ] 6.2 Complete `probes/source_rweak_row_cross` preparation/runner/reducer/local checkpoint validation with M/E ownership handoff; verify its offline and model-path checks, per-row budgets, scientific attribution and benchmark separation.
- [ ] 6.3 Migrate retained logit-lens base and successors into `probes/logit_lens`; verify package-local imports, named-site tests and old hash-bound sources remain separately recoverable.
- [ ] 6.4 Migrate Human13 output-QP/magnitude intervention and required panel/runtime helpers into `probes/human13`; verify finite-panel entry/config tests and no silent merger with N256 solver semantics or historical duplicates.
- [ ] 6.5 Document one real invocation/config per maintained entry with research-owner links; verify explicit package test discovery and independent-checkout imports/offline entries. Do not create packages for unselected historical directions or empty future helpers.

## 7. Local workflow simplification and default-tree contraction — lead plus returned owners

- [x] 7.1 Update branch policy, infra guide, project context and OpenSpec config for the single base, selective knowledge intake, substantive synthesis updates and direct ordinary execution; verify no local default requires whole-tree admission for an ordinary producer.
- [ ] 7.2 Simplify migrated producer artifact calls to basic run context and existing leaf operations; verify unrelated dirty files do not force admission, unrequested chains/journals are absent and selected strict publication/recovery counterexamples still fail correctly.
- [ ] 7.3 Remove preserved unselected historical producers and their dedicated configs/tests/docs only after retained dynamic/import/persisted-reference closure checks; verify every remaining experimental implementation has a maintained direction owner and historical recovery locations are explicit.
- [ ] 7.4 Check replacement residue and maintenance reduction across code, exports, configuration and dedicated tests; verify no duplicate implementation or compatibility shim remains without a documented retained obligation. Keep the rejected cuts listed in design unchanged.

## 8. Retirement and integrated acceptance — lead

- [ ] 8.1 Recheck each eligible direction's source recovery, knowledge intake, output accessibility, dirty content and holders immediately before retirement; verify DORA/self and COCO/self code dependencies are resolved or historically preserved first.
- [ ] 8.2 Retire the former infra lane after content handling and rule updates; verify the fixed research base lock, production worktrees, excluded bridge-cache tree, remote refs and shared configuration remain unaffected.
- [ ] 8.3 Run final relevant interface/package checks, independent-checkout entry tests, link/residue checks and strict OpenSpec validation; record standards and user-intent conformance separately, with no unresolved blocking counterexample.
- [ ] 8.4 Commit accepted changes in explicit logical batches, preserving unrelated dirt; report commits, maintained packages, removed surfaces, historical recovery/evidence locations and exact CPU/model validation scope. Mark only fully satisfied tasks complete.
