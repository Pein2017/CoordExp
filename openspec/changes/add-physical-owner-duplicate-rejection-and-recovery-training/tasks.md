## 1. Freeze Evidence and Synthetic Contract

- [x] 1.1 Add synthetic `normal A -> normal B -> duplicate B -> duplicate B -> normal C -> normal D` fixtures that preserve original and replay prefix identities.
- [x] 1.2 Add failing StateBank tests for duplicate-trajectory provenance, strict owner eligibility, row-deletion receipts, burst credit, and historical-profile isolation.
- [x] 1.3 Add failing pure-loss tests for field-balanced complete-row scores, positive and duplicate gradient directions, finite 32-bit floating-point math, and row-length invariance.

## 2. Implement Typed Duplication Events and Loss

- [x] 2.1 Extend strict config with opt-in local, cleaned, and combined duplication profiles while preserving all historical profiles.
- [x] 2.2 Add typed duplicate-trajectory evidence and event-family flags to StateBank validation, serialization, replay metadata, and record counts.
- [x] 2.3 Implement the pure field-balanced duplicate rejection and recovery loss with separate description and coordinate diagnostics.
- [x] 2.4 Wire the local term and cleaned complete-row family through loss planning, event weights, denominators, post-update margin replay, and run diagnostics.
- [x] 2.5 Add deterministic family-stratified scheduling and fail-fast validation for every multi-family profile, including combined and local duplicate-rejection windows.

## 3. Build the Experiment-Local Data Factory

- [x] 3.1 Add one duplicate-trajectory StateBank assembler that consumes exact rollout rows and a reviewed physical-owner ledger.
- [x] 3.2 Emit immutable Source-preservation-only, recovery-positive-only, local rejection and recovery, duplicate-cleaned imitation, and combined matched banks plus one allocation receipt.
- [x] 3.3 Prove one credit unit per burst, image balancing, exact integer row deletion, first-owner retention, uncertainty exclusions, identical Source dose, and matched optimizer windows in focused tests.

## 4. Execute the Representative Real Smoke

- [x] 4.1 Create crop-reviewed owner labels for the initial mechanism roster and select one trusted duplicate-then-recovery trajectory for smoke.
- [x] 4.2 Build tiny matched banks and run one optimizer update for each trained control and treatment.
- [x] 4.3 Verify finite gradients, expected local margin direction, effective burst weights, checkpoint reload, and ordinary clean greedy inference. Deliberately omit a separate before-versus-after cleaned-row teacher-forced loss replay because ordinary clean rollout is the stronger behavioral check.
- [x] 4.4 Record the smoke receipt and resolve all conclusion-critical implementation or intent findings.

## 5. Run the Bounded Training and Evaluation Screen

- [x] 5.1 Assemble the 256-image reviewed or high-confidence event pool, reporting admitted and excluded owner evidence without treating unmatched official predictions as hallucinations.
- [x] 5.2 Launch eight-graphics-processing-unit controls and treatments, then add exact 6-update and 9-update Source-only controls to isolate optimizer-update count while explicitly retaining event-composition and Source-exposure differences as residual confounds.
- [x] 5.3 Evaluate clean greedy physical-owner gains and losses, strict duplicate candidates, Source-owner retention, geometry, validity, the 240-image never-trained cohort, and the 12-image human-refined panel.
- [x] 5.4 Make the scale decision: do not inflate the current bank because most additional immediate-recovery candidates are concentrated in one image; move larger exact-rollout collection and row-level review to a separate expansion unit.
- [x] 5.5 Write bounded results, update the research decision and project memory, validate OpenSpec and research graph links, and record that no long-running job remains from this unit.
