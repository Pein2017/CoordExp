## 1. Freeze scope and independent review

- [x] 1.1 Create the isolated worktree from the accepted HEAD and record excluded dirty paths, report identity and GPU budget; verify the isolation-baseline.json receipt and unchanged canonical branch identity.
- [x] 1.2 Identify actual input and DoRA-binding consumers, resolve their inherited source bindings, and record the shared/profile design; verify six migratable profiles and retained historical source snapshots without editing any manifest.
- [x] 1.3 Complete proposal, delta spec, design and this task list; run strict OpenSpec validation and obtain one independent Astra audit of the frozen artifacts for token/span identity, lazy preparation, source closure and safe integration; resolve blocking counterexamples before code changes.

## 2. Real input vertical slice

- [x] 2.1 Capture unchanged two-real-row prompt/media/grid/target/span outputs and relevant baseline test results; preserve them with their code/profile/input identity in the acceptance root and demonstrate sensitivity to prompt, ordering and wrong-media changes.
- [x] 2.2 Implement the shared planned-example operation using existing image/prompt/encoding owners; verify the real processor-only generation-plus-target slice equals the captured baseline, loads no model, plans/renders once per row and preserves explicit native materialization.
- [x] 2.3 Add the direction-local CPU inspection entry and explicit ordered-ID or count/seed selection; verify its actual CLI on two real rows plus invalid/duplicate/oversized selection, and that it does not bypass the old strict preflight.

## 3. Migrate callers and remove duplicate mechanics

- [x] 3.1 Migrate DORA request/target/preflight assembly while retaining Source256 gates and artifact fields; verify exact old/new saved-input projections, EOS/spans and corruption rejection, then delete superseded duplicated preparation.
- [x] 3.2 Migrate Logit Lens and Human13 request construction and all four duplicated config adapters, including inference pipeline; verify unchanged request policies and targeted native/inference/package tests plus a residue check for the removed copies.
- [x] 3.3 Consolidate Source256 binding in runtime.py after its input edits are complete and migrate the six admitted selective-preservation profiles; verify ordered parameter identity, fixed surface, frozen complement, errors and one-step gradient/update equivalence through a real caller-level CPU fixture, preserving DDP/version-snapshot order.
- [x] 3.4 Update existing fresh-execution source snapshots for introduced input dependencies; verify changed shared helper bytes affect current execution identity while historical staged sources and manifests remain untouched.

## 4. Whole workflow acceptance

- [x] 4.1 Document shared mechanics versus profile responsibilities and executable selection→inspection→replay/update→existing paired-diagnostics examples; run the documented CPU entry and saved-result consumer and verify forced/natural and missing-score meanings remain distinct.
- [x] 4.2 Measure old/new CPU preparation on fixed real rows with operation counts, median/range timings and code-diff statistics; record actual results without claiming unmeasured GPU throughput or scientific improvement.
- [x] 4.3 Execute the final fixed-tree real-model slice within one simultaneous GPU and 60 cumulative GPU minutes; verify prepared-input reuse, exact zero-update replay/score parity, selected update/frozen identity and required downstream consumer behavior, preserving counters, runtime/source identity, exit status and resource receipt.
- [x] 4.4 Run focused integrated regression, inspect any baseline failures without weakening checks, run strict OpenSpec validation and git diff --check; record separate standards and intent acceptance plus unresolved limitations.

## 5. Commit and safe integration

- [x] 5.1 Commit only the authorized logical batches with exact staged diff inspection and verification receipts; confirm excluded canonical dirty files and frozen producers have not been changed by this task.
- [x] 5.2 Reconcile the latest research-probes committed target in an isolated integration candidate and replay affected acceptance; verify the active task has released affected executable dependencies before writing the canonical worktree.
- [x] 5.3 Merge the accepted candidate into research-probes while preserving unrelated dirt and its lock, verify the resulting commit and current entry, and retire the temporary worktree only after actual merge, clean status and evidence-preservation checks.
