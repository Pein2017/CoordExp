interface:
  display_name: "Audit Review"
  short_description: "Gate CoordExp runs, claims, and promotions"
  default_prompt: "Use $audit-review to return a read-only evidence-backed decision on this CoordExp run, artifact, claim, launch, promotion, or compatibility gate."
