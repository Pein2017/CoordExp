extends:
  - ../prod/compact_full_support2_instance_trie_gaussian_softce_a5.yaml
  - ./common_prodlike.yaml

training:
  artifact_subdir: compact_full_instance_trie_gaussian_softce_a5_tiny
  run_name: smoke-compact-full-instance_trie_gaussian_softce_a5-tiny
  max_steps: 1
  per_device_train_batch_size: 1
  effective_batch_size: 1
  per_device_eval_batch_size: 1

debug:
  enabled: true
  train_sample_limit: 24
  val_sample_limit: 8

experiment:
  surface: smoke
  ablation_id: A5-instance-trie-gaussian
  claim_scope: smoke
