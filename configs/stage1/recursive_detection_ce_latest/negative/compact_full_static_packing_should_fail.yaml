# Negative contract: this profile documents the expected parser/runtime failure
# for latest compact recursive detection packing. It is not a launchable smoke.
extends:
  - ../prod/compact_full_support2.yaml
  - ../smoke/common_prodlike.yaml

training:
  artifact_subdir: compact_full_static_packing_should_fail
  run_name: negative-compact-full-et-rmp-ce-static-packing-should-fail
  max_steps: 1
  per_device_train_batch_size: 1
  effective_batch_size: 1
  packing: true

packing:
  static_packing: true
  padding_free_packed: false

debug:
  enabled: true
  train_sample_limit: 1
  val_sample_limit: 1
