extends:
  - ../production.yaml
  - ./common_prodlike.yaml

training:
  artifact_subdir: candbal_close005
  run_name: smoke-candbal-close005

benchmark:
  group_id: stage1_set_continuation_smoke
  intended_variable: repaired candidate-balanced continuation with 5pct final-close calibration

experiment:
  title: Stage-1 set-continuation smoke candbal close005
  purpose: Procedural smoke for the repaired production configuration on a tiny sample.
  tags:
    - stage1
    - set-continuation
    - smoke
    - candidate-balanced
    - close005
