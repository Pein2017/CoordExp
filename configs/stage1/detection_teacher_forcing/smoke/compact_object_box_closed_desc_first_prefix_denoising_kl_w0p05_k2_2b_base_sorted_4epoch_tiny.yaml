extends:
  - ../prod/compact_object_box_closed_desc_first_prefix_denoising_kl_w0p05_k2_2b_base_sorted_4epoch.yaml
  - ./common_prodlike.yaml

# Tiny 8-GPU launch-health check for the full-wrapper sorted desc-first
# prefix-denoising production target.

training:
  artifact_subdir: compact_object_box_closed_desc_first_prefix_denoising_kl_w0p05_k2_2b_base_sorted_4epoch_tiny
  run_name: smoke-compact-object-box-closed-desc-first-prefix-denoising-kl-w0p05-k2-2b-base-sorted-4epoch-tiny
  max_steps: 2
  num_train_epochs: 1
  per_device_train_batch_size: 1
  effective_batch_size: 8
  per_device_eval_batch_size: 1
  packing_min_fill_ratio: 0.1
  packing_drop_last: false
  packing_length_precompute_workers: 1
  static_packing_cache:
    root_dir: temp/static_packing_smoke/prefix_denoising_object_box_closed_desc_first_kl_w0p05_k2_sorted_4epoch_tiny

debug:
  enabled: true
  train_sample_limit: 32
  val_sample_limit: 4
