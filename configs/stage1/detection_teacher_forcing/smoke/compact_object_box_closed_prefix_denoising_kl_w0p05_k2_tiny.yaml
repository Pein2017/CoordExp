extends:
  - ../prod/compact_object_box_closed_prefix_denoising_kl_w0p05_k2_2b_base_sorted_2epoch.yaml
  - ./common_prodlike.yaml

# Tiny launch-health check for the full-wrapper prefix-denoising target.

training:
  artifact_subdir: compact_object_box_closed_prefix_denoising_kl_w0p05_k2_tiny
  run_name: smoke-compact-object-box-closed-prefix-denoising-kl-w0p05-k2-tiny
  max_steps: 2
  num_train_epochs: 1
  per_device_train_batch_size: 1
  effective_batch_size: 1
  per_device_eval_batch_size: 1
  packing_min_fill_ratio: 0.1
  packing_drop_last: false
  packing_length_precompute_workers: 1
  static_packing_cache:
    root_dir: temp/static_packing_smoke/prefix_denoising_object_box_closed_kl_w0p05_k2_tiny

debug:
  enabled: true
  train_sample_limit: 4
  val_sample_limit: 1
