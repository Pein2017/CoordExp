extends:
  - ../prod/compact_full_support2.yaml
  - ./common_prodlike.yaml

training:
  artifact_subdir: compact_full_tiny
  run_name: smoke-compact-full-detection-teacher-forcing-tiny
  max_steps: 1
  per_device_train_batch_size: 1
  effective_batch_size: 1
  per_device_eval_batch_size: 1

debug:
  enabled: true
  train_sample_limit: 1
  val_sample_limit: 1
