artifacts:
  run_dir: /data/CoordExp/output/infer/coco1024_val200_lvis_proxy_mixed_objective_sota

confidence:
  fusion:
    w_geom: 0.7
    w_desc: 0.3
  desc_span_policy: best_effort
  empty_desc_policy: geom_only
