# Production-like smoke: Stage-2 AB Channel-B only (bbox-only max60, ckpt-1516),
# using vLLM server mode (same rollout hyperparameters as prod).
extends:
  - ../prod/b_only.yaml
  - common_prodlike.yaml

training:
  output_dir: output/stage2_ab/smoke_bbox_max60_ckpt1516/b_only_vllm_server
  logging_dir: .tb/stage2_ab/smoke_bbox_max60_ckpt1516/b_only_vllm_server
  run_name: smoke_bbox_max60_ckpt1516_b_only_vllm_server

