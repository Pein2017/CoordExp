# Smoke variant: Channel-A-only iter1.
# Diff vs prod: runtime naming and sample limits only.
extends: ../prod/a_only_iter-1.yaml

training:
  output_dir: output/stage2_ab/smoke/a_only_iter1
  logging_dir: tb/stage2_ab/smoke/a_only_iter1
  run_name: smoke_a_only_iter1
