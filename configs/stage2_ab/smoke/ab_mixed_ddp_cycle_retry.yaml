extends: ab_mixed_ddp_cycle.yaml

stage2_ab:
  schedule:
    b_ratio: 0.85
  n_softctx_iter: 2
  coord_gate_weight: 1.0

training:
  run_name: smoke_ab_mixed_ddp_cycle_retry
  output_dir: output/stage2_ab/smoke/ab_mixed_ddp_cycle_retry
  logging_dir: tb/stage2_ab/smoke/ab_mixed_ddp_cycle_retry
