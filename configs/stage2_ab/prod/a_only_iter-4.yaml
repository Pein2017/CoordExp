# Stage-2 AB ablation: Channel-A only (expectation loop), epoch=2.
# Only the stage2_ab schedule differs from other ablation variants.
extends: a_only_base.yaml

training:
  output_dir: output/stage2_ab/prod_bbox_max60_ckpt1516_ablation_ep2/a_only_iter4
  logging_dir: tb/stage2_ab/prod_bbox_max60_ckpt1516_ablation_ep2/a_only_iter4
  run_name: prod_a_only_bbox_max60_ckpt1516_ep2_iter4
  # Keep A-only ablation lightweight (no rollout eval loop).
  eval_strategy: "no"
  do_eval: false

stage2_ab:
  n_softctx_iter: 4
