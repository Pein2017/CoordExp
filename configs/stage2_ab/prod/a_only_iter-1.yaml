# Stage-2 AB ablation: Channel-A only (expectation loop), iter=1, epoch=2.
# Keep key toggles explicit even when inherited.
extends: a_only_base.yaml

training:
  output_dir: output/stage2_ab/prod_bbox_max60_ckpt1516_ablation_ep2/a_only_iter1
  logging_dir: tb/stage2_ab/prod_bbox_max60_ckpt1516_ablation_ep2/a_only_iter1
  run_name: prod_a_only_bbox_max60_ckpt1516_ep2_iter1
  eval_strategy: steps
  do_eval: true

stage2_ab:
  schedule:
    b_ratio: 0.0
  n_softctx_iter: 1
  channel_b:
    mode: micro

custom:
  extra:
    rollout_matching:
      rollout_backend: vllm
      vllm:
        mode: server
