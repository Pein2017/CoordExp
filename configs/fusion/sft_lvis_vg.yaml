# DORMANT LEGACY EXAMPLE
# ----------------------
# This archived training config is preserved so future reactivation work keeps a
# concrete authored example. It is intentionally not runnable today because the
# canonical training schema rejects `custom.fusion_config`.

extends: ../base.yaml

model:
  model: model_cache/models/Qwen/Qwen3-VL-4B-Instruct-coordexp

training:
  output_root: ./output
  logging_root: ./tb
  artifact_subdir: fusion/fusion_example
  run_name: fusion_lvis_vg_example
  packing: false

custom:
  fusion_config: configs/fusion/examples/lvis_vg.yaml
