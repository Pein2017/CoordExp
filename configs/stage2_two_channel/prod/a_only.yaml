extends: desc_first_a_only_eval_vllm_colocate.yaml

# Canonical Stage-2 production A-only profile:
# - training rollouts on HF (inherited)
# - eval_step rollouts on vLLM colocate (inherited)
# - sleep mode disabled by default (inherited)

# Under DDP we require bounded phase barriers to prevent deadlocks/hangs if ranks diverge.
stage2_ab:
  channel_b:
    ddp_phase_timeout_s: 180.0
