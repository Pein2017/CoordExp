extends: ../prod/2b-ab_mixed_coco1024_bmajority_channel_b_triage_posterior.yaml

training:
  run_name: smoke_4steps-2b-triage_posterior_vllm_server
  artifact_subdir: stage2_ab/smoke/b_majority_coco1024_triage_posterior_vllm_server_6srv2lr_eval_4steps
  max_steps: 4
  eval_strategy: "steps"
  eval_steps: 2
  save_strategy: "no"

data:
  dataloader_num_workers: 0

custom:
  train_sample_limit: 128
  val_sample_limit: 8
