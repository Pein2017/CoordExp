extends: ../prod/2b-ab_mixed_coco1024_lvis_proxy_channel_b_pseudo_positive_dup_targeting-from_ckpt1564_merged.yaml

training:
  run_name: smoke_4steps-2b-lvis_proxy-ckpt1564_merged-checkpoint_ddp
  artifact_subdir: stage2_ab/smoke/2b_lvis_proxy_pseudo_positive_dup_targeting_ckpt1564_merged_checkpoint_ddp_4steps
  max_steps: 4
  logging_steps: 1
  save_delay_steps: 0
  eval_strategy: "steps"
  eval_steps: 2
  save_strategy: "steps"
  save_steps: 2

data:
  dataloader_num_workers: 0
  dataloader_prefetch_factor: null
  dataloader_persistent_workers: false

custom:
  train_sample_limit: 96
  val_sample_limit: 4

stage2_ab:
  schedule:
    b_ratio: 0.0
