# Round-2 tiny Stage-2 trie-CE standard-COCO overfit competition.
#
# Promoted policy: tail-append FN insertion with explorer-supported weak FP context.
# The previous 256-step run did not save checkpoints, so this leaf restarts from
# the same A2 Stage-1 checkpoint and saves the final 512-step state for follow-up.

extends: ./compact_full_et_rmp_ce_ckpt3664_hf_coco80_view_train64_noeval_256steps_stage2_trie_tail_append_weak_fp.yaml

training:
  run_name: overfit_512steps-coco80_view-train64-noeval-stage2_trie_ce-tail_append-weak_fp-compact_full-et_rmp_ce_ckpt3664
  artifact_subdir: stage2_ab/ablation/coco80_view_stage2_trie_ce_overfit_round2/train64_512steps_tail_append_weak_fp
  max_steps: 512
  save_strategy: steps
  save_steps: 512
  save_total_limit: 1

rollout_matching:
  repetition_penalty: 1.10
