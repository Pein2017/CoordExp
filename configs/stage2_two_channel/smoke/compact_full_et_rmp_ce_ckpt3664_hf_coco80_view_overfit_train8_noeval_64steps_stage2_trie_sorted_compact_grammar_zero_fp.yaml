# Tiny Stage-2 trie-CE overfit probe with sorted FN insertion and grammar-constrained compact decode.
#
# Prepared-rollout note:
# - residual-trie supervision replays the inherited prepared-rollout JSONL
# - rollout_decode_policy: compact_grammar is provenance for the rollout source;
#   it does not regenerate compact-grammar attempts during training
# - pair this leaf with a compact-grammar prepared JSONL before treating it as a
#   decoder-policy comparison rather than a config/launch probe

extends: ./compact_full_et_rmp_ce_ckpt3664_hf_coco80_view_overfit_train8_noeval_64steps_stage2_trie_sorted_zero_fp.yaml

training:
  run_name: smoke_64steps-coco80_view-overfit_train8-noeval-stage2_trie_ce-sorted-compact_grammar-zero_fp-compact_full-et_rmp_ce_ckpt3664
  artifact_subdir: stage2_ab/smoke/coco80_view_stage2_trie_ce_overfit_probe/sorted_compact_grammar_train8_noeval_64steps

stage2_ab:
  channel_b:
    rollout_decode_policy: compact_grammar
