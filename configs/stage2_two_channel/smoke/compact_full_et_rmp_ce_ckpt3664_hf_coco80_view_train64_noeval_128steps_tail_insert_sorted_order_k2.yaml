# Larger Stage-2 standard-COCO training-only preflight.
#
# Ablation: reduce triage posterior rollout count as a lower-cost/noise control.
# K=2 is no longer a pseudo-positive profile because promotion requires
# full K-of-K consensus with K>=4.

extends: ./compact_full_et_rmp_ce_ckpt3664_hf_coco80_view_train64_noeval_128steps_tail_insert_sorted_order.yaml

training:
  run_name: smoke_128steps-coco80_view-train64-noeval-tail_insert-sorted_order-k2-fallback1p0-compact_full-et_rmp_ce_ckpt3664
  artifact_subdir: stage2_ab/smoke/coco80_view_larger_preflight/train64_128steps_tail_insert_sorted_order_k2

stage2_ab:
  channel_b:
    pseudo_positive:
      enabled: false
    triage_posterior:
      num_rollouts: 2
      rollout_temperatures: [0.0, 0.7]
