# Tiny Stage-2 standard-COCO training-only overfit probe.
#
# Ablation: rebuild Channel-B target order after FN insertion using top-left
# sorting, while keeping dataset/sample object order randomized.

extends: ./compact_full_et_rmp_ce_ckpt3664_hf_coco80_view_overfit_train8_noeval_64steps.yaml

training:
  run_name: smoke_64steps-coco80_view-overfit_train8-noeval-sorted_insert-random_order-fallback1p0-compact_full-et_rmp_ce_ckpt3664
  artifact_subdir: stage2_ab/smoke/coco80_view_overfit_probe/sorted_insert_random_order_64steps

custom:
  object_ordering: random

stage2_ab:
  channel_b:
    insertion_order: sorted
