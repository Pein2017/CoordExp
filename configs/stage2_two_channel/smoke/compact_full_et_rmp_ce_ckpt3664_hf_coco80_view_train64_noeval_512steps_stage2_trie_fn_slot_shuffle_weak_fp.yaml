# Round-2 tiny Stage-2 trie-CE standard-COCO overfit competition.
#
# Promoted policy: FN slot shuffling with explorer-supported weak FP context.
# This keeps the flexible insertion policy in the competition without sorted bias.

extends: ./compact_full_et_rmp_ce_ckpt3664_hf_coco80_view_train64_noeval_256steps_stage2_trie_fn_slot_shuffle_weak_fp.yaml

training:
  run_name: overfit_512steps-coco80_view-train64-noeval-stage2_trie_ce-fn_slot_shuffle-weak_fp-compact_full-et_rmp_ce_ckpt3664
  artifact_subdir: stage2_ab/ablation/coco80_view_stage2_trie_ce_overfit_round2/train64_512steps_fn_slot_shuffle_weak_fp
  max_steps: 512
  save_strategy: steps
  save_steps: 512
  save_total_limit: 1

rollout_matching:
  repetition_penalty: 1.10
