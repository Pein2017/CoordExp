# One-step Stage-2 trie-CE launch smoke.
#
# Purpose:
# - exercise the trie Channel-B training path before longer overfit ablations
# - preserve the same COCO-only data/checkpoint/objective surface as the train8 probe
# - complete quickly enough for startup validation

extends: ./compact_full_et_rmp_ce_ckpt3664_hf_coco80_view_overfit_train8_noeval_64steps_stage2_trie_tail_append_zero_fp.yaml

training:
  run_name: smoke_1step-coco80_view-overfit_train8-noeval-stage2_trie_ce-tail_append-zero_fp-compact_full-et_rmp_ce_ckpt3664
  artifact_subdir: stage2_ab/smoke/coco80_view_stage2_trie_ce_startup/tail_append_train8_noeval_1step
  max_steps: 1

rollout_matching:
  train_monitor_dump:
    max_samples: 4
