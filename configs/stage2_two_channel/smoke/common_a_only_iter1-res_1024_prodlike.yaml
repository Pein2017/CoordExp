global_max_length: 3072

training:
  max_steps: 2
  eval_strategy: "no"
  save_strategy: "no"
  save_steps: 1000
  eval_steps: 1000
  logging_steps: 1
  output_dir: output/stage2_ab/smoke/a_only_iter1_res1024_random_order
  logging_dir: tb/stage2_ab/smoke/a_only_iter1_res1024_random_order

data:
  dataloader_num_workers: 0
  dataloader_prefetch_factor: null
  dataloader_persistent_workers: false

custom:
  train_sample_limit: 128
  val_sample_limit: 8
