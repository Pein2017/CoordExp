# Larger Stage-2 standard-COCO training-only preflight.
#
# Ablation: compare deterministic FN slot shuffling against the current
# tail-append + sorted-order baseline on a larger 64-sample train slice.

extends: ./compact_full_et_rmp_ce_ckpt3664_hf_coco80_view_train64_noeval_128steps_tail_insert_sorted_order.yaml

training:
  run_name: smoke_128steps-coco80_view-train64-noeval-fn_slot_shuffle-sorted_order-fallback1p0-compact_full-et_rmp_ce_ckpt3664
  artifact_subdir: stage2_ab/smoke/coco80_view_larger_preflight/train64_128steps_fn_slot_shuffle_sorted_order

stage2_ab:
  channel_b:
    insertion_order: fn_slot_shuffle
