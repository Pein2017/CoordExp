---
title: Sorted Crossing-Boundary Owner Release and Realization - Independent Review
description: Primary and secondary contract audits plus Fable-5 scientific qualification of the frozen crossing-boundary result.
type: investigation
role: research-review
authority: non_normative_research
unit_id: 2026-08-03-sorted-crossing-boundary-owner-release-realization
topic: qwen3-vl-dense-enumeration
status: complete
updated: 2026-08-04
---

# Independent Review

## Review boundary

The reviews covered the frozen unit and immutable run rooted at:

```text
/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/
  2026-08-03-sorted-crossing-boundary-owner-release-realization/
  20260804T020853Z/
```

They did not review unrelated dirty-worktree changes and did not authorize
training or architecture.

## Primary contract audit

The finalized primary analysis returned **PASS** with no P0/P1/P2 finding.
The audit independently reproduced:

- all frozen denominators: U=`26`, L=`25`, exact U/L=`24`, matched-E=`12`,
  unmatched-E=`14`;
- `24` interpretable owners and branch counts `17/6/1`, plus two ambiguous;
- the routed two-thirds decision and greedy-only `15/22` sensitivity;
- all 17 displaced-subtag cells: ten both, five likelihood-only, two
  greedy-only;
- the twelve greedy displacers, zero equality with crossing E's owner, and
  zero covered-before-P displacers; and
- create-or-identical publication, source hashes, self-seals, and focused
  tests.

The primary audit was bounded to the frozen analysis contract. It made no
secondary or training claim.

## Secondary capture and merge

The prelaunch scorer audit passed. A real HF smoke then admitted cached
execution with maximum selected-logit difference
`1.71661376953125e-05 < 1e-3` and exact semantic parity.

The complete capture contains twelve shards and exactly 64 requests:
`26/26/12`, with no quarantine. The immutable merge revalidated literal token
segments, selected-token sums, modified-minus-baseline deltas, signs, plan
joins, runtime identity, and source bindings. Its identical rerun was a no-op.

## Secondary analysis revision and closure

The first independent audit of `secondary-analysis-v2` returned **HOLD** for
two analysis-only problems:

1. it pooled raw deltas across images despite the frozen prohibition; and
2. its receipt claimed request rows were preserved verbatim although 64
   request rows were regrouped into 38 owner rows.

Capture and merge were not invalidated. The analyzer and tests were corrected,
and a new immutable `secondary-analysis-v3` was published.

The closure audit returned **PASS** with no new conclusion-bearing finding:

- no pooled cross-image median, min/max, quantile, quartile, or ordered raw-
  value array remains in JSON, Markdown, or receipt;
- global aggregation is limited to discrete finite/sign counts;
- 64 unique request readouts map exactly to 38 owner rows;
- all 192 segment tuples match the sealed merge for baseline sum, modified
  sum, delta, token mean, token count, and sign;
- missing and extra readouts are both zero;
- the receipt truthfully declares row transformation and exact per-segment
  preservation;
- output hashes and self-seal pass; and
- an identical analyzer rerun returned `no_op_identical_rerun`.

Lead verification additionally ran 201 focused secondary tests and 643
crossing regressions; Ruff, `py_compile`, and `git diff --check` passed.

## Fable-5 scientific review

The independent Fable-5 review returned **QUALIFIED PASS**. It reproduced the
primary branch/subtag counts and all secondary per-owner deltas, then narrowed
the interpretation:

- the route is legitimate under the frozen rule, but seven of seventeen
  displaced owners are single-channel and two non-frozen sensitivities fall
  below two thirds;
- the benign substitution is an exact same-length in-place replacement, not a
  control for adding a row, recency, or position shift;
- coordinate disruption is not universal; the strongest tail appears in
  same-description cases, while description sign rates alone do not separate
  from benign controls;
- the `16` owners negative in both catch-up positions are compatible with
  independent sign rates and are not cross-arm confirmation;
- category-local matching leaves cross-category displacement invisible; and
- a geometrically overlapping downstream E may be a corrupted rendition of
  inserted C, making suppression healthy rather than harmful.

The review therefore rejected a broad catch-up-damage claim and recommended
one CPU-only geometric relation discriminator before any further GPU probe.

## Final disposition

The result is accepted as a bounded description of fragile within-category
identity scheduling. The routed successor is the CPU-only C/E and
target/displacer geometric relation analysis described in [results.md](results.md).
No other successor, treatment, training run, or architecture is admitted by
this review.
