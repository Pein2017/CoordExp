---
title: Native owner learning at broader scale with independent repeat and cache evidence
description: User-approved fixed-prefix scale fit, greedy-repeat supply, and bounded cache-intervention transfer, with independently frozen evaluation.
type: investigation
role: research-unit
authority: non_normative_research
architecture_promotion_status: not_promoted
implementation_status: authorized_within_goal
unit_id: 2026-09-12-native-owner-scale-and-state
topic: qwen3-vl-dense-enumeration
status: complete
evidence_status: verified
updated: 2026-09-12
---

# Native owner scale and state

Final disposition: **lead-accepted, closed; promising natural transfer with
preservation/new-loop debt; no checkpoint or architecture promotion**.
Read [results](results.md) for the accepted evidence and next proposal.
The single authoritative closeout receipt is
`/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-12-native-owner-scale-and-state/iteration-receipt.json`
(SHA256 `69381cc9122fb54bfbabbb43ef714fbccd0e9938f0483e09c98931d01c9e9f00`).
Historical launch grants below are provenance, not pending work.

## Current contract and authorization

From unchanged Stable50, does one broader fixed-native-prefix complete-row
repair fit improve natural trusted-owner recovery and retention, under a
fixed acquisition denominator and independently frozen evaluation?

The user accepted all four grill-me choices: three lanes below; fixed128
candidate images with at most2 nominated repair candidates/image and at most32
admitted packages; no scale fit below16 packages; approximately32 positive
exposures/package; and an independently frozen evaluation panel with32
preselected images for source-blind mixed-prediction owner review. There is
no compute ceiling. Finite scientific stops remain; no retry-until-success.
The subsequent user correction fixes output max tokens at3084.

Earlier completed units remain closed. Flat L1 remains. The user's subsequent
team-guide revision allows Astra-low direct implementation ownership whenever
expected cost through acceptance favors it; no mandatory retry ladder. Keep
productive Sol/Luna owners, distinguish environment/brief defects from repeated
contract misses, and reconcile write/live-job ownership before replacement.
Root owns this file, grants and final acceptance. This changes routing only,
not the frozen research arms, dose, review rules or stop conditions.

## Historical motivation and competing explanations

- Trusted complete-row branches already produced natural two-image recovery;
  conditional attainability is not equivalent to complete composability.
- Screened donut25 supplies real instances, including omissions, but its
  compacted teacher fit did not produce natural recovery. Do not make that
  single image the main population or a gate for this fit.
- KL/margin and earlier QP preservation proxies do not guarantee actual owner
  retention. Measure natural gains/losses, not only loss fitting or fewer rows.
- Earlier raw-softmax repeat acquisition gave0/768 events; that comparison
  did not test active negative learning. Deployment greedy supply is distinct.
- Coordinate KV perturbations can release useful conditional continuations,
  but wrong-owner norm matching also works on the donut. Direction/amplitude,
  path access and visual grounding remain alternatives to an owner ledger.

Sources: [previous synthesis](../2026-09-12-parallel-owner-research/results.md)
and [repeat-event result](../2026-09-11-positive-branch-vs-repeat-event/results.md).

## Common identity, meaning and acceptance boundaries

- Cwd is the permanent research-probes base. Start revision `cfdd679e7`, clean
  before this unit's writes. Follow `docs/BRANCH_AND_WORKTREE_POLICY.md`.
- Anchor is Stable50 `positive7-support50-81/training/adapter`, not Source,
  screened25, fixedP32, C32 or D17. Reuse the exact base, special-token delta,
  adapter and execution identity from accepted receipts; bind fresh packets.
- Natural endpoint: original image/prompt, greedy T0, top-p1, RP1, FP32/SDPA,
  `max_new_tokens=3084`, unchanged geometry/parser/matching. No KV patch,
  supplied object prefix, output filtering or GT edit in natural endpoints.
- Conditional intervention: the total assistant-output allowance is3084,
  including supplied output-history/opener tokens; prompt/image tokens are
  excluded. Run the entire remaining allowance. No short horizon substitutes
  for complete owner/burden evaluation. Forced rows get no autonomous credit.
- A positive is an independently supported single physical instance, either
  GT-backed or visually reviewed unlabeled, absent from the supplied history.
  GT-unmatched alone is never negative. Class/extent/group ambiguity stays
  unknown. Identity/provenance must not be fabricated as a GT annotation.
- Each main package preserves literal native h, complete c and independently
  validated successor witness w. A trustworthy box is not by itself proof of
  a compatible transition. Protect actual witnessed spans, not hypothetical
  future objects. Do not supervise EOS from an incomplete annotation list.
- Strict duplicate uses the existing any-class native-pixel IoU >0.95 rule;
  each later valid row counts once. Separately review physical repeats below
  the threshold; do not silently change the training/event predicate.
- Record original population, eligible, nominated, executed, valid, admitted,
  and held counts; malformed/capped model outputs are outcomes, not missing
  runs. Preserve loss/fitting, natural recovery and conditional recovery as
  different evidence surfaces.
- No architecture, dynamic-prefix fit, QP fit, negative-learning fit, production
  promotion, outward publication, Git commit, cleanup or memory write is
  authorized by this unit. Reversible scoped probe implementation is authorized.

## Lanes, ownership and dependencies

| Lane | Scientific outcome | Initial physical GPUs | Owned surfaces |
|---|---|---|---|
| A scale | Fixed128 acquisition yield; one admitted-bank fixedP fit vs Stable50 |0,1,2,3| `scale/`, `probes/native_owner_scale/scale.py` and its tests |
| B repeat | Supply of deployment-greedy strict-repeat rows and reviewed drift misses; no negative fit |4,5| `repeat/`, `probes/native_owner_scale/repeat.py` and its tests |
| C state | Frozen small cross-scene KV intervention transfer and preservation; no architecture fit |6,7| `state/`, `probes/native_owner_scale/state.py` and its tests |
| evaluation | Freeze independent256 and blind-review32; evaluate baseline/candidate without selection feedback |CPU initially| `evaluation/`, `probes/native_owner_scale/evaluation.py` and its tests |

One root assigns named L1 owners. No child agents or automatic reviewers.
Current owners: A `/root/sol_high_native_owner_scale`; B
`/root/luna_max_repeat_supply`; C `/root/sol_high_cache_state_transfer`;
evaluation initially `/root/luna_max_frozen_owner_evaluation`, now transferred
to `/root/astra_low_eval_materialization_finish` after v10 failed during raw
image-path materialization. Luna stopped writes/launches; successor owns that
same package, not an additional review lane. Preparation was dispatched
after user acceptance. No new GPU launch is implied by a worker dispatch.
The evaluation owner coordinates exclusions with all lanes before any new
holdout model outputs. A owns the candidate128 identities and exports that
manifest to B/C; those lanes must not independently expand its acquisition
population. B/C can reuse already exposed evidence for their own bounded
panels; all such images remain excluded from fresh evaluation.

Existing `probes/parallel_owner_research/training.py` is reused. A is the sole
potential editor for a demonstrated load-bearing shared-training defect;
other owners request a correction instead of copying or concurrently editing it.
Do not overwrite old receipt-bound execution snapshots. Each owner freezes
its local outline and packet before model work and reports one real slice
through persistence and the consuming metric before expansion. Lead closes
that boundary once, not a committee or duplicate audit.

### A: acquisition and scale stop

Freeze128 candidate images before new repair outcomes, with baseline-informed
strata covering repeats/drift, missing/early stopping and normal controls;
report available strata instead of manufacturing quotas. Nominate at most2
literal c rows/image before intervention outcomes; source can include existing
model proposals and visually validated GT or unlabeled candidates. Report
candidate provenance strata. At most256 forced-c continuation attempts, each
at the full remaining3084 allowance. No alternative-c search after failure.
Select at most32 admitted c+w packages by a frozen outcome-independent priority;
prefer distinct images before a second package from one image. No fill beyond
the original128. Below16, close A as supply shortfall without training.
The source universe is the already-exposed Stable50384 `eval_records` in
`2026-09-11-positive-progress-matched-control/endpoint-preparation/packet.json`
under the raw investigation root; exclude that whole universe from fresh256.
A real nonduplicate w is required under h+c, but complete-suffix cleanliness
or zero global owner loss is not an additional admission filter. Record those
full-suffix outcomes separately; local witness validity is not global safety.

At16--32, freeze the bank, references, full schedule and objective. Use batch2
positive records with explicit mean2, paired conditional-w KL10, retained
normal-reference KL100 and margin10 from the successful history recipe;
32 exposures/package means16*N optimizer steps (256--512). Preserve equal
exposure for odd N with a deterministic cycling schedule. References exclude
active training images and use the actual retained denominator. Preserve
the inherited optimizer/hyperparameters unless a concrete incompatibility
requires a lead ruling. One terminal fit, no dose/seed/coefficient sweep.
This tests an operational scale package, not an isolated causal diversity law.

### B and C: independent stops

B freezes its source census and review selection before conclusions. Produce
all later strict-repeat rows, deployment prefixes and raw lineage; inspect a
bounded stratified physical-owner review for below-threshold drift and invalid
extents. Signal counts do not prove negative learning efficacy. No new sampled
event-risk comparison or penalization objective is required for this census.

C freezes at most8 exposed scenes spanning loop, missing and normal behavior
before seeing new treatment outputs. Use an existing coordinate-carrier
intervention with native/self and the direction/amplitude control necessary
to challenge the owner-specific interpretation. Freeze exact arms and boundaries
in `state/unit.md`, no per-image tuning or extra layer/norm sweep after outcomes.
A null/shortfall in B/C closes only that lane; neither gates A's learning.

### Evaluation and interpretation

Freeze256 evaluation images not used for this investigation's selection,
training, candidate review or earlier exposed transfer panels; document the
exclusion provenance without claiming pretraining/SFT disjointness. Preselect32
by identity before model outputs for source-blind union-prediction review.
Do not call a union-proposal reviewed ledger exhaustive scene recall.

Natural primary evidence: admitted-target recovery, annotated and reviewed
owner gains/losses, burden (strict repeats, invalid geometry, malformed rows,
cap/EOS and output length), disjoint reference/other/fresh panels. Training
exposure panels are not independent transfer. Paired fresh differences and
uncertainty are reported without tuning to this panel. If A stops below16,
no trained endpoint or training/transfer claim is invented.

Exploration can yield a candidate with preservation debt; that is not an
automatic checkpoint adoption. Close at the frozen final endpoint or declared
lane supply stop, preserve negative findings and name remaining hypotheses.

## Resources, receipts and continuation

### Current continuation boundary

All lanes and the frozen endpoint are complete. A admitted16 packages over11
images from156 fixed nominations, then executed one256-step fit on8 ranks,
32 exposures/package and54 disjoint reference images. Saved-adapter cold
scores and rank identities passed root checks. V1 raw train/dev availability
failure was before any forward/step; v2 corrected only the materializer binding.
Evaluation v10 failed before inference on relative image resolution; v11
corrected ephemeral request projection against frozen image bytes and completed
all640 natural outputs, then cold consumption. Failed artifacts remain intact.

Source-blind32 review covers all419 proposals exactly once, with uncertainty
retained; root separately viewed all11 admitted paired cards. Final74 focused
tests passed. Donut learning succeeds naturally, but cup210457 develops a new
drift loop. B establishes strict-repeat supply, not negative-fit efficacy.
C reproduces the known donut K-only release without positive owner transfer
to the other3 scenes. See [results](results.md) for separated panel outcomes.

The original2-row fresh baseline slice has post-hoc identity backfill rather
than live model-identity receipts; this limitation is not retroactively repaired.
No model invocation or monitor remains needed for this unit. The earlier wake
registration failed at app-server thread/read and no monitor was armed; root
used native long waits to completion. Further comparisons require a new frozen
research contract; no automatic promotion or additional training follows.

Raw root:
`/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-12-native-owner-scale-and-state/`.
Each owner records immutable inputs/commands, measured forwards, wall time,
allocated GPU time, memory, all terminal statuses, and consumer evidence in
its existing lane receipt. No separate scheduler or generic accounting service.
Reservations are exclusive; return GPUs on completion and root reallocates
them to A/evaluation. Expected random stress occupancy is not a launch blocker.
Never stop unrelated jobs. Use long event-driven waits and quiet run logs.
No final handoff claims asynchronous continuation without a confirmed armed
monitor. Root remains active with native waits otherwise.

## Launch and acceptance receipts

- C gate-v1 launch granted on GPU6 for417044 and the frozen four reads;
  packet SHA256 `5a1208ed226a71a2d08a18194e84c6b1f1cc5a840cc9b8bbff5a7e4746762215`.
  Root inspected the actual K-only/norm-match and complete cached decode
  boundaries and independently ran9 focused tests, all passed. This is a
  launch grant, not execution acceptance or a scientific result. Remaining
  C scenes await the gate consumer, without repeating the completed slice.
- A selection-v1 was superseded before any model call, preserved rather than
  overwritten: v2 uses32 genuine normal/no-FN controls (zero nominations),
  first strict-repeat boundaries rather than near-cap tails, and one existing
  visually reviewed unlabeled donut candidate. Source384 -> raw128 comprises
  25 repeat/drift,71 missing/early and32 normal;156 nominations over86 images.
  The0.3 nomination overlap screen is conservative HOLD, not physical identity.
  Selection-v2 SHA256 `587c74c627dbe09f0eee1574933ec373eee2d984bb4cb6edc887e462f66ef7cd`.
- A four-call acquisition-slice-v2 launch granted on GPUs0--3 after root
  inspected the real native h+c/full-budget producer and ran8 tests, all passed.
  Acquisition packet SHA256 `a79b855edaaab7284f5a8e382cd548d82bebd817d4958d19e8d552a50006d012`.
  No complete-bank acquisition or training admission is implied yet.
- B completed its CPU census candidate without model calls; GPUs4,5 were
  reassigned to evaluation. B's bounded physical review/lineage closeout is
  still active. Evaluation fresh256/blind32 selection is frozen and its7
  CPU tests passed root replay; actual baseline producer gate is pending.
- A first slice was technical-invalid before loading:4 ranks,0 forwards.
  Root independently verified unchanged artifact files/semantics/tensors;
  only inspector naming-derived version/fingerprint changed from Swift to
  Infras. A one-way exact rename bridge, with changed-bytes/unknown-version
  rejection tests (9 passed), is accepted. Same4-job corrected slice granted
  under acquisition SHA256 `4ab45d6ce49d64f1934595fca4e55b1f655ed1a358decbcd63341a9536b0f831`.
- C gate completed exit0,9585 forwards. Root verified persisted full-token
  metrics and exact archived native/self/A-K references. A-K reproduces
  TP50 1->11,410/EOS,repeat1; norm-matched B-K remains capped with TP1.
  This is fixed-donut direction evidence only. Remaining3 scenes are granted
  under packet-v2 SHA256 `ce26a0b3f30b59d88ed060e445ce83b2068ea7c790a54011fbad869eca5b8a82`.
  The only continuation change canonicalizes native `im_end` EOS labels;
  root reproduced the old predicate rejecting an actual native EOS and the
  corrected predicate accepting it, plus10 focused tests passed. Completed
  gate artifacts/runner remain unchanged; no duplicate gate execution.
- Corrected A slice passed all4 terminal/outer exits and source-bound cold
  reduction. Root inspected all literal outputs/budgets and the visual sheet:
  three intact-terminal histories immediately emitted EOS; one repeat-boundary
  case emitted a possible w. That w still needs physical review. The result
  is evidence of the predeclared successor-supply risk, not a fitting failure.
  Complete the same156 nominations by running remaining152 and reusing4 sealed
  slice rows; enforce a156-unique-ID merged denominator. No outcome-adaptive
  candidate/prefix change or training grant follows from mechanics acceptance.
- Evaluation baseline slice64523/395633 granted on GPUs4,5 under packet
  SHA256 `af00587af08de26f5d7fe87be30c142003ba9baa651451ffc171cf32e613e8be`.
  Root verified frozen256/blind32 cardinalities/disjointness,9 tests and real
  native producer empty-prefix/full3084 semantics. Remaining254 wait for its
  durable cold consumer; baseline is not blocked on a future candidate adapter.
