# Frozen cross-scene coordinate-carrier state transfer

Status: **lead-accepted and closed**. Lane C
owner: `sol_high_cache_state_transfer`. This file specializes the owning
[`../unit.md`](../unit.md) without changing its claim or stop boundary.

## Question, contrast, and stop

Does the previously useful A-region **K-only** coordinate-carrier intervention
change complete conditional continuations across a fixed multi-scene set while
preserving native annotated owners, rather than only releasing one donut loop?

Each case keeps Stable50, original image and prompt, literal native history,
native MRoPE, four coordinate positions across all 28 text layers, and one
common natural next-row opener. The frozen arms are:

1. fresh untouched native continuation;
2. exact native/self K transplant (no-op parity control);
3. A-region K-only donor transplant;
4. same-scene wrong-owner B K direction globally L2-matched to A's K delta.

All values stay native. The norm is one CPU-float64 L2 across every layer and
the four K coordinate positions; it is not per-layer. This is the smallest
direction/amplitude control that follows the old finding that A K-only worked
on 417044, A V-only did not, and full-KV norm-matched B also worked. Full-KV,
V-only, background, random, layer/head, amplitude, boundary, and seed sweeps
are not part of this transfer panel.

Stop after these four scenes and three cache arms. No replacement or tuning
follows a negative, technical-ineligible, capped, malformed, or owner-losing
cell. This is conditional post-admission evidence, not autonomous recovery,
an owner ledger, an architecture result, or a general detector claim.

## Pre-frozen panel and direct-photo carriers

The exact exposed IDs were notified to the independent evaluation and scale
owners before any treatment output: `9813,158044,417044,477415`.

|ID|Frozen role|History carrier A|Distinct spatial control B|Source behavior|
|---|---|---|---|---|
|9813|normal finite control|left seated driver person|foreground right person|EOS100; TP50 9/9; repeats0|
|158044|finite mixed missing/repeat|handheld remote, emitted row3|white teddy bear on orange chair|EOS246; TP50 4/17; repeats5|
|417044|known-positive donut loop|real clipped left-edge donut|separate interior donut|cap3084; TP50 1/15; repeats291|
|477415|non-donut chair loop|real lower-left chair back|separate foreground chair back|cap3084; TP50 2/27; repeats4|

9813/417044/477415 reuse the already root-admitted instance-state boundaries
and photo rulings byte-for-byte. 158044 is also previously exposed; its saved
Stable50 remote and teddy prediction regions were inspected directly on the
original photo before treatment. A/B image-key counts are matched per scene
and the regions do not share selected keys. Preparation writes direct-photo A/B
cards and hashes under the fixed raw root; these cards certify only physical
support and separateness, not exhaustive scene ownership or GT correctness.

The finite 158044 row is a deliberately mixed missing/repeat stratum, not a
manufactured pure omission quota. GT-unmatched predictions remain unknown
until directly reviewed; annotation-relative FP is never equated with
hallucination.

## Execution, evidence, and root launch gate

Raw root:
`/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-12-native-owner-scale-and-state/state/`.

Every branch uses greedy T0/top-p1/RP1, FP32/SDPA. The total assistant action,
including supplied native history and opener, is at most3084 tokens. Each
branch runs to EOS or its entire remaining allowance. Supplied history owners
are separately scored and receive no free-suffix/autonomous credit.

The first production-shaped gate is exactly 417044 on physical GPU6. It must:

- reproduce the archived fresh native, exact self, and A-K full suffixes;
- persist raw donor tensors, mask receipts, realized norms, raw tokens/text,
  MRoPE position/delta hashes, cache-consumption evidence, source hashes,
  parameter-version immutability, model identity and terminal resources;
- cold-reload native plus all three cache arms through the unchanged parser and
  matcher, retaining full and free-suffix owners, all drops/invalids/repeats,
  EOS/cap and raw rows;
- pass exact native/self full-suffix equality.

Only after root accepts that gate once may the frozen workers run: GPU6 handles
9813 then477415 under one model load; GPU7 handles158044. The gate is not
rerun. The final cold consumer joins all four scenes and stops. Expected upper
bound: three model loads, at most16 vision forwards, and at most49,036 decode
forwards/tokens selected by the four native plus12 cache continuations at the
frozen boundaries. Actual
model calls, GPU/RSS peaks, wall time, terminal exits and artifact hashes are
reported from receipts, not inferred from this bound.

No model/GPU work is authorized by this preparation record itself.

## Completed gate and versioned continuation

Root granted only the exact417044 gate bound to packet-v1 SHA256
`5a1208ed226a71a2d08a18194e84c6b1f1cc5a840cc9b8bbff5a7e4746762215`.
The one invocation exited0. It exactly reproduced archived native, self and A-K
suffixes; the cold consumer verified all four branches. Native/self remained
the same capped trajectory. A-K again ended at410total tokens/EOS, gained10
IoU50/60 annotated owners with0lost, and reduced strict repeats291 to1.
The new same-norm B-K control remained capped with0 gains/losses and297 strict
repeats. Realized K L2 was157.50425095 for A and157.50425119 for B; therefore
this gate strengthens direction specificity at the fixed donut history but is
not cross-scene evidence by itself.

Gate receipt:9585 model forwards,4 vision forwards,714.712s wall time, peak
CUDA allocated10,257,931,264 bytes/reserved10,884,218,880 bytes, peak RSS
11,746,439,168 bytes. Exact artifacts and terminal hashes are under `gate-v1/`;
the immutable executed runner SHA is
`c79a426536258e7cc922cbf6ad9e03320eb87533377d1406aca01c819a2dccc7`.

After the gate stopped, root identified that native generation names ordinary
EOS `im_end`, while the lane had accepted only local `eos`. A new test first
failed at import, then passed after adding explicit `im_end`→`eos`
canonicalization while retaining fail-closed early-`length` and unknown-stop
checks. Ten focused tests now pass. The completed gate is not rerun or rewritten.
The remaining-panel packet is versioned separately as `packet-v2.json`, SHA256
`ce26a0b3f30b59d88ed060e445ce83b2068ea7c790a54011fbad869eca5b8a82`;
it binds the exact old packet, gate, receipt, consumer and executed runner.
The launcher additionally requires an exact root acceptance receipt for this
v2 packet and gate before starting the remaining workers.
