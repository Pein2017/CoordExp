# Serialization-Only Successor Authority

Status: `active_user_authorized_exception`  
Scope: `serialization_only_successor_exception`  
Decision date: `2026-08-06`  
Source thread: `019fd043-5abb-7eb1-997c-d11a8316281a`

## Owning decision

The user explicitly supersedes the prior interpretation that this unit's
global repair allowance is exhausted and authorizes one successor exception
for the gate-v2 terminal serialization seam. This is new authority. It does
not claim that the frozen `unit.md` was unambiguous about unit-level versus
per-run-root repair allowance, and it does not rewrite that file.

S four-coordinate `geo_sorted_xy` step-2444 remains the only decision-owning
substrate. A3 step-2445 remains an optional, single matched secondary contrast
only if a qualified S observation raises a commit-wrapper-specific question.

## Exact permitted correction

At the natural runner's callback-metadata boundary, omit the Python objects
`score_bias` and `score_bias_callback` from persisted metadata. Preserve the
JSON-safe actuator receipt, arm/protocol identity, selected positions, scope,
mask, mass, and layer-consumption evidence.

The exception does not authorize:

- `default=str`, a permissive serializer, or weaker canonical/finite JSON;
- changes to model kwargs, attention masks, bias dose, prefix, tokens,
  positions, MRoPE, arm order, K01 tolerance, MATH backend, or actuator
  semantics;
- reuse or overwrite of the immutable v1/v2 roots or recovery of their lost
  in-memory trajectories;
- training, A2 attribution, wrapper/special-token changes, production decode,
  architecture promotion, production launch, staging, committing, or pushing.

## Required evidence before GPU

The successor must prove on CPU that the real K14 factory flows through natural
release into a complete S-gate result which canonicalizes and deterministically
writes `result.json`, `runtime_identity.json`, and `terminal_summary.json`.
Persisted metadata must contain no `FixedDoseScoreBias`, while the additive
mask, selected positions, scope, mass/consumption receipts, and synthetic
decoded endpoint remain unchanged. The full conclusion-critical focused suite
and installed-Qwen mask/consumption probe must pass.

An independent reviewer must accept the exact diff, test evidence, and new
identities. A new pre-GPU v4 receipt must bind this file and the immutable
`s-gt5001-live-gate-v3` root before launch.

## Continuation and stop rule

If gate v3 is mechanically valid, execute the already sealed 200-context S
support plan without rebuilding or reordering it, then run only the minimum
decision-bearing S matrix admitted by the frozen predicates. Crossover, one A3
contrast, and P4 remain conditional on their existing scientific gates.

If a new technical failure is not uniquely CPU-reproducible and completely
verifiable before another GPU launch without changing the estimand/operator,
stop. Do not enter an unbounded repair loop.
