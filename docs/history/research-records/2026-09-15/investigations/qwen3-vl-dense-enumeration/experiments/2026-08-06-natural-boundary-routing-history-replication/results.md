---
title: S-Primary Natural-Boundary Static Routing and History Admission Replication Result
type: investigation-result
status: complete_static_direction_qualified_training_hold
updated: 2026-08-07
---

# S-Primary Natural-Boundary Static Routing and History Admission Replication Result

> **Superseded disposition.** The sections through "Successor decision" below
> describe the v1/v2 `bounded_technical_hold` and are retained verbatim as
> immutable technical lineage. The serialization successor subsequently ran
> gate v3 and the full K/N/H cohort successfully. The current disposition is
> owned by "Successor completion: gate v3 and the sealed K/N/H cohort" at the
> end of this file.

## Disposition

This no-training unit ends in `bounded_technical_hold`. It does not answer how
static image routing and natural pre-opener history divide responsibility on S
`geo_sorted_xy` step-2444. No S arm, cohort direction, A3 contrast, training
route, architecture, wrapper/token change, or production decode change is
scientifically qualified by this unit.

The CPU admission and launch preparation are valid bounded infrastructure
evidence. The decision-bearing live gate is not: both immutable S
`gt:5001:15` attempts ended in technical invalidity, and the frozen one-exact-
repair allowance is exhausted. Support completion, cohort probes, crossover,
and A3 work therefore did not run.

## Completed preparation

- The exact census contains `784` S/A3 rows over the frozen `392` physical
  owners. Unassessed support remains unassessed rather than negative evidence.
- The sealed S support plan contains exactly `200` contexts and `77,428`
  scalar forwards across eight fixed shards. It was not executed or rebuilt
  from gate outcomes.
- Pre-GPU receipt v3 binds the unit, checkpoint, code, tests, output root, and
  production-shaped `16` query-head / `8` KV-head GQA evidence. Its file
  SHA-256 is
  `f13968e21f56ff635438c86855abf4ef81e6230a7b5671de739b824c219aa5b7`.
- Focused pre-GPU v3 evidence passed `99` tests. This establishes launch
  readiness only; it is not a model result.

## Live gate attempts

### Attempt v1: no-op parity failure

Immutable root:

`/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-08-06-natural-boundary-routing-history-replication/s-gt5001-live-gate-v1`

K01 full-vocabulary no-op parity drifted by `0.00011730194`, above the frozen
`1e-4` tolerance. No later arm ran. The failure was traced to mathematically
equivalent 2D/native and explicit 4D GQA paths selecting different CUDA SDPA
kernels. The exact repair forced `SDPBackend.MATH` for every real model call;
it changed neither the tolerance nor any operator, prefix, token, position,
MRoPE, mask, or checkpoint identity.

### Attempt v2: terminal receipt serialization failure

Immutable root:

`/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-08-06-natural-boundary-routing-history-replication/s-gt5001-live-gate-v2`

The repaired run exited with code `2` after approximately nineteen minutes.
Its persisted failure is:

`value is not canonical finite JSON: Object of type FixedDoseScoreBias is not JSON serializable`

Artifact identities:

- `failure.json`: `3df629611ab3e28afc734f19e2e31872e711dd71eeb29cb087087cb3ee64c89d`
- `failure.stderr`: `1151d77e2856c2a2b0d25f788f4f2e547e8f38c497940828344dba04bdf261f6`
- executed gate runner:
  `001dbb59255f852b4d392c0bc91dcfd7f8cbbbcd651ed10e0a11df2feaa29aff`

The failure is a terminal materialization seam. The K14 scalar-step factory
returns `score_bias` and `score_bias_callback` as diagnostic-only metadata.
The natural-boundary runner correctly excludes them from model kwargs but
persists them under `callback_metadata`; the gate then attempts to hash the
complete result with canonical JSON. The Python `FixedDoseScoreBias` object is
therefore rejected. This is not evidence about admission, STOP, owner
selection, routing, suppression, reallocation, or transcription.

The process wrote no arm-level result before failing. Although the exception
arose at final result hashing, in-memory trajectories disappeared with the
process and cannot be reconstructed from the failure receipt. No partial arm
is admitted post hoc.

## Claim boundary and next decision

The previous A3 `gt:2299:2` result remains only post-opener conditional oracle-
routing sufficiency and does not substitute for S. The old D10/D20 evidence
remains a pre-opener grammar/admission effect plus seeded conditional-identity
null, not a global dynamic-history null.

This unit does not authorize another repair by renaming or overwriting a run
root. A further live attempt requires a new user-owned decision that explicitly
supersedes the exhausted repair/stop rule, followed by an independent review of
the exact serialization-only change. Until then, the sealed support plan is
held and all eight GPUs are released for other work.

The independent [failure and claim audit](review.md) accepts this bounded HOLD,
artifact characterization, and narrative consistency while accepting no arm-
level scientific evidence.

## Successor decision

On `2026-08-06`, the user supplied the owning decision required above and
authorized one strictly serialization-only successor exception. The active
scope and unchanged prohibitions are owned by
[serialization-successor-authority.md](serialization-successor-authority.md).
This reopens prospective gate-v3 evidence; it does not alter the bounded HOLD
or admit any evidence from v1/v2.

The successor pre-GPU v4 receipt is sealed at
`/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-08-06-natural-boundary-routing-history-replication/pre-gpu-receipt-v4/pre-gpu-receipt.json`
with file SHA-256
`ad09d12b0fb627bd3705d8b5906cb7168e0d66df67fb56ba64213d8dd0da6cee`
and self SHA-256
`4a875dd2af9805a035c60a174ac33b845c103a06814a7fc9404b4fc93100ee42`.
Its CPU evidence records `110` passing tests plus the installed-Qwen additive
mask, all-layer consumption, production GQA `16/8/2`, and block23 mass-shift
checks. Independent review returned `PASS_FOR_ONE_S_GATE_V3`; this authorizes
only the fresh gate, not support or a scientific conclusion.

## Successor completion: gate v3 and the sealed K/N/H cohort

This section supersedes the disposition above. It does not rewrite the v1/v2
lineage, which remains immutable technical history with no admissible arm.

### Gate v3 completed

The authorized fresh gate ran and completed at

`/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-08-06-natural-boundary-routing-history-replication/s-gt5001-live-gate-v3`

on event `gt:5001:15` with `status: completed`, the full fifteen-arm order
`K00, K01, K10, K11, K12, K13, K14T, K14B, N00, N01, N10, N20, H00, H10, H20`,
and result self SHA-256
`9719f2efe77f399ca66985659d902e33087be79bfdd29762643c6045cbed00fc`. The
case-level mechanical receipt for that gate is owned by
[s-gate-v3-evidence.md](s-gate-v3-evidence.md); this section does not restate
it.

### Sealed cohort evidence

The K/N/H cohort then executed across eight shards and was analysed into the
formal v3 evidence:

| Artifact | Raw SHA-256 | Semantic self SHA-256 |
| --- | --- | --- |
| `s-k-n-h-evidence-native-fn-supersession-v3/evidence.json` | `77f90bc48f8126ee1b071767308db603e1b64c0d19a4ca3e5ad8906cfc837598` | `e04cb63540f9e3a1b27b938ada14f694f6b7208564adcaf2c82656915c62bb95` |
| `s-k-n-h-evidence-native-fn-supersession-v3/evidence.receipt.json` | `fa11800ec7ad832646162f77c9e5a6df71d2e9ca951144e40b2fd060df46bf8e` | `80aa70f46899474ee1f8646c02d9c061babfac9bd58534f4cfa1d75aa9df12fd` |

Schema `s_natural_boundary_k_n_h_evidence.v3`, `status: complete`,
`decision_owner: S`, `shard_count: 8`.

### Cohort denominators and outcomes

- **`11` events over `8` distinct images**: `1584`, `2299`, `4134`, `5001`,
  `13348`, `13923`, `14439`, `16228`. All case events were analysed.
- **K10 hard B-exclusive target release: `4/11`, on `4` images** — `gt:2299:29`,
  `gt:5001:15`, `gt:13348:14`, `gt:16228:15`, on images `2299`, `5001`,
  `13348`, `16228`. This clears the frozen checkpoint floor of at least `3`
  events and `2` images, so `checkpoint_level_status` and
  `static_direction_claim_status` are `qualified`.
- **H20 latest-row history: `9/11` factor-qualified, `2/11` grammar-disruptive**
  (`gt:1584:17`, `gt:5001:15`). H20 is the only dynamic factor with a non-zero
  qualified count.
- **H10, N10, N20 are unusable as dynamic factors** on this cohort: H10 and N10
  are `0/11` qualified and `11/11` grammar-disruptive; N20 is `0/11` qualified
  and `9/11` grammar-disruptive.
- **K13 applicability**: `4` measured, `7` not applicable because same-class
  competitor keys are unavailable in the current scalar prefix.
- **No native STOP occurred in any arm of any event.**
- `training_claim_status` is `hold`.

### K14 finite salience: HOLD

Finite soft salience qualifies on **exactly `1` event and `1` image**
(`gt:2299:23`, image `2299`), below the frozen checkpoint floor. Its
`checkpoint_status` is `hold`. Nothing about a graded or trainable soft routing
mechanism is established by this unit.

### Claim boundary

The qualified static direction is **oracle routing and target transcription
sufficiency only**: hard B-exclusive image-key restriction supplied from ground
truth can make S emit a row that strictly matches the target owner. It must not
be described as a natural owner slot, an owner field, a covered-set pointer, or
a trainable soft routing mechanism, and it carries no training claim.

The evidence's `next_step_flags` record `authorize_a3`, `authorize_crossover`,
`authorize_p4`, and `authorize_training` all `false`, with the explicit note
that outcome flags are conditional observations conferring no execution
authority. The downstream 2026-08-07 crossover unit was authorized separately by
the user's prior conditional Phase 5 decision, not by these flags.

### Downstream consumer

This cohort evidence is the frozen, immutable source for
[2026-08-07 S K10-H20 Natural Crossover](../2026-08-07-s-k10-h20-natural-crossover/results.md),
which selected exactly the `K10`-positive, `H20`-qualified, non-degenerate
intersection `gt:2299:29`, `gt:13348:14`, `gt:16228:15`. That unit's fresh
implementation reproduced all nine of this unit's `K01`/`K10`/`H20` endpoint
vectors, `9/9` — a strong cross-implementation determinism receipt for the
runner, actuator seam, and finalizer. Its own crossover result is
source-specific `unqualified`, and it does not change any disposition recorded
here.

## Support-completion lineage

An independent read-only artifact audit followed this unit's support lineage
through the cohort evidence's own `input_bindings` rather than by root suffix,
and returned `PASS` with `P0=0`, `P1=2`, `P2=5`. The audit executed no model,
no GPU, and no scientific code.

### Frozen plan and execution

The sealed plan is `support-completion-plan-v1/plan.json`, raw SHA-256
`1b7e97af291b58aec50849ae09aa883148d55610c281a9edb543fce46ec21d4c` with
`plan_content_sha256`
`30991d2e11b23697bb355b2d3962e77479b9a1c48f3e59dd23c95c1363f73ea6`. It holds
exactly `200` frozen native-FN completion contexts, all unique; its
`context_ids_sha256` and `plan_content_sha256` both recompute exactly.

Eight shards executed with **exact assignment and exact order**: the executed
context union equals the plan set with zero missing and zero extra, shards are
pairwise disjoint, and per-shard executed order is byte-identical to plan
order. Every shard receipt binds the same `plan_content_sha256`. There was no
outcome-adaptive rebuild or reorder.

Realized work equals expected work in every shard, totalling **`77,428`
realized scalar forwards**, with `resumed_reused_scalar_forward_count = 0` and
`failure_count = 0` across all eight:

| shard | contexts | expected | realized | resumed | failures |
| --- | --- | --- | --- | --- | --- |
| 0 | 21 | 6 757 | 6 757 | 0 | 0 |
| 1 | 22 | 7 012 | 7 012 | 0 | 0 |
| 2 | 25 | 11 819 | 11 819 | 0 | 0 |
| 3 | 18 | 8 208 | 8 208 | 0 | 0 |
| 4 | 25 | 9 845 | 9 845 | 0 | 0 |
| 5 | 24 | 8 511 | 8 511 | 0 | 0 |
| 6 | 33 | 13 867 | 13 867 | 0 | 0 |
| 7 | 32 | 11 409 | 11 409 | 0 | 0 |
| **total** | **200** | **77 428** | **77 428** | **0** | **0** |

All `200` observations carry the single status `measured`; no context has a
duplicate or missing disposition. Shards `0-5` and `7` are the
`support-execution-v3` receipts; the authoritative shard-6 receipt is
`support-execution-v5-supervised-shard6/shard-6/receipt.json`, raw SHA-256
`375364cab328543f3a5e3e88ac6f634cd70067152cf265b92ca99aa8e213d66b`.

### Authoritative merge

The authoritative merge is `support-merge-v6`: ledger
`s-step2444-complete-support.json` raw SHA-256
`c3ab9eca420c3ee2965c7ee72bd12208cf34264ec0238c226f8d7d3b399b64f6`, and
`merge-receipt.json` raw SHA-256
`9e16375461edd7a7906f7bfff0c720786f3f23ca686222de4eb27050facd66c4` with self
SHA-256 `7b6dc645889f5883d2c76e9c2463468d3c6caf478b29d1e9de08b1b8d1a3918b`. It
binds exactly one receipt per shard index `0-7` by both path and hash.

The ledger holds `220` records = `200` completion contexts + `20` retained
already-measured native FNs, of which **`14` are `verified_support`**.
`support-merge-v5` is superseded and orphaned: it binds an identical eight-shard
set and differs only in that the same `20` retained records carry
`support_rule = null`; no status, count, or verified flag differs, and no
artifact references it.

Census-v3 binds this merge explicitly by path and by
`sha256 = c3ab9eca…64c6`, giving `row_count = 784`, `measured_S_count = 220`,
`verified_S_count = 14`, `event_count = 11`, `image_count = 8`,
`dynamic_only_S_count = 0`, and `support_completion_candidates = []` — nothing
remained unassessed.

### From 14 verified to 11 admitted

All `11` admitted events are a strict subset of the `14` verified-support
owners. The three that are verified but not admitted — `gt:13348:1`,
`gt:2685:28`, `gt:4134:23` — fail a **support-blind geometry predicate**:
each has `geometry.status = "unavailable"`, `launch_eligible = false`, and
`reason = "required_region_empty_or_unavailable"`, hence
`eligible_except_support = false`. That flag is computed in the base census-v2
from pair geometry alone, before any support measurement existed, and the v3
geometry supersession is gated on it. The narrowing therefore cannot be
outcome-adaptive. Two of the three survive in the record as
`uncovered-same-image-owner` inside the admitted events' geometry, so they are
not dropped, only not targeted.

### Two field-scope traps

**Disposition label seam.** Three rows carry `support_verified = true` while
their `disposition` reads `support_measured_not_verified`. That label is the
*else* branch of `verified_support ∧ eligible_except_support ∧
geometry.launch_eligible`, so it also absorbs verified rows that fail the
geometry predicate. Nothing that gates admission uses the label, and
`verified_S_count = 14` counts them correctly. **Use `verified_S_count`, never
a disposition tally**: a disposition count yields `11/220` instead of the
correct `14/220`.

**Scope of `verified_S_count = 14`.** It is native-FN-scoped. The census
separately contains `26` S owners with `support_verified = true`; the extra
`12` are `native_already_covered` owners carrying verified support from the
prior 2026-08-05 ledger, outside the 220-record completion ledger. Both numbers
are correct and must not be conflated.

### Shard-6 transport and supervision adjudication

Shard-6 is the largest shard (`33` contexts, `13,867` forwards, ~1.6× the
mean). It appears in three roots, and the lead has adjudicated their status:

- `support-execution-v3/shard-6` and `support-execution-v4-shard6` produced
  only transport logs ending after model load — **no failure receipt, no
  verbatim stderr, no partial result**. The frozen unit requires a persisted
  failure log or verbatim stderr before a new technical invalidity may be
  declared and the exact repair consumed. These two deaths are therefore
  **unattested transport/process deaths, not established operator or run-root
  mechanical invalidities**, and they are not scientific or technical model
  evidence.
- The user instructed that processes killed by the Codex restart be relaunched
  as needed, then flagged the repeated shard-6 falls, requested Opus/Fable
  review, and authorized a direct GPU relaunch. That is owning authorization
  for a **transport and supervision rerun**, not authorization to change the
  operator or estimand.
- The v5 run changed only external supervision and observability. It retained
  the exact plan, census binding, support rule, and context order; recorded
  `resumed_reused_scalar_forward_count = 0`; recomputed all `13,867` forwards;
  and produced a receipt schema-identical to the seven v3 receipts.

**Adjudication: no exact scientific repair allowance was consumed by v5, and
the lineage remains interpretable.** The v3 and v4 shard-6 roots are immutable
transport failures and superseded; they are referenced by no receipt and cannot
be double-counted.

Supervisor evidence for the successful run: `oom_kill_before: 66` and
`oom_kill_after: 66` (the host OOM-kill counter did not advance during v5),
`exit_code: 0`, `elapsed_seconds: 9736`, `receipt_present: true`, and 162
healthy heartbeat samples. Resource exhaustion remains the most consistent
explanation for the two earlier deaths, but that is **inference only** — no
kill record for v3 or v4 survives.

### Recorded seams from the support audit

Two P1 and five P2 findings are recorded in [review.md](review.md). None
changes a count, an admission, or the downstream cohort. In particular, the
sealed plan receipt was never back-annotated — its
`realized_scalar_forward_count` and `measured_wall_time_seconds` remain `null`,
so realized `77,428` is provable only by summing the eight shard receipts.
