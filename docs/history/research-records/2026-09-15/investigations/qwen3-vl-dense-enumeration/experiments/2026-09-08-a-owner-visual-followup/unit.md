---
title: Personal owner inspection and five-route A-owner benchmark
status: complete
architecture_promotion_status: not_promoted
updated: 2026-09-08
---

# Current authority

Closeout: [personal visual findings](visual-readout.md) and
[five-route benchmark acceptance/cost (preserved separately in the operational archive)](/data/CoordExp/.worktrees/research-probes/research/archive/self-rollout-a-owner-visual-followup-benchmark/readout.md). All requested
views, exact case attribution, five candidate outcomes and fresh CPU acceptance
checks are complete. Astra-low and Astra-medium qualify this engineering fixture;
the three Sol candidates remain HOLD for the documented distinct reasons.
This is not global Sol retirement or a scientific training-result claim.
Feedback is pending; no successor experiment is launched automatically.

The user requests root to personally inspect the previous24 review boxes with
view_image and the separate A1x7-pixel action, keep feedback pending, focus on
A + owner, and continue benchmarking Sol-medium/high/xhigh versus
Astra-low/medium. The prior suggestion to retire the default Sol route is
superseded. The user supplies the rates in `pricing-user-20260908.json`.
Use these with separate cached/uncached input and output, rather than treating
token counts as dollar costs. The benchmark readout reports measured worker
cost and explicitly excluded shared acceptance costs.

Root owns visual judgments, interpretation, benchmark acceptance and routing.
Previous raw artifacts and blind reviewer labels remain immutable. This is a
new follow-up, not an extension or rewrite of the completed first-wave metrics.
No optimizer update, new rollout bank, architecture, config or memory changes.
The harder benchmark adds one bounded native forward/backward qualification
per route, without parameter updates; it is not scientific training evidence.

## Primary research question and stop

From the sealed Source A16/K4 bank, does visual inspection support a real owner
for the specific1x7 action whose continuation found more annotated owners?
Root verifies its exact downstream owner changes and personally re-examines
the24 auxiliary P boxes using both tight and sufficiently wide context crops.
Category error alone never rejects an owner. Unmatched does not establish
hallucination; a narrow box may bind part of an existing object rather than a
new instance. Visual validity and downstream causal utility stay separate.

Root personally views all24 tight/context pairs and the A source/target/context,
records observed support, uncertainty and deviations from the prior model
review, and stops. Enlargements use integer nearest-neighbor native pixels,
not generated detail. No five-class population prevalence claim is possible
from the stratified24. Hypothesized non-owner routing cues remain hypotheses
until the image/action identity and subsequent gained/lost owner identities
are checked. No reward promotion or penalty is silently added.

The user emphasizes that surrounding pixels often make an otherwise
unrecognizable crop identifiable. Root therefore separates contextual entity
support, single-instance binding, localization quality and novelty/duplication.
Tight-crop standalone recognizability is not required, description is not a veto,
and the earlier4/16 strict conjunction is not a count or upper bound on valid
owners. Wider context for tiny/ambiguous boxes remains within the same24.

## Matched benchmark

The first CPU-only brief was superseded by the user's harder-task request.
One Sol-medium worker was interrupted before full dispatch; its partial usage
is separate from the new matched sample. No other route ran that earlier task.

One practical native replay/masked-credit task, specified in `benchmark-brief-v2.md`,
is independently assigned to five fresh no-history workers with no helpers:
Sol-medium, Sol-high, Sol-xhigh, Astra-low, Astra-medium. Each owns a disjoint
candidate directory and output root. They may not inspect each other's work
or root's acceptance implementation. All receive the identical brief; only
their candidate slug and physical GPU differ. Four predetermined A cells are
replayed, not regenerated. All five actual consumers run; root compares exact
masks/positions/weighted scores, independent raw references, real-gradient
receipts and unchanged-parameter evidence, then replays CPU consumers.

Per-worker bound:35 minutes engineering/verification, one candidate plus at
most one bundled correction for a demonstrated acceptance failure. Record
initial completion and any correction usage separately. No broad repository
scan, session telemetry access by candidates or new dependencies. Each route
gets one assigned A100 and at most5 minutes from model open through GPU release;
no automatic GPU retry. Dispatch: Sol-medium GPU0, Sol-high GPU1, Sol-xhigh GPU2,
Astra-low GPU3, Astra-medium GPU4. At admission all five have0-3MiB used and the
host has about975GiB available RAM. Shared activity may change; preserve it.

Primary benchmark endpoint is correctness under the same acceptance invariant;
compare cost and latency among accepted candidates. Record cached/uncached
input, output (reasoning not double-counted), priced token cost if applicable
rates are available, task wall, repair count and attributable lead takeover.
Raw token totals or worker completion alone do not decide retirement. One
task per route is descriptive evidence, not universal model superiority.

Feedback is pending, with no new activity. The prior B order stress is closed;
do not introduce a new memory module. Stop this follow-up after personal visual
inspection, exact1x7-case attribution, and five-route cost/quality comparison.
