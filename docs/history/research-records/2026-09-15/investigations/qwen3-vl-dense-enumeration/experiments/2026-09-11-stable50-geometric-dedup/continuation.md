# Closed invocation: Stable50 geometric dedup

**No active producer or armed monitor remains.** The repaired pipeline2646225
completed32 updates, natural384 and all verifiers. The lead does not promote
the candidate: TP50−3 and geometry-invalid rows+100 fail the frozen criteria.
See [results.md](results.md) for the decision and evidence. No extra training,
weight sweep or preservation-case addition is pending. The pointers below
are retained history, not authority to resume another run.

The user authorized autonomous execution without repeated confirmation.
Read [unit.md](unit.md) for the frozen semantics and stop rule; do not reopen
the closed recursive-admission pool or architecture discussion.

## Settled repaired retry

Completed detached pipeline PID: **2646225**. Accepted input SHA:
`749fee8e60bee1a8a06e5deb6f018ac6270df2dbd0c2feac7b2eb667c9f83fc4`.
All scientific packet fields match the original exactly.28 CPU tests pass.
The pipeline performed real checkpoint-off/on parity, the two-step capped-
stream gate, the remaining32-step dose and natural384; all phase exits were0.
The real parity passed with **zero** loss/gradient differences. The capped
first-two-step gate passed: initial KL0, unchanged frozen bytes and identical
reduced gradients/adapter/Adam across all8 ranks. Rank5 update1/2 elapsed
339.21/367.59s, approximately28.38s for that second step.

The root attempted one goal-independent monitor registration with key
`stable50-dedup-checkpoint-retry-2646225-v1`; it failed at
`resolve_delivery_target` with `app-server thread/read timed out`.
**No monitor is armed.** Do not blindly retry or infer a future wake. Root
remained active and observed pipeline completion with the task-local inotify
terminal wait. The failed registration did not affect producer execution.

The first detached invocation2638082 has **settled failed**. The SDPA forward
on3084-token online streams exhausted
80GB before the first completed global update. Root preserved all initial
artifacts and producer source copies under `technical-invalid-attempt-01`.
Do not resume or reinterpret that partial run.

Sol implemented the bounded activation-checkpoint repair; root corrected the
real PEFT module-owner lookup and added its hierarchy regression. Rank1 now
performs a real short-case checkpoint-off/on loss/gradient comparison (two
extra forwards, no optimizer updates) before the same32 updates. Cap, hard
images, dtype and loss have not changed.

## Settled original producer (historical pointer)

- Root: `/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-11-stable50-geometric-dedup`
- Settled original pipeline PID:2638082. Archived `pipeline-process.json` and
  `pipeline-launch.json` bind its command, source and input hash.
- Input SHA: `b7c839bc80da55f3b424bbee6beaa74f68b8c01f8fc029f8ec0374e683d7cc4c`.
- Pipeline calls the new trainer `run`, then trainer `verify`, then eight
  natural-evaluation shards, merge and verify. It has no extra scientific arm.
- Completion/failure: archived `pipeline-terminal.json`; log signatures
  `PIPELINE COMPLETED` / `PIPELINE FAILED` in `pipeline.log`.
- Do not infer a live monitor from this note. Only an actual `state=armed`
  receipt establishes a durable wake. The root initially remains active for
  the event-driven first-two-step gate.

## Completed acceptance procedure (reproduction pointers)

1. Reconcile this invocation from exact terminal/launch receipts. Do not rerun
   it merely because a tool wait expired. If a wake is delivered, call the
   monitor's decision-status endpoint once before reading scientific outputs.
2. Inspect `training/two-step-smoke.json`, `training/receipt.json`, eight rank
   terminals, and saved adapter identity. The first two steps are part of32;
   all32 have rank-equal reduced gradient/adapter/optimizer hashes. Four real
   capped3084-token streams are included. No more doses are authorized.
3. Inspect complete `evaluation/{consumer,reduction,resources}.json` and eight
   process exits. Re-run the three modules' narrow CPU tests and producer
   verification, not a new broad audit. Relevant commands from research-probes:

   ```bash
   python -m pytest -q probes/dora_owner_learning/tests/test_geometric_dedup.py tests/test_geometric_dedup_train.py probes/dora_owner_learning/tests/test_geometric_dedup_eval.py
   python -m probes.dora_owner_learning.geometric_dedup_train verify
   python -m probes.dora_owner_learning.geometric_dedup_eval verify
   ```

4. Report all384, online8, reference56, remaining192 and dev128. No masked
   denominator or NMS. Report gained/lost/retained owners, IoU50/60/80, strict
   and looser overlap, parser drops, invalids, lengths, EOS/cap identities and
   all seven protected targets. GT was not used for the negative signal.
5. Use the shared renderer on the seven changed duplicate-training images
   plus the zero control and decision-bearing new failures, then personally
   `view_image` a bounded set. Test coordinate jitter versus actual removal;
   don't promote the UL scalar or strict95 metric alone.
6. Close the one candidate with the unit's fixed primary criteria, visible
   debts and no held-out/architecture/default-checkpoint promotion. If it
   fails, report the fixed-dose result and stop rather than adding weights,
   doses or new preservation cases. Update results, index and compass once.

## Ownership and known corrections

Root owns the research record, packet/evaluator and final acceptance. Sol-high
implemented `geometric_dedup_train.py` and repo-root trainer tests. Luna-max
implemented `geometric_dedup.py` and its probe tests. All edits are uncommitted;
preserve earlier unrelated probe/record dirtiness. No Git/memory-store change.

Pre-launch lead replay:25 tests passed; all64 real saved layouts reproduced
native repeat counts and EOS/cap masks. Initial online1280 / reference6047 KL
states and495 selected later rows. The helper's first fake-tokenizer suite
missed native EOS lookup; real-tokenizer RED/GREEN fixed it. Trainer empty-KL,
first-two frozen-byte proof and later zero-gradient handling were corrected
before freeze. No model call used those rejected implementations.
