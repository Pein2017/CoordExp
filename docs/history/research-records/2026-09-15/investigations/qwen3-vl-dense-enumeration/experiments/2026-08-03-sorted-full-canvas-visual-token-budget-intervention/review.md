---
title: Sorted Full-Canvas Visual-Token-Budget Intervention - Independent Review
description: Independent contract and scientific audits that accepted the artifact chain, invalidated the treatment arm, and narrowed the successor route.
type: investigation
role: research-review
authority: non_normative_research
unit_id: 2026-08-03-sorted-full-canvas-visual-token-budget-intervention
topic: qwen3-vl-dense-enumeration
status: complete
updated: 2026-08-03
---

# Independent Review

## Review boundary

The review gate examined the fixed run:

```text
/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/
  2026-08-03-sorted-full-canvas-visual-token-budget-intervention/
  20260803T161241Z/
```

It covered the declared protocol, preparation and scoring implementation,
representative smokes, all twelve treatment shards, artifact seals, analyzer,
calibration, frozen denominators, final gates, and proposed claim. It did not
re-run GPU inference.

The independent scientific reviewer was Fable-5 at `xhigh`, task
`/root/full_canvas_final_science_audit`. The final message was emitted at
`2026-08-03T22:16:11.990Z` with message identifier
`eea9282c-e39b-461d-a5fd-fcac7a709c8c`. The review remains advisory; the
research lead reconciled it against the executable evidence before closing
[results.md](results.md).

## Contract audit

The contract audit found no decision-bearing discrepancy:

- all `12/12` treatment receipts declared complete frozen-overlay capture;
- `1,211` query groups and `167,579` localization score rows reconciled;
- all exact receipt and score file digests matched current bytes;
- aggregate seal
  `ca822d8f659af7573dbc13c45982e9a1863a18ac1de37a3e26f655dedda1cbbc`
  reconstructed;
- all `2,548` allowed owner-context entries were observed;
- the `70` discovery calibration observations and both complete retention
  denominators were reproduced; and
- CPU-only full reanalysis reproduced the published report.

The sole process finding was stale unit and task lifecycle metadata. It did not
affect any score, threshold, denominator, or verdict and is corrected in this
closeout.

## Scientific audit

The scientific audit accepted the artifacts and rejected positive mechanism
interpretation:

- nominal persistent recovery was `14/63`, including `11/51` persons across
  seven images;
- native-true-positive retention was `64/71 = 0.9014`, passing by one owner;
- resolved-owner retention was `86/114 = 0.7544`, failing the frozen `0.90`
  gate; and
- the final minimum-decisive conjunction was therefore false.

The reviewer independently reproduced every gain and loss and decomposed the
28 resolved losses. Only five would pass under the predecessor thresholds in a
non-authoritative cross-arm diagnostic. Nine had treatment-calibrated support
at scored but retention-ineligible adjacent contexts. Crediting the union of
both generous diagnostics still reached only `98/114 = 0.860`, below the
frozen gate. The arm failure is therefore not a marginal threshold artifact.

## Review-owned narrowing

The review changed the continuation route in four ways:

1. The `14/63`, `11/51`, and restricted `3/9` counts must always carry the
   label *nominal and scientifically void*.
2. Full-canvas densification closes under this interface; no third scale,
   crops, threshold motion, or scale-targeted training follows.
3. The cheapest next evidence is a CPU-only reachability-prevalence description
   of the `114` supported-but-native-missed owners using the predecessor
   presentation.
4. Image `4134` remains the next small GPU-bearing route/repetition
   discriminator only after the existing-data description; complex
   commit/binding/contrastive training remains held.

## Residual limitation

The adjacent-context relocation count is a lower bound because the intervention
did not execute a complete context-flexible retention scan for every control.
This cannot rescue the failed arm. It is a design lesson for any future
input-side intervention: a presentation-robust preservation criterion must be
validated prospectively on controls before it is used to interpret recovery.
