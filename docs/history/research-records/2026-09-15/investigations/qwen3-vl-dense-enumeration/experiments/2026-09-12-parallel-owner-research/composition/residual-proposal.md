# New residual-pair proposal: CPU supply only

Status: **candidate; no new model call or fitting grant**. The first eight-branch
result and original redundant pair aliases remain immutable. Root requested
this bounded follow-up census, not automatic replacement of the original B.

Continuation: root admitted both residual instances and froze the
[residual admission unit](residual-unit.md). It supersedes the four-call
proposal below with **two new calls**, reusing the exact accepted P+A baselines.
The executable packet is READY; a physical GPU assignment and launch grant are
still absent. No fitting follows automatically.

## Supply and proposed targets

Within the same two images and the already frozen strong Source K4 bank,
13 distinct sampled-supported owners remain absent from actual P+A free output:

-351017 (10):90429,91179,94212,667309,2094819,667769,476801,1489041,1870183,96050.
-417044 (3):1083042,1083135,1083564.

This is a CPU geometric/matching count, not13 visually certified omissions.
Every witness retains its exact source seed, full row, token IDs and sample
history in `residual-candidates-v1/candidates.json`, SHA256
`a39233b68b2fc40011c697559c12fb958421a4bb982d4682368217f18a0bd114`.
There are no new seeds, images, samples, annotations or model outputs.

Propose one residual B per image, selected for visible physical identity:

1. **351017 B=667769**, the narrow wine glass directly before the right person.
   Source seed2026090602 row16, IoU50=.678877. Actual P+A has no overlapping
   wine-glass prediction (best same-category IoU0); nearest any-class box is
   the right person, IoU.030661. The candidate rectangle visibly follows one
   wine glass, distinct from the other generated glasses.

   `<|object_ref_start|>wine glass<|object_ref_end|><|box_start|><|coord_736|><|coord_363|><|coord_778|><|coord_567|><|box_end|>`

2. **417044 B=1083042**, the chocolate-topped foreground donut just left of the
   previously forced sprinkled donut A. Source seed2026090601 row10,
   IoU50=.788121. Actual P+A best any/same IoU0. The candidate visibly addresses
   that distinct donut, not its adjacent sprinkled neighbor.

   `<|object_ref_start|>donut<|object_ref_end|><|box_start|><|coord_505|><|coord_497|><|coord_597|><|coord_566|><|box_end|>`

Both remain **root visual-admission candidates**; card FP labels are not scene
truth. Candidate selection uses the exposed P+A outcome by explicit design,
so this cannot be called an unselected prevalence estimate.

Important alternative:351017's proposed B lies to the right of A, but417044's
B lies to the LEFT of A. Indeed all three417044 residual candidates are left
of A. Its proposed P+A+B therefore tests a backward spatial transition absent
from the original x-sorted witness order. A failure could reflect this local
transition rather than general composition incapacity. Do not hide that asymmetry.

## Cheapest discriminator proposed for a NEW grant

One Stable50 inference load on a subsequently root-assigned free GPU; four
continuations total: for each image, exact retained P+A free-release diagonal,
then P+A+newB full-row release. Original prompt, fp32 SDPA, RP1 and full3084
action cap. No alternate B, prefix, order, seed or image after outcomes.

Primary incremental usefulness should compare P+A+newB against the ACTUAL P+A
owner set, not merely Stable50's sole old person. Require newB's forced row to
match its owner, retain A and all previously P+A-recovered owners, natural EOS,
and no worse repeat/invalid/malformed/annotation-relative FP burden. Report
IoU50/60/80 and any geometry changes separately. This is a proposed stricter
conditional admission contract that root must freeze, not a silently imposed
new learning gate. If it fails, close this residual pair rather than switch B.

Planning estimate: four bounded continuations, at most12336 new tokens;
one load and four image forwards. Earlier same-image P+A releases were58 and110
total tokens, so the diagonal component is cheap; newB release length is
unknown. Preserve raw trajectories, actual allocation and terminal exit.

## Exact recursive learning meaning, only if admitted and later granted

- A is learned at original commonP; B is learned at exactP+A. A target and B
  target each retain their literal admitted whole-row tokens.
- SingletonA, singletonB and joint use IDENTICAL per-record prefixes and
  per-owner exposures wherever a record is active. A/B are never competing
  next-row targets at the same prefix.
- SingletonB's isolated endpoint is conditional competence at P+A; it does
  not imply that singletonB autonomously discovers A or reaches that prefix.
  Joint's decision-owning endpoint remains one entirely natural completion.
- Any fixed conditional protection must use genuine useful incumbent successor
  rows, not A/B spans. Its source-owner population and dose require an explicit
  root ruling because actual P+A now has more owners than Stable50 itself.

No three-fit packet is frozen yet. The shared two-rank training route is already
qualified separately and will not receive a duplicate mechanics smoke.

## Exact expansion paths

Raw root:
`/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-12-parallel-owner-research/composition/`.
Candidate artifact: `residual-candidates-v1/candidates.json`.
Cards: `residual-candidates-v1/cards/0000_coco2017_train_000000351017_prediction_comparison.png`
and `residual-candidates-v1/cards/0001_coco2017_train_000000417044_prediction_comparison.png`.
