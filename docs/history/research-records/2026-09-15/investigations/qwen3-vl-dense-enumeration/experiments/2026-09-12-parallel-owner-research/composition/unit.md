# Composition lane: first-wave candidate contract

Status: **root-admitted two-case labels; eight-branch acquisition granted**.
Owner: `/root/astra_xhigh_composable_learning`. Training remains ungranted.
Authority: [portfolio](../portfolio.md); the root owns launch grants and acceptance.

## Question and finite first wave

From unchanged Stable50, can two individually trustworthy complete-row repairs
be learned jointly in one natural greedy output while preserving old owners
and output validity? A-only, B-only, joint are different support workloads, not
an equal-total-dose objective-superiority claim. Parameter surface stays the
current language DoRA; no architecture, new GT, or checkpoint promotion.

The old Source train256 K4 strong-output bank has 43 strong witnesses. CPU
replay finds 13 witnesses on six images with at least two owners still missed
by Stable50. It does not establish Stable50 conditional usefulness. Two images
(72583,575303) retain prior unresolved visual/instance holds. Four candidate
images are displayed, without filling the provisional 4–8 quota:

| Image | Candidate pair | Candidate stratum / decision |
|---|---|---|
|351017|478719,499060: distinct background/right people|Two physical omissions in an already exposed capped-loop image; root admission pending|
|417044|1082918,1080038: distinct front/right donuts|Two physical omissions in an already exposed capped-loop image; root admission pending|
|158044|1662968,1663075: books|One vertical-book omission versus upper-stack extent/group ambiguity; HOLD as pure omission|
|496747|1614837,381281: table/chair|Table already represented with a larger box; mixed localization/partial-chair control only, not two omissions|

Images 351017 and417044 each have exactly one Stable50-matched old owner, the
first person row. Hence later incumbent-owner suffix preservation may be empty.
This is a scientific limitation, not a reason to invent successor targets or
call the images representative omission cases. First-wave acquisition proposes
only these two candidate omission pairs; no backfill or new sampling.

## Immutable input route

- Raw lane root:
  `/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-12-parallel-owner-research/composition/`
- `inventory-v1/inventory.json`, SHA256
  `8cc2f88c2c173933de33b1a4d628e400d30014c09fecff21211fcbfbfcf6ff05`.
  Binds original source plan, raw sample shards, literal token boundaries,
  replayed sample matching, current Stable50 consumer, and image/prompt records.
- `visual-selection-v1.json`: four candidate pairs, not accepted training labels.
- `visuals-v3/candidates.json` and `visuals-v3/cards/manifest.json`: context-card
  pointers, complete target identities, and the exact focus omissions. The cards
  show ONLY the nearest Stable50 predictions versus the two sampled target rows;
  renderer matching is not an official metric. Full outputs remain in inventory.
- Stable50 adapter:
  `.../2026-09-10-selective-owner-learning-autonomous/positive7-support50-81/training/adapter`;
  fingerprint `024e46a512491b15d8715218c9fe7970707e7449f8b354b40ae37122c2c7ac9b`;
  adapter safetensors SHA256
  `94d6aa4183af437a63fcc1c407430c05f16f66df6d69887babf8395d80df653d`.

## Requested first real acquisition / smoke

GPU0 only, one fp32/SDPA unmerged Stable50 load; GPU1 remains reserved but unused.
Original prompt/media, RP1 greedy, full-action cap3084. Four branches per admitted
image: natural; retained first-person prefix P plus sampled A; P plus sampled B;
P plus sampled A then sampled B, followed by unrestricted greedy suffix.
Pair order follows the original sampled row order, not forced numerical owner-ID
order. The P row is old-owner-covered, and neither target occurs in P.

The first natural output must exactly reproduce retained Stable50 tokens and
native score; the first complete-row intervention must round-trip through the
native consumer with forced/prefix/free partitions and actual prompt/media
identity. Preserve all raw rows and explicit EOS/cap/invalid/malformed counts.
After this critical real path passes, finish exactly eight continuations for
the two proposed images. No automatic target replacement, alternate insertion
search, seed expansion, or training follows a failed admission.

Primary admission: supplied target rows remain correctly assigned at IoU50;
the completion retains old Stable50 owners without worse FP, strict repeats,
invalid rows or other parser drops, and reaches native EOS. The forced targets
are not autonomous gains. A-only/B-only may already recover both targets;
that is redundant repair/loop-escape evidence, not proof of learning composition.

Planning envelope (estimates, not observations): eight continuations, at most
24672 generated tokens; at most eight image forwards and24680 model forwards;
one model load; peak CUDA allocation expected approximately10–16GiB based on
prior same-route inference, with32GiB diagnostic bound; one-hour invocation
watchdog. This finite acquisition is not a portfolio spending ceiling.

## Training contract proposed after actual conditional admission

- Three fresh Stable50 clones,32 fixed optimizer updates each; no dose sweep or
  extension. Same exact A/B row and prefix in singleton and joint arm.
- Positive loss is complete-row summed NLL, summed across active records and
  divided by the fixed number of admitted pair images. Each active owner gets
  one unit-weight exposure per update; joint does not halve the owner's weight.
- Common fixed normal bank excludes active images. Exact CPU check found zero
  overlap of normal56 with all four displayed candidates; retain all56 if the
  admitted two-image panel remains unchanged. KL weight100 and margin weight10
  in every arm, with current full-vocabulary competitor and accepted floor.
- Conditional KL10 may protect only the SAME admitted useful incumbent-owner
  successor records in all arms, with literal explicit masks. A/B target spans
  never appear as protected supervision. If there is no usable incumbent
  successor, report it for root to freeze conditional0 commonly before fitting.
- AdamW lr1e-5, betas(.9,.999), eps1e-8, weight_decay0, foreach false;
  global gradient clipping1. Shared trainer owns two-rank qualification.
- Endpoints: milestones0/8/16/32 natural pair-panel reads, final common exposed
  guard read selected before training; exact guard identities are pending root
  freeze. No use of the separate fresh-transfer panel.
- Primary result: per-image autonomous A/B incidence, jointly recovered pairs,
  old-owner gains/losses at IoU50/60/80, TP/FP/F1, repeats, invalid/other malformed,
  row starts, EOS/caps. Fixed-prefix likelihood is diagnostic only.
- If singleton fails, report learned composition unanswered at this dose, not
  joint impossibility. If both singletons learn and joint loses owners, report
  the parameter/history distinction only as supported by actual replay evidence.

## Preparation and stop

`python -m pytest -q probes/parallel_owner_research/tests/test_composition.py`
passed4 tests, including deliberate partial/multiple-row and hidden-invalid
counterexamples. The full real CPU inventory replay completed successfully.
Two visualization-interface failures are preserved in visuals-v1/v2; v3 supplies
the renderer's required paired raw/scored focused geometry views. No scientific
model output or accepted producer was changed.

Root inspected both complete focus cards and admitted two distinct people in
351017 and two distinct donuts in417044 as physical omission targets. Root
accepted P after the first person, without accepting focused-card FP labels as
physical truth. The acquisition packet supersedes the417044 display aliases:
A=1082918 (sample row14), B=1080038 (sample row19), with unchanged owner/row
identities. The authoritative acquisition input is
`acquisition-preparation-v1/input.json`, SHA256
`727b61c62087e866f5a65527aac1ec9e4c0913bf4e0df0dcc3903c8b7780114d`.

Training condition remains a root-owned ruling after acquisition: teaching both
A and B at P would be fork competition, not recursive supervision. Teaching B
at P+A keeps B's condition fixed across singleton-B/joint, but singleton-B
conditional competence must then be distinguished from autonomous recovery.
The preliminary32-step recipe above is NOT a training grant or frozen condition.

Current stop: exactly eight root-granted acquisition branches, then report
actual singleton/joined usefulness and recommend a precise learning condition.
Training cannot begin before actual witnesses, a root-frozen condition, and the
shared two-rank real slice pass. Five focused CPU tests pass before acquisition.
