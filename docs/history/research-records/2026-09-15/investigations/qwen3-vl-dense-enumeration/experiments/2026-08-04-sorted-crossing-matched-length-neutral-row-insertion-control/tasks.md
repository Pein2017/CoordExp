---
title: Sorted Crossing Matched-Length Neutral-Row Insertion Control - Tasks
description: Preparation, same-run score capture, replay validation, analysis, and independent-audit checklist for the bounded neutral-row control.
type: investigation
role: research-tasks
authority: non_normative_research
unit_id: 2026-08-04-sorted-crossing-matched-length-neutral-row-insertion-control
topic: qwen3-vl-dense-enumeration
status: complete
updated: 2026-08-04
---

# Tasks

## Frozen design

- [x] Bind the completed crossing and geometry units and their immutable
  artifacts.
- [x] Freeze the twelve-image-only claim boundary; prohibit image `2299`.
- [x] Freeze the nine-owner material decision set, twelve-owner specificity
  stratum, and five-owner infeasibility ledger.
- [x] Define deterministic, score-blind neutral-row selection and disclose the
  unavoidable duplicate and sorted-route-regression confounds.
- [x] Freeze the `-1.0` nat materiality rule, validity gates, asymmetric routes,
  non-voting sensitivities, and stop boundary.
- [x] Keep strict-matcher-unmatched rows unknown-neutral and separate exact-row
  likelihood from eventual owner coverage.

## CPU preparation

- [x] Implement the sibling plan builder and deterministic neutral-row
  selector without loosening predecessor frozen request vocabularies.
- [x] Reconstruct norm-1000 geometry and exact row-token lengths from sealed
  registries; fail closed on any identity or denominator drift.
- [x] Emit exactly 21 neutral-row requests, 21 same-run clean-`C` replays, and
  12 benign-reference replays with identical scored-`E` token IDs across the
  `C` and `N` arms.
- [x] Seal all source, checkpoint, config, tokenizer, runtime, prefix, inserted
  row, scored-row, candidate, and image identities before capture.
- [x] Add focused tests for every candidate predicate, total selection order,
  infeasibility reasons, token binding, denominator counts, no-retokenization,
  image/owner isolation, and create-or-identical planning.

## Runtime and capture

- [x] Run one real matched-`E` and one real unmatched-`E` end-to-end smoke,
  covering same- and different-description rows.
- [x] Verify cached-versus-uncached selected-logit and argmax parity within the
  frozen `1e-3` tolerance, or use the existing uncached fallback/quarantine
  contract.
- [x] Re-check shared GPU headroom immediately before launch; use batch size
  `4` when safe and reduce only as needed without evicting unrelated work.
- [x] Capture all 54 paired requests and preserve raw rows, per-shard receipts,
  terminal failures, runtime identity, and quarantine ledger.
- [x] Enforce baseline replay, all-`C` positive-control replay, sentinel-owner,
  benign-reference, scored-token-identity, and maximum-quarantine gates before
  reading a scientific route.

## Merge, analysis, and closure

- [x] Implement merge-time request completeness, duplicate, cross-owner,
  scored-token, replay, parity, and quarantine validation.
- [x] Compute owner-level relative `C` and `N` coordinate deltas, specificity
  gate, frozen route, named non-voting sensitivities, and descriptive strata.
- [x] Preserve raw exact-row likelihood separately from visual support,
  strict matching, owner emission, and final coverage.
- [x] Render only evidence-bearing owner-level tables or plots; inspect any
  generated images with `view_image`.
- [x] Run focused tests, Ruff, `py_compile`, `git diff --check`, artifact-seal
  verification, create-or-identical rerun, and the research-graph check.
- [x] Obtain an independent Fable-5 scientific audit bound to the immutable
  artifact and exact source tree.
- [x] Write `results.md`, append the post-execution audit to `review.md`, and
  update the experiment router and current mechanism map once.

## Stop boundary

- [x] Stop after one audited route. Do not add a second neutral row, uncovered
  control, position-gap arm, sampling, free rollout, image `2299`, training,
  or architecture work inside this unit.
