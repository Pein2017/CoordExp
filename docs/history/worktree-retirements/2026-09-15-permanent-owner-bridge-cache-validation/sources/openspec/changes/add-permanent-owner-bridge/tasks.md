## 1. Wave 1 - Typed Stage 1 data and atom inventory

- [x] 1.1 Add failing strict-config tests for the versioned Qwen3-VL-2B bridge profile, fixed layer seam, K/key/value widths, exact S COCO/no-resize-1024/global-12000 lineage, explicit `warm_start_expand_dora` source payloads, `ordinary_partial` trust, four-presentation schedule, eight planned-step midpoint/end teacher-forced eval landmarks over one fixed `geo_sorted` eval rendering, loss/cap ramps, optimizer values, and rejection of Stage 2 or independent layer fields.
- [x] 1.2 Extend training config and resolution with the bridge profile and the exact Stage 1 production leaf; keep every existing bridge-disabled config resolving unchanged.
- [x] 1.3 Add failing renderer tests for stable source-owner ids, atomic full-row permutations, `geo_sorted -> random-1 -> geo_sorted -> random-2` reproducibility, distinct random presentations, and unchanged wrapper/coordinate semantics.
- [x] 1.4 Extend rendered, encoded, and packed records with explicit annotation trust, typed owner rows, description/geometry token regions, boundary positions, and gold covered/uncovered source-owner sets without cross-segment references or inferred trust.
- [x] 1.5 Version pack-cache payloads and fingerprints for annotation trust/provenance, presentation identity, realized order, owner records, bridge profile, tokenizer/processor/renderer identity, and forward sources; reject old or scalar-density payloads rather than upgrading them.
- [x] 1.6 Add failing core tests for four atoms per carrier, shared-latent multi-head outputs, normalized key/value dimensions, complete box decoding, deterministic slot symmetry breaking, and image-static inventory reuse.
- [x] 1.7 Implement the bridge profile, atom inventory module, RMS utilities, and bounded diagnostic types under shared Qwen ownership.
- [x] 1.8 Add failing global-assignment tests covering two owners in one carrier, adjacent-carrier duplicate proposals, injectivity, deterministic ties, locality-to-full-inventory fallback, complete assignment, impossible-assignment failure, trusted negatives, and masking of every ordinary unmatched atom regardless of overlap.
- [x] 1.9 Implement image-global Hungarian assignment with the specified geometry/objectness/locality cost, detached assignment semantics, full-inventory fallback, and fail-before-loss behavior when any labeled owner remains unmatched.
- [x] 1.10 Run targeted config, template, packing, cache, atom, and assignment tests; run `openspec validate add-permanent-owner-bridge --strict`; search for scalar-density and untyped owner-record residue.
- [x] 1.11 Complete separate Wave 1 engineering-standards and intent-contract audits; resolve all reproducible P0/P1 implementation findings before Wave 2, without adding a model-quality gate.

## 2. Wave 2 - Causal row write, routing, and objectives

- [x] 2.1 Add installed-Qwen structure tests that prove the 28-block profile, post-block-20 interceptor, blocks 21-27 integration path, post-final-RMSNorm interceptor, canonical `input_ids`/visual replacement, and fail-closed behavior on structural drift.
- [x] 2.2 Implement the scoped bridge model wrapper and typed per-forward context without copying the upstream Qwen forward loop or using process-global hook state.
- [x] 2.3 Add causal forward tests proving zero-initialized parity, segment-local injection, opener-through-box-end latch coverage, ten-percent row RMS clipping, visibility of the row write in the final box-end boundary but not at the block-20 seam, and exact branch-context restoration without doubled diagnostics under non-reentrant activation-checkpoint recomputation.
- [x] 2.4 Implement the rank-256 bilinear owner-conditioned row adapter, zero initialization, safe-step cap ramp input, and per-token bounded metrics.
- [x] 2.5 Add router/admission tests for normalized atom-plus-null scores, low-objectness suppression without high-salience boost, trustworthy-domain masking, detached scalar availability-only admission, three-percent RMS cap, no owner-value mixture before opener, and no `L_AR` gradient from admission into key/objectness/null parameters.
- [x] 2.6 Implement final-boundary key routing, learned null, aggregate availability admission, teacher-forced gold-owner latch, and row-close route-before-clear ordering.
- [x] 2.7 Add failing `L_atom` and `L_route` tests for global matched boxes, trusted negatives versus all-masked ordinary unmatched atoms, normalized uncovered-set mass, covered-owner competition, order indifference, trusted final null, ordinary final masking, and zero-local-eligibility distributed reduction.
- [x] 2.8 Implement `L_atom` and `L_route` as typed loss terms with FP32 objective math, planned-step numerators/denominators, metrics, and non-finite gates.
- [x] 2.9 Add failing four-branch 2x2 owner-use tests from one identical boundary prefix that distinguish correct versus swapped same-image values for description and geometry, exclude wrappers, prevent earlier-row KV contamination, balance activation counts, select hard/random pairs deterministically, give no credit to a generic pulse, and prove gradients reach selected values plus the shared atomizer.
- [x] 2.10 Implement the shared-prefix four-continuation `L_use` lane with gradients on all correct/swapped sides through row adapter/value/atomizer and explicit eligible-count normalization; keep wrong-image, permutation, and bridge-zero interventions out of training.
- [x] 2.11 Add optimizer-surface tests for frozen vision/merger, exact DoRA/embedding/bridge groups, fresh optimizer state, learning rates, zero-initialized row output, and failure on missing or duplicated parameters.
- [x] 2.12 Implement joint trainable-surface discovery and the fresh AdamW/cosine optimizer groups with safe-step `L_route`, `L_use`, and row-cap ramps.
- [x] 2.13 Run targeted Qwen forward, cache/KV, loss, distributed-normalizer, and optimizer tests plus the existing bridge-disabled parity suite; compare checkpointing on/off logits, losses, bridge/DoRA/embedding gradients, and logical diagnostic counts for an ordinary branch plus all four `L_use` branches; validate OpenSpec and inspect gradient/identity-use receipts from a tiny synthetic backward.
- [x] 2.14 Complete separate Wave 2 engineering-standards and intent-contract audits; resolve all reproducible P0/P1 implementation findings before Wave 3, without using recall or rollout behavior as acceptance criteria.

## 3. Wave 3 - Four-presentation training and permanent checkpoints

- [x] 3.1 Add schedule tests proving one complete source-example pass per presentation, geo cache reuse, two distinct random caches, uninterrupted global safe-update accounting for ramps, EBS 24 derivation, ten-percent warmup, cosine progression, teacher-forced eval exactly at each predetermined presentation midpoint/end planned step over one fixed `geo_sorted` eval rendering, preservation of event identity when that step's optimizer update is skipped, no inference eval during training, and no optimizer reset at boundaries.
- [x] 3.2 Implement the continuous four-presentation cache-backed iterator and planned-step schedule, with deterministic distributed sharding and presentation-boundary receipts.
- [x] 3.3 Wire bridge context, matching, four shared-prefix owner-use branches, four losses, bounded metrics, and identical no-grad teacher-forced eval through the supervised trainer while keeping full-wrapper CE and existing bridge-disabled training unchanged.
- [x] 3.4 Add checkpoint tests for atomic adapter plus selected-embedding plus `owner_bridge.safetensors` publication, manifest companion identities, tensor hashes/shapes/dtypes, profile identity, failure cleanup, presentation checkpoints, and final alias rules.
- [x] 3.5 Implement permanent bridge payload save/load inspection and bridge-aware checkpoint publication without adding an exact optimizer-resume promise.
- [x] 3.6 Add bounded artifact writers for assignment/slot summaries, matched box quality, ordinary-unmatched masked counts, `P_U/P_C/p_null`, top-k routes, admission/row RMS ratios, swap margins, loss denominators, group gradient norms, presentation identity, scheduled/effective update identity, and deterministic trace panels.
- [x] 3.7 Add an exact S-lineage production config that resolves the step-2444 parent with `adapter.seed_mode: warm_start_expand_dora`, exact adapter and selected-token payload paths/fingerprints, original COCO-80 train/eval paths, no-resize 1024 limits, global length 12000, `ordinary_partial` trust, four Stage 1 presentations, eight planned-step midpoint/end teacher-forced eval landmarks over one fixed `geo_sorted` eval cache, EBS 24, agreed learning rates/weights/ramps, frozen vision/merger, and bridge profile.
- [x] 3.8 Run CPU/synthetic end-to-end training tests plus the smallest available real-tokenizer/model fixture that exercises rendering through checkpoint reload; verify no rollout actor, replay, hard coverage mask, or Stage 2 runtime is reachable.
- [x] 3.9 Run targeted training pipeline, schedule, checkpoint, artifact, protected-baseline, and cache-rebuild suites; validate OpenSpec and update accepted architecture/operator docs for the new optional permanent composition.
- [x] 3.10 Complete separate Wave 3 engineering-standards and intent-contract audits; resolve all reproducible P0/P1 implementation findings before Wave 4, without requiring a favorable training metric.

## 4. Wave 4 - Dynamic HF execution, smoke runs, and production launch

- [x] 4.1 Add strict inference-config and payload-identity tests for explicit bridge paths, exact companion composition, missing/extra/incompatible payloads, profile drift, forbidden checkpoint-final inference resolution, and bridge-plus-vLLM rejection before GPU setup.
- [x] 4.2 Extend owner-neutral model composition so dynamic HF loads base, DoRA, selected embeddings, and permanent bridge exactly once and records their validated identities.
- [x] 4.3 Add batched greedy lifecycle tests for atom prefill, boundary admission, native opener, best-non-null hard latch, opener/null disagreement, row retention, box-end route-before-clear, native stop, unequal per-sequence phases, and absence of cross-sequence leakage.
- [x] 4.4 Implement explicit per-sequence `BridgeDecodeState` through the HF generation/cache lifecycle and fail closed on unsupported branching or cache reordering; do not add a hard covered mask or forced token operation.
- [x] 4.5 Extend inference manifests and bounded traces with bridge fingerprint/profile, route/null mass, selected atom, latch/clear events, RMS ratios, and terminal lifecycle-integrity failures while preserving existing raw/scored/parser artifacts.
- [x] 4.6 Add serialization round-trip and dynamic-HF parity tests that compare in-memory versus reloaded bridge logits/states on a deterministic fixture and prove bridge omission fails rather than approximates.
- [x] 4.7 HISTORICAL ACCEPTANCE ONLY - this checkbox records that the algorithm formulas and their independent references were accepted on the pre-Wave 5 tree. It is not launch evidence: the battery itself MUST be re-executed on the repaired/frozen Wave 5 tree under reopened 4.8, and no pre-Wave 5 receipt may satisfy any launch gate. Run the algorithm-accuracy battery against independent references: brute-force injective assignment, FP64/permutation-invariant set routing, shared-prefix four-branch swap/common-pulse cancellation/value-and-atomizer gradient reach, RMS clipping, zero-bridge parity, block20-to-final causal visibility, activation-checkpoint on/off forward-and-gradient parity, packed/isolated parity, full-TF/forced-token incremental parity, reload parity, and batched lifecycle isolation.
- [x] 4.8 REOPENED (stale after the W8 collective choreography failure; its prior pass predates the Wave 5 runtime repair and the owner-use packing lane). Re-execute the complete task 4.7 algorithm-accuracy battery plus all targeted training and inference suites, bridge-disabled regression tests, strict OpenSpec validation, and import/residue checks on the repaired/frozen Wave 5 tree; retain exact commands, versions, tolerances, and receipts. The 4.7 algorithm formulas and their reference derivations remain accepted and are not redesigned, but every launch-bearing receipt MUST come from this re-execution; a stale pre-Wave 5 receipt cannot satisfy 4.13 or 4.14.
- [x] 4.9 Run a short exact-module single-rank GPU smoke from the S checkpoint with production activation checkpointing through all four losses, safe backward/optimizer steps, bridge checkpoint publication/reload, teacher-forced eval, and batched greedy HF decode. Decode evidence is prepared by materializing an HF inference config plus its companion `*.materialization.json` receipt under `configs/coordexp_swift/infer/materialized_owner_bridge/`, whose `final_checkpoint` path, `owner_bridge_payload_fingerprint`, `lineage_attestation_fingerprint`, `checkpoint_event_sha256`, and `receipt_sha256` MUST resolve to the smoke checkpoint this task actually produced. The tracked historical `owner-bridge-stage1-w1-fresh-d9d9768.{yaml,materialization.json}` pair is the pre-Wave 5 instance of that artifact and is bound to smoke checkpoint `...owner_bridge_stage1_single_gpu_ebs24_train1_val1-20260810T021144Z/checkpoints/step-4`; retain it unedited as provenance, while only the fresh frozen-tree `80ded9a` W1/W8 pairs may satisfy 4.13 or 4.14.
- [x] 4.10 After Wave 5 lands, rerun the bounded eight-rank Accelerate smoke derived from the exact production config with COCO/no-resize-1024/global-12000 semantics, global EBS 24, distributed loss denominators, checkpoint collectives, reload, and one bounded decode artifact. Entry requires an identical one-step choreography digest across all eight ranks (task 5.9); acceptance requires reaching the bounded step budget with at least one safe optimizer update, no watchdog timeout, and no control-gather failure. The 2026-08-10T02:50:27Z run that failed at `completed_steps: 0` does not satisfy this task.
- [x] 4.11 Record both smokes, the two-rank choreography acceptance, and the packed-versus-isolated receipt as mechanical/algorithm evidence only; require finite gradients, complete matching, exact identity, rank-identical collective sequences, no lifecycle leakage, and successful reload, but do not require smoke recall, duplication, atom AP, or stopping improvement. State explicitly that the prior W8 failure is runtime choreography evidence and carries no model-quality meaning.
- [x] 4.12 REOPENED (stale: the audited frozen tree predates the Wave 5 runtime repair, the unwrapped-branch change, and the owner-use packing lane). Re-freeze the implementation target after Wave 5 and obtain fresh independent engineering-standards and intent-contract audit verdicts; resolve and re-audit only accepted P0/P1 findings until the fixed tree passes.
- [x] 4.13 After Wave 5 and the reopened 4.8/4.12 pass, revalidate all readiness receipts against the frozen tree, inspect live GPU process/utilization/memory ownership without evicting unrelated work, select a unique collision-safe production artifact root, and create an at-most-once eight-rank launch command/guard.
- [x] 4.13a Implement and test the explicitly authorized one-time recovery successor before touching the canonical ledger: preserve and fixed-hash-bind the original claim, intent, activation, preflight, eight admissions, failed binding, and logs; keep the v1 claim validator unchanged; reserve the fixed recovery-policy attempt-1 successor by durable `O_EXCL` before nonce/payload derivation and never unlink it; bind the parent run root and a recovery-specific intent identity; preserve the guarded `run.json`-before-model/optimizer ordering proof; select exactly one closed-set claim/path by nonce before distributed initialization; reject unreadable/partial residue, successful-parent evidence, hash/run-root drift, reused nonce, ordinal drift, or any third activation; and obtain a fixed-target lifecycle audit after temporary-ledger red/green and regression tests pass.
- [x] 4.14 When all of Wave 5, the re-executed algorithm battery under 4.8, the choreography acceptance battery, tasks 4.9-4.13, and the recovery-successor gate in 4.13a pass on one frozen tree, and eight GPUs have safe headroom, use the lead-only executor to publish the one canonical append-only successor and launch the exact four-presentation Stage 1 production training through eight-rank Accelerate exactly once; record parent and successor identities, command, process ids, visible devices, code/tree identity, config fingerprint, parent checkpoint, and run root.
- [x] 4.15 Confirm all eight ranks initialize and at least one safe optimizer step plus finite/gradient/artifact heartbeat completes; then provide the durable run identity, monitoring/failure handoff, eight-landmark teacher-forced eval schedule, and post-final natural greedy HF evaluation command. If the recovery activation fails or remains uncertain before that heartbeat, retain all evidence and stop without a further activation.

## 5. Wave 5 - W8 collective choreography repair and owner-use performance lane

Scope guard for the whole wave: execution shape only. No task here may change pair sampling, loss weights or formula, ramps, data/order/annotation trust, EBS, pack-cache identity, or any inference path. Tasks 4.13-4.15 remain gated behind this wave.

- [x] 5.1 Record the W8 failure as runtime choreography evidence in `research/`: bind the run root `outputs/smoke/coordexp_swift_owner_bridge/...accelerate8_ebs24_train256_val64_4epoch_warmup0p1-20260810T025027Z`, `status: failed`, `completed_steps: 0`, the Gloo `Connection closed by peer` terminal error, and the per-rank counters quoted from `outputs/smoke/coordexp_swift_owner_bridge_readiness/d9d9768/w8-rank-logs/none_1ulisqjw/attempt_0/{0..7}/stderr.log` (ranks 0/4 enqueued 77; ranks 1/2/3/5/6/7 enqueued 79 and hung on `SeqNum=78 BROADCAST` under the 600s watchdog). Because `outputs/` is gitignored, quote the decisive lines into the research record rather than citing the paths alone. State explicitly that the failure carries no model-quality meaning.
- [x] 5.2 Restore the module-private `_RANK_REPORT_CONTROL_TIMEOUT_SECONDS` constant from 1800 to the bounded 120 and keep the static test that reads the constant and asserts it stays strictly below the effective NCCL watchdog timeout. Do not add a config-schema surface for it, and do not record the timeout change as the repair.
- [x] 5.3 Add failing tests for one wrapped anchor forward per physical accumulation slot per rank across real, shadow, and no-data slots, one combined backward per slot, and a defined zero-gradient contribution for every trainable parameter on a shadow slot.
- [x] 5.4 Replace the parameter-only shadow loss shape with a deterministic differentiable dummy anchor forward through the wrapped model feeding the existing connected-zero surface; keep gradient synchronization on the final slot of the window.
- [x] 5.5 Move every `L_use` auxiliary branch forward to the unwrapped model over the same `Parameter` objects; add tests proving DoRA, selected-embedding, and bridge gradient reach is unchanged, that local branch count no longer changes the collective sequence, and that eligible-count numerators, global denominators, and world-size scaling are byte-identical to the pre-repair definition.
- [x] 5.6 Add a real two-rank data-parallel acceptance test with uneven eligible branch counts and mixed real/shadow slots that asserts one anchor forward per slot per rank, one optimizer update per window, gradient parity against a single-process reference, and an identical one-step W8-shaped choreography digest of ordered collective kinds and counts; no mocked process group.
- [x] 5.7 Add failing equivalence tests for the truncated owner-use branch: segment-local causal prefix plus candidate row must reproduce full-pack branch logits, `L_use`, and gradients within declared tolerance, with MRoPE position ids, image-token mapping, and visual carrier states preserved verbatim.
- [x] 5.8 Implement the truncated branch lane and the packed multi-branch forward with FA2 `cu_seq_lens` isolation; add tests and a receipt covering realized `cu_seq_lens_q`/`cu_seq_lens_k` boundaries, packed-versus-isolated equality, absence of cross-branch and cross-prefix attention, and fail-closed behavior when a branch boundary is missing.
- [x] 5.9 Capture the one-step W8 choreography digest across all eight ranks on the repaired tree and confirm it is rank-identical before rerunning the full bounded W8 smoke under task 4.10.
- [x] 5.10 Validate OpenSpec strictly, run the bridge-disabled and single-rank regression suites, and confirm no pair-sampling, loss, data, EBS, cache, or inference behavior changed; retain exact commands, tolerances, and receipts.
- [x] 5.11 Add failing tests for a guard-only parallel cache-inspection lane that uses at most 16 workers, validates every declared payload chunk exactly once, preserves manifest order and owner/order evidence byte-for-byte against serial inspection, and fails closed on corrupt or missing chunks without entering the cache-build seam.
- [x] 5.12 Implement process-parallel chunk validation plus compact owner/order evidence under the pack-cache/materialization owners, make the Stage 1 launch guard use it without changing cache semantic identity or ordinary training readers, and retain a serial-versus-parallel wall-clock/RSS receipt before considering it for a future frozen launch tree.
