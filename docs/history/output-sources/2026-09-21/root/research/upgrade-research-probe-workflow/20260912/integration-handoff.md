COMPLETED: canonical fast-forward merge and temporary worktree retirement are finished. See completion.json and committed acceptance.md for final state. No rearm or additional GPU work is required. The following packet is historical.

# Deferred canonical integration

Current task: research-probes-优化, root `01a0947d-63a4-7f63-8442-1674f87586e2`.
User authority persists: implement after proposal + independent audit without another approval; merge after active research releases affected executable dependencies. Do not send messages to other user tasks, stop their jobs, restore their dirty files or unlock the canonical worktree.

Candidate: `/data/CoordExp/.worktrees/research-probes-upgrade-20260912`, branch `codex/research-probes-upgrade-20260912`, clean at `5904e8ec244932a62b14e3d93c53ebdcf83bb9f1`. Target is locked `/data/CoordExp/.worktrees/research-probes`, branch `research-probes`, last observed `ef6d44d1196a7043deeef90cefc90136bce9859f`. The target is still the tested base and an ancestor of the candidate. No canonical merge has occurred.

Implementation and validation are finished. Read `lead-acceptance.json`, `integration-candidate.json`, and committed `openspec/changes/upgrade-research-probe-workflow/acceptance.md`; do not repeat the investigation, grill, design or independent review. OpenSpec tasks 1.1–5.1 are done; only 5.2/5.3 remain. Ordered commits are a66ec668c, bd8dcee, 5904e8ec2. All25 changed/new Python hashes and all8 final runtime log/receipt hashes matched after the commits. GPU work is finished: total conservative0.825686 minutes of60; do not rerun the model merely for Git movement.

CPU suite baseline971passed/14failed; final1014passed/same14failed. No new failures. Real one-image model slice: one pixel materialization, four forwards, one update, exact old/new logits and same-backend scores,588selected tensors updated, frozen complement unchanged. Cold CPU recheck passed; only cross-device log-softmax roundoff≤1.2e−6. Exact evidence and limits are in acceptance.md.

The blocker is the still-active task “推进实验”, ID `01a06f6d-2336-7670-a929-8c4d56ed54ba`, host `remote-ssh-discovered:pein-train`, cwdcanonical. The latest App list reported active. Eight of34 initial excluded dirty files had live changes; candidate paths overlap none. Preserve those live changes; old hashes are an ownership baseline, never restoration authority. `wait_threads` once returned not-found despite `list_threads` still showing active; this is not release evidence.

The initial thread_idle registration failed before arming: stage=prepare_condition, app-server thread/read timed out (10.6 seconds). That failure is preserved in merge-wake-monitor.failed-thread-read.json; no monitor ID was returned. A new log_pattern monitor is being armed against the verified public active rollout, matching the current turn task_complete or turn_aborted/turn_failed records appended after registration. The active turn is01a09430-b272-7ee3-87c6-3cddcbb3b6f8; the last recorded tool iswait_agent(timeout_ms=3600000), not a completed task. Active physical path is in active-research-rollout-path.txt. Rotation/truncation is observer failure, not success. Receipt will be saved as `merge-wake-monitor.json`. On its delivered pointer call `wake_me_up_status(monitor_id, view="decision")` exactly once. Idle, expiry, disappearance or observer failure is never proof of research completion/release. Inspect the task's actual latest result and relevant live dependencies. If idle only handed off continuing workers/jobs, the boundary is not released. Do not automatically rearm; a genuinely new occurrence requires an explicit fresh bounded monitor with a new idempotency key and rearm_of after inspecting the witness.

After actual release:
1. Reinventory all registered worktrees, target HEAD, lock and dirty ownership. Read the latest canonical AGENTS requirements. Reconcile any new committed target into the isolated candidate; resolve only authorized conflicts and rerun checks warranted by changed execution dependencies. Unchanged Python hashes retain the fixed-tree runtime evidence.
2. Verify the candidate contains the current target and has no dirty-path overlap. Capture the current unrelated dirty hashes immediately before integration. Merge the accepted candidate into canonical with ff-only when possible, preserving the lock. Verify resulting HEAD and unchanged unrelated dirty bytes; run the current inspection entry/affected focused acceptance as appropriate.
3. Record actual merge and complete tasks5.2/5.3 with explicit documentation commits. Preserve runtime receipts and staged source snapshots. Retire the temporary worktree only when clean, merged and evidence replay remains possible. Current cold-consumer script verifies recorded original source paths; before retirement either retain the source checkout or explicitly support a verified relocation in the task-local reader while leaving original passing scripts/receipts unchanged. Do not leave an advertised command knowingly broken.
4. Final response must distinguish implementation, verification, actual merge and worktree retirement. No push is requested.

Memory was used for isolation guidance. If referenced in final, cite MEMORY.md:2065-2066 and2102-2103, rollout01a0470a-cf06-7061-80d0-3dc3a5afa3aa, using the required final memory block. Do not update memory without a user request.
