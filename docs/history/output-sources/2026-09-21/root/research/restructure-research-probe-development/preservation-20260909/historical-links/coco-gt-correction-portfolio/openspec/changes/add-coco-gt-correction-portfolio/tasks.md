## 1. Seal the experiment-specific bank

- [x] 1.1 After a new apply request, create the direction worktree from clean `research-probes` HEAD, transfer only the owning change/unit, and record source commit plus planning hashes; verify branch/worktree identity, scoped Git status and unchanged historical inputs before edits.
- [x] 1.2 Implement the narrow bank builder using the unit's one-event-per-image rule and exact Source offsets; verify train/dev disjointness, annotation/token/media digests, all-image anchor counts, both cuts, canonical uncovered targets and the terminal EOS/partial-tail counterexamples against real source records.
- [x] 1.3 Seal paired full/selected-row masks and fixed denominators; run one caller-facing check proving identical R/M strings and selected-row coefficients, zero prompt/prefix loss, unchanged canonical EOS, and rejection of a deliberately altered owner/token identity. Record standards and scientific-contract verdicts separately; no unresolved P0/P1 before the next group.

## 2. Qualify the real update and cold consumer

- [x] 2.1 Implement the single probe trainer with declared DoRA versus output-residual surfaces, full-vocabulary image-normalized loss and the sealed dose; verify finite real gradients, exact trainable parameter layout, frozen base/vision/embedding state and unchanged historical entrypoints, without adding a generic dispatcher.
- [x] 2.2 Implement atomic completed-update save/resume and the probe-specific cold HF consumer; verify both surfaces load their actual payload, output removal restores Source, nonzero W changes the intended logits but not same-prefix hidden states, and wrong/missing/foreign-arm state fails closed. Remove any temporary parallel save/load path introduced during the slice, not historical code.
- [x] 2.3 Run a bounded real-image maximum-length qualification for both surfaces: one-/two-rank loss and update agreement, at most two optimizer steps, save, one interrupted-boundary resume comparison and fresh cold decode/evaluation. Record effective forward counts, peak GPU/RSS, wall time and payload size; inject one representative mask/payload fault to demonstrate check sensitivity. Lead records separate standards and intent-contract verdicts; mechanical-only evidence does not count as a scientific arm.

## 3. Execute the frozen four-arm pilot

- [x] 3.1 Seal the resolved recipe, resource estimate, code/bank IDs and exact native decode topology; verify the qualification fits the proposed runtime ceiling and reconcile existing GPU jobs/output roots before launching. Any material contract change goes back to the user rather than silently adjusting dose or population.
- [x] 3.2 Launch R/B/M/W independently on two GPUs each with durable job IDs, isolated roots and one invocation owner; verify each arm reaches exactly the fixed completed-update count or records its terminal mechanical state, with no extra rounds, dev-based selection or inter-arm warm start.
- [x] 3.3 Cold-evaluate final valid arms on the unchanged train256/dev128 and matching Source reference; verify all-image denominators, raw natural outputs including caps, payload lineage and unchanged evaluator settings. Report per-arm owner gains/losses at IoU50/60/80, debt counts, lengths and the three paired R contrasts.

## 4. Close at the actual evidence level

- [x] 4.1 Write the bounded result and update this unit/router, distinguishing promising, mixed, memorization-compatible and mechanically unresolved outcomes; lead reproduces the decision-bearing counts from saved artifacts. Stop after the terminal pilot evaluation with no sweep, combined winner or promotion.
- [x] 4.2 Inspect actual team handoff/rework evidence; if it demonstrates a general coordination defect, make one scoped native-team-skill correction and run its existing validator plus a concrete sensitivity/example check. Otherwise record that no skill change is warranted. Do not add a hypothetical team framework.
- [x] 4.3 Run targeted research-document/link/frontmatter checks, `openspec validate add-coco-gt-correction-portfolio --strict`, and scoped `git diff --check`; record implementation acceptance separately from scientific disposition and confirm no unowned files, obsolete new entrypoints, duplicate jobs or unfinished P0/P1 corrections remain. Archival/publication is not implied.
