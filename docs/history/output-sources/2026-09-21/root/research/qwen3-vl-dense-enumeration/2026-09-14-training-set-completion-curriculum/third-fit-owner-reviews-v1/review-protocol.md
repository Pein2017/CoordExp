# Third-fit step32 physical review contract

Review one assigned image only. Output is candidate until root acceptance. Use its packet under third-fit-review-packets-v1/image-<12digit>/packet.json. Target is target-owners-complete-v4.json (232 atomic owners across11 images; six crowd annotations separate). Parent actual native step16 is parent16-v4-physical-ledger-v1/ledger.json, SHA256 f0536ed6a5a158f8bf296f1e736a5b0bdbee9d52349179f4b0ef1064bfcfce5e. Parent geometry-qualified coverage totals155/232, with symmetric root image25274 geometry rulings and two parent-present v4 admissions.

Inspect the original image, target and prediction overlays, and bbox context/tight crops for each distinct valid visual group. Same-image crop sheets are allowed; no multi-image montage. Every original raw prediction must survive in the decisions. An exact-signature shared crop can support multiple raw members, but those remain repeated model predictions. Different boxes on the same owner are also repeats even below IoU.95. Invalid or malformed rows can be marked invalid_output from raw parser evidence; never infer physical FP merely from invalidity or GT-unmatched status. Do not repair generated axes for credit.

Use raw physical identity AND reasonable geometry for coverage; class errors are separate. Count an owner once if any parsed-valid, reasonable row with true_unique/repeat identity supports it. Preserve every fixed owner in the missing/covered partition. Respect root overrides and geometry uncertainty. Unknowns remain unknown. Verified new objects receive pending_new IDs and reference proposals, but do not change the fixed v4 denominator without root admission.

Write exactly review.json plus any same-image evidence/validation script inside your assigned image directory under third-fit-owner-reviews-v1. Schema: third_fit_step32_owner_review.v1. Top-level fields: image_id, checkpoint_step:32, target_catalog (path and sha256), source_packet (path and sha256), status:candidate_ready, raw_row_count, decisions (flat JSON list), summary, new_owner_candidates, validation.

Each decision has prediction_id, generated_order, visual_group_id, owner_id (fixed-v4 ID, pending-new ID, or null), physical_status (true_unique/repeat/false/unknown/invalid_output), extent (reasonable/wrong/unknown), class (verified/wrong/unknown), coverage_eligible (bool), direct_CE {bbox:positive|mask,description:positive|mask}, reason, evidence_paths. Repeated physical owners never receive direct positive CE; a verified unique owner with reasonable geometry can have bbox positive and class masked. Unknown or invalid/false/wrong-extent rows have both fields masked. New candidates remain masked until root admission.

Summary: target_owner_count, covered_owner_ids, missing_owner_ids, covered_owner_count, missing_owner_count, physical_repeat_row_count, confirmed_false_row_count, physical_unknown_row_count, invalid_output_row_count, class_wrong_row_count, class_unknown_row_count, raw_stop_reason, cap_debt, parent_covered_owner_ids, retained_parent_owner_ids, lost_parent_owner_ids, newly_covered_fixed_owner_ids. Report whether the stage03 appended GT/reference repair is qualified, and identify earliest observed repeat/structural issue when possible. No all-stage pass claim. Validate all raw prediction IDs, exact group membership, coverage eligibility and set partitions, source/evidence hashes, and class/extent/mask field domains.

Scientific question: did verified absent-owner support enter natural greedy before raw errors or stopping degeneration? No training/eval/GPU, no source edits or denominator mutation. Stop after the assigned single-image review and reproducible candidate validation.

Current target SHA256: b5c6534259a46583466dc5b92ae0b4b7854cc5adc4de8bc84801f3d96bc69f64. Read second-fit-root-rulings-v1/rulings.json and admissions.json where relevant. Use current full references for the TV including stand/base (99937), full plant (323322), clear serving vessel (219546), rear wheel (388795), white spoon (528944), and corrected L08/L09 persons (25274). Raw generated extents remain unmodified. Third-fit uses a synthetic complete corrected teacher; this review only judges the actual empty-prefix native step32 output.

User ruling 2026-09-15: Persist every view_image interpretation locally as review-notes.jsonl after each viewed group; complete all raw rows. Confirmed valid unlabeled owners enter annotation JSONL as an additional unlabeled list after root admission, with stable ID, full bbox, separate class status and reusable local evidence. Review.json remains the full per-row result. Preserve unknown/held candidate decisions as a reusable pending ledger.
