# Root grant: one exact A17 reconstruction labeled D, then cold reload

Root freshly accepted13 focused tests and current source-bound input validation.
The execution-loop delta preserves the A path with zero margin force and adds
per-step archived scalar/gradient/adapter/optimizer equality. No redundant A2
GPU smoke is needed: prior actual weight-zero parity plus fail-at-first-step
checks cover this selected-state reconstruction.

Only `/root/sol_high_training_recovery` owns this invocation. Reconcile an
existing matching D17 process/receipt before launch; join valid work rather
than relaunch. Expected shared stress occupancy does not require waiting.

- Producer SHA416f8760c435f540142add93588146e6636912769da471f78259ff3b7038026e.
- Input `trainer-preparation/inputs.json`, SHA
  4490888949446462e7fb4f917fe0b7ad1c9a3fc9ea39cc3cbad3d06d572731da.
- Exact selection and resource limits are bound by `selection.json` and
  `trainer-preparation/resource-envelope-D17.json`.
- Invoke `reconstruct_D17`, then upon a completed verified receipt `cold_D17`,
  exactly as recorded in `trainer-preparation/proposed-commands.json`.
- Output root `D17/` beside this file. One17-update all8GPU reconstruction:
  1961model/image forwards,1768backwards,136sync,80source refs,56final refs,
  8loads,zero sampling. Cold adds one load/three scores, at most600s.
- Expected final adapter tensor state is
  80c9c39c42e9e76dfeda8b10abf7a66928bbf4fd1e1a7c1df79af6a47e1c5dd4;
  all17 archived state/optimizer/gradient oracles must match, not just final.
- Complete raw logs and exit status retained; long event-driven waits, no
  short polling or sleep loops. A failed/incomplete stage is not research null.

No automatic retry, extra update, endpoint, old-artifact overwrite, source
mutation, GT/token/architecture change or promotion is granted. Return the
sealed receipt, exact cold equality and resource outcome; then stop. Root will
separately accept and authorize the single endpoint.
