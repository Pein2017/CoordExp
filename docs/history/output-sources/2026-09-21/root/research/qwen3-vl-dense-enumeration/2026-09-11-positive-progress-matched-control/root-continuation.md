# D17 matched-progress control — completed, no active continuation

Owner: root. Date:2026-09-11 UTC. The user-approved one-control unit is complete.
Do not resume any worker/launch grant from earlier messages. All selected
training, cold and endpoint evidence is present and freshly root-verified.

Authority and reading path:
- Unit/results: `/data/CoordExp/.worktrees/research-probes/research/investigations/qwen3-vl-dense-enumeration/experiments/2026-09-11-positive-progress-matched-control/{unit,results}.md`.
- `root-acceptance.json` owns current scientific/technical/stop disposition.
- `selection.json` pins A after17 updates, scores before18, not A18.
- `D17/receipt.json`, `D17/cold-check.json`, `endpoint-D/result.json`,
  `endpoint-D/consumer.json` and `endpoint-D/conditional-consumer.json` are
  exact source artifacts. Root13 focused tests, native receipt and cold
  equality, and actual endpoint merge verification all passed.
- `root-diagnostics.json` owns outside-reference/posthoc slices, exact owner
  geometry and the four root view_image observations.
- `visual-review-v2/` is accepted. Earlier `visual-review/` contains partial C
  adapters from a root-side list/tuple interface failure before any render;
  preserve it. No model or research output was rerun because of that failure.

Decision: C's reference/aggregate advantage survives0.75% scalar positive-NLL
matching; it is not loss-free or uniform transfer. C versus D gains27TP50
in384 images, but reference56 contributes+31 and outside328 contributes-4.
D has two caps in protected references, and more successful owners on two
positive cases. Posthoc excluding those caps leaves C+7TP50 but more parser
drops. Equal positive NLL does not equate32vs17 optimizer/KL exposures or full
conditional distributions. No unique margin/KV/owner-ledger mechanism claim.

No checkpoint/architecture/GT/independent-holdout promotion. The next suggestion
is a fresh unselected retained-model evaluation before more training. This is
only a proposal; no new run, equal-KL control, coefficient or dose sweep is
covered by the completed grant.

All Sol-high/Sol-xhigh/Luna-max L1 workers completed their scopes; no L2.
Endpoint recorded PIDs were checked absent. Full raw logs and exits retained.
One wake registration failed after10.2s at resolve_delivery_target / app-server
thread/read timed out. No monitor was armed; root used long native event waits.
There is no wake registration to cancel and no background task left to join.

Preserve unrelated dirty work, all old accepted sources and source artifacts.
No git staging/commit/cleanup or managed-memory write was requested or done.
