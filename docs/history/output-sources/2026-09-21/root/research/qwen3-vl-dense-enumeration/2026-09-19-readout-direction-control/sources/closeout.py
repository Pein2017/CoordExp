"""Parent candidate synthesis; no outcome-selected arms or composite quality score."""
import json,hashlib,datetime,csv
from pathlib import Path
R=Path('/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-19-readout-direction-control');U=Path('research/experiments/2026-09-19-readout-direction-control')
def bind(p):return dict(path=str(p.resolve()),sha256=hashlib.sha256(p.read_bytes()).hexdigest())
def main():
 d=json.loads((R/'reduction.json').read_text());v=json.loads((R/'verification.json').read_text());plan=json.loads((R/'execution-plan.json').read_text());b=json.loads((R/'binding-verification.json').read_text());pilot=json.loads((R/'runtime/qualification-receipt.json').read_text());scale=json.loads((R/'runtime/scaleout-receipt.json').read_text());assert d==json.loads((R/'reduction-recheck.json').read_text());assert len(d['cells'])==v['cells']==121;assert not b['unresolved'];assert sum(c['control_equal'] is True for c in d['cells'])==22
 cost=dict(model_forwards=pilot['model_forwards']+scale['aggregate_model_forwards'],vision_forwards=pilot['vision_forwards']+scale['aggregate_vision_forwards'],gpu_seconds=pilot['gpu_seconds']+scale['aggregate_gpu_seconds'],tensor_bytes=pilot['tensor_bytes']+scale['aggregate_tensor_bytes']);now=datetime.datetime.now(datetime.timezone.utc).isoformat()
 selection={x['id']:x for x in json.loads(Path(plan['selection']).read_text())['boundaries']};positions=[]
 for c in d['cells']:
  boundary=selection[c['boundary_id']];prefix=boundary['native_tokens'][:boundary['source_row']['end']];steps=[];row=0
  for i,token in enumerate(c['tokens']):
   distance=next((j for j,x in enumerate(reversed(prefix)) if x==151648),None);role=['x1','y1','x2','y2','box_end'][distance] if distance is not None and distance<=4 else 'row_boundary' if prefix[-1]==151649 else 'description_or_wrapper'
   steps.append(dict(free_offset=i,absolute_action_offset=len(prefix),lexical_free_row_index=row,role=role,token_id=token));prefix.append(token);row+=int(token==151649)
  positions.append(dict(cell=c['id'],steps=steps))
 (R/'positions.json').write_text(json.dumps(positions,indent=2)+'\n')
 with (R/'outcomes.csv').open('w') as f:
  fields=['boundary','policy','longest_exact_run','longest_near_run','invalid_rows','malformed_openers','complete_rows','stop','native_near_returns','alternate_repeat_starts'];writer=csv.DictWriter(f,fieldnames=fields);writer.writeheader()
  for c in d['cells']:
   m=c['metrics'];writer.writerow(dict(boundary=c['boundary_id'],policy=c['policy'],**{k:m[k] for k in fields if k in m and k!='alternate_repeat_starts'},stop=c['stop'],native_near_returns=len(m['native_near_return_rows']),alternate_repeat_starts=len(m['alternate_repeat_starts'])))
 scientific='True normalization is not uniquely privileged for numerical recurrence on this fixed panel. Reflected and several fixed balanced-sign controls also reduce runs without shorter termination or additional invalidity in some cases. Direction-specific outcomes remain heterogeneous; generic perturbation/path sensitivity remains viable, with no physical-recovery or training-origin conclusion.'
 result=dict(status='INTEGRATED_CANDIDATE',acceptance='unreviewed_by_root',created_at=now,unit=U.name,scientific=scientific,technical='121/121 cells,22 exact unchanged controls,33308 saved steps CPU reconstructed',cost=cost,summary=d['summary'],unsupported=[],live_jobs=[],limits=['Eight vectors are fixed correlated-panel interventions, not88 independent images or a bestseed search. No outcome-weighted success scalar.','Magnitude equality is at identical FP64 states; FP32 rounding and later divergent histories are explicitly separate.','Numerical near-row recurrence is not physical-owner identity; another loop/EOS/shorter output is not recovery.','T/U is a model-package comparison; no untie-only, training-origin or universal mechanism inference.','Target outputs only; companion outputs not evaluated.','Pilot OS exit was not observed; candidate-complete artifacts and ended process are observed. All8 scaleout wrapper exit codes are observed0.'],tie={'count':sum(len(x['exact_ties']) for x in v['reports']),'case':'tied-417044-failure--sign23 offset125, original shadow152004/152005 exactFP32 tie','handling':'Recorded maximizing score equality; no numerical tolerance change or model rerun.'},evidence=[bind(R/n) for n in ['execution-plan.json','sign-vectors.json','reduction.json','verification.json','binding-verification.json','outcomes.csv','positions.json','ARTIFACTS.md']])
 (R/'result.json').write_text(json.dumps(result,indent=2)+'\n')
 lines=['# Readout direction at matched coordinate-change magnitude','',f'Candidate completed {now}; root acceptance pending.','',scientific,'','## Fixed panel and technical evidence','','The original11 component-study failure prefixes, not the later crossover cuts, each received original/full/reflected/sign19–26 continuation. All121 cells complete; no exclusions, extra seeds or model reruns. All22 original/full controls reproduce their accepted saved outputs exactly. All110 first divergences from original are coordinate-to-coordinate; each retains all11 shadows at the same original state.','',f"CPU reconstruction matches saved scores exactly across33,308 steps. FP64 magnitude residual is at most {max(x['fp64_magnitude_max_abs'] for x in v['reports']):.3g}; maximum finalFP32 absolute-change rounding residual is {max(x['fp32_change_magnitude_rounding_max_abs'] for x in v['reports']):.3g}. The saved-score corruption check detects+0.01. All eight500+/500− sign vectors were hashed before GPU calls and reused unchanged across both models/scenes/roles/steps.",'','## Entire fixed-direction comparison','','Each table cell is **longest near-run / invalid rows / stop / free complete rows**; R=32-row stop, E=EOS. Full per-cell exact runs, native return, alternative patterns and first forks are in `outcomes.csv` and `reduction.json`. These numerical dimensions are not combined into a quality score.','', '| Boundary | Original | Full | Reflected | s19 | s20 | s21 | s22 | s23 | s24 | s25 | s26 |','|---|---|---|---|---|---|---|---|---|---|---|---|']
 for bid in selection:
  cc={c['policy']:c for c in d['cells'] if c['boundary_id']==bid}
  def fmt(p):
   c=cc[p];m=c['metrics'];return f"{m['longest_near_run']}/{m['invalid_rows']}/{'E' if c['stop']=='eos' else 'R'}/{m['complete_rows']}"
  lines.append('| '+bid+' | '+' | '.join(fmt(p) for p in plan['policies'])+' |')
 lines+=['','All cells have zero malformed openers and zero512-token caps. EOS and alternative-pattern counts overlap; lower run length does not establish distinct physical owners.','', '## What discriminates the hypotheses','','- **Untied5586:** original/full longest near-run30. Reflected gives11, and sign20/21/22/23/24/25 give20/12/14/10/20/6, all32 rows with zero invalid rows. Numerical relief is therefore not unique to the norm-equalizing direction, even without early stopping or extra invalidity.','- **Tied417044:** full gives6 with one invalid row; sign20/22/23/24 each gives1 with zero invalid rows, all at32 rows. These fixed controls supply another counterexample to uniqueness; no owner recovery is inferred.','- **Untied417044:** full and sign20/22/23/24 all give1 and zero invalid rows, but terminate after26–29 rows. This comparison retains termination as a competing explanation.','- **Damage remains:** reflected and several signs add invalid boxes on885; full itself lengthens recurrence on885/632. Untied7511 full shortens the run but adds one invalid row and stops earlier. No fixed direction is a uniformly beneficial policy.','', 'For transparency, counts of shorter/longer runs versus original (not success rates):','', '| Direction | T shorter/longer (5) | T invalid/EOS | U shorter/longer (6) | U invalid/EOS |','|---|---|---|---|---|']
 for p in plan['policies'][1:]:
  a=next(s for s in d['summary'] if s['model']=='tied' and s['policy']==p);z=next(s for s in d['summary'] if s['model']=='untied' and s['policy']==p);lines.append(f"| {p} | {a['shorter_near_run_than_original']}/{a['longer_near_run_than_original']} | {a['invalid_rows']}/{a['eos']} | {z['shorter_near_run_than_original']}/{z['longer_near_run_than_original']} | {z['invalid_rows']}/{z['eos']} |")
 lines+=['','## Runtime and archival limits','',f"All8 GPUs used. Total {cost['model_forwards']:,} batch forwards, {cost['vision_forwards']} vision passes, {cost['gpu_seconds']:.3f} allocated GPU-seconds ({cost['gpu_seconds']/3600:.3f} GPU-hours), {cost['tensor_bytes']:,} reported tensor bytes. All owned processes and Luna-max child ended.414 bindings verified with no unresolved source gap.",'','Pilot exit0 had initially been inferred incorrectly; the original receipt is preserved and corrected to null/unobserved. Artifact completion, exactcontrols and endedPID are verified; scaleout has actual0 exits. One CPU verifier failure was an exact FP32 shadow topk/argmax tie, preserved and corrected to test the maximizing score while recording both indices; no producer/score/tolerance changed.','', 'Root-owned catalog result registration is the only expected knowledge-check gap. Accepted predecessors remain unchanged. Exact commands/artifact map: `'+str(R/'ARTIFACTS.md')+'`.','', '## Claim boundaries','']+[f'- {x}' for x in result['limits']]
 (U/'results.md').write_text('\n'.join(lines)+'\n');state=json.loads((U/'state.json').read_text());state.update(lifecycle='closed',evidence='unreviewed',state_as_of=now,disposition=scientific,result=str(U/'results.md'),state_source=str(U/'results.md'),next_action='Root independent acceptance; worker stops. No automatic successor.');(U/'state.json').write_text(json.dumps(state,indent=2)+'\n');print(json.dumps(dict(result=str(R/'result.json'),cost=cost)))
if __name__=='__main__':main()
