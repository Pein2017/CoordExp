# First-coordinate mediation evidence

- `execution-plan.json`, `selection.json`: frozen two original417044 failure prefixes × nine continuing readouts × forced0/1;36 cells,18 concordant controls. First five tokens and offset5/x1 fixed before execution. This is a post-hoc mechanistic follow-up, not held-out efficacy.
- Frozen signs are hash-bound directly to the accepted direction-control `sign-vectors.json`; no new directions or coefficients.
- `sources/`: exact producer snapshots and dependency bindings, including pre-launch guard correction. Accepted predecessors unchanged.
- `runtime/<shard>/<cell>/release.json`: complete tokens/stop, raw and readout winners, one target-only offset5 carrier override, actual emitted token and next native-input witness. Every other step is greedy under its continuing operator.
- Same cell `trajectory.pt`: actual head inputs, raw/allnine operator coordinate scores, applied scores, carrier scores, override flags and emitted tokens. Carrier values are an injection mechanism, not model/readout predictions. No KV transplantation or input-embedding edit.
- `reduction.json`: primary metrics ONLY after the first completed partially controlled row; first-row geometry/validity and whole-continuation metrics kept separately. No controlled owner gets recovery credit.
- `verification.json`: independent CPU formula/choice/consumption verification, all18 exact controls and corruption tests for wrong forced position/token.
- Shard/cell/launch receipts: model/input/producer hashes, costs and actual exits. Parent `integrated-terminal.json` is the package candidate, not child completion.

CPU reconstruction:

```sh
python -m probes.training_set_completion.first_coordinate.reduce --root /data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-19-first-coordinate-mediation --out /tmp/first-coordinate-reduction.json
python -m probes.training_set_completion.first_coordinate.verify --root /data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-19-first-coordinate-mediation --out /tmp/first-coordinate-verification.json
```

No coefficient uniqueness, abstract coverage ledger, physical recovery or training-origin claim. Coordinate0 may legitimately denote clipping. EOS/shorter output/another pattern is not enumeration recovery. All eight fixed signs remain in the comparison.
