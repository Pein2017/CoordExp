# Owner-recurrence artifact index

- `panel.json`: frozen two executable cases / one HOLD, same native batches, literal nine-token rows and attempted-token budgets, config/bank/source identities.
- `source-manifest.json`, `verification.json`: finite eight saved-candidate screen, sixteen O/N source integrity checks; no new proposal generation.
- `admission/result.json`: immutable worker all-HOLD candidate; `root-admission.json` owns execution-root corrections for two cases and physical/extent limits. Neither is cross-study lead acceptance.
- `admission/root-detail/`: selected full-image plotted boxes and enlarged local views; no added image information or training labels.
- `producer.py`: exact native replay; `producer-v2.py`, `producer-v2.diff`, `mechanical-amendment.json`: same generation with early-EOS-safe sparse diagnostic finalization.
- `runtime/<image_id>/<arm>/raw.json`: all four complete token sequences, native text, EOS/cap, supplied interval, row token log probabilities.
- `runtime/<image_id>/<arm>/sparse-logits.pt`: full-vocabulary logits and actual final head inputs at declared/reached offsets and first replacement fork. No full KV, attention or all-step hidden-state archive.
- `runtime/<image_id>/<arm>/receipt.json`: model/runtime/input/position identities, no-mutation checks, producer hash, counts, time and success/failure.
- `logs/*.log`, `logs/*.exit`, run scripts and shell PIDs: command and exit evidence. Successful execution is separate from scientific benefit.
- `reduce.py`: independent native-parser/one-to-one known-bank reduction with symmetric supplied-known-owner union exclusion. Physical identity is a separate evaluation sidecar.
- `preparation/`: unlaunched initial prototype, change diff, and accounting fixture; not qualified model evidence.
- `team-observations.md`: bounded briefing/admission/CPU-check corrections. No skill or persistent-memory changes.

## Closed candidate

- `result.json`: candidate technical/scientific receipt; `terminal.json`: final settlement witness.
- `known-result.json`, `known-result-replay.json`: exact independent CPU replay with full/common/supplied/free match ledgers and per-owner G/L.
- `physical-review.json`, `review/`: bounded physical changed-cluster evidence and unresolved cases.
- `context-repeat-counts.json`: free repeats with earlier prefix/supplied rows as references, plus free-internal counts.
- `decision-scores.json`: candidate row log probabilities, first-fork ranks/margins and sparse-capture positions.
- `final-verification.json`: all32 source bindings, companion/prefix/row/EOS checks, native score identity, no model jobs.
- `knowledge-check.json`: only unresolved frontier-state link, owned by cross-study lead; `knowledge-tests.log` passed.

Reproduce the independent consumer from the owning checkout:
`PYTHONPATH=. python /data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-17-owner-recurrence-row-branch/reduce.py --output /tmp/owner-recurrence-replay.json`
No model call is made. Sparse captures omit offsets after natural EOS, recorded explicitly; they do not extend generation.
