# Decision-relevant correction

Root parallelized runtime implementation with admission. The bounded candidate screen failed physical-owner admission, so implementation was premature. The unlaunched prototype was preserved under preparation rather than added to maintained probe code. Worker reported compilation, lint and a CPU interval-invariant check; no GPU or native-runtime qualification occurred. Future equivalent work should admit at least one concrete physical contrast before implementation. This is a briefing/dependency correction, not evidence about a model family capability ceiling.

The candidate reviewer initially called a GT-matched shelf box a clear owner. Root inspected the original image and identified stack/group extent. Reviewer revised to HOLD. Annotation identity alone is not physical single-owner identity.

Root follow-up corrected an over-strict all-HOLD ruling: focused book/bird views allow bounded owner attribution despite crowd context and reference-IoU mismatch. Matching category is a preference where feasible, not a universal gate. Two cases proceed; one selected ambiguous case remains HOLD. Prototype retained for this execution only; real native-arm bookkeeping bug fixed before model launch. Toy-only initial CPU checks were replaced by checks of the shared real transform seam. Actual native replay remains the technical gate.
