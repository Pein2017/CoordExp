# Decision-relevant team corrections

- Anchor worker reused accepted physical evidence and returned book/477415 branches HOLD rather than inventing identity from reference IoU. Root kept the admitted bird contrast and continued the independent norm-timing branch. The anchor prose's generic A/B/C supplied exclusion does not apply to scored-only C; the frozen StageA panel excludes A/B only.
- Runtime worker initially forwarded StageA into the predecessor's one-row-only interface and scaled companions during pulses. Root identified that neither implements the declared history/target-only contrast; correction used the existing prefix mode and target-only readout edit, with saved companion replay required.
- Root corrected two remaining mechanical boundaries before any runtime launch: common native-prefix checks may lie inside a supplied full prefix, and post-withdrawal capture starts at the actual pulse end. Production replay remains the acceptance gate, not worker self-report or helper checks.

No model capability ranking follows from this package. No skills or persistent memory were changed.

StageC summary acceptance: worker hardcoded the scored C row as accepted A and used fixed nine-token chunks, which could corrupt recurrence rates. Root preserved the candidate, corrected A against literal native tokens, used complete wrapper spans, and added a malformed-gap falsification. Root also separated trajectory-supplied IDs from the symmetric union and preserved explicit semantic-malformed zero. No model execution changed. Future briefs should require reading the exact anchor token binding rather than copying a remembered template.
