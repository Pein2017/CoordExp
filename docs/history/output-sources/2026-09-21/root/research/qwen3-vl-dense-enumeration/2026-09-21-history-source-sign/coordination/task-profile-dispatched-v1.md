# Execution assignment: history-source sign and native row choice

Status: authorized for bounded execution; scientific results require lead acceptance.
Authorization: on 2026-09-21 the user approved the lead's two proposed probes,
designated the existing worker below, and requested an English execution assignment.
The lead selected one persistent execution owner with bounded internal parallelism.

## Pair, continuity, and ownership

- Lead: `01a0c1f3-dbef-7b63-b2da-8dc7072cea8d`, verified `gpt-6-astra/ultra`.
- Worker: `01a0ba19-8004-7400-b8af-98ce9b9df0cd`, verified idle,
  `gpt-5.6-sol/xhigh`. Preserve this previously user-selected override;
  do not switch models, permissions, cwd, or global defaults.
- Cwd: `/data/CoordExp/.worktrees/research-probes`.
- This assignment supersedes the previous instruction to wait, only for these
  two new probes. The small-coordinate-perturbation branch remains closed.
- You own implementation, source qualification, runtime, evidence production,
  and candidate integration. The lead owns scientific decisions and acceptance.
- You may use at most two internal native subagents for independent, disjoint
  lane implementation/execution. Preserve the existing Luna routing for routine
  bounded execution. Do not delegate research meaning or interpretation to Luna;
  send consequential scientific questions to the lead. No further delegation,
  second sidebar worker, scheduler, or delegated review chain is authorized.
- Freeze shared inputs once before parallel execution. One writer owns shared
  admission, fixtures, and integration; assign lane-specific files separately.

## Read only the decisive predecessors

- `research/experiments/2026-09-19-recurrence-phase-decision/handoff-to-new-lead.md`
- Its `lead-results.md` and current `state.json`: accepted, closed; first-ever
  subgroup is 6/16, not 4/16. Do not repeat completed acceptance.
- `research/experiments/2026-09-18-covered-history-common-tail/results.md`:
  relative full-row preference and first-coordinate effects have opposite signs;
  absolute inhibition and abstract coveredness were not identified.
- `research/experiments/2026-09-18-native-coordinate-branch-completion/results.md`:
  the retained R16 repeated rows beat retained new-owner rows after paying the
  entry-token probability. Small retained mass is not a global optimum.
- Current `research/CONVENTIONS.md` and the relevant existing runtime entrypoints.

The two Pro replies are advisory context, not extra work requirements:
`/data/CoordExp/.codex/attachments/ed6c6032-f074-414b-a83c-0e4253a3df35/Pasted text.txt`
and `/data/CoordExp/.codex/attachments/e3419696-becd-4eed-afc7-2cec0fbb88cd/Pasted text.txt`.

## Outcome and shared admission

Produce two independently interpretable candidate results:

1. Whether a specified historical object's source paths have a signed,
   target-specific net effect on current competition and actual reselection.
2. Whether the unchanged native distribution contains a credible unvisited-owner
   complete row with greater probability than the actual greedy repeated row.

Lane B does not depend on a positive Lane A result. Qualification failure in one
lane must not block the other. No repair, population-recall, training-origin,
abstract owner-memory, or global sequence-optimality claim is requested.

Use existing original-policy mature tied2444 and untied+axis2444 trajectories
from the accepted panels/census. Bind exact source tokens, model package,
embeddings, decode/config, image, annotation version, positions and artifact IDs.
Do not regenerate a cohort, use normalized histories as native, or pool R16 with
mature models. Tied/untied are separate strata, not an untie-only causal contrast.

Select at most six distinct images, at most one target-first-revisit failure state
per image/model (maximum twelve). Use deterministic eligibility/order before
seeing intervention outcomes, not outcome-based case replacement. Select up to
one corresponding at-risk non-target-revisit control per failure state, matched
on model, description, generation stage and relevant history exposure. Controls
need not remain healthy forever. Preserve enriched/prospective source strata.

For Lane A, locally verify A (already emitted exactly once, about to be revisited),
C (another covered owner), and N (a credible unvisited owner). Prefer comparable
descriptions and source length/recency/geometry, with residual mismatches recorded.
Audit only these anchors, their prior occurrences and plausible confusions using
existing evidence plus bounded full-image/context views. This is target A's first
revisit, not necessarily the image's globally first recurrence. Lane B requires
A and N; lack of C must not make its otherwise valid state ineligible.

Do not expand annotation review to obtain a preferred donor. Report each rejected
case and reason. Identity-ambiguous cases may retain descriptive numerical labels
but are not replacements for the owner-specific primary denominator; do not add
a third numerical experiment. UNKNOWN/unmatched is not a physical negative.
Fewer eligible images are acceptable; no eligible support means local HOLD.

## Lane A: history-source sign

Question: From the same native pre-revisit state, does cutting source A's
computational paths change A's competition and next-row reselection selectively,
compared with cutting C and a matched non-target historical source?

Freeze four scientific conditions: native, cut-A, cut-C, matched non-target cut.
A no-op replay is mechanical qualification, not another scientific arm. Keep
literal tokens, image, positions, readout and decode policy fixed. No layer, dose,
head, temperature, or alternative-corruption sweep.

Use one specified attention-path barrier with recomputation of affected successor
states, including contextual relay. A last-query-only mask with native successor
KV is a different intervention and is not a substitute. The native row-end query
anchor must also be independent of quarantined content under the barrier: merely
using an `i > end` mask can leave a content-bearing boundary token or residual.
Do not insert a new token to manufacture a common release state. If the native
boundary cannot be qualified under this contract, return that case as HOLD.

Before scientific execution, demonstrate both:
- Native/no-op replay matches actual source decisions with full-vocabulary
  numerical error small relative to decision margins.
- Under the cut, changing quarantined source content to other legal same-length
  content, while all external tokens/positions remain fixed, leaves the tested
  downstream full logits invariant within a predeclared numerical tolerance.
  Include a positive sensitivity check showing the validator rejects a leaking
  path. Use the smallest real-entry example, not helper-only success.

Freeze small coherent row sets for A/C/N independently of intervention outcomes,
at most four unique complete rows per owner. Reuse existing credible extents;
never combine coordinate tolerances into identity-incoherent boxes. Score original
full-vocabulary probabilities, retain individual row scores, finite-set log mass,
A-vs-N and C-vs-N contrasts, A-vs-C contrast, and each set's construction rule.
These are tested candidate masses, not complete owner probabilities.

Separate the first common-prefix fork margin, full-row scores, all-vocabulary
competition and actual free next-row outcome. Opposite signs are a result type,
not something to average away. Describe the source-cut net effect on relative
competition; loss of competitor facilitation can mimic own-owner suppression.
Neither a crossed sign nor absolute softmax probability proves an inhibitory
coverage circuit. The source states also carry spatial/image/context information.

Release naturally from the same row boundary with the barrier active through at
most one complete row. Record opener/description/EOS and complete-row identity.
Then remove the intervention by rebuilding original-model state from the actual
emitted tokens; continue for at most eight more complete rows, subject to the
original source token cap. No-barrier controls use the corresponding replay path.
This short follow-up measures conditional route consequences, not a separately
identified maintenance/relapse mechanism.

Report A/C/N, other credible owners, UNKNOWN, invalid geometry, malformed output,
EOS and administrative caps separately. Do not score early EOS as zero recurrence
or use only jointly long branches as the overall intervention denominator.

Stop this mechanism interpretation if source isolation fails or matched cuts
produce only nonspecific damage. A null is local to the cut and support; do not
claim absence of memory. Do not extend to repeated-state or relapse experiments.

## Lane B: native row-choice witness

Question: At a mature-model native target-revisit state, is there a credible
unvisited-owner complete row more probable than the actual greedy repeated row
under exactly the original distribution?

Use the frozen native states and coherent candidate sets, including the actual
greedy row. Maximum thirteen unique rows per state (actual row plus up to four
per A/C/N); fewer are acceptable and C is optional. This is the finite-candidate
diagnostic, not an open-ended search project. No beam expansion or global MAP
certificate is required in this package.

Score every complete serialized row with original full-vocabulary probabilities,
including the entry/forced-token cost and terminator. Never renormalize inside
candidate coordinates or omit a low-probability entry. State exactly which common
description is conditioned on; do not generalize that result to all descriptions.
Retain native row-boundary behavior separately.

A credible N row beating actual greedy is a positive local witness. It does not
prove row MAP chooses N: a different repeated row may score still higher. A
negative finite search is unresolved outside its tested support. Owner-mass
aggregation and global optimization are not primary endpoints.

For each positive witness, optionally supply only the highest-scoring qualifying
N row from the frozen set, rebuild original state and release native greedy for
at most eight complete rows. Label the supplied row and external owner selector
explicitly; only subsequent rows are freely generated. This tests route value,
not a deployable decoder or an empty-prefix repair. Do not hunt for a better
candidate after observing the continuation.

## Bounds, execution, and stopping

These are ceilings, not targets, across both lanes including qualification and
failed attempts: 4 allocated GPU-hours, 40,000 model-forward invocations,
144 free-release cells, and 16 GiB retained payload. Track every bound and stop at
the first reached. Latest explicit user instruction: all eight GPUs are available
and compute utilization should be maximized. After lane qualification, distribute
independent model/state/control cells across all eight GPUs whenever work is ready;
overlap the two lanes and CPU evidence processing to minimize avoidable idle time.
Use one execution owner with eight GPU shards, not eight independent schedulers.
Do not duplicate experiments solely to fill devices.
Start with one production-shaped example per lane, then execute the frozen panel
if that lane qualifies. At most one bounded repair/requalification attempt after
a failed real-entry qualification per lane; further difficulty returns to lead.
No budget increase, cohort expansion or changed scientific contract without lead
resolution. Report counters as measured/reserved correctly, not GPU utilization.

Reuse current entrypoints and source identities; no generic runtime framework.
Use bare `python`/`python3` and the configured runtime, normally ms. Shared random
GPU stress occupancy is expected; launch authorized work directly and react only
to concrete conflicts. One invocation per owned job; retain logs, exits, failure
receipts, immutable source snapshots and downstream-readable outputs.

No training, checkpoint-stage sweep, embedding/readout edits, annotation writes,
RP/NMS policy changes, fresh sampling cohort, tiny-coordinate rescue search,
full attention/KV archive, Notion, memory writes, cleanup, commits or publication.
The user's existing no-wake-me-up preference remains in force.

## Owned surfaces and candidate return

Own only the following new units, corresponding probe directories and outputs:
- `research/experiments/2026-09-21-history-source-sign/`
- `research/experiments/2026-09-21-native-row-choice/`
- `probes/training_set_completion/history_source_sign/`
- `probes/training_set_completion/native_row_choice/`
- `/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-21-history-source-sign/`
- `/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-21-native-row-choice/`

This profile owns the dispatch boundary; create proportionate unit/state records
that reference it and freeze exact admissions/candidates before model comparisons.
Do not duplicate the full profile into another planning framework. You may add
the two catalog entries without overwriting existing dirty edits. The lead owns
frontier/question integration and final acceptance. Preserve all pre-existing
dirty work, old experimental sources, manifests and sealed results.

Return one stable integrated candidate with separate lane verdicts, exact artifact
paths/hashes, admissions and HOLD denominators, signed effects, native-row witness
comparisons, outcome/debt table, changed paths, acceptance commands, bounds and
all job/subagent terminal states. Include a deterministic saved-evidence reducer
and meaningful falsification for the source-cut and probability-accounting seams.
Do not treat identical deterministic replay as additional scientific replication.

Communicate in English with the lead. Send decision-bearing questions/checkpoints
and the final candidate to the lead using an available task-message surface; if
absent, use the established App Server route without changing settings or starting
a duplicate active turn. Deliver to this lead ID, not the old lead from handoff.
Persist a unique delivery receipt; unknown delivery must be reconciled before retry.
No periodic unchanged updates and no wake plugin. If direct delivery is unavailable,
save the candidate and report the exact transport gap in your task.

Use `candidate`, `NEEDS_CONTEXT`, `HOLD` or `BLOCKED`. Stop after the package;
no self-acceptance, successor design/launch, or silent semantic substitution.
