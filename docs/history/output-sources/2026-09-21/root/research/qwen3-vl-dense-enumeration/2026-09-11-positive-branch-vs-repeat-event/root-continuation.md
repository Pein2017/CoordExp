# Resumed overnight research: shared checkpoint endpoint first

**Superseded continuation pointer:** the shared endpoint is complete. Read
`goal-continuation.md` in this same raw root for the active goal, two new probe
owners/contracts and current no-GPU-grant preparation state. Sections below
are preserved chronology, not current launch instructions.

## Latest user authorization overrides the discussion pause below

The user subsequently explicitly authorized a persistent goal, revisiting prior
discussion/research, maximizing useful all8GPU utilization, long waits/preferred
wake monitoring, and dynamic model/effort/prompt decisions under flat L1 only.
`create_goal` succeeded at2026-09-11T16:32:39UTC for this root task; no token budget
was specified. Read `get_goal` after compaction to retain the active scope.
Current independent L1s: `sol_high_training_recovery` owns endpoint execution;
`sol_max_sampling_research` owns one CPU acquisition diagnostic and at most3
ranked proposals (no GPU/implementation authority). The two wake registrations
so far failed at resolve_delivery_target / app-server thread/read timeout;
neither returned armed. Keep active long `wait_agent` fallback, do not invent
a monitor, poll, relaunch work, or perform shared-runtime repair.

The user resumed autonomous overnight exploration, permits data/GPU use and
flat L1 workers (Sol xhigh/max may be tried), and optionally permits the existing
Co-DETR evaluator. Main-thread boundaries communicated: joint owner/burden
criteria; Co-DETR remains auxiliary evidence, not automatic GT/training labels;
short reversible experiments before scale; major architecture/token/GT changes
remain proposals. No further user confirmation is currently needed.

Current step: `endpoint-A-grant.json` authorizes the exact frozen eight-worker
384+6 read once from A's real receipt. B is not separately executed because of
exact shared weight identity; never fabricate a B consumer. Runtime owner is
`/root/sol_high_training_recovery`. Root accepts endpoint evidence and then
chooses a bounded next discriminator; no automatic training sweep.

## Superseding current checkpoint

Both full32 runs and cold reloads are now root-accepted. Full-B survived the
second network interruption; replacement `sol_high_training_recovery` joined
the same PID and performed only the previously authorized cold reload.
`full-A-admission.json` and `full-B-admission.json` are current acceptance owners.
All32 adapter states/final weights match A/B: B had0/768 strict events. Existing
initial Stable50 h-only greedy records immediately repeat at all3 h (IoU1.0);
root verified this in `signal-location-evidence.json`.

The latest user explicitly requested a suitable pause for research and
Sol/Luna delegation discussion. We are paused **before endpoint inference**.
No GPU job belonging to this phase remained at the root's final process check.
The owning research `results.md`, `unit.md`, and `delegation-evidence.md` plus
the compass/index are updated. Do not launch the reference next steps below
until the user resumes; old autonomous grants do not override this pause.
Exact A/B identity also warrants discussing one shared-checkpoint read instead
of pretending a duplicated endpoint comparison is informative. No source,
threshold, estimator, or endpoint-scope change has been silently executed.

## Earlier launch and pending-step reference (not current launch authority)

Authority: `/data/CoordExp/.worktrees/research-probes/research/investigations/qwen3-vl-dense-enumeration/experiments/2026-09-11-positive-branch-vs-repeat-event/unit.md`.
User cancelled the network-switch pause. Continue autonomously; no user approval is pending.

Root granted `sol_high_branch_training_route` one sequential all8 fullA32+coldA,
then independent all8 fullB32+coldB, via `full-pair-grant.json`. On failure stop,
preserve evidence, diagnose only the concrete failure; no automatic relaunch.
Do not confuse a worker settlement notification with scientific acceptance.

Current trainer `bc6d1728dc5c156b6d617ecb9b99a71b1e81ee305e338c2f1476b3cdc9035308`;
input `f10e82fa7106c2c0a24f5f8612299c28e68fdfbbef1c72539fb869efe5d3a83e`.
Both accepted same-arm resource envelopes and bound smoke/cold receipts live in
`trainer-preparation/`. Root replayed50 CPU tests, sealed smoke consumers,
exact8-rank counters, and cold3 zero deltas. Initial failed `smoke-A/` is retained.
B smoke D1=0/48; no negative-learning efficacy conclusion is justified by that.

After worker completion:
1. Inspect fullA/fullB sealed receipts via trainer `verify_receipt`,32updates,
   same source initialization/fresh optimizer, actual event counts/all768 outcomes,
   cold reload, counters/resources, movement and positive-gradient projection.
   Do not rerun completed training or change dose/threshold/sampler.
2. Endpoint owner `sol_high_native_escape_witness` is idle, route CPU-accepted.
   Frozen producer `endpoint_eval.py` SHA
   `abc4391a37d6edaa8e50d9a728bf86f602fbabad82f8cfe8d5310c86b365ec39`;
   packet `endpoint-preparation/packet.json` SHA
   `560006e73f3f0fc416e7d58751fe96c936aba9478fb6b320ab6118db2bcd5053`.
   Root replayed9 tests including runtimeB labels and over-limit terminal rejection.
   Assign exact sealed adapter receipts, then grant eight-worker endpoints one
   arm at a time,384 natural+6conditional each. Use prepared commands/rank bounds;
   no new review or extra model-load smoke is needed for this already accepted route.
3. Fresh merge/verify and paired reductions; root inspect target changed images.
   Separate fixed h choice, h+c successor survival, and original-prompt behavior.
   Record TP50/60/80/F1 gains/losses, strict repeats, invalid/malformed/raw rows,
   EOS/cap and event signal supply. Unmatched is not automatic hallucination.
4. Close this fixed32 contrast once, update results/unit/index/compass and a
   lead receipt. No prefix refresh, sweep, extra cases or checkpoint promotion.

Prior native-witness and multiplicity panels are complete and closed. Root
visually admitted only3 c and first w rows for this training; not entire suffixes.
Keep historical payloads and all unrelated dirty checkout work untouched.
