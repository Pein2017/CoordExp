import json,hashlib,re,collections
from pathlib import Path
import torch
from transformers import AutoTokenizer
BASE=Path('/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration');OLD=BASE/'2026-09-16-corner-loop-mechanism';R=BASE/'2026-09-16-endpoint-loop-readout-state';R.mkdir(exist_ok=False)
read=lambda p:json.loads(p.read_text())
def bind(p):return dict(path=str(p),sha256=hashlib.sha256(p.read_bytes()).hexdigest(),size_bytes=p.stat().st_size)
p=read(OLD/'panel.json');tok=AutoTokenizer.from_pretrained(p['config']['model']['base_model'],local_files_only=True)
coords=[tok.convert_tokens_to_ids(f'<|coord_{i}|>') for i in range(1000)];assert coords==list(range(151670,152670))
selected=[];saved=[]
for c in p['cases']:
 rows=[]
 for b in c['boundaries']:
  rows.append(dict(key=f"P-row{b['row_1based']}",history=c['P_action_ids'][:b['offset']],tokens=b['observed']['tokens'],row=b['row_1based'],origin='P16 natural literal path; conditional/off-policy for other checkpoints unless exact prefix match',stage='early/onset' if b['row_1based']<=4 else 'escape' if b['row_1based']==138 else 'established repeat regime',saved_row=b['row_1based']))
  for label in p['checkpoints']:
   f=OLD/f"runtime/{label}-{c['image_id']}/row{b['row_1based']}-observed-logits.pt";d=torch.load(f,map_location='cpu',weights_only=True);saved.append(dict(binding=bind(f),coordinate_argmax={k:int(v[coords].argmax()) for k,v in d.items() if k in ['x1.full','y1.full','x2.full','y2.full']},hidden_state_available=False))
 if c['image_id']==477415:
  ids=c['P_action_ids'];end=ids.index(151649)+1;rows.insert(0,dict(key='P-row1',history=[],tokens=ids[:end],row=1,origin='P16 natural first row; other checkpoints conditional',stage='first-row border control',saved_row=None))
  f=BASE/'2026-09-16-corner-loop-bridge-factorial/runtime/C01/raw.json';raw=read(f);ids=raw['rows'][1]['token_ids'];starts=[i for i,x in enumerate(ids) if x==151646]
  for free in [46,62]:
   start=starts[137+free-1];end=ids.index(151649,start)+1
   rows.append(dict(key=f'C01-free{free}',history=ids[:start],tokens=ids[start:end],row=137+free,origin='R16 C01 supplied-row137 conditional free continuation; off-policy for other checkpoints',stage='relapse onset' if free==46 else 'all999 relapse',saved_row=None))
 selected.append(dict(image_id=c['image_id'],case=c,rows=rows))
sources=p['sources']+[bind(OLD/'panel.json'),bind(OLD/'producer.py'),bind(OLD/'lead-acceptance.json'),bind(BASE/'2026-09-16-corner-loop-bridge-factorial/runtime/C01/raw.json')]+[x['binding'] for x in saved]
for name in ['2026-09-16-corner-loop-bridge-factorial','2026-09-16-corner-loop-one-bin-control','2026-09-16-corner-loop-y1-one-bin-control']:sources.append(bind(BASE/name/'lead-acceptance.json'))
inputpath=Path(p['config']['data']['input_jsonl']);data=[json.loads(x) for x in inputpath.read_text().splitlines()];counts=[[0]*1000 for _ in range(4)]
for row in data:
 for obj in row['objects']:
  for j,s in enumerate(obj['bbox_2d']):counts[j][int(re.fullmatch(r'<\|coord_(\d+)\|>',s)[1])]+=1
(R/'teacher-exposure.json').write_text(json.dumps(dict(source=bind(inputpath),images=len(data),counts_by_role=dict(zip(['x1','y1','x2','y2'],counts)),scope='Current canonical train256 teacher-bin inventory only; not presentation-weighted or full pretraining history'),indent=2)+'\n');sources.append(bind(inputpath))
maxlen=max(len(c['case']['prompt_ids'])+len(row['history'])+len(row['tokens']) for c in selected for row in c['rows']);tokens=3*sum(len(c['case']['prompt_ids'])+len(row['history'])+len(row['tokens']) for c in selected for row in c['rows'])
packet=dict(schema='endpoint_readout_state.v1',estimand='Under identical literal slot histories, does endpoint coordinate dominance persist after removing fixed output-row norms and bias, through context-specific final-state alignment?',config=p['config'],checkpoints=p['checkpoints'],coordinate_ids=coords,cells=selected,sources=sources,saved_logits_inventory=saved,bounds=dict(cells=9,forwards=36,coordinate_slots=144,input_tokens=tokens,max_sequence_tokens=maxlen,new_generated_tokens=0,allocated_gpu_seconds=7200,elapsed_seconds=7200,peak_memory_per_gpu_bytes=48*1024**3,raw_tensor_bytes_ceiling=200*1024**2),runtime='FP32 SDPA native exact_history_inputs full-prefix bs1; extraction, not original heterogeneous-batch generation; no parity reruns')
(R/'panel.json').write_text(json.dumps(packet,indent=2)+'\n');(R/'prepare.py').write_text(Path(__file__).read_text())
U=Path('research/experiments')/R.name;U.mkdir();(U/'unit.md').write_text('# Endpoint readout versus state\n\n'+packet['estimand']+'\n\n## Predecessor recap\n\nAccepted measured cache/full parity and identical current coordinate readouts; category/full-extent edits recover16 owners transiently then relapse; both one-bin controls reproduce C00. Local substitution series remains closed. This is a conditional readout sensitivity diagnostic, not physical FN improvement or repair.\n\n## Frozen panel\n\nAll3 checkpoints use identical literal P paths:351017 rows1/2/16,417044 rows1/2/16,477415 rows1/4/16/138; also477415 C01 free rows46/62 (conditional relapse). Each row supplies exact literal preceding tokens to score x1/y1/x2/y2.36 forward calls,144 coordinate vectors,9 checkpoint-image cells. P paths are natural for P; cross-checkpoint histories are conditional unless exact natural prefix verified. C01 paths have supplied row137 and are conditional for R, off-policy for others. First-row border examples are geometry examples, not newly verified physical labels. No clean interior x1 baseline is promised.\n\nSaved27 observed-row tensor files contain logits but no hidden states. New calls capture only missing final head inputs plus native logits, validating affine reconstruction and agreement with saved coordinates at reused seams. No cache/full controls or generation repeated.\n\n## Decomposition\n\nBind actual1000 token IDs, base rows, delta rows, effective input/output rows, bias and tied status. Capture h at actual output head input after final normalization. Reconstruct z=W h+b; report max error relative to coordinate argmax margin and saved logits. Compute all1000 cosine scores and equal-norm/no-bias scores offline, endpoint0/999 versus strongest interior, ranks/margins/norm/cosine/bias terms. This is coordinate-conditional sensitivity, not a full-vocabulary decoding intervention. Bias absence makes h norm irrelevant to coordinate argmax; input effects remain upstream. Numerical role values alone do not imply loops. Current train256 counts are bounded exposure only.\n\n## Bounds and stop\n\n'+json.dumps(packet['bounds'])+'\n\nOne fixed extraction package, no adaptive rows/layers/seeds/coefficients, no image/KV/embedding interventions, training or new dataset. No additional model calls unless a bounded mechanical repair fits hard package ceilings; failures remain technical-invalid. Stop after CPU reduction/hypothesis table and one proposed causal contrast, pending lead acceptance. No owned jobs remain at terminal.\n\nExact immutable inputs/runtime paths and literal token lists: '+str(R/'panel.json')+'\n')
s=dict(schema_version=1,unit_id=R.name,lifecycle='ready',evidence='none',disposition='frozen_conditional_readout_state_extraction',state_as_of='2026-09-16',protocol=str(U/'unit.md'),result=None,state_source=str(U/'unit.md'),boundary='36 fixed forwards,144 slots,9 cells; conditional mechanism diagnostic only',not_authorized=['training','new causal interventions','coordinate sweep','new agents','automatic successor'],next_action='Extract fixed panel then CPU reduce and stop');(U/'state.json').write_text(json.dumps(s,indent=2)+'\n')
pth=Path('research/experiments/catalog.jsonl');entry=dict(id=R.name,title='Endpoint readout norm versus conditional state alignment',kind='experiment',topics=['history-repetition-stopping'],record_root=str(U),protocols=[str(U/'unit.md')],result_records=[],reading_entry=str(U/'unit.md'),tracking='current',state=str(U/'state.json'));pth.write_text(pth.read_text()+json.dumps(entry)+'\n');idx=Path('research/index.md');idx.write_text(idx.read_text()+f'\nEndpoint diagnostic: [unit](experiments/{R.name}/unit.md), [state](experiments/{R.name}/state.json). Frozen conditional readout/state extraction; no repair intervention.\n')
print(json.dumps(packet['bounds']));print('saved tensor files inspected',len(saved))
