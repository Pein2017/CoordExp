import json,pathlib,hashlib
R=pathlib.Path(__file__).parent;P=R.parent/'2026-09-18-owner-recurrence-onset';S=R.parent/'2026-09-17-history-rereading-mechanism/human-evaluation/annotation-snapshot-v1'
def bind(p):
 p=pathlib.Path(p);return dict(path=str(p),sha256=hashlib.sha256(p.read_bytes()).hexdigest(),size_bytes=p.stat().st_size)
p=json.load(open(P/'panel.json'));native=p['group']['rows'][p['target']]['generated_token_ids'];hist=native[:49];prelude=native[49:54];assert prelude==[151646,15007,332,151647,151648];assert native[54]==151670
m=json.load(open(S/'manifest.json'));checks={k:bind(v['path'])==v for k,v in m['source'].items() if isinstance(v,dict) and {'path','sha256','size_bytes'}<=v.keys()};assert all(checks.values());img=m['image_bindings']['417044'];assert bind(img['resolved_path'])['sha256']==img['sha256']
(R/'annotation-identity.json').write_text(json.dumps(dict(checks=checks,image=img,count=63,ignore='Absent explicit ignore; unmatched UNKNOWN',working=bind(S/'working.norm.jsonl')),indent=2)+'\n')
sources=p['sources']+[bind(P/'panel.json'),bind(P/'lead-acceptance.json'),bind(P/'runtime/native/capture.pt'),bind(P/'runtime/native/receipt.json'),bind(P/'runtime/scores/readout.pt'),bind(R.parent/'2026-09-18-covered-history-common-tail/lead-acceptance.json')]
packet=dict(schema='native_coordinate_branch.v1',config=p['config'],group=p['group'],target=p['target'],image_id=417044,coordinate_ids=p['coordinate_ids'],coefficients=p['coefficients'],sources=sources,history=hist,prelude=prelude,native_row=native[49:59],branches=[0,52,30],native_raw=p['native_raw'],accepted_native=bind(P/'runtime/native/receipt.json'),accepted_capture=bind(P/'runtime/native/capture.pt'),reference_scores=bind(P/'reduction.json'),search=dict(width=4,max_suffix_tokens=8,length_penalty=0,tie='descending raw cumulative logprob then ascending lexicographic token-id sequence',terminal_retention='terminal paths compete in same global width4 beam and are carried unchanged; any pruned terminals retained in ledger with mass',expansion='full-vocabulary softmax, per-parent top4 sufficient for global top4; no masks or geometry rejection',terminal='box_end completes if exactly3 coordinate tokens after x1, otherwise malformed; EOS ends; other non-coordinate special token structural termination; ordinary text allowed until8-token cap',greedy='separate argmax path, same stops; ties lowest token ID',scoring='common native prelude + forced x1 raw probability + suffix cumulative raw logprob; no conditional renormalization for branch comparison',cache='full-prefix use_cache=False at every node; immutable tuple parent histories and CPU logit memoization only, no shared KV'),bounds=dict(planned_max_per_branch=44,planned_max_forwards=132,hard_forwards=2000,gpu_seconds=3600,tensor_bytes=536870912,final_beam_paths=12,greedy_paths=3,new_inspected_rows=15))
(R/'panel.json').write_text(json.dumps(packet,indent=2)+'\n')
