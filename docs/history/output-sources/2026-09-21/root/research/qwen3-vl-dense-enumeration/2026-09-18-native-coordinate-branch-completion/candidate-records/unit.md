# Native coordinate-branch completion at confirmed donut recurrence

At genuine native donut417044 row6 (row start49, x1 action54), do fixed x1=0/52/30 branches yield a repeated, distinct, ambiguous or malformed complete row under original R16, and does the bounded width4 search retain a higher-likelihood nonrepeated row than original greedy?

Root authorization2026-09-18: lead01a0a3d5-dc45-7693-8467-4801aa7190df owns decisions/acceptance/successors; existing worker01a0a81a-9e32-7db1-bd07-86fa601f4276 owns this package. Apply lead-worker/research-flow; no team, training or successor. Model remains ranking-trained R16, original paired embeddings, same image and original heterogeneous native bs4/FP32/SDPA/RP1/padding/MRoPE.

## Evidence and change

Accepted onset supplies genuine49-token history, actual common5-token prelude and x1 logits: original A wins finite B row references; equal norms reverse primary B's full-row ordering but not A0-B52 fork. Accepted common-tail is a separate synthetic-history comparison and not the new prefix. July19 complete-candidate decomposition already distinguishes local greedy versus whole-row likelihood; this is not a new mathematical finding. New evidence will classify coherent branches from the real recurrence rather than treating coord0 as an owner label or forcing a whole B box.

`panel.json` at `/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-18-native-coordinate-branch-completion/` freezes all tokens/bindings. Keep ambiguous native rows3/4 in the actual history, with their prior identity HOLD. Read current annotation bytes against accepted63-positive snapshot; no label/input substitution or negative census.

## Frozen search and termination

Branches0,52,30 are fixed before outputs. Only x1 is supplied counterfactually. Subsequent logits are original and untransformed. For each branch retain a separate greedy path and one width4 beam, no semantic reranker, geometry/syntax mask, length normalization or probability renormalization. Score with raw full-vocabulary logsoftmax in FP64 from actual FP32 logits. Cross-branch full-row score includes common5-token prelude and **raw forced-x1 log probability**, never treating it as1. Report suffix-conditional score separately.

Start width1 after x1. Each step expand every active parent to its top4 children under full-vocabulary softmax (sufficient for global top4), combine with unchanged terminal paths, sort by descending cumulative raw logp and then ascending lexicographic token-ID sequence, keep global4. Ties use exact numeric score and lexicographic IDs; greedy argmax ties use lowest token ID. Terminal paths occupy beam slots, cannot extend, and if pruned retain their token/probability record. Every unexpanded and pruned mass remains explicit. No separate growing terminal pool or best-of oracle substitution.

Stop a path on box_end (complete only with exactly3 additional coordinate tokens), EOS, any other non-coordinate special token (structural termination), or8tokens after x1. Ordinary nonspecial text is unrestricted until termination/cap; malformed/invalid/capped paths are retained. A correct four-coordinate row may have invalid geometry; do not reject it during search. No next object or free scene rollout. At most12beam-final paths plus3greedy paths; identical paths retain both provenances.

## Shortest execution and qualification

Use existing exact_history_inputs/native input/model loader. Every queried node is a **fresh full-prefix forward with use_cache=False**, original companions padded at their original stop positions. There is no mutable KV branch to clone/share. Reuse identical prefix logits only as immutable CPU tensor observations; never reuse foreign text/image cache. A deterministic tree check verifies immutable parent prefixes, terminal carry/mass/ties and forced-token score inclusion. After search, requery the root from fresh inputs and require exact logits/positions, detecting accidental shared-state contamination. Every final path gets an independent complete-row full-prefix rescore and chosen-token-versus-all-competitor ranks/margins.

Real-entry gate: branch0 current x1/head/positions match accepted native capture within decisive margins, and its unmodified greedy row agrees exactly with saved native row6 at all selected decisions. Do not repeat old3084-token generation. Branch0 completes qualification before independent52/30 launches. A positive all-competitor margin certifies deterministic greedy only from that precise prefix; top-k membership does not. Common original prelude and forced coordinate scores are kept separate from suffix.

Planned maximum132batch calls: per branch1root/prelude, at most37unique decision queries (29beam+8greedy), at most5final rescoring calls,1fresh root no-op. Memoization may reduce this. Hard ceiling2000calls,1allocated GPU-hour,512MiB retained tensors; one zero-scope mechanical correction at most, all failures preserved/count. No scale-out beyond these three branches.

## Outputs and interpretation

Retain every explored decision's full-vocabulary logits/head inputs/positions, immutable parent/token tree, chosen ranks/margins, complete row/suffix scores, pruning ledger and disjoint captured/pruned/capped mass. Effective base+shared-delta readout identity stays fixed. Finite row-event mass is not total owner probability or scene-trajectory probability. A higher-likelihood nonrepeated retained row supports finite local realization/search loss; absence is only absence in this search. No global sequence optimum or training implication.

Review unique completed final rows only (ceiling15) with full-image overlays/local context/current positives and admitted A/B. Classify repeated A, unvisited B, other known/uncertain coverage, physical HOLD and invalid separately; do not assign identity from x1/IoU alone. Keep extent error versus owner discovery separate. No physical precision claim from annotation-unmatched.

Stop after fixed search, physical sidecar, independent saved-output reconstruction, candidate records/cost/artifact map and job closure. Root integrates frontier and accepts. No larger beam, escape rollout, seeds, images, checkpoints, normalization, sampling, RP tuning, QKV/layers, labels, training, Notion, memory writes or destructive cleanup.
