from pathlib import Path
import json,hashlib,os,datetime,shutil
R=Path(__file__).parent;U=Path('research/experiments/2026-09-18-native-coordinate-branch-completion')
def read(p):return json.loads(Path(p).read_text())
def bind(p):
 p=Path(p).resolve();return dict(path=str(p),sha256=hashlib.sha256(p.read_bytes()).hexdigest(),size_bytes=p.stat().st_size)
def write(p,d):Path(p).write_text(json.dumps(d,indent=2)+'\n')
d=read(R/'reduction.json');physical=read(R/'physical-score.json');p=read(R/'panel.json');native=next(x for x in d['paths'] if x['id']=='b0-p0');summaries=[];jobs=[]
for b in [0,52,30]:
 t=read(R/f'runtime/{b}/tree.json');rec=read(R/f'runtime/{b}/receipt.json');assert (R/f'branch{b}.exit').read_text().strip()=='0';assert not Path(f"/proc/{rec['pid']}").exists();jobs.append(dict(branch=b,pid=rec['pid'],exit=0,alive=False))
 paths=[x for x in d['paths'] if x['branch']==b];best=max(paths,key=lambda x:x['full_row_logprob']);greedy=next(x for x in paths if 'greedy' in x['provenance']);assert all(x['stop']=='complete' for x in paths)
 summaries.append(dict(branch=b,greedy=greedy['id'],greedy_box=greedy['box'],greedy_logp=greedy['full_row_logprob'],best=best['id'],best_box=best['box'],best_logp=best['full_row_logprob'],best_minus_original_greedy=best['full_row_logprob']-native['full_row_logprob'],best_suffix_logp=best['suffix_logprob'],physical=physical['rows'][best['id']]['physical'],forced=t['forced_x1'],beam_retained_conditional_mass=t['beam_conditional_retained_mass'],discarded_conditional_mass=t['beam_conditional_discarded_mass'],enumerated_pruned_terminal_count=sum(bool(x['stop']) for s in t['ledger'] for x in s['pruned']),enumerated_pruned_count=sum(len(s['pruned']) for s in t['ledger']),final_path_count=len(paths)))
assert d['forwards']<=2000 and d['gpu_seconds']<=3600 and d['tensor_bytes']<=512*1024**2
result=dict(status='candidate',technical='valid; native parity and independent saved-node/pruning/full-prefix reconstruction passed',scientific='No retained higher-likelihood nonrepeated owner row. Width4 improves repeated A realization; forced52 yields credible B but lower full-row probability;30 partial-owner identity HOLD.',branches=summaries,cost={k:d[k] for k in ['forwards','gpu_seconds','tensor_bytes']},paths=d['paths'],physical=bind(R/'physical-score.json'),panel=bind(R/'panel.json'),annotation=bind(R/'annotation-identity.json'),reduction=bind(R/'reduction.json'),jobs=jobs,failed_model_cells=0,model_retries=0,finals=dict(beam_provenances=12,greedy_provenances=3,unique_paths=14,completed_valid=14,malformed=0,invalid=0,eos=0,capped=0,physical_HOLD=5,confirmed_repeated_A=5,confirmed_unvisited_B=4),limits=['Finite beam prunes almost all conditional mass; no global optimum or owner mass claim.','HOLD branch30 rows all score below original, so their identity cannot reverse this finite higher-likelihood verdict.','Original ambiguous history rows3/4 retained; native prefix preserved.','R16 includes ranking; no pure-SFT or training-origin inference.','Coordinate token is not an owner label. No evidence of coverage repair from row-only completion.'])
write(R/'result.json',result)
lines=['# Native coordinate-branch completion — candidate','',result['scientific'],'','At genuine native donut417044 row6/action49 (x1/action54), original R16 FP32/SDPA bs4 runtime, original readout, three fixed branches and width4 row-only search. All prior history including rows3/4 HOLD is unchanged. Root acceptance is pending.','','| x1 | Greedy row / logp | Best retained row / logp | Best minus original greedy | Physical status |','|---|---|---|---|---|']
for s in summaries:lines.append(f"| {s['branch']} | {s['greedy_box']} / {s['greedy_logp']:.6f} | {s['best_box']} / {s['best_logp']:.6f} | {s['best_minus_original_greedy']:+.6f} | {s['physical']['classification']} |")
lines+=['','All scores include raw common prelude and forced x1 probability. Branch52 has a much easier conditional suffix, but its x1 is rank50, margin−2.856792 to the original winner; omitting that cost would mis-rank branches. Branch30 x1 is rank2, margin−0.228556. No normalization was applied.','','The best x1=0 row departs at y1/action55:259 rather than greedy256, rank4 and chosen-minus-best margin−0.192986. It improves full-row logp by0.472242 but still denotes repeated A. The actual greedy suffix has positive all-competitor margins (minimum0.011030), certifying deterministic row replay from this exact start. The best52 and30 paths first differ from native at x1/action54; every selected token rank/margin is in result.json. Their subsequent beam deviations do not certify greedy selection.','','Four52 rows denote admitted B with extent variations; all five0 rows denote A. Five30 boxes are small partial regions at adjacent upper-left donuts/reflection: dominant physical identity and coveredness remain HOLD, not FP. Frozen IoU50 matches agree on A/B and leave30 unmatched; this is separate from visual evidence. Fourteen unique final rows inspected via full-image family context and all local extents; no additional census.','','All14 finals are complete and geometry-valid; none EOS, malformed or capped. There are12 beam and3 greedy provenances (one duplicate). Four terminal hypotheses were pruned in30 and remain recorded. Conditional beam captured mass is0.000360656/0.003289912/0.000240585 for0/52/30; omitted/pruned mass remains explicit. These are finite row events, not owner probability or scene probability.','','Technical checks: fresh full-prefix/no-KV branching prevents parent-cache sharing; immutable CPU observations only. Root requery is bit-exact. Native0 matches saved onset through row completion; largest2×logit-error/argmax-margin is0.01393. Independent complete-prefix rescoring differs by at most4.27e-6 nats. Saved-output reducer reconstructs every selected probability, parent choice, pruning/tie decision and mass balance. Forced-token omission falsification passes. Input/checkpoint/annotation bindings remain unchanged; no parameter mutation.','','Strongest surviving alternatives: finite search missed distinct high-likelihood completions; model prefers repeated A among these realizations; coordinate30 may denote partial geometry rather than a coherent new owner. No global search optimality, coverage repair or loss/training prescription follows. This package stops here.','',f"Cost: {d['forwards']} batch model forwards, {d['gpu_seconds']:.3f} allocated GPU-seconds, {d['tensor_bytes']/1024**2:.2f}MiB tensors;3 successful producer exits,0 model failures/retries, no owned jobs. One CPU scoring import needed PYTHONPATH=.; no model re-execution.",'',f'Artifacts: `{R}/result.json`, `ARTIFACTS.md`, `reduction.json`, `physical-score.json`, `runtime/`, `terminal.json`. Reconstruct with `python {R}/reduce.py` and `PYTHONPATH=. python {R}/physical_score.py`. Frozen protocol: `unit.md`.']
(U/'results.md').write_text('\n'.join(lines)+'\n')
s=read(U/'state.json');s.update(lifecycle='closed',evidence='unreviewed',disposition=result['scientific'],state_as_of=datetime.datetime.now(datetime.timezone.utc).isoformat(),result=str(U/'results.md'),state_source=str(U/'results.md'),next_action='Root independent acceptance; worker stopped');write(U/'state.json',s)
cat=Path('research/experiments/catalog.jsonl');lines=cat.read_text().splitlines()
for i,line in enumerate(lines):
 e=json.loads(line)
 if e['id']==U.name:
  e.update(result_records=[str(U/'results.md')],reading_entry=str(U/'results.md'));lines[i]=json.dumps(e,ensure_ascii=False)
cat.write_text('\n'.join(lines)+'\n')
(R/'ARTIFACTS.md').write_text('''# Artifact map
- panel.json: exact genuine49-token history, common prelude/native row, model/config/group/companions,61 source bindings, bounds and frozen branches.
- annotation-identity.json: current export verification against accepted63-positive snapshot; image and annotation provenance. No ignore semantics inferred.
- sources/producer.py and sources/unit.md: frozen executed producer/protocol; run.sh and branch*.log/.exit: commands/exits.
- runtime/{0,52,30}/root.pt: full-vocabulary logits and actual final head inputs for common prelude/x1, positions, exact action tokens and input/mask hashes.
- runtime/*/nodes.pt: every explored original-distribution decision; immutable suffix-keyed logits/head inputs/positions/token identities. No KV shared or saved.
- runtime/*/rescored.pt: independent final full-prefix rows, all logits/head inputs and next-boundary logits.
- runtime/*/readout.pt: effective base+shared-delta coordinate rows, IDs, norms; no bias.
- runtime/*/tree.json: parents, children, terminal retention/pruning, unexpanded mass, path provenance and per-token chosen-versus-all margins/ranks.
- runtime/*/receipt.json: exact model/input identity, producer/panel hash, forwards/time/peak GPU memory, no-mutation/no-op checks.
- reduction.json + reduce.py: independent saved-output probability/tree/rank input reconstruction; every final path and cost.
- physical-score.json + physical_score.py: frozen parser/matcher scoring, explicit separate visual classifications and HOLD. Use PYTHONPATH=. from research-probes.
- physical/index.json, row-*.png, row-*-context.png, contact.png:14 unique final box overlays and local contexts.
- result.json: authoritative candidate science/technical/cost/job summary. terminal.json and manifest.json: stable candidate binding/closure.
Full-vocabulary local logits, head inputs, token paths, positions and effective coordinate readout are captured. No attention, internal-layer states or KV archive; no full free rollout beyond current row.
''')
print(json.dumps(dict(cost=result['cost'],jobs=jobs,branches=[dict(branch=s['branch'],best=s['best'],delta=s['best_minus_original_greedy']) for s in summaries])))
