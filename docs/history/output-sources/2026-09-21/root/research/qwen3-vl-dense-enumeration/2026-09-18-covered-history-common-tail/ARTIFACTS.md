# Covered-history/common-tail artifact map

## Reconstruct the contrast

`panel.json` binds fixed R16/model/paired embedding/config/native heterogeneous batch, original media/grid/prompts, h9tokens, common C10tokens, four29token supplied histories, four10token candidates, coefficients, predecessor acceptance and finite budgets. `source-snapshots/unit.md` is the frozen protocol. `annotation-identity.json` rechecks accepted63-positive snapshot against current export and image bytes. No inferred ignore/negative regions.

`review/admission.json`, `review/triple-full.png`, `review/triple-local.png`: h person is disjoint from admitted A/B/C. C is saved original row2, the distinct bottom-left donut; A and B retain two accepted extents. Two h/C row checks plus four reused displayed realizations, within12 new-row ceiling. Spatial progression differences remain explicit.

## Executed evidence

- `runtime/{A1,A2,B1,B2}/scores.pt`: four coherent candidate rows per supplied history. Each stores full-vocabulary logits and actual final head inputs for10 scored row tokens plus the separately captured next boundary; full MRoPE positions, literal history, candidate tokens and decoded text. Candidate histories diverge after their first token fork.
- `runtime/*/incremental.pt`: raw original-readout logits/head inputs/positions for action29..39 on one fixed A1 candidate per supplied history.39 supplied actions consumed through original native generate path; capture abort occurs before action39 selection. No autonomous target output or forced EOS.
- `runtime/*/readout.pt`: actual effective base+shared-delta1000 coordinate rows, input rows, coordinate token IDs, original FP64 norms/factors and absent bias.
- `runtime/*/parity.json`:44 total full/native position/argmax checks, raw numerical differences, exact original companions and forced histories.
- `runtime/*/receipt.json`: checkpoint/input/source identities, parameter invariance, PID/device, counts, elapsed time and peak memory. `run.sh`, per-condition logs/PIDs/exits retain launch evidence.
- `source-snapshots/covered_history_common_tail.py`: executed producer. It uses the accepted model loader, native preparation/generation and exact-history scorer; no shared source or predecessor byte was changed.

## CPU reconstruction and result

`reduce.py` → `reduction.json` reconstructs all16 raw and normalized full-row probabilities/log probabilities, token contributions, all common-prefix forks, complete D and E matrices, separate first-fork E, readout/position/cache checks and cost. Equal-norm scoring uses full-vocabulary softmax after FP64 scaling rounded to FP32; it is not a free generated route. `matrix.tsv` and `contrasts.tsv` are small flat views of the same matrix.

`verify.py` → `independent-verification.json` independently computes selected-logit minus logsumexp row scores and32 E values, verifies physical prefix/common-tail identities, all44 incremental positions, source hashes, completed producers and a corrupted-common-tail falsification.

`result.json`, `terminal.json`, `artifact-bindings.json`, `candidate-records/` retain the stable unreviewed candidate and reconstruction index. Root acceptance remains separate. `knowledge-check.json` records the root-owned frontier integration boundary.

Absent: free escape rollouts, attention/QKV/layer archives, training/gradients, new labels, checkpoint variants, samples or extra owner search. All model calls are the16 scientific score calls plus160 incremental qualification forwards. No cleanup deletions.
