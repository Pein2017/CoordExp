import hashlib,json,time
from pathlib import Path
R=Path(__file__).resolve().parent;U=Path('research/experiments')/R.name
read=lambda p:json.loads(p.read_text())
def bind(p):
 p=Path(p).resolve();return dict(path=str(p),sha256=hashlib.sha256(p.read_bytes()).hexdigest(),size_bytes=p.stat().st_size)
p=read(R/'panel.json');d=read(R/'result.json');assert (R/'run.exit').read_text().strip()=='0';assert read(R/'admission.json')['status']=='passed'
for b in p['sources']:assert bind(b['path'])==b,b['path']
assert hashlib.sha256((R/'producer.py').read_bytes()).hexdigest()==p['producer_sha256'];live=[];rawbindings=[]
for cell,b in d['runtime_bindings'].items():
 assert bind(b['path'])==b;rec=read(Path(b['path']));assert bind(rec['raw']['path'])==rec['raw'];rawbindings.append(rec['raw']);f=Path('/proc')/str(rec['pid'])/'cmdline'
 if f.exists() and str(R).encode() in f.read_bytes():live.append(rec['pid'])
assert not live;elapsed=time.time()-read(R/'launch.json')['start_unix'];assert elapsed<7200
c=d['cells'];assert c['D00']['primary_free_only']['matched_count']==c['D10']['primary_free_only']['matched_count']==0
lines=['# Endpoint loop: natural donor image × fixed C00/C10 history','', '**Candidate; independent saved-output reduction and technical admission passed. Lead acceptance pending.** Two scientific donor suffixes and one mechanical identity continuation completed. No next intervention was launched.','', '## Decision','', 'Natural-image substitution changes the continuation under BOTH histories, but does not release useful donor enumeration. The C00 history remains an invalid coordinate loop; C10 moves into mostly valid-coordinate repeated person boxes. Both yield0/6 donor known-owner matches and cap after the full1851 free tokens. This establishes current visual-input sensitivity of trajectory form, not image independence and not successful visual grounding or trap repair.','', 'The result does NOT satisfy the proposed stronger pattern “C00 trapped while C10 produces donor-specific known content”: C10 has no donor-known matches. It also does not isolate a selective trap mechanism merely because C10 changes sooner. The deliberately mismatched1233-token history remains a strong alternative explanation; natural donor pixels do not make that history on-policy.','', '## Primary free-suffix-only outcome','', '| Image/history | Free matches / bank | FN | Valid / invalid complete | Malformed | Strict valid repeats | Literal repeats (invalid subset) | UNKNOWN | Free tokens / stop |','|---|---:|---:|---:|---:|---:|---:|---:|---|']
for key,label in [('O00','477415 / C00 reused'),('O10','477415 / C10 reused'),('D00','7116 / C00 new'),('D10','7116 / C10 new')]:
 x=c[key];l=x['primary_free_only'];b=x['burden'];lines.append(f"|{label}|{l['matched_count']}/{l['target_count']}|{l['fn_count']}|{b['valid']} / {b['invalid_complete']}|{b['malformed']}|{b['strict_valid_repeats']}|{b['literal_repeats_free']} ({b['literal_invalid_repeats_free']})|{b['unknown']}|{x['free_token_count']} / cap|")
lines+=['', 'Primary owner matching receives ONLY free suffix tokens. All1233 supplied tokens, including accidental donor matches, are excluded. Original C00/C10 were independently rescored in the same free-only view; original C10’s16 free owners are retained in the result identity ledger, while the old common+free18 is descriptive predecessor evidence only. Both donor free-owner ID lists are empty. UNKNOWN predictions are not relabeled false positives.','', 'The donor’s existing empty-prefix R16 baseline has4/6 matches, four valid rows, no parser/geometry debt or strict repeats, and EOS. Its4 matched label strings are compatible with the annotated inventory. No baseline GPU rerun occurred. Both donor conditional suffixes match0 against BOTH the donor and original banks, so there is neither known donor-content recovery nor geometric persistence of the old16 matched objects. Original C10 matches16/27 with16 compatible label strings on its own bank and0 against the donor bank.','', 'Class-agnostic IoU50 one-to-one matching remains primary. The frozen target representation lacks the matcher’s verified-class status flag, so its formal class-status counts remain unknown; separate exact normalized description-string compatibility is reported diagnostically, without changing labels/status or upgrading annotation-unmatched predictions. Complete per-match owner/prediction descriptions are saved.','', '## Trajectory, endpoints and recurrence','', '| Cell | First changed free token vs paired original (zero-based) | Immediate row | First nonzero x1 | Endpoint coordinate-token occupancy |','|---|---:|---|---|---:|','|O00|reference|chair [0,999,999,999]|never|822/822 (100%)|','|O10|reference|person [100,545,174,660]|row1|648/822 (78.83%)|','|D00|70 (row8 y2,999→76)|chair [0,999,999,999]|never|624/822 (75.91%)|','|D10|4 (row1 x1,100→0)|person [0,999,999,0]|never|238/822 (28.95%)|','', 'D00 matches the first70 free tokens exactly (including the first64-token window), then varies y2 while x1=0,y1=x2=999 persist in ALL205 complete rows. The first7 rows are the original zero-height corner; later inverted-height cycles include chair[0,999,999,70] for65 consecutive rows from row9 and chair[0,999,999,193] for66 from row140. It never returns to the exact original corner after row8 and never reaches all999, but remains wholly invalid. Fewer endpoint tokens is not useful recovery.','', 'D10 differs immediately at the first coordinate. The first8 complete rows are invalid; from row9 it produces valid small upper-left person boxes, with x1=0 in ALL205 rows. The longest exact run is person[0,75,60,121] for53 rows from row75; several alternate boxes recur. It emits205 person rows and no boat row despite the donor’s boat/person inventory. There are187 strict valid repeats and197 annotation-unmatched UNKNOWN predictions. This is a changed repetitive regime, not repaired enumeration.','', 'Original C10’s known-coverage interval and later invalid/all999 relapse remain the accepted control: all999 chair starts at free row63 and lasts143 rows. Every cell emits205 complete rows plus one malformed tail, reaches total3084 action tokens and never EOS. Full1851-token free horizon was retained; no improvement is attributable to early stopping. Per-role endpoint occupancy, all complete raw boxes and exact run lists are in result.json.','', '## Donor selection and immutable execution','', 'Eligibility was predeclared over the256 existing Source256 train records/saved R16 readbacks only.58 qualified; image7116 was the deterministic smallest image ID. Exclusions: original477415 and3 companions. Required EOS, no malformed/geometry debt or strict repeats,≥2 known matches, exact grid[1,52,78], native1248×832 dimensions and prompt IDs/length1362. No outcome-driven preference or new donor search. The CPU-only unchanged native processor confirmed the actual grid, prompt and media identity. qualification.json records every candidate/filter outcome and source hash.','', 'The original heterogeneous bs4 contains460038,477415,496747,511251; donor runs replace only position1 with7116. Companions/order/padding,attention/prompt/native MRoPE,model/adapter/paired original embeddings/readout,FP32-SDPA/RP1 and budget are unchanged. The narrow processor now supplies the ENTIRE target action interval[0,1233), preserving every literal token; all companion and free-step logits remain native. It does not replace native incremental execution with full-prefix prefill.','', 'One mechanical I00 replay reproduced the exact original C00 full target and ALL3 companion sequences/stops. Loaded model, generation settings, prompt-position hashes and sampled cache/position metadata match original C00/C10. Saved C10 reuse was admitted because it consumes exactly the same full literal history/model/runtime and the new supply mask stops at the same boundary; no second control run. Both donor runs preserve identical companions and positions; full-history identity is explicitly supplied/off-policy, not claimed natural. Exact source diff and runtime mask receipts are retained.','', 'The independent CPU reducer validates raw free slices and consumes only those slices; a supplied-row exclusion fixture yields1 match with a deliberately included synthetic full-image row and0 when excluded. No scientific label or bank was changed. Runtime and source bindings, original controls, donor healthy baseline and the current readout/state lead acceptance are all hash-bound.','', '## Causal interpretation and remaining alternatives','', '| Account | Evidence and limit |','|---|---|','|Visual conditioning still affects loop dynamics|Same literal history/model/readout plus changed target image changes both full suffixes. This rules out exact image-invariant continuation for this donor contrast only.|','|History-constrained repetitive state|C00 stays invalid; C10 enters a valid repeat cycle without known content. History/category affects route, but donor-specific access is not demonstrated.|','|Image/history mismatch|Donor pixels are natural but136+ supplied original-image rows are off-policy. Mismatch can itself induce schematic continuation; this panel cannot separate it from failure to use available visual evidence.|','|Fixed readout endpoint advantage|Remains unchanged and cannot by itself explain the between-image changes. Prior equal-norm diagnostic and its upstream-input limits remain intact.|','', 'No initial-entry causality, universal image-independence, K/V malfunction or embedding defect follows. Lower invalidity/endpoint occupancy alone would be misleading here because duplication and UNKNOWN burden replace the old failure. Further causal selection belongs to lead after acceptance; no K intervention, another donor, training or automatic successor is proposed for immediate execution.','', '## Cost and evidence','', f"Actual9252 native bs4 forwards =3084 mechanical admission +6168 scientific calls. Allocated GPU time{d['cost']['allocated_gpu_seconds']:.3f}s ({d['cost']['allocated_gpu_seconds']/3600:.5f} GPU-hours); launch-to-closeout{elapsed:.1f}s. Two donor cells ran concurrently after admission. All exit codes0; no model retry or owned live job. Hard10000-call and2-hour GPU/elapsed ceilings respected.",'']
for file in ['qualification.json','panel.json','producer.py','producer.diff','admission.json','reduce.py','result.json','run.sh','launch.json','terminal.json']:
 lines.append(f'- [{file}]({R/file})')
lines+=['', 'Raw/runtime receipts: [I00]( '+str(R/'runtime/I00/receipt.json')+' ), [D00]( '+str(R/'runtime/D00/receipt.json')+' ), [D10]( '+str(R/'runtime/D10/receipt.json')+' ). Each binds its complete raw batch tokens.','', 'Predecessors: [accepted factorial](../2026-09-16-corner-loop-bridge-factorial/results.md), [accepted readout/state](../2026-09-16-endpoint-loop-readout-state/results.md). Original records and producer/consumer bytes were preserved.','', 'Technical status: valid admitted native execution and independent free-only reduction. Scientific status: visual-input sensitivity of repetitive dynamics without known donor recovery; candidate pending lead acceptance. STOP.','']
# Use ordinary absolute Markdown links without incidental inner spaces.
lines=[x.replace(']( ','](').replace(' )',')') for x in lines]
(U/'results.md').write_text('\n'.join(lines));s=read(U/'state.json');s.update(lifecycle='closed',evidence='unreviewed',disposition='candidate_visual_input_changes_loop_form_no_donor_known_recovery',result=str(U/'results.md'),state_source=str(U/'results.md'),next_action='STOP; lead acceptance and any further decision are separate. No new donor or K intervention.');(U/'state.json').write_text(json.dumps(s,indent=2)+'\n')
cat=Path('research/experiments/catalog.jsonl');items=cat.read_text().splitlines()
for i,line in enumerate(items):
 x=json.loads(line)
 if x['id']==R.name:x.update(result_records=[str(U/'results.md')],reading_entry=str(U/'results.md'));items[i]=json.dumps(x)
cat.write_text('\n'.join(items)+'\n');idx=Path('research/index.md');s=idx.read_text();old=f'Visual-maintenance [unit](experiments/{R.name}/unit.md), [state](experiments/{R.name}/state.json): frozen donor7116 x C00/C10; identity admission then two suffixes only.';new=f'Visual-maintenance [candidate](experiments/{R.name}/results.md): donor7116 changes both repetitive trajectories but yields0/6 known free owners in either history; both cap. Its [state](experiments/{R.name}/state.json) is closed/unreviewed pending lead acceptance. No successor launched.';assert old in s;idx.write_text(s.replace(old,new))
files=[R/x for x in ['qualification.json','qualify.py','panel.json','prepare.py','producer.py','producer.diff','admit.py','admission.json','reduce.py','closeout.py','result.json','run.sh','run.exit','launch.json']]+[U/x for x in ['unit.md','results.md','state.json']]
terminal=dict(status='candidate_for_lead_acceptance',exit_code=0,live_owned_jobs=live,verdict='Image substitution changes both repetitive trajectories but neither recovers known donor owners; mismatch confound remains.',cost=d['cost'],elapsed_seconds=elapsed,next_package_executed=False,bindings=[bind(x) for x in files],runtime_receipts=d['runtime_bindings'],raw_outputs=rawbindings);(R/'terminal.json').write_text(json.dumps(terminal,indent=2)+'\n');print('Stable candidate, all sources verified and jobs finished')
