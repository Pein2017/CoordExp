# Wrapper-history factorial artifact map

All paths below are relative to this output directory unless absolute. Candidate evidence only; cross-study root owns acceptance.

- Frozen contract: `source-snapshots/unit.md`; executed producer: `source-snapshots/wrapper_history.py`, with `producer.diff` against the accepted history producer. Live source is hash-bound in every receipt.
- Inputs: `panels/F.json`, `panels/S.json`, `bindings.json`, `reuse-check.json`. Panels and receipts bind R16/base/original paired embeddings, effective FP32/SDPA/greedy policy, all four native requests, prompt IDs, image/media/grid/tensor identities and exact supplied row. No model input uses the annotation export as substitute media.
- Commands/cost: `launch-admission.sh`, `launch-factorial.sh`, `logs/{cell}.{pid,start,end,exit,log}`, `cost.json`.
- Six primary new executions: `runtime/{native-F,self-F,W,RW,HW,RHW}/raw.json`, `receipt.json`, `capture.pt`. Raw includes all four complete token sequences, decoded text and real stopping reason. Receipts include model/input/source identities, fork logits/ranks and runtime cost.
- `capture.pt` retains selected final-normalized head inputs and full-vocabulary logits at action offsets63/67/68/76/103 if reached; residual states after layers13/14/17/20; actual MRoPE positions; trace of consumed tokens/physical cache positions; original, applied and restored paired H/W historical K/V at layers14..20; current-position K/V at67/68 for all28layers. Every tensor retains shape/dtype in the PyTorch payload. These are selected snapshots, **not** a complete attention/KV archive. No attention probabilities or full trajectory hidden states are promised.
- Technical admission: `cpu-check.json`, `verify-native-F-self-F.json`; source-grounded negative checks reject shifted positions and incorrect wrapper role. `verify-W-RW-HW-RHW.json` validates target-only substitution, exact restoration and unchanged companions. `restoration.json` compares RHW layer20 to native S and head/logits to the accepted R20 intervention.
- Saved-output scoring: `reduce.py` imports the accepted parser/matcher at `../2026-09-16-endpoint-loop-natural-readout-norm/reduce.py`. `reduction.json` keeps old10/current14 banks separately, full/prefix/supplied/free ledgers, one-to-one assignments, strict and invalid literal recurrence, UNKNOWN, EOS/cap and primary symmetric supplied-union exclusion. `independent-output-check.json` binds a second exact reconstruction.
- Annotation version: `../2026-09-17-history-rereading-mechanism/human-evaluation/annotation-snapshot-v1/` and accepted `evaluation-v1/result.json`. No explicit ignore fields exist. Unmatched is UNKNOWN; no physical-negative or exhaustive-truth claim.
- `factorial-decision.json`: eight-cell fork/coverage/debt table, frozen specificity gate and exact-prefix/model/input original-cache reuse. Reused W-off raw/receipts/captures remain in predecessor `runtime/{native-F,residual-only,cache-only,joint}`; no predecessor writes.
- Closeout: `result.json`, `terminal.json`, `artifact-bindings.json`, `candidate-records/`, `knowledge-check.json`. No lead acceptance is implied.

Reconstruct from worktree `/data/CoordExp/.worktrees/research-probes` with `PYTHONPATH=. python <this directory>/reduce.py <new output path>`; compare JSON to `reduction.json`. Technical checks are `verify.py native-F self-F`, `verify.py W RW HW RHW`, and `restoration.py`. Do not rerun predecessor closeouts.
