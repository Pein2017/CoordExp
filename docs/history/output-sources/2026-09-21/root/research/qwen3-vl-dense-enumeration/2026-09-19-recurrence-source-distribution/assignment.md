# Released parallel research package: recurrence source and distribution

## Authority and ownership

Root lead: 01a0a3d5-dc45-7693-8467-4801aa7190df (verified Astra ultra).
Execution/integration worker: 01a0a81a-9e32-7db1-bd07-86fa601f4276 (verified Astra low, idle before dispatch).
Cwd: /data/CoordExp/.worktrees/research-probes.

The user's current message approves the proposed next phase, explicitly asks you to open multiple subagents and execute multiple lines concurrently, broadens attention beyond donuts toward distributions across images, and asks about adjacent coordinate embeddings. Execute this package now. Root owns scientific acceptance and successor decisions. This supersedes the previous sequential-backup ordering ONLY for the four bounded lanes below.

Read and apply lead-worker, native-agent-team-guidance, research-flow and the relevant execution skill. Reconcile your existing children and external jobs first. Use up to FOUR concurrent child owners, reusing fitting contexts where possible. Children MUST use gpt-5.6-luna at max or lower supported effort; no higher family, no full-history fork that inherits Astra, and no grandchildren. Prefer max for measurement correctness. You integrate coupled runtime work yourself rather than asking a child to become another scheduler. Share one frozen panel/sources file by reference; one writer per code and record surface. No reviewer chain.

All eight GPUs are available; expected stress occupancy is not a gate. Coordinate concrete device ownership between your lanes and reassign released devices; avoid simultaneous independent writers/producers on a device by accident. Batch compatible measurements after one real-entry qualification. Do not force eight resident model copies for CPU-only work. Prefer completed useful throughput over merely filling VRAM.

No training, optimizer, embedding/head edits, RP/temperature rescue sweep, checkpoint-stage sweep, broad owner census, source annotation changes, Notion, managed memory writes, cleanup of other jobs, or new infrastructure framework. Preserve all unrelated dirty work. Do not restart accepted prior runs. Unknown is not a physical false positive; numerical near-repeat is not confirmed repeated physical identity.

## Accepted evidence and increment

Read the current research frontier plus the closest result for your assigned lane. Coordinate-margin-provenance is lead-accepted and closed. It gives exact score accounting, not a causal layer localization. Only1 changes no saved route in coordinate-pair-readout; 0>1 remains positive after equalization in all eight captured states, including nonzero global winners. Sparse shadow-winner disagreements are finite route certificates, not evidence of a unique loop circuit.

covered-history-common-tail already measured P+A/B+C with two extents each: all16 full-row contrasts and all16 first-fork contrasts have opposite signs. Do not relabel this as a new experiment. The old owner-recurrence-displacement-control was admission-HOLD, not a model null. Old endpoint-loop-image-history used another scene and remains confounded by foreign history. The new spatial unit changes the location of the SAME scene.

Reuse mature tied2444 and untied-plus-axis2444 bindings from 2026-09-18-untied-highconfidence18-natural; do not reconstruct identities from prose. Its 145 unique images /580 outputs cover four conditions and overlapping annotation strata. Untied versus tied is a package comparison, not an untie-only estimate. Existing production val200 evidence may be analyzed as a separate runtime stratum, never silently pooled. Keep original and normalized policies separate.

## Lane A: broader recurrence distribution and frozen panel

Unit: 2026-09-19-recurrence-distribution-census.
Question: in frozen natural outputs, which categories, spatial regions, sizes, sequence phases and annotation/density contexts concentrate numerical recurrence, and does the pattern recur in a prospectively selected cohort?

First reduce all145 existing mature-panel images and their four conditions, plus separately qualified production val200 records if cheaply available. Retain source/model/policy/runtime/selection strata. Then freeze exactly128 additional unique images by deterministic random selection (seed19) from the actual eligible processed-data manifest BEFORE seeing model outputs, excluding prior train256, dev128, fresh128/sentinel and high-confidence images; resolve image identity by split+ID/path, not bare numeric ID. Bind exclusions and actual selected row/image hashes. Run only original-readout natural greedy for both mature packages, RP1 and the qualified native cap3084, at most256 new outputs. This new cohort estimates its sampled population, not COCO in general. If the eligible source has a concrete identity conflict, hold new generation only and continue existing saved-output work; do not guess paths. No replacement based on difficulty or outcome.

Report separate exact-repeat, existing <=8-bin same-description near-repeat, valid/invalid geometry, onset position, longest consecutive run, excess rows, cap/EOS and endpoint occupancy. Keep the existing predicate implementation and clearly separate all-pairs counts from repeated-row counts: long runs have quadratic pair counts. Include 0,999 AND interior recurrence. Category tables need image exposure denominators (images emitting that category, plus total sampled images), not only raw repeated-token counts. Separate category frequency, output length, small-object/annotation-density proxies and crowd/group ambiguity; density computed from already duplicated outputs is endogenous. Use saved pre-burst/annotation context as imperfect proxies and label them. Report rare-category uncertainty; no unsupported claim that only a few classes repeat. Bootstrap/uncertainty unit is image, not tokens or pair edges. An unmatched change alone is not owner loss.

Freeze the shared mechanism panel BEFORE new intervention outcomes: at most24 original-policy burst boundaries and24 non-recurrent proxy boundaries. Use the existing earliest-run selector (>=3 exact, else >=3 near) and metadata-only stratification across images, predicted categories, coordinate regions, size and checkpoint; at most one of each type per image/model. Include known val885/5586/7511/14038/632 and donut417044 where their actual bound source qualifies; birds309264 may be a non-recurrent control and must not be forced into a failure stratum. Include new-cohort cases through the same frozen deterministic selection rule, not rescue responsiveness. Do not wait for exhaustive physical review. Record proxy mismatches. If fewer than24 failures exist, use the available cases without a failure hunt. Lane B/C/D may develop and qualify concurrently using the existing frozen sources, then consume this final panel unchanged.

## Lane B: visual position x history position

Unit: 2026-09-19-recurrence-spatial-source.
Question: does the numerical recurrence location follow moved visual content, moved coordinate history, or fixed coordinate/canvas preferences, compared with non-recurrent controls?

For each admitted panel boundary: same scene in a common canvas, I0/Idelta crossed with H0/Hdelta. Move ALL history boxes with the same affine map; preserve descriptions, order, count and token count. One horizontal displacement magnitude and two signs, chosen from geometry/processor grid BEFORE outcomes, clearly separating the old and new 8-bin windows. No clipping, no lost content, identical image scale/interpolation/canvas dimensions/grid and text-position conventions. Rebuild true native prefix state for each image/history condition. Do not transplant old image cache.

Primary interpretable comparison is coherent00 versus11, with outputs mapped back.10/01 are deliberate mismatch diagnostics, not detection-quality controls. Distinguish canvas borders from moved scene borders, particularly full-frame predictions. Validate that the common-canvas00 preserves enough grounding and the failure pattern for a maintenance claim. Report every excluded/no-longer-recurrent case and its source denominator; do not tune padding/shift to recover a desired burst. If all relevant failures vanish or healthy grounding collapses, close spatial admission-HOLD/limited evidence rather than searching transforms. This does not block other lanes.

At the common row boundary capture opener/EOS competition. At an explicitly supplied identical description/wrapper capture x1 scores for fixed old/moved coordinate windows, full-vocabulary competitors and margin; label this conditional forced-description measurement separately from free continuation. Keep controlled earlier fields identical when comparing later-role diagnostic logits. Record log probability ratios for old versus moved windows, coordinate-family mass and appropriate mapped coordinate-distribution shifts. Raw full-vocabulary probabilities only; no window-renormalized quality claims.

All free continuations use original readout, RP1, up to32 complete rows/512 new tokens/EOS. At most7 unique transform cells per panel state (00 shared between signs):48x7=336 continuations, plus proportionate real-entry parity controls. Report recurrence/alternative recurrence, invalid, cap/EOS, direction of location shift and selective differences against non-recurrent proxies. Supplied rows receive no recovery credit. Local trusted owner witnesses are optional; absence or ambiguity limits physical claims only. No bulk image review.

## Lane C: conditional numerical-repeat probability

Unit: 2026-09-19-recurrence-conditional-mass.
Question: does sustained native greedy recurrence coexist with low conditional probability of the frozen numerical repeat event, or does the conditional distribution place substantial mass there?

Use the same at-most48 ORIGINAL native panel states, independent of spatial admission. Freeze the current description as a conditioning choice; report next-row opener/description-entry evidence separately. Define the event as a complete legal bbox plus required terminator within the existing same-description <=8-bin historical-repeat union, counting overlaps once. State explicitly that this is NOT total physical duplicate probability. Retain invalid/format outcomes and any separate literal-invalid recurrence event without quietly conditioning them away.

Use256 independent short ancestral draws per state from ORIGINAL full-vocabulary softmax, temperature1, no top-k/top-p truncation, RP1, fixed recorded RNG streams. Sample only the bounded next-box grammar interval; a grammar escape is a non-event/format outcome, not retried or forced into a coordinate. Derive the needed fixed token horizon from the actual serializer before launch. Do not run long sampled continuations. At most12,288 short draws. One self-consistency/known-event test must distinguish native probabilities from constrained-coordinate sampling. Report binomial uncertainty per state and image-level summaries; no made-up certainty from many tokens. Compare failed and proxy states with declared conditioning mismatch.1-q is NOT new-owner probability. No automatic prefix-tree expansion or temperature sweep; return ambiguous intervals as such.

## Lane D: input-coordinate continuity and readout interpretation

Unit: 2026-09-19-coordinate-input-continuity.
Question: when nearby numerical inputs are substituted, is a large downstream change already present in actual input geometry, amplified during forward propagation, or mainly a small-margin greedy switch?

CPU first: use Codegraph at the exact worktree for actual coordinate init/load/forward ownership, then verify source and effective tensors. Audit actual trained INPUT embeddings and OUTPUT rows separately for tied/untied, including base+delta/DoRA path where applicable. Do not assume iid random initialization: existing evidence says digit-derived means plus structured features; verify exact implementation. Show adjacent versus distance-matched/nonadjacent contrasts, norm/cosine/Euclidean distances and sharp local discontinuities across all1000 bins; numerical proximity alone is not a proof of vector proximity. Account for same-token reuse at x1/y1/x2/y2, position/role context, geometry validity and sorted order.

Reuse existing numerical-feedback/coordinate-margin saved captures where they answer the question. At most16 preselected panel states (balanced failure/proxy, multiple scenes) may receive a bounded fixed-history paired forward audit: native input and feasible +/-1 adjacent input, fixed suffix and positions, measured at immediate and one fixed later boundary. Validity/order or uncertain same-owner semantics are reported, not assumed. This estimates numerical input sensitivity, not semantic equivalence when identity is unclear. Capture actual input delta, compact per-layer residual-change summaries, final centered full-logit change, winning margin/rank, and coordinate-distribution shift. No free rollouts in this lane, no layer/KV patch search, no dense attention/KV dump, <=512 additional native forwards including qualification. Qualify no-op and instrumentation parity on the actual runtime. Reuse controlled suffix semantics; a forced fixed-suffix score is not a natural generated trajectory.

Separate smooth numerical outputs from physical-instance identity. A one-bin first-coordinate difference may trigger changed later coordinates or owner choice; do not call it a one-pixel perturbation of the whole decoded box. Smooth input geometry alone cannot guarantee greedy trajectory stability. Conversely, do not presume all sensitivity is benign argmax behavior: compare score change against native margin and failed/proxy controls. CPU-only common-output-shift/gauge note may use saved tensors to clarify that norm effects are real at this parameterization but absolute row norms are not a unique function-level cause. Keep input unchanged and shift ALL output logits/rows equally for the equivalence control; correctly transport the original operator instead of silently redefining it.

## Shared execution, cost and return

Freeze each unit's question/denominators/conditioning/selection/stop before its scientific run; one estimand per unit, concurrent independent units are explicitly authorized. You alone own shared helper changes and catalog/frontier integration. Children own their assigned unit/probe/output subdirectories. Prefer the existing native runtime, selectors, reducers and tensor capture paths; do not build a new scheduler or measurement framework. Independent lanes may finish/HOLD without blocking others.

Overall ceiling: the fixed scientific counts above plus proportionate qualification, at most24 allocated GPU-hours. Stop and report if this is insufficient rather than silently adding samples/seeds/transforms. Runtime mechanical fixes that preserve the contrast are allowed; do not rerun completed science unless its evidence was invalidated. Save raw tokens, source/prefix/image/model bindings, conditioning, compact scores, selected captures and standalone CPU reducers; preserve useful intermediate artifacts but avoid redundant multi-GB cache dumps. Report actual model calls, GPU-seconds, bytes, failed/admitted/analyzable denominators and every remaining owned process. Use all8 devices when parallel model work exists, with one owner for allocation. No GPU-idle polling.

Send root one launch/ownership milestone once children and scientific entry are concretely ready, then only findings/blocks that can change a decision. Use direct App Server delivery as in the successful coordinate-margin-provenance delivery; NO wake-me-up plugin or queue-only completion claim. If root is active use supported steering with expected turn identity; if idle use turn/start. Save a unique direct-delivery receipt; transport uncertainty is not permission to resend blindly.

Return one integrated candidate with per-lane result/status, exact artifacts and CPU acceptance commands, changed paths, source/denominator integrity, interpretation limits and stopped children/jobs. All child returns must be reconciled; do not end early merely after compaction while children run. No self-acceptance, no policy promotion, and no successor launch. Root will independently verify and decide the next step. Worker stops after direct delivery of the completed or genuinely blocked package.
