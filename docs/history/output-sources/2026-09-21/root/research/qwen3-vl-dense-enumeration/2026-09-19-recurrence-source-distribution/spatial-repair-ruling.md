# Root ruling: one bounded Lane B implementation repair

Decision: APPROVED, conditional on the concrete gates below. This is permission to execute the original spatial contrast correctly, not scientific acceptance or a new arm.

Root inspected integration/spatial-counterexample/counterexamples.json and the preserved prepare.py/producer.py source. The horizontal map applies width/translation to all four coordinates; the stopper counts description ends; these change the conditioned trajectory and prevent reducer-only recovery. Root also checked the accepted numerical_feedback/metrics.py and select.py: literal invalid triples are retained, and near runs require all-pairs <=8, not an adjacent-only drifting chain. Thus the proposed corrections restore the frozen meaning rather than change the predicate.

The original assignment explicitly authorizes runtime mechanical fixes preserving the contrast and rerunning completed science only when its evidence was invalidated. That condition is met. Do NOT close B solely because its prior attempts were invalid.

## Authorized extent

- Keep the exact immutable45-state panel SHA005bc6deff209bc96ce469e8cbc20d3dbd30a7e424a7ff18a0f7f8942a1bdb49, two mature model packages, original readout/RP1, fixed canvas/displacement/two signs, and original conditioning/selection rules.
- One corrected pass, up to45x7=315 unique corrected cells plus proportionate real-entry qualification. Qualification cells that fully satisfy the corrected protocol are reused, not rerun. Re-evaluate the original canvas/admission gates with corrected00; a locally inadmissible state remains HOLD and does not trigger replacements. No tuning transforms or choosing only retained failures.
- Preserve all prior raw outputs, snapshots, receipts and attempted-cell/cost counts as technical-invalid. Put corrected prepared inputs and outputs under a distinct attempt/version; bind them to the unchanged source panel and corrected code. Never merge invalid and corrected scientific estimates or overwrite sealed bytes.
- A/C/D continue independently and are not reopened by this repair. Root scientific acceptance remains pending.

## Gate before corrected broad launch

1. One bundled CPU RED/GREEN pass on the actual producer/preparation/consumer functions must falsify the archived buggy versions and pass corrected versions for ALL reported counterexamples: horizontal x-only map, independently expected y preservation, role-correct inverse, true complete-box row stopping, literal invalid triples, nontransitive near-run drift, and fixed old/moved windows. Forward/inverse agreement alone is insufficient because both old functions shared the same bug. Use a known pixel/box witness independent of the mapper. Check the full45-state input construction for invariants before rollout.
2. Reuse accepted numerical recurrence primitives where practical; avoid another privately reimplemented predicate. In this unchanged-height canvas y1/y2 remain unchanged. Old/moved x1 event windows are fixed for each source/sign ACROSS all four factorial cells, not derived from whichever history happens to be supplied. Preserve full-vocabulary probabilities and the declared window support; do not silently substitute a point-bin statistic for a window.
3. Count complete serialized boxes, including geometrically invalid but complete boxes, in the FREE suffix only. Description-end tokens do not count. Distinguish native EOS from an EOS injected to enforce a32-row budget; record the latter as row_cap, never natural EOS. Preserve512-token limit and supplied-row credit exclusion.
4. One small production-shaped real-entry qualification through saved raw output AND the corrected CPU reducer must cover the changed geometry/conditioning/stopping/window path and unchanged runtime identity. Retain a no-op/source identity check where applicable. Do not scale on helper tests alone. Worker independently checks the counterexamples and real-entry receipt before release; another root approval is NOT required once these stated gates pass.

## Budget and stopping

The package24 allocated-GPU-hour ceiling is unchanged and includes ALL invalid attempts, pilot work, corrections and A/C/D.4408 seconds summed producer wall time is not automatically a measured GPU-occupancy total. Before launch, save a defensible remaining-budget bound from process launch/exit logs and reserved device intervals; where timestamps are incomplete, use a conservative upper bound based on elapsed intervals and maximum device ownership, explicitly labeled estimated. Missing accounting must not count as zero. Instrument corrected attempt device/time occupancy. If the remaining budget cannot be bounded or is insufficient, hold B only and return the unaffected lanes with the gap.

If the corrected real-entry gate fails, stop the broad launch and return its precise counterexample; do not start a second broad repair cycle under this ruling. If the corrected model evidence is scientifically null or admission fails, close that result without a new arm. No extra reviewer or owner-enumeration gate.

Return one integrated package with separate invalid-attempt and corrected-attempt status/cost/denominator, checks, source hashes and process exits. Direct completion delivery remains required; no wake-me-up. Stop after candidate delivery.
