# Optional Stage2 component-test admission

Frozen before any Stage2 capture/patch output. This refines only the optional component branch; the six declared coarse-depth bidirectional residual comparisons remain unchanged.

A single effective residual site is sufficient to trigger the declared cache-versus-token rebuild comparison. Calling an interval stable enough to spend the two attention/MLP component runs additionally requires either (1) two neighboring tested coarse depths transferring the donor argmax in the same direction, or (2) one depth with both directions transferring their donor argmax. Both require valid self/native controls and margins exceeding measured control discrepancy. Otherwise omit component runs and retain the coarse intervention result, without further layer search.

This conservative allocation rule avoids treating an isolated near-tie as justification for deeper decomposition. It introduces no scientific cell, changes no primary outcome, and does not redefine a failed optional admission as a scientific null.
