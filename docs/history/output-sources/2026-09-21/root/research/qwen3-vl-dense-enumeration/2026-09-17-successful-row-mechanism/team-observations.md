# Decision-bearing implementation corrections

Stage1 worker prepared the fixed panels and existing-runner commands; parent independently checked source hashes and first SS native replay before launch.

Stage2 worker supplied native hooks and CPU checks. Parent took ownership before GPU qualification after preparation expanded. Parent corrected request data-root routing to the native group JSONL, bound the actually executed modeling source, and required complete native/self target replay plus original54-step argmax checks. These repairs preceded Stage2 model execution. The worker stopped writing before parent launch.

Native S/F capture reproduces all target/companion tokens and Stage1 fork logits/head inputs exactly. S self-patch is tensor/cache/token exact. This same-source real-entry gate admits independent patches concurrently with the remaining F self-control; no causal verdict is valid unless that control also passes. No extra reviewer or worker hierarchy.
