# Mature geo_sorted_xy composition comparison

User request: compare consistency using the trained geo_sorted_xy adapter on a few samples; judge practical differences through IoU/mAP, with coordinate-bin differences as diagnostics. User accepts a few bins in principle. No exact new bin, AP or IoU acceptance threshold has been inferred. This experiment does not itself modify production admission or archive unfinished tasks.

- Decision: measure how much BF16 materialization changes this mature model's parsed detections on a frozen small validation cohort.
- Contrast: dynamic HF base + DoRA + selected embedding delta versus HF executing the authenticated dense BF16 materialization of those same components. This isolates composition within HF; it is not yet a vLLM engine comparison.
- Checkpoint: `/data/CoordExp/outputs/research/eight-coordinate-bbox-supervision/2026-08-05-closeout/artifacts/training/four-coordinate-xy/checkpoints/step-2444`.
- Native reference: archived `artifacts/inference/four-coordinate-xy-val200/configs/resolved.json`; system/user prompts verified character-for-character equal to checkpoint training configuration. The later Human13 FP32 repair configuration is not this run's numeric reference.
- Fixed numeric/decode settings: BF16, FlashAttention2, patch-embed linearization enabled, batch 4, greedy temperature 0, top_p 1, n 1, repetition penalty 1.1, max_new_tokens 512. No training.
- Cohort: [cohort.json](cohort.json), 32 images / 296 GT objects. COCO139 is a named anchor, plus 31 random other validation rows selected with seed 20260912; no filtering by model outputs. GT counts range 1–28. Image pixels range 905216–1048576. Input SHA256 `2e97810c11fc3935b96196767378d1ab68a7571f555919ee0e5960240d764969`.
- Ordering: original `(x1,y1)` object order validated and retained; source_order is an explicit transport setting because this contracted checkout lacks the geo_sorted_xy enum. Only image paths are rebased relative to the subset JSONL, resolving to the exact source files; the current real loader verified all 32 rows. GT assistant content is excluded by the ordinary inference prompt builder; source_order must not change prompt text, image or existing record order.
- Primary evidence: the same 32 rows in each arm, complete current raw/scored/provenance/config/trace/image-plan/manifest/summary surfaces, matched downstream evaluation receipts, small-cohort COCO AP@[.5:.95]/AP50/AP75. Report eligibility/parse/truncation failures without asymmetric filtering.
- Auxiliary evidence: exact text/tokens and prediction-count changes, class-aware one-to-one box agreement IoU and coordinate drift, unmatched boxes and changed classes separately. Predicted-box agreement does not establish GT correctness. Small-cohort AP is descriptive and is not a full-validation noninferiority claim.
- Strong alternative: a model/prompt/delta/score/row mismatch or truncation could masquerade as composition drift. Retire these from effective identities and consumer artifacts before interpreting results.
- Work bound / stop rule: one authenticated materialization and one 32-row decode/score pass per arm; at most 512 generated tokens per image per arm (32768 across both), no model updates. Stop on a concrete incompatible identity or invalid real entry; ordinary scoped corrections may rerun the failed stage with failure evidence preserved. No full training, no model/topology matrix, no output-based cohort expansion. GPU execution owned by Sol on GPUs6/7; analysis by Luna; root owns cohort and final conclusions.
- GPU-hour budget: no ceiling per the existing user instruction; the finite workload above is the stop rule. Preserve timing/exit/resource receipts. Shared occupancy is not a reason to wait for idle GPUs or stop unrelated jobs.

Current state: both 32-row HF arms completed; paired identities and direct evaluator receipts passed. Corrected IoU>=0.5 pair analysis passed lead counterexample, boundary and all-row checks. Small-cohort diagnostic AP changed from0.380992 to0.364849. Production admission remains unchanged; this result is not a full-validation equivalence claim.
