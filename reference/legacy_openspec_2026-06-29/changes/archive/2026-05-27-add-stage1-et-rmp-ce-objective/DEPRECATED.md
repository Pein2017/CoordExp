# Deprecated

This completed change is retained as historical Stage-1 objective context.

The active Stage-1 objective direction has moved to the current compact
teacher-forced / trie-oriented surfaces captured in stable docs and specs.
Archive this change with `--skip-specs`; do not sync its older transition
requirements as new stable behavior.
