# LVIS v1

Use `public_data/run.sh lvis` for LVIS v1 annotation/image download, conversion,
rescaling, coordinate-token conversion, and validation. A missing processed
LVIS root is reported as absent; it is not a successful provenance validation.
