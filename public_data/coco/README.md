# COCO 2017

Use `public_data/run.sh coco` for download, instance conversion, rescaling,
coordinate-token conversion, and validation. Use
`python -m public_data.scripts.build_coco_views` to build the retained
`coco80/full` and `coco80/len-12000` views into a fresh target.
