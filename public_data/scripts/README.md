# Retained entries

Retained modules cover COCO/LVIS download and conversion, shared rescaling,
coord-token conversion, COCO view construction, pipeline orchestration, and
JSONL validation. Invoke Python entries as `python -m public_data.scripts.<name>`.
