# Are the continuing effects concentrated in coordinate logits0 and1?

## Authority and decision

Unit: `2026-09-19-coordinate-pair-readout`. Root releases and accepts one bounded
inference-only investigation under the user's overnight delegation. Existing
916-worker remains Astra-low; at most two useful Luna-max-or-lower children with
disjoint execution ownership and no descendants. No automatic successor.

The [direction control](../2026-09-19-readout-direction-control/results.md) showed
alternate fixed directions can reduce numerical recurrence. The [first-coordinate
factorial](../2026-09-19-first-coordinate-mediation/results.md) then found that
initial x1=1 is conditionally sufficient under untied original readout, but not
tied; it is not necessary for the four association-positive sign policies.
Sign23/24 can preserve the entire original first row when forced0 and diverge at
the next x1 instead. Thus later decisions remain an active causal alternative.

**At the same11 original failure prefixes, do sign23's changes to logits0 and1
alone account for its recurrence effects, or are changes to other coordinate
logits needed?**

This tests the support of an existing readout intervention, not another selected
early-token patch. Earlier [all-coordinate normalization](../2026-09-16-endpoint-loop-natural-readout-norm/results.md)
and fixed-state endpoint accounting do not identify this two-logit intervention.
The root's saved-state arithmetic predicts a first0→1 switch for the pair under
both donut models, whereas either singleton leaves tied first x1 at0. Those are
post-hoc immediate shadows, not predictions of complete continuation quality.

Sign23 is the lowest-seed association-positive policy whose forced0 untied route
preserved the entire original first row. Its selection and pair choice are
outcome-informed. All11 existing failure boundaries, including adverse/null
cases, are retained; this is not a fresh-population or policy-promotion test.

## Fixed six operators and sources

Reuse the exact original completed-row prefixes, T/U models, images, prompts,
native companions/masks/positions, FP32/SDPA, serializer, horizon and frozen
sign23 vector from the accepted direction study. Do not use later crossover cuts
or first-coordinate override histories. T/U remains a package comparison.

Let z_k be the original coordinate score and d_k=s23_k*(alpha_k-1)*z_k, with the
already frozen alpha and sign23. Apply, at every future step:

| Policy | Coordinates receiving the unchanged sign23 increment |
|---|---|
| original | none |
| sign23 | all1000 |
| only0 | {0} |
| only1 | {1} |
| pair01 | {0,1} |
| except01 | all except{0,1} |

No coefficient refit, energy rescaling, sign change or new random seed. Use the
existing FP64 arithmetic and final FP32 cast; retain native values bitwise on
unmodified coordinates and every noncoordinate token. At an identical state,
singleton increments sum to pair01, and pair01 plus except01 reconstruct sign23.
This is a score-support ablation; total perturbation magnitude intentionally
differs, unlike the preceding magnitude-matched direction test.

Exactly66 fresh target continuations maximum, including22 fresh original/sign23
controls and qualification. Natural greedy, RP1, no forced token, input-vector
change, KV transplant, sampling, coordinate ban, EOS change or extra mask. Stop
each at natural EOS,32 complete rows or512 tokens. All generated rows are free;
do not import the previous unit's exclusion of its first controlled row.

## Outcomes and interpretation

Use the existing whole-free-continuation exact/near runs, native/alternate return,
invalid/malformed, EOS, lengths and cap metrics. Numerical near rows retain the
existing description-matched8-bin comparator. Also retain supplementary
all-pairs exact/near counts to expose separated repetitions; these are not a
new acceptance gate or physical identity measure. Preserve debt and termination alongside
recurrence; no weighted quality scalar or physical recovery inference.

Report every boundary with all six policies. Pair01 versus original tests its
conditional sufficiency; sign23 versus except01 tests its contribution inside
the complete intervention. The two singletons distinguish suppressing0 from
changing competition through1 and their joint threshold effect. Neither a first
winner flip nor exact reproduction of the full sign23 path is a success gate.

If pair01 works and except01 loses the effect, this supports a sparse readout
contribution there, not a unique internal circuit or training cause. If both
work, redundant routes remain. If only the complete policy works, a distributed
or joint effect remains; do not expand into a search over other bins. If effects
do not generalize beyond the selected donor example, keep the case boundary.
Coordinate0 and1 are legal values; do not infer that they should be banned or
that suppressing repetitions proves improved physical owner coverage.

Keep raw/applied/six-policy shadow scores, actual final head input, full-vocabulary
winning competitors, row/role positions, EOS/family probabilities and emitted
tokens. At each first branch difference use the SAME saved original state;
later divergent histories remain separately attributed. No all-layer archive.

## Qualification, bounds and closure

Reuse qualified producer/consumer logic and change only the static score support.
Snapshot all new producer bytes before launch; preserve predecessor bindings.
Qualify one six-policy untied donut group, counted in66: source identity, controls,
coefficient/support reconstruction, bitwise preservation outside the mask, actual
greedy emission, persistence/readback, and one CPU wrong-support sensitivity test.
All22 original/sign23 continuations must exactly reproduce accepted source outputs.
No unsupported source substitution or tolerance relaxation.

Use all8 GPUs with existing native batch semantics. Ceiling4 allocated GPU-hours,
50000 batch forwards and12GiB tensors including qualification and failed attempts.
One bounded mechanical correction per affected seam is allowed; preserve failures
and reuse completed cells. Return NEEDS_LEAD for identity, meaning or measured
budget conflicts. Stop at the complete panel; no new bins, coefficient/role/dose
sweep, training, checkpoints, annotations or population evaluation.

Output root:
`/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-19-coordinate-pair-readout`.
Worker owns unit state/results, output root and minimal probe delta; root owns
broad routers, acceptance and subsequent decisions. Integrate the candidate,
CPU reproducer, hashes, costs and job closure before the persistent worker alone
writes `integrated-terminal.json` and appends `INTEGRATED_CANDIDATE`, `NEEDS_LEAD`
or `BLOCKED` to `coordination/events.log`. Then stop; no automatic next package,
visual/annotation expansion, production changes, Notion or memory writes.
