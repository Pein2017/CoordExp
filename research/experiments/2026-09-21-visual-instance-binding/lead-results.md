# Visual instance binding: accepted bounded result

Lead-accepted on 2026-09-21 with redundant-replay execution debt. The
[independent package acceptance](/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-21-spatial-progress-gate/lead-acceptance.json)
owns verification, cost, and closure. The [worker result](results.md) preserves
the candidate, exact measurements, and source links unchanged.

H2 predicted that A and N candidate rows would lose differentiated visual support
at recurrence: erasing either region would affect the same candidate source, or
separation would degrade relative to the matched control. The admitted evidence
runs against that prediction. Erasing A lowers its native/actual row likelihood;
erasing N lowers the fixed N row more strongly than the A rows. This separation
survives at all three admitted recurrence boundaries and the matched control.
Unrelated masks have much smaller effects. H2 is therefore downgraded on these
finite candidate rows; cross-effects remain.

The sole primary evidence is qualification-01: three failure states and one
control across two images, with four conditions each (16 cells). Eight failure
and two control candidates remain admission HOLDs from the frozen six-image,
11-trajectory pool. The role-wise response matrices retain x1, conditional y1,
joint corner, and complete-row effects separately. On tied14038, A removal changes
actual-A x1 by only -0.0937 and N x1 by -0.0490, while the joint-corner changes are
-0.8119 and +0.6740. Stronger later separation does not establish distinct native
x1 owner selection. Supplied N coordinates may enable local grounding after a
choice that native decoding does not make; the remaining selection-versus-grounding
alternative is unresolved. No physical owner recovery, global instance
representation, vision-pretraining origin, or H1 support is established.

Root verified 91 unique bindings across the integrated evidence, all 128 Lane B
retained source copies, and all 12 masks, unchanged complements, comparable areas,
and local-ring RGB fills. Native trace parity and clean compositor parity pass.
The four Lane B v2 products reproduce byte-for-byte through a read-only replay;
all 64 primary/audit condition-candidate rows agree exactly. The duplicated
scientific-01 runs under states/ remain audit-only: excluded from scientific
denominators, aggregation, selection, and replication claims, with full cost
retained. The reported pre-ruling chronology is consistent with retained launch,
terminal receipt, and log filesystem timestamps; it is not independent event-time
instrumentation and does not justify a claim of violating the later ruling.

The optional release was skipped. Lane A closes separately as
[technical HOLD](../2026-09-21-spatial-progress-gate/lead-results.md), with H1
untested. Both lanes are closed; no further model call or successor is authorized.
