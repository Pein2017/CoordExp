# Successful-row mechanism — lead-accepted bounded result

Technical and evidence checks independently passed; scientific evidence is bounded to one selected bird309264 history. No policy or general mechanism is promoted. The [frozen protocol](unit.md) and artifact root `/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-17-successful-row-mechanism/` own this package. No additional model work is pending.

## Coherent row factors

| Supplied row | Free known matches | Free invalid rows | Target tokens / stop |
|---|---:|---:|---|
| SS: [78,609,106,641] | 3 | 0 | 163 / EOS |
| FF: [1,551,36,596] | 0 | 312 | 3084 / cap |
| Sx/Fy: [78,551,106,596] | 4 | 0 | 154 / EOS |
| Fx/Sy: [1,609,36,641] | 0 | 315 | 3084 / cap |

The route follows the coherent S x-coordinate pair in this factorial. Position and extent remain confounded; this is not an isolated x1, owner-novelty or attention effect. Complete-row log likelihood favors failed FF (-18.357) over SS (-19.308). Saved S/F target and companion sequences replay exactly.

## Conditional mediation

The first postrow fork is action67, x1: S chooses coord131; F chooses coord0. Native/self token, logit, readout-input and sampled-cache checks are exact. The final-head-input swap transfers the donor decision and route in both directions, an expected controllability control.

| Residual output patched | S into F | F into S |
|---|---|---|
| Layer6 | remains failed | remains successful |
| Layer13 | remains failed | switches to failed |
| Layer20 | switches to successful | switches to failed |

Layer20 S-to-F yields the same entire output as the head swap: four known matches, EOS154, no invalid rows. Reverse head/layer20 swaps yield the same failed capped output. Layer13 reverse also fails but has a different later sequence. These identify intervention-sensitive mediators, not a unique origin or circuit.

Crucially, rebuilding the ORIGINAL unpatched cache from the same F history plus the emitted coord131 reproduces the entire rescued target and all companion sequences exactly. The unpatched rebuild control reproduces native F. Extra injected K/V at layers21–27 is therefore unnecessary for this exact rescue: the emitted choice is sufficient. This does not establish general cache irrelevance.

Separate layer20 attention-output and MLP-output S-to-F replacements both retain coord0, zero known free matches, 312 invalid later rows and cap. Both touch the intended component and downstream cache. Neither component alone is sufficient. Algebraic residual decomposition assigns signed projections of the S/F output difference to inferred incoming residual (0.668), attention (0.026), and MLP (0.306); these are descriptive vector accounting, not causal percentages. Incoming residual was inferred from the decoder equation, not directly captured.

## Physical identity and credit limits

The successful routes preserve the same seven reviewed free owners: four identities linked to annotations and three credible unlabeled birds. The extra known41529 match is the previously reviewed middle-left grey bird with changed extent, not a new physical owner. SS still has only three geometric matches. This correction is an evaluation sidecar; predecessor annotations and evidence are unchanged.

The right white bird recurs through different head/body extents despite zero strict box repeats. Upper-left boxes and supplied-row identities remain HOLD. Failed factorial routes re-emit credible prefix owner A; successful free suffixes do not (free-future retention0/1), but A remains in the common prefix in every full output. This is not full-output disappearance. Stage2 excludes the entire intervention-generated first row symmetrically, preserves per-condition ledgers, and uses physical supplied-owner exclusions within comparison groups; known exclusion unions are empty. Reverse interventions lose the three known S matches and do not retain the seven reviewed S free owners in their valid-box region. Unmatched and invalid rows are not automatically physical false positives.

These uncertainties can alter physical totals and precision claims; they do not reverse exact replay, the factorial token routes or the cache-rebuild equivalence. No clean enumeration algorithm or policy is promoted.

## Verification, cost and boundary

Independent saved-output reduction is JSON-exact for both manifests (20 full cells); raw regex row/invalid counts, EOS/cap and 22 model-identity receipts pass. A supplied-owner exclusion falsification fixture passes. Self patches, donor-head parity, component copying, companion invariance and cache/rebuild receipts are retained.

Actual cost: 20 native batches, four row-score forwards and68 prefix-extraction forwards, totaling39,368 model forwards;5,295 allocated GPU-seconds (1.471 GPU-hours),2,506 seconds first-to-last execution window. All model jobs exited0; no live owned job remains. F2 confirmation was omitted. No cleanup was performed because retained evidence remains useful. Tensor inventory records52 files /319,249,868 bytes; complete historical KV and attention archives were not captured.

Strongest surviving accounts are history-conditioned spatial/order routing carried in accumulated residual state, possible reassertion of the original history after earlier patches, and a generic trajectory/format attractor. Owner-ledger representation, training origin and embedding defects remain unestablished.

One next discriminator for a fresh lead decision: compare the ineffective layer13 current-token S-to-F patch with the same patch plus a narrowly bounded, position-matched paired K/V substitution for the supplied-row history at one downstream site, with self controls. This would test history rereading versus missing earlier state; no such experiment is authorized or launched here.

Reconstruction and exact artifact paths are in `ARTIFACTS.md`; final machine receipts are `result.json` and `terminal.json`. The lead acceptance receipt is `lead-acceptance.json` under the artifact root; the original candidate and terminal are preserved. Local research frontier integration is complete.

## Independent lead acceptance

The lead revalidated all332 bound package files and source snapshots, replayed both saved-output reductions (20 cells), and reran the native/self/head/residual, component-copy and same-token cache-rebuild checks on CPU without altering candidate evidence. All22 model jobs exited0, producer PIDs are absent, and the summed39,368 forwards match the cost receipt. One bounded interpretation audit found no blocker; the lead also inspected the plotted Stage2 comparison. The evidence supports a conditional emitted-choice rescue without the additional injected K/V, not cache irrelevance, a layer20-specific cause or a fully solved repetition mechanism. The seven reviewed physical owners remain seven when grey-bird annotation agreement improves. No model producer was rerun for acceptance.
