# Is the normalization direction special at matched score-change magnitude?

## Authority and predecessor

Unit: `2026-09-19-readout-direction-control`. Root releases and accepts this
bounded inference investigation under the user's September18 overnight grant;
916-worker executes as Astra-low. Child agents may only use Luna-max or lower,
at most two disjoint owners and no descendants. No automatic successor.

The [component comparison](../2026-09-18-readout-common-component/results.md)
shows that neither analytic term alone generally explains normalization, and
additive score increments can cross a discrete boundary. The [history crossover](../2026-09-18-history-readout-crossover/results.md)
distinguishes persistence after withdrawal, continuing readout dependence, and
timing failures across mature-model cases. These results do not yet distinguish
the normalization direction from generic score perturbation of the same size.
Old [pulse and sampling evidence](../2026-09-17-repetition-history-mechanism/results.md)
changes routes but is not a coordinatewise magnitude-matched readout control.

**At the same11 mature-model recurrence boundaries, does the true norm-equalizing
direction reduce numerical recurrence more consistently than fixed directions
with the same absolute change to every coordinate logit at an identical state?**

The strongest alternative is that generic small score changes release or alter
some fragile trajectories without any special role for norm correction. This
unit tests direction, not coefficient strength, input embeddings, training origin,
or physical owner recovery. Direction-specific evidence would still leave
coordinate smoothness and structured parameter geometry as alternatives.

## Frozen panel and operators

Reuse the11 `failure` boundaries from the accepted component experiment, at their
ORIGINAL completed-row prefixes, with the same mature T/U checkpoints, FP32/SDPA
runtime, image bytes, prompt, masks, positions, companions and serializer. Do not
use the later4/8-row crossover prefixes or select only the successful cases.
T/U remains a model-package comparison, not an untie-only intervention.

For the original coordinate logit z_k and the frozen factor
alpha_k=median_j(||w_j||)/||w_k||, define delta_k=(alpha_k-1)*z_k. Compare:

1. Original: z_k.
2. True normalization: z_k+delta_k, using the existing qualified arithmetic.
3. Reflected direction: z_k-delta_k.
4. Eight fixed random-sign directions: z_k+s_k*delta_k, with500 plus and500 minus
   signs over the1000 coordinate IDs, sampled without replacement for seeds19–26.

Freeze and hash each exact sign vector before any model call; reuse that vector
across every image, model, coordinate role and step. Signs are fixed readout
operators, not stochastic decoding. There is no coefficient search or best-seed
selection. The eight sign vectors are eight correlated-panel interventions,
not88 independent images. Report each vector, not only the best or an average.

At the SAME state all non-original operators change each coordinate score by
the same absolute amount in FP64. Preserve the previous full-normalization
implementation and validate its FP32 rounding; do not change it to make bitwise
algebra prettier. Noncoordinate logits stay bitwise unchanged. The current
T/U alpha ranges are approximately[.941,1.025]/[.939,1.022], so reflected scales
are positive; recheck effective-row identities and ranges before launch.

Apply the selected fixed operator at every future step, with natural greedy,
RP1, no masks or sampling, until EOS,32 complete target rows or512 tokens.
Eleven policies times11 boundaries gives at most121 fresh continuations,
including22 fresh original/full controls and all qualification cells. Companion
outputs are not interpreted. No new cases, healthy panel, temperature or dose arm.

## Evidence and decision

Use the existing suffix exact/near-run, native return, alternate repeat,
invalid/malformed, EOS and cap metrics. Retain all output lengths and stop
reasons beside recurrence: earlier termination, invalid geometry or another loop
does not establish improvement. Near rows use the accepted description-matched
all-pairs8-bin criterion; no physical identity is inferred from that proxy.

Report per boundary the true direction against reflected and all eight sign
controls, with changes from original and the competing error/stop outcomes.
Do not invent an outcome-weighted scalar or tune a threshold after seeing data.
Highlight whether direction differences persist on cases without additional
invalidity or shorter termination; keep unsupported/ambiguous comparisons visible.
Full normalization is a fixed comparator, not an independently sampled policy.

Preserve raw/full/applied shadow scores, full-vocabulary competitors, role/row
positions, EOS/family probabilities, actual final head input and actual target
tokens. At the first divergence from the original branch, reconstruct all11
operators on that SAME saved original state. An emitted-token switch or exact
route identity alone is not the scientific outcome. Later different histories
must not be interpreted as controlled same-state score comparisons.

If reflected/random directions work similarly, the perturbation alternative
remains viable on this panel; do not label norm scale the identified cause. If
true normalization is more consistent, conclude direction-specific evidence
against a magnitude-only account, not a universal mechanism or learned repair.
Mixed case-specific results stay mixed. Stop without new seeds or coefficient
tuning when the fixed panel is complete.

## Runtime and terminal

Reuse qualified code and metrics; add the smallest fixed-operator delta. Do not
rewrite sealed predecessor producers. Bind all model/config/source/prefix/sign
identities in the existing output manifest and snapshot new producer code before
launch. First qualify one actual11-policy group, counted in the121 cells: target
history/masks/positions, unchanged-policy overlap, operator and magnitude formulas,
noncoordinate preservation, emitted winner and artifact readback. Include one
CPU corruption sensitivity check on saved operators. All22 original/full controls
must reproduce the accepted saved overlaps under unchanged horizon/conditions.

Use all8 GPUs for independent case/model work; expected stress load is not a gate.
Ceiling8 allocated GPU-hours,100000 batch forwards and16GiB tensor artifacts
including failed attempts. Measure the first group; return NEEDS_LEAD if the panel
cannot fit or requires a semantic change. One bounded mechanical correction per
affected seam is permitted, with failed evidence preserved and completed cells
reused. No training, stage sweep, architecture change, labels or population eval.

Output root:
`/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-19-readout-direction-control`.
Worker owns this unit state/results, output root and minimal probe delta. Root
owns catalog/index/questions and acceptance. Integrate the complete CPU-replayable
candidate, costs and ended/live jobs before the persistent worker alone writes
`integrated-terminal.json` and appends `INTEGRATED_CANDIDATE`, `NEEDS_LEAD` or
`BLOCKED` to `coordination/events.log`. Stop there; no self-acceptance, automatic
next package, Notion, managed memory or production changes.
