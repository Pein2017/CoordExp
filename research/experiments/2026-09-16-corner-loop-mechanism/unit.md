# Corner-loop phase1: fixed-history decision and execution diagnosis

Status: phase1 lead-accepted and closed, no training. The [result](results.md) and its independent acceptance receipt own the bounded conclusion. Phase2 is proposed only.

From Bnormalized64/P16/R16 with identical frozen coordinate embeddings/readout, where do slot-level conditional preferences differ on images477415,351017,417044, after retiring native incremental-cache versus full-prefix execution mismatch?

The paired16 repair study is closed without promotion. Prior repeat-origin results found image/history dependence; checkpoint/history crossing separated entry from continuation. Older417044 coordinate-cache interventions rescued continuation, but norm-matched wrong-region deltas also worked, background/Gaussian did not, and K-only/V-only were not norm-matched. These are older checkpoints and establish neither an owner ledger nor a cache bug. This phase changes to today's fixed checkpoints and exact slot decisions; it does not rerun a suffix matrix.

Freeze `panel.json` under `/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-16-corner-loop-mechanism`. It binds source/checkpoint/embedding hashes, raw-row validation, prompt/media IDs and full P history.477415 boundaries are rows4/16/138 (first P/R divergence, established corner loop, P exit); secondary boundaries rows1/2/16 (entry, divergence, loop). Rows include complete invalid geometry. The same supplied P history is compared across all checkpoints; post-divergence B/R histories are off-policy. These history lengths are not a controlled repetition-dose experiment.

At each boundary score the literal P row and one coherent trusted-bank canonical owner row not matched by earlier prefix boxes at IoU50; it is an alternative, not uniquely legal. At477415 exit also score the preceding literal repeated corner row at that SAME boundary. Internal candidate prefixes are teacher-forced likelihood diagnostics, not phase2 forcing/free continuation. Record category tokens, admission/EOS and four coordinate slots, full-vocabulary logits/ranks/margins. No generated tokens.

Native cache starts from the prompt and consumes every literal P token via actual Qwen generation preparation. Full replay uses existing exact_history_inputs and native MRoPE derivation. Assert positions, image/grid, prompt and cache lengths; test identical full/no-op replay and cloned-cache identity. Compare full-vocabulary discrepancy to argmax margin. Material parity failure: different argmax with winning margin >1e-3 and >10 times no-op discrepancy; stop attribution. Smaller flips are numerical ambiguity, not bugs. All measured discrepancy/margin ratios remain reported, including same-argmax near ties. FP32/SDPA throughout. Primary execution batch1 is matched across cache/full; additional duplicated-image/history batch4 full replay measures shape sensitivity but does NOT recreate the original heterogeneous natural bs4 batch. Saved natural outputs therefore remain provenance, not guaranteed singleton token parity.

Nine independent image/checkpoint cells, at most eight GPUs concurrently,27 boundaries,57 candidate rows including exit repeated competitor. Ceiling6500 model forwards, no free decoding;1800 seconds per cell intended bound. Reuse native loading, bound requests, exact replay and batching helpers; no orchestration abstraction. Snapshot producer, log exits, bind artifacts. If a technical counterexample appears, preserve it and stop mechanism attribution; do not label it a scientific negative.

Deliver raw provenance, cache parity and slot contrasts, strongest surviving alternative and ONE next causal intervention/control proposal, not execution. No phase2 token forcing, embedding/KV edits, labels, training, new model family or sweep. Preserve predecessor and unrelated dirty files. Lead decides acceptance and any next phase.

Closeout clarification: the preceding-row candidate at row138 is literally P row137, person [0,0,999,999]. The earlier wording "repeated corner row" is inaccurate for that candidate. Its original tokens and numerical results are preserved; the repeatedly emitted chair [0,999,999,999] was scored separately at row16. This limits the claim to the measured history and selected slots.
