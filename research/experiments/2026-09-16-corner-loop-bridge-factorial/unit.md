#477415/R16 recent-row category × geometry sufficiency

Fixed2×2 causal diagnostic, no training. The [result](results.md) is lead-accepted and closed; its independent receipt owns acceptance. Any next phase has a separate contract.

From the frozen R16 checkpoint and its own first136 complete natural rows, does editing row137 category and/or y1 enable useful freely generated known-owner coverage under the unchanged total3084-token horizon and matched original heterogeneous bs4 runtime?

## Predecessor and distinction

[Phase1](../2026-09-16-corner-loop-mechanism/results.md) is lead-accepted: cache/full-prefix slot parity passed; R retains P's local conditional row138 exit on supplied P history. But that history already contains row137 person [0,0,999,999]. The original R history remains looping. Older history crossing separated entry/maintenance, and coordinate-K/V amplitude controls showed direction-and-magnitude effects without proving an owner-specific ledger. This phase tests one recent bridge under R's own history, not embedding-origin bias, longer-history swaps or another KV study. No accepted predecessor is rewritten.

## Freeze

Authoritative packet: `/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-16-corner-loop-bridge-factorial/panel.json`.
It binds R16 adapter, original paired embeddings, model/config, bank27, original pixel/grid/prompt, source rows and exact token histories. Row137 starts at action offset1224, is nine tokens, and ends at1233. Common history is literal first136 R rows, including invalid geometry. No reserialization or filtering.

| Cell | Category | [x1,y1,x2,y2] |
|---|---|---|
| C00 | chair | [0,999,999,999] |
| C10 | person | [0,999,999,999] |
| C01 | chair | [0,0,999,999] |
| C11 | person | [0,0,999,999] |

Category changes only row token1; geometry changes only row token5. x1 remains0 and x2=y2 remain999. The geometry factor changes zero height into full-image extent, confounding validity/area/extent by design. These supplied rows are diagnostic histories, never owner labels or training targets.

## Native execution and gate

Reuse the exact original shard05/batch_index6 order:460038,477415,496747,511251. Prompt widths1320/1362/1320/1320 produce left padding42/0/42/42. Native generation keeps finished companions padded; all four cells retain this policy. Original companions stop at11/77/294 tokens and are mechanical batch context, not added research cases.

Use existing generate_continuations with the original FP32/SDPA, greedy RP1, top_p1/top_k0 and explicit model-default reset. One request-local logits processor verifies the exact pre-edit history and fixes only the complete row137 for intervention cells; all logits after it, including next-row admission/EOS, are untouched. Common history is consumed token-by-token via native generation, not compact-prefill with a different batch. Record MRoPE/cache seams and argmax/margin on a reproducible identity fork.

C00 performs an untouched complete native run and must reproduce all four original token sequences and stops, including the entire target suffix. It is both identity gate and scientific control, executed once. Only after it passes may C10/C01/C11 run concurrently. If identity fails, preserve the fork and stop; only a concrete mechanical discrepancy can justify bounded repair, never a baseline switch or batch/backend sweep.

Each target has1851 free tokens after1233 supplied action tokens. Total action cap stays3084. EOS remains available immediately after the completed supplied row; no next opener is forced. Intended maximum12336 model forward calls across four cells; hard ceiling20000 total/5000 per cell including any mechanical controls. Intended cell wall time≤1800 seconds; no adaptive horizon expansion. Up to four GPUs used; spare cards do not add axes. Durable tmux script/logs/exits survive task return.

## Credit, outcomes and stop

Use the frozen native parser and one-to-one known-owner matcher. Exclude supplied row137 from owner matching in EVERY arm. Primary population is common first136 + free suffix; report free-only owners, new coverage versus common history, lost owners and assignment swaps. Supplied rows may serve as duplicate-history references, separately from free-row errors. Unknown stays unknown.

Report immediate complete free row, first exit from corner coordinates and x1=0, recurrence, native parser drops/fragments, complete invalid geometry, strict later-row repeat proxy, free lengths and EOS/cap. Useful evidence requires additional free known-owner coverage plus joint burden accounting; immediate EOS alone is not improvement. Isolated C10/C01 sufficiency or C11-only sufficiency is local to this history. None useful means this one-row bridge is insufficient, not that other histories/representations cannot help.

Stop after four full suffixes and independent saved-output reduction, or a bounded technical blocker. Recommend at most one next discriminator; do not execute it. No further rows/images, visual census, labels, model/layer/head/seed/dose sweeps, loss/checkpoint changes, KV/embedding edits or training.
