# History rereading at the selected bird decision

Unit: `2026-09-17-history-rereading-mechanism`; question: history-repetition-stopping. Authorized inference-only successor; candidate acceptance belongs to the cross-study lead.

## Contract and predecessor

From the accepted R16 anchor and original54-token bird309264 history, does substituting S paired historical K/V for the supplied row during layers14–20 change the first postrow x1 decision and subsequent useful enumeration, alone or with S current-token residual after layer13, under exact native replay/self/restore gates?

The [accepted predecessor](../2026-09-17-successful-row-mechanism/results.md) settles coherent x-pair route sensitivity, bidirectional layer20 mediation, unsuccessful S-to-F layer13 and isolated layer20 component patches, and exact token-only rescue after original-cache rebuild. It does not locate the mechanism origin. This successor tests one historical source interval rather than another output actuator. Useful continuation, retention and output debt remain the objective; token switching alone is insufficient.

## Fixed surfaces and intervention

Checkpoint, original paired embeddings, original image/prompt/processor, native heterogeneous bs4 order and padding, FP32 SDPA, RP1, greedy, EOS and total3084 action cap inherit the accepted S/F panels and source receipts. Do not substitute the new annotation-image root for model inputs.

Action67 predicts x1 (S coord131; F coord0), consuming action66 box_start. The target is batch index1. Cache storage index for generated action t is padded prompt width+t. Historical supplied row is action[54,63); later common wrapper action[63,66) and current token66 are distinct. Intervene only at action67 and zero-based layers14..20. K is already Qwen q/k-normalized and RoPE-rotated in the actual cache; V is the matching native value. Copy both from the same donor, same physical positions and image. Never transplant prompt/vision, earlier tokens, wrapper positions or companion entries. Layer13 residual replacement is current token only.

Before attention, retain recipient region and replace paired K/V; after that attention returns, restore recipient historical entries in the actual updated cache. The current position at that layer is never overwritten by this cache patch. Its native computation and downstream current K/V may change through the residual/attention intervention and persist. Capture and distinguish those changes. No parameter edits or attention/backend redesign.

## Finite panel and gates

Two 68-forward donor extractions S/F, verified against saved native prefixes, fork logits and head/layer13 states. Full native F and self F controls verify every target/companion token and selected states. Self copies F residual and F paired K/V then restores. Source-grounded CPU falsification must reject a one-position offset and a wrong donor-token source; real-entry trace binds input action66, positions, row and wrapper boundaries. R13-only repeats the accepted failed route under the new instrumentation.

Main factorial on F recipient: native (00), S residual only (10), S historical row K/V only (01), both (11). Reuse controls as cells. Main historical interval is not expanded after outcomes. Capture selected layer13/14/17/20 outputs, actual normalized head inputs, full fork logits, source/recipient/after/restore history entries and current-position cache slices; selected later offsets68/76/103 show consequences.

If either historical-cache main cell changes the raw winner from coord0 (not just a small margin), select joint11 first if effective, otherwise01, for exact emitted-token original-cache rebuild and unpatched rebuild control. This separates emitted-token effects from retained current-position state. One conditional specificity cell is permitted only at this effective gate: identical selected intervention with the already preselected failed F2 seed26 historical-row K/V instead of S K/V; retain S residual if selected11. F2 is bird[49,455,100,568], saved failed route, same row length and source positions. Its different displacement/norm is recorded; this is failure-source specificity, not a norm-matched null. A new68-forward F2 extraction is conditional. If main winner remains0 in both cells, skip these conditional branches and close the selected null/mixed contrast without a sweep.

The strongest alternative is construction of a donor-like or generically perturbed mixed state. Factorial separability, failed-donor specificity when admitted, and restoration/rebuild constrain this but cannot prove selective native use or unique source. A null leaves later common wrappers, other intervals and upstream information unresolved. No compulsory donor-restoration arm, F2 outcome search, head/layer scan, new images or sampling.

## Accounting and human annotations

Preserve all tokens, complete/invalid/malformed/literal-repeat rows, EOS/cap and original bank matches. Primary post-intervention owner accounting starts after completed row8 (action72), excluding the union of supplied/intervention-generated identities symmetrically; keep full, supplied, per-condition and exact-exemplar ledgers, empty retention denominators and identity HOLDs. Reuse verified physical anchors; annotation-unmatched is not physical FP. No broad visual review.

Separately locate verified exports under `/data/CoordExp/public_data/coco/rescale_32_1024_bbox/` for five user IDs. Freeze exact rows/images/coordinates/provenance/explicit ignore semantics; map to unchanged old model images. Missing ignore fields do not imply negative regions. Once admitted, rescore saved outputs using one immutable version and retain old/new crosswalk; inability to admit only HOLDs evaluation support, not mechanics.

## Budget and artifacts

Intended5 full batches (native/self/R13/KV/joint), at most3 conditional full batches (selected rebuild, native rebuild control, failed-donor specificity), plus2 or3 short extractions. Hard ceiling10 full batch attempts and256 additional forwards, totaling31096 forwards,4 allocated GPU-hours,2 elapsed execution hours,3GiB package tensors. Failed attempts consume the same ceiling; no expansion to rescue a null. Freeze sources/panels before model execution; no mutation of executed sources. Eight GPUs permit parallel independent admitted jobs only.

All raw/runtime/exit/source-position/restore/tensor/evaluation evidence lives under `/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-17-history-rereading-mechanism/`. Root owns coordination/ and global frontier; this worker owns the unit and candidate. No training, publication, Notion, project-memory, old-artifact rewrite or automatic successor. Stop after finite branches, independent saved-output checks and final candidate with no owned jobs. Cleanup only demonstrably unreferenced owned intermediates, recording paths/bytes.

## Literature boundary

Read [Temporal Causal Drive](../../literature/papers/2609.02000-temporal-causal-drive.md) and [Gaze Heads](../../literature/papers/2606.14703-gaze-heads.md) as limits: fixed-length conditional margins avoid a sequence-likelihood denominator slope; actuation is not native selection. No substantive external method is imported or reproduced, and no external result explains this local failure.
