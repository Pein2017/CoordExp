# Recurrence onset, coordinate role, and delayed free release

## Authorization and ownership

The user asked the root lead to continue mechanism probes and explicitly designated the new persistent worker `01a0ba19-8004-7400-b8af-98ce9b9df0cd` (9-19-worker). Root `01a0a3d5-dc45-7693-8467-4801aa7190df` owns research meaning, independent acceptance and successors. The new worker is idle in `/data/CoordExp/.worktrees/research-probes`, currently configured by the user as `gpt-5.6-sol` / `xhigh`; preserve that configuration and permissions. This user-designated task configuration takes precedence over the lead-worker skill's default Astra-low pairing. Do not change global settings. Old 916-worker `01a0a81a-9e32-7db1-bd07-86fa601f4276` has finished its package and has no execution ownership here.

Worker owns this unit's implementation, runtime, reducers and candidate records. Up to three Luna-series children, maximum `gpt-5.6-luna` / `max`, are authorized for bounded disjoint work; no grandchildren. Worker owns their shared interfaces and integration. Root keeps broad research routers and final acceptance. The user wants the lead to make decisions, not an autonomous worker research program.

## Background for a fresh worker

Our eventual objective is fewer missing physical objects in incomplete-label images under original-image, empty-prefix natural greedy. Current focus is explaining numerical/physical duplication bursts, not maximizing exact GT matches or fitting another tiny teacher bank. GT can be incomplete, imprecise or ambiguous; unmatched is not automatically hallucination or FP. A repeated numerical box is not proof of the same physical owner. Overlapping boxes can denote separate valid people. Keep these distinctions throughout.

The Qwen3-VL-2B model serializes objects autoregressively: object opener, description tokens, description end, bbox start, four coordinate tokens x1/y1/x2/y2, bbox end. Coordinates are 1,000 bins. Confirm actual tokenizer IDs at runtime; parser literals are not identity evidence. Mature model comparisons use two step-2444 packages: tied geo_sorted_xy and separately trained untied+axis. Their training differs beyond tying, so they do not isolate tying causally. Original readout, natural greedy and RP=1 are the controls. Keep exact media, native batch companions, padding, MRoPE positions, backend and checkpoint/special-token payload bindings.

Tiny-set SFT and more elaborate fitting already established finite-task capacity. Completion/ranking learning sometimes traded recovered matches for old-owner loss or repetition. Do not reopen training as a mechanism shortcut. Output-only coordinate-row norm equalization has materially reduced some invalid/repetitive trajectories, but benefits vary by model/image; some loops survive, some worsen. It proves a decision-control surface, not where SFT went wrong. Strong local row preference or greater whole-row likelihood does not guarantee useful greedy enumeration. Conversely, the selected physical donut onset sometimes genuinely prefers a repeated owner's full row over supplied uncovered-owner candidates, so do not universalize a search-error explanation.

Input initialization is actually `natural_adjacent`: shared projected linear/Fourier features, not independent random vectors per bin. Adjacent high-frequency phase can be about 0.805 radians. Trained E (input) and W (readout) must be separated, especially untied. Prior exact residual accounting localizes contributions to scores, not a unique causal layer or loop module. No all-layer attribution sweep is needed here.

The latest four-lane package is independently lead-accepted. Its stable entry is `/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-19-recurrence-distribution-census/integration/lead-acceptance.json`, with integrated `results.md` and `ARTIFACTS.md` beside it:

- A: 580 mature outputs plus 256 prospective outputs; fresh128 numerical near-repeat exposure 7/128 tied versus10/128 untied+axis, no caps. Categories extend beyond donuts. Mature enriched and prospective strata remain separate.
- B: corrected spatial panel has 12/21 failure and23/24 proxy admissions,10 local HOLDs. Moving scene/history shifts supplied-description coordinate scores, yet coherent movement still recurs9/12 and11/12 by sign. No physical recovery or uniquely fixed-endpoint cause. The original B implementation was technical-invalid and excluded.
- C:45 states,11,520 supplied-description full-softmax short draws,441 legal numerical-repeat events. Failure-state median q=.0273/.0625;1-q is NOT new-owner probability. Seven proxy unions are empty structural zeros. A small box-event probability is not proof of diffuse owner probability or sampling repair.
- D:16 states,32 +/-1 edits, ALL input edits y2, one immediate x1 winner switch and no later fixed-suffix switches. This cannot establish x1-input or free-trajectory continuity. Actual initializer and trained input/output geometry were checked independently.

A's original overwritten prelaunch/source JSON bytes and one D original shard receipt remain disclosed archival gaps. Reuse surviving source/runtime/panel bindings; do not invent recovered originals or let archival housekeeping become a mechanism experiment.

Read selectively: `research/index.md`, `research/CONVENTIONS.md`, latest four-lane accepted results, then these nearest counterexamples:

1. `research/experiments/2026-09-18-numerical-recurrence-feedback/results.md`: all four coordinate roles already tested at25 boundaries, fixed target rows+1/+2/+4 and276 immediate-free continuations. Small same-role effects also occurred at proxies; most free changes returned to native/other recurrence. A generic repeat of coordinate perturbation is not novel.
2. `research/experiments/2026-09-19-first-coordinate-mediation/results.md`: first x1=1 helped one untied donut route, not tied; other interventions worked while first x1 stayed0. One token can mediate a route without being universally necessary/sufficient.
3. `research/experiments/2026-09-18-owner-recurrence-onset/results.md` and `2026-09-18-native-coordinate-branch-completion/results.md`: physical donut diagnosis uses an earlier R16 package, not the current mature2444. Do not transplant its physical labels/token offsets to these routes. First possible and first confirmed physical recurrence differ.
4. `research/experiments/2026-09-17-successful-row-mechanism/results.md`: token-history and cache mediation were already tested on selected routes. Do not present another saved-route replay as a new discovery.

## One active question and incremental value

From the frozen mature natural traces, does the effect of a one-bin x1 or y2 history edit on the next free decision and numerical recurrence differ across seed/first-repeat/third-row phases, and does that effect persist when the next two complete native rows are supplied before free decoding resumes?

This is cross-image phase/role generalization plus a delayed-free contrast. It is not a new algorithm or a repeat-count-only experiment. Native row-stage measurements ask whether a detectable change precedes the first numerical repeat, appears only afterwards, or is ordinary context sensitivity also present at proxies. The delayed-free branch removes early divergent OUTPUT TOKENS but retains the edited old row and model-dependent hidden-state effects. It cannot by itself distinguish direct rereading from hidden-state relay, nor establish an owner ledger. Simple distance decay, different release phases, generic small-margin instability and synthetic-history effects remain alternatives.

## Frozen sources, phases and denominators

Use only the accepted original-policy sources bound by:

`/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-19-recurrence-distribution-census/shared-panel.json`

SHA256 `005bc6deff209bc96ce469e8cbc20d3dbd30a7e424a7ff18a0f7f8942a1bdb49`, plus sibling `shared-sources.json`. There are21 failure and24 proxy boundaries. Preserve split-aware image identity, source groups, model package, old/prospective stratum, all45 original IDs and their source snapshots. No new image selection or source natural rollout. Existing original tokens are the source of truth; new native/no-op releases only qualify the actual consumer route.

CPU preparation derives the EARLIEST chronological consecutive triple whose three same-description boxes are all pairwise within8 coordinate bins. Literal triples are a tagged subset, not prioritized over earlier near triples. The predecessor exact-first selector can pick a later episode: preserve a crosswalk to its original source_row, and report every earlier numerical match. Do not call this physical onset or assume the seed has never appeared before. Invalid boxes remain in a separately tagged stratum; adjacent-only chaining is not a pairwise near cluster.

For the21 existing failure sources, freeze phases after completed seed row r, after r+1, and after r+2, where r begins that earliest triple. These are entry/maintenance phases of the earliest qualifying burst, NOT necessarily the first numerical repetition anywhere in the image. Restrict any first-ever-repeat wording to sources with no earlier match; report the others separately. Missing phases or native bridge rows produce local HOLD/missing cells, never replacements. Native profiling covers row r-1 (if present), r,r+1,r+2,r+3 (if present), with opener/description/delimiter/coordinate/next-boundary positions explicitly identified. For the24 proxies profile the selected source row and its next row; they are local comparison contexts, not guaranteed healthy trajectories or exact matched controls. All earlier-repeat flags and row indices are retained.

For expensive counterfactual free branches select at most16 failure sources,8 per model. Within each model put explicitly named legacy images (train417044; val885,5586,7511,14038,632) first, then other sources in stable `(split,image_id,source_id)` order. Fill only from the existing21; do not require both models on a scene or replace an ineligible cell. Include at most4 proxy sources (2/model), preferring bird train309264 then the stable order above, and avoid duplicating an already selected source phase. Freeze source IDs, phases, token/row offsets, mutations and run matrix BEFORE intervention outcomes. Bird is a proxy unless its own frozen trace actually qualifies; precision annotation is not a failure admission gate.

## Contrasts and measurements

At each chosen completed-row phase, independent variants are native, x1-1,x1+1,y2-1,y2+1. Out-of-vocabulary directions are missing cells; no alternate bin search. Record geometry validity, sorting-stratum changes, native edited-token rank/logp and whether each edit changes those properties. Do not discard unfavorable or invalid outcomes posthoc. A one-bin change is not assumed physically equivalent.

Two release modes share each edit:

- Immediate: edited/native completed history -> original greedy free continuation.
- Native bridge: retain that exact edited/native row, supply the NEXT TWO complete unmodified native rows, then release original greedy. Include the unchanged control at this same later boundary. Never compare an immediate endpoint directly with a later endpoint as though they had equal history. No teacher row receives discovery credit.

Reuse the same source/native context through each boundary. Cache is rebuilt natively for changed tokens; no donor KV, stale-cache reuse, image transform, geometry mask, repetition penalty, normalization during generation or changed EOS. Free target horizon is16 complete generated rows or256 generated tokens or naturalEOS, whichever occurs first. Whole-row stopping counts bbox completions in the generated suffix, not description-end tokens or supplied bridge rows. Report token-cap fragments, invalid/malformed outputs and shortEOS explicitly.

The decision-owning endpoint is the paired first-free choice/margin at each immediate or delayed release, interpreted alongside the fixed-suffix phase profile. The16-row free outcome is secondary descriptive evidence, not a necessary physical-recovery gate. Record token distance from the changed input to every target; x1 and y2 changes also differ in position, so role differences alone do not identify embedding geometry.

Primary paired evidence per model/source/phase/role/direction/release mode:

1. Native row-stage full-vocabulary scores: observed-token probability/rank, all-competitor top1 margin, EOS/opener competition at legal row boundaries; at each coordinate, full coordinate family plus probability in the frozen same-description historical coordinate windows, endpoint mass and entropy. Do NOT multiply separate coordinate marginals into a coherent owner/box probability. Record actual forced context for every slot.
2. Native versus edited FIXED-suffix targets at both release boundaries: centered full-vocabulary score change, exact winner/top2, original winner's new margin, input-vector delta from actual trained E. In the immediate next row, also record the four coordinate slots under the same supplied native row prefix. For each role keep its own actual conditional context.
3. Free branches: first unforced argmax divergence (position/role/top competitors/margin), complete tokens/stops, near/exact repeated rows, all-pairs counts separate, longest run, valid/invalid/malformed rows and known matches against the same already-bound annotations where available. Retain correspondence to the original recurrence pattern and whether any old-context repeat is distinct from within-free-output repeats. Supplied/edited/bridged rows receive no recovery credit.
4. Compare edit-minus-native outcomes WITHIN each release boundary. Across-phase patterns are observational effect heterogeneity, not a controlled repetition-count effect. Compare recurrence at common exposed free-row horizons (e.g.8 and16 when reached), and report shorter endings separately; don't assign zero recurrence to unobserved suffixes or call earlyEOS a rescue.

Save enough for CPU reanalysis: exact source/config/model/prefix/action-position bindings; raw tokens and stage maps; full FP32 vocabulary vectors at registered native/fixed intervention slots and at first free forks (including each branch's own conditioning); final head inputs and coordinate E/W identities; score reductions and counters. Keep vector filenames indexed to consumed prefix length and next action offset. No full KV cache, all-position per-layer dump or new attention/layer hook campaign. Full free traces need selected logits/top competitors, not a full-vocabulary tensor for every generated token. No attention/logit-lens plot is a prerequisite to the primary experiment.

## Runtime, ownership and budget

Reuse `untied_shared.py`, `untied_natural.py`, `numerical_feedback/`, `coordinate_continuity/` and established native source planning. Inspect actual entrypoint/position semantics before changing code; use Codegraph if callable for symbol relationships and verify decisive behavior with source/checks. Do not introduce another scheduler or compatibility framework. New probe code belongs only in `probes/training_set_completion/recurrence_phase_decision/`; shared helper changes require parent-owned reconciliation and a demonstrated need. Old output roots and candidate snapshots are immutable.

Use all8 GPUs for disjoint scientific work after a minimal real-entry slice. Stress occupancy is expected: launch directly and react only to actual conflict/OOM. Avoid duplicate native work or deliberately failed launches merely to occupy devices. Native-source adapters and the common phase/row matrix have one writer: the persistent worker. Up to3 Luna workers may independently own bounded capture/release/reduction packages after shared semantics are frozen; shared runtime integration remains the parent responsibility. No nested reviewer chain.

Package hard ceilings:12 enclosing allocated GPU-hours including qualification/failed attempts;160,000 model-forward invocations;520 unique free-release cells (16 failures x3 phases plus4 proxies, x5 variants x2 release modes);32GiB new tensor/artifact payload. These are maxima, not utilization targets. Redundant controls may be reused only when exact source/prefix/release identities match. Size and forward/runtime estimates before broad launch; do not silently shrink primary scope, increase budget or add scientific arms. Return a local HOLD and the concrete tradeoff to root when necessary.

Qualify the changed execution seam with a real native/unchanged and one changed prefix per actual model/source family; preserve full score vectors BEFORE comparing, consumed token counts, attention masks and positions. `logits_to_keep=1` means the last consumed input position; do not reintroduce old source[-1]/target[0] six-position misalignment. Keep full-vocab tolerance2e-4, exact native winner and saved trace comparison at comparable positions. Differences too small relative to replay discrepancy are unresolved, not effects. At most16 gate forwards, plus ONE bounded CPU-demonstrated mechanical correction with at most16 further gate forwards; beyond that ask root rather than silently restarting. Any semantic/denominator/source conflict holds the affected branch. An ordinary implementation repair with unchanged meaning does not require serial lead approvals.

Before broad release, demonstrate the actual generated-suffix complete-row stopper, role-specific token edit, invalid-box retention, triple pairwise predicate and prefix-position readback using known counterexamples. Reuse existing tests; no broad test suite or additional reviewer is required. One saved-output independent parent reduction must reject a deliberate wrong-role/wrong-position or lost-invalid fixture. Preserve all attempts, exact launched source/selection bytes and receipts BEFORE execution so recent archival gaps do not recur.

## Interpretation, stop and return

What changes the next decision:

- Phase/role-selective low-margin changes BEFORE first-repeat output, versus changes only after repetition, would sharpen where a later intervention should target. Ordinary similar changes at proxies support generic sensitivity instead of a burst-specific cause.
- Immediate changes that disappear after the native bridge are consistent with dependence on early divergent tokens or decay, NOT proof of exclusive token mediation. Persistent later effects show the old edit still matters under a common token bridge, NOT direct-rereading versus relay localization.
- Changed first choices with persistent later recurrence distinguish local token perturbation from a durable numerical escape. No different-box or low-repeat result alone establishes useful new owners.

Stop at this matrix's completion, local evidence failure, exhausted cost, or a meaning-changing ambiguity. No additional temperature sweep, source expansion, force-to-known-owner rescue, layer patch, embedding rewrite, training, checkpoint-stage sweep, annotation mutation, Notion, memory write or successor. The worker stops after returning one stable integrated candidate, with CPU commands, immutable manifest, actual denominators/HOLDs, figures only if immediately useful, cost/failure ledger and ended children/jobs. Root alone marks lead-accepted.

Use concise English internal messages. Send only a first production-shaped milestone, a decision-changing conflict, and the final candidate. Direct-send final completion to lead task `01a0a3d5-dc45-7693-8467-4801aa7190df` through the existing same-host App Server route, saving a delivery receipt. No wake-me-up plugin. If lead active use supported steer; if idle use a single turn/start; if delivery is unknown inspect before retrying. Persist a stable candidate even if transport fails; do not stay alive solely to monitor. The final candidate is not acceptance.
