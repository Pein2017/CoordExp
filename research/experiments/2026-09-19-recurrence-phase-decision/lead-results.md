# Recurrence phase decision — lead acceptance

**Lead-accepted with one derived-summary correction.** [Independent receipt](/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-19-recurrence-phase-decision/lead-acceptance.json) owns acceptance. The immutable [worker result](results.md) and [candidate manifest](/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-19-recurrence-phase-decision/candidate-manifest-final.json) remain preserved; the [root-corrected summary](/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-19-recurrence-phase-decision/coordination/root-acceptance/corrected-summary.json) supersedes their first-ever-repeat subgroup count. No model output or frozen source changed.

## Accepted result and correction

The unit completed451 ready releases and45 native profiles/150 rows, with352 edited-versus-same-boundary-native comparisons. Keep25 bridge HOLDs and22 out-of-vocabulary edit directions separate. **6/16 selected failure sources**, not4/16, have r+1 as their first-ever numerical repeat: both tied/untied val885 begin their first qualifying triple at row0 and first repeat at row1. Root reparsed raw native tokens; frozen plan and crosswalk already record them correctly. The other four are tied/untied5586, tied417044 and tied-train169872. This corrects a derived subgroup, not the experiment matrix or primary scores.

The first unforced token changes in4/352 comparisons, all opener/EOS switches in one untied train196924 source. These are termination choices at a complete-row boundary, **not the count of coordinate or full-trajectory changes**. Root verified all four from full FP32 vocabulary vectors and actual emitted tokens; the smallest winning margin is0.005674, above observed replay discrepancies.

There are120/352 first free-trajectory divergences across15 source/model identities. Root's descriptive saved-output audit locates114 at coordinate slots (x1:25,y1:10,x2:47,y2:32) and6 at row boundaries. Thus coordinate sensitivity is widespread within this selected intervention set even when the opener remains stable. Among114 complete first-divergence row pairs,78 still have the same description and maximum coordinate difference at most8 bins. This numerical closeness does not prove physical identity. These are posthoc descriptive counts, not a new primary endpoint or population rate.

Of166 edits with both release modes, trajectory divergence is absent in both94 times, immediate-only24, bridge-only3 and both45. An edit can affect later free generation even when two native output rows are supplied in between. Direct old-row rereading, propagation through altered hidden states, different release phase and distance decay remain unresolved. The shared token bridge does not make the internal histories identical.

At jointly observed16-row failure horizons, longest near-run is lower/same/higher in11/102/6 immediate comparisons and8/98/5 bridge comparisons. Unobserved short-EOS horizons are not assigned zero repetition. Invalid geometry (85 complete boxes in78 cells) remains in the numerical ledger. Neither divergence nor reduced numerical recurrence establishes new-owner recovery.

## Scientific disposition

This completes a cross-image phase/role and delayed-free contrast. Small input edits can affect coordinates or stopping and can survive a native-token bridge, but the result supplies no consistent removal of recurrent behavior or general first-ever-onset signature. The phase labels are observational histories, not a controlled repeat-count manipulation. The four first-free opener switches all lie outside the corrected first-ever subgroup.

Close the small-perturbation branch; do not extend its dose, phases, seeds, temperatures or layer search solely to obtain an escape. The broader mechanism question remains open. A later proposal needs a contrast that separates genuinely different explanations of recurrence, not merely another coordinate/trajectory change. No training, checkpoint sweep, embedding edit, new data or successor is released by this acceptance.

## Verification and evidence boundary

Root verified21 candidate bindings, reran the complete CPU reducer byte-identically (SHA256 `a9dbb04fec27059ab5b9aa4d0e005cefc8222a7f24d766dbcae9a7f2050c6a88`), ran parser/invalid/position selfchecks, independently reconstructed the subgroup and fork-role counts, checked four full-vector switches, verified all20 model-job exits and observed the worker idle with no owned live model process. One bounded Luna semantic pass found no bridge/control blocker:213 ready bridges match the declared native rows and47 native bridge controls reproduce their native suffixes. Parent capture/free parity for451 cells has max full-vector difference4.84e-5 with zero winner mismatch; original-source trace qualification is only top2 except the explicitly tested full-vector families.

Artifact scope is narrower than a complete state archive: full vectors exist at registered fixed/native positions and initial release, while later free decisions retain selected/top-competitor evidence. Full vectors for every later trajectory fork and per-cell complete MRoPE arrays were not saved. These omissions limit future internal reanalysis; they do not invalidate the accepted paired outcome claims, and no GPU rerun is ordered to fill them.

Cost is54,206 model calls and2.476 receipt-summed device-hours across20 successful GPU jobs; this is not measured kernel utilization. Worker closure disk usage is2.96GiB. Root verification adds no model calls. The original worker summary, superseded reduction-01 and all operational evidence remain available.
