# History-source sign: worker candidate

Status: `candidate`; lead scientific acceptance pending. `protocol_compliance=violated` because one excess real-entry qualification was run. The lead ruled the saved scientific tensors admissible for review with that debt; this is not retrospective compliance.

## Decision-bearing result

The barrier qualified on the admitted tied image 14038 boundary, and both source cuts changed full-vocabulary competition. The signed changes do not provide a target-specific failure mechanism: cutting A reduced A-versus-N in both the failure and matched non-revisit control, while cutting C increased A-versus-N in both. Cutting C increased C-versus-N rather than suppressing C in both boundaries. First-fork margin changes also had the same signs across the matched pair. This supports a local source-dependent route effect under this cut, with competitor facilitation or broader spatial/context effects remaining viable. It does not identify an abstract coveredness inhibitor or a failure-specific causal effect.

The native failure first row is the frozen A revisit `[795,670,850,697]`; the native matched-control first row is the frozen N owner `annotation:-125`, `[788,670,850,697]`. Both cuts changed the first numerical row, but the cut-row owner identities have only geometric similarity and remain `HOLD`. Actual owner reselection is therefore unresolved. Across the six releases there are 54 complete valid rows, zero malformed rows, zero invalid geometries, zero EOS stops and six administrative nine-row caps. Only the two exact native first rows have independent A/N identity here; C and other-owner confirmed release counts are zero, and the other 52 owner labels remain `UNKNOWN/HOLD`, not physical negatives.

## Population and execution denominators

- Frozen source pool: 11 mature original-policy failure trajectories on six images.
- Eligible owner-grounded failure boundaries: 1; failure `HOLD`: 10.
- Admitted matched boundary pairs: 1; control admission `HOLD`: 0.
- Executed/analyzable failure trios: 1/1; executed/analyzable control trios: 1/1.
- Scientific cells: six complete cells = two trios on one tied image, not six trios or six images.
- One first cut-A release launch failed before a model forward and was repaired in a distinct immutable directory; it is not a second successful cell.

## Within-boundary effects

Every value is cut minus that boundary's own native baseline. Log masses are finite tested candidate-set masses. Failure values use the frozen sets. Control values are the lead-authorized `POSTHOC_CORRECTED_SENSITIVITY`: the exact mislabeled control `A-actual-greedy` row was removed symmetrically from all three control A sets, changing A support from 3 to 2 without adding or substituting a row. The frozen as-run control A mass and dependent effects are `INVALID` for owner interpretation and remain only in the audit artifact.

| Boundary | Cut | ΔA mass | ΔC mass | ΔN mass | Δ(A−N) | Δ(C−N) | Δ(A−C) | First-fork margin Δ |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| Failure | A | +0.144614 | +0.159474 | +0.251203 | -0.106588 | -0.091729 | -0.014860 | -0.513935 |
| Failure | C | -0.262789 | +0.277912 | -0.348547 | +0.085758 | +0.626459 | -0.540701 | +0.171654 |
| Matched control, corrected sensitivity | A | -0.333023 | +5.622210 | +0.019739 | -0.352762 | +5.602471 | -5.955233 | -0.496979 |
| Matched control, corrected sensitivity | C | +0.031437 | +3.221857 | -0.076283 | +0.107720 | +3.298140 | -3.190420 | +0.603758 |

The failure-versus-control comparison inherits the posthoc sensitivity label. The control's C source is a tv while its A/N rows are books, so the retained description mismatch also limits specificity.

## Technical qualification and falsification

The passing fourth directory used six forwards. Source-trace top-two maximum error is `6.67572021484375e-05`, maximum error/margin is `0.024447031431897557`, the causal-4D no-op full-vector error is zero, and all five legal same-length source-replacement checkpoints have zero full-vector error. The deliberately leaking `q>=end` mask changes the boundary vector by `0.30979442596435547` and is rejected; the required barrier starts target queries at `end-1` and blocks source-content keys `[start,end-1)`. All successor states are recomputed from the literal prefix without native successor KV reuse.

Qualification lifecycle: attempt 1 failed before model entry (0 forwards), attempts 2 and 3 failed after six forwards each, and attempt 4 passed after six forwards. Three real-entry qualifications were run against an allowed maximum of two, so `qualification_attempt_overrun=1` and the excess six forwards remain in cost. In attempt 3, candidate replay retained the final token while its compact logit tail was compared at the following position. Attempt 4 used the query immediately preceding each scored token: candidate replay uses `row_tokens[:-1]`, first-fork replay uses only the native prefix, and ordinary/no-op replay uses `native[:source_end-1]`. This is the autoregressive trace alignment correction; admission, tokens, image, positions, source boundary, barrier, candidate sets, tolerances and outcome selection were unchanged. Attempt 3 remains excluded.

The passing qualification producer SHA is `7068ea05e91b7bb3028fb3b7b66b790c5b97adab0b7660475e4b39fd5d20315e`. The first scientific cut-A launch used the same producer and failed at zero forwards because the instance method `cell()` collided with the `self.cell` dictionary. Producer `abfb1c681b89416a52e5007da21e5e713e9ce2606fccf60777eafe7f4dd7344c` adds the explicit repair argument/directory and renames the method to `_run_cell()`; model inputs, replay, masks, candidate scoring and qualification logic are unchanged. This execution-only delta is why the passing barrier qualification applies to all six completed cells. Exact source snapshots for these production hashes were not retained, so the candidate reports receipt-bound identities and the recovered exact patch without claiming archived source bytes. The current runtime has a later receipt-classification-only change and is not mislabeled as either production binary.

## Protocol debts and scope

1. Qualification overrun: one excess real-entry attempt, six forwards.
2. Frozen control candidate error: the preparer assumed every actual next row was A. In the non-revisit control, `A-actual-greedy` SHA `0739bb69023add13cbffe7cecf0656bf6980c9bf4f9c811c2e7d3e5398f2a442` is independently the N owner `annotation:-125`, not designated A `annotation:-141`. The versioned CPU correction excludes only that row in all three control cells. Original admission, result and tensor bytes remain unchanged.

This is one matched pair and one barrier definition. A null or nonspecific result is local to this cut and support and does not show absence of memory. Source rows also carry spatial, image and contextual information. Short continuations are conditional route consequences, not a separately identified maintenance or relapse mechanism.

## Stable evidence and replay

- Shared admission: `/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-21-history-source-sign/selection/shared-admission.json`
- As-run deterministic reduction: `/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-21-history-source-sign/reduction-integrated.json`
- Corrected control evaluation: `/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-21-history-source-sign/control-sensitivity-v1.json`
- Integrated candidate: `/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-21-history-source-sign/candidate-manifest-v3.json`

```sh
PYTHONPATH=. python probes/training_set_completion/history_source_sign/runtime.py --selfcheck
PYTHONPATH=. python probes/training_set_completion/history_source_sign/reduce.py --selfcheck
PYTHONPATH=. python probes/training_set_completion/history_source_sign/correct_control.py --selfcheck
```
