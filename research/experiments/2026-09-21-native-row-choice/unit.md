# Native row-choice witness

Status: authorized, worker execution in progress; lead acceptance required.

Owning dispatch: `../2026-09-21-history-source-sign/task-profile.md`. Shared source and candidate admission is frozen once at `/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-21-history-source-sign/selection/shared-admission.json`.

## Frozen question

At each admitted mature-model native target-first-revisit boundary, does any frozen credible unvisited-owner complete row have greater original-distribution probability than the actual greedy repeated row?

Three of the 11 frozen source trajectories are owner-grounded for this finite diagnostic: tied and untied image 885, plus tied image 14038. Eight identity-ambiguous trajectories are HOLD, not negative evidence. C is optional.

Every scored row includes the opener, description, four coordinate tokens and terminator under the original full vocabulary. The finite A/C/N construction and actual row are frozen before scores. A positive N witness is local; a negative result is unresolved beyond tested rows and neither result certifies global row MAP or owner mass.

## Evidence, bounds and stop

Qualification compares every actual-row token log probability with the accepted native trace and must detect dropped-entry or dropped-terminator accounting. The unit shares the history-source-sign package ceilings and stops with its frozen three states. Only the highest-scoring qualifying N row may receive one optional supplied-row continuation, fixed before observing that continuation, for at most eight subsequent native-greedy rows.
