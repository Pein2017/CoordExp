# Fixed readout policy: fresh128 paired inference and physical differences

## Question and predecessor

From frozen R16, does the accepted all-coordinate output-row norm equalization improve known-owner coverage on128 images not used by current train256/dev128 or the four policy-selection cases, without hiding incumbent losses, new loop/error debt or physical losses among UNKNOWN outputs? This is inference only, not training, a norm-origin claim or policy promotion.

The accepted four-image natural diagnostic recovered12/16 known owners on417044/477415 with clean EOS, did not rescue351017 and retained healthy7116. It established a selected-case output-policy effect, not general benefit. Readout/state, selective token edits and image/history evidence remain closed. This successor tests extrapolation and damage, collecting same-treated-state shadow decisions; broader coordinate reshaping rather than endpoint-specific correction is the strongest alternative.

## Population and intervention

Freeze128 uniformly selected eligible existing processed images using seed19, sorted unique image IDs before sampling. Exclude current train256/dev128 and351017/417044/477415/7116; record source pool and exclusion identities before GPU work. The source pool is the current train.coord.jsonl plus val.coord.jsonl union:122218 unique images,121834 eligible after384 exclusions; seeded sample has126 train and2 val images,919 known targets. This is a combined processed-pool sample, not a new official validation split. Freshness is relative to this research selection, not a claim about base pretraining. Keep processed rescale_32_1024 length12000 data, prompt, actual image geometry, native FP32/SDPA, empty prefixes, greedy RP1 and total cap3084. Cohort preparation manifest and final panel bind exact inputs/banks/groups. No annotations are changed. A bound runtime view rebases image paths and sorts the identical object records by x1 then y1 to satisfy the frozen geo_sorted_xy template; source coord views are y-ordered. The original exact128records remain unchanged; object multisets are verified equal.

O is original greedy. N applies the existing fixed FP64 median-coordinate-norm / effective-FP32-row-norm coefficients at EVERY step to ALL1000 coordinate logits of EVERY sample; cast to original logit dtype. Effective output rows include shared delta. Non-coordinate logits including EOS remain bitwise unchanged. Input/shared parameters remain untouched. Coordinate versus non-coordinate competition can change. Coefficients and R16/paired embeddings inherit the accepted four-image binding. No tuning, masks, dynamic switching, training or new model.

Fresh groups are32 frozen heterogeneous batches of4, same order/left padding/execution settings for O/N. Four old diagnostic groups remain separate and provide exact native replay admission and shadow extension, never enter fresh population estimates. Their previous accepted outputs/results remain immutable. One old paired batch admits the new capture path before fresh release; every repeated old output must match its saved respective policy exactly or the discrepancy is investigated technically.

## Measures and shadow evidence

Per image: known IoU50 class-agnostic matched owner identities, FN, gains G=N\O, losses L=O\N, retention and swaps; malformed/geometry-invalid rows, strict valid duplicates and literal invalid repeats, UNKNOWN, length/EOS/cap, endpoint occupancy by role and first policy fork. Report pooled counts, per-image paired distribution and image-bootstrap95% intervals (10000 resamples, seed19); no token-level independent-trial claim. Report O structurally healthy (EOS, zero invalid/malformed/strict repeats) versus unhealthy strata, legal-border owner retention (bank coordinates containing0/999), and new cap/repeat/invalid/malformed cases separately.

At every active N step INCLUDING observed EOS, record raw and scaled full-vocabulary argmax, top-two margins, offset and role on the SAME N history. Exclude padding after EOS. Save full before/after logits and actual final head-input hidden state at the first shadow disagreement per image only. No extra model calls. Exact rollback certificates require deterministic execution and argmax tie policy: no disagreements after t certifies identical remaining literal suffix from the N state at t; continued disagreement does not prove physical harm without intervention. Capped certificates extend only to the recorded horizon. Shadow actions are not automatically correct learning labels.

## Physical difference review (fixed32 image budget)

After paired reduction, draw32 fresh images using frozen seeded19 stratified random selection. Exclusive strata in order: ANY known loss; gain without loss; no known G/L but any error/stop burden change; remaining. Allocate up to8 uniformly per stratum, then round-robin remaining slots over strata with unselected members until32. Selection uses aggregate strata only, never appealing boxes; freeze IDs, inclusion probability n_h/N_h and weights before review. Each stratum is randomly sampled. Old four cases are not added to this review budget.

Render paired predictions under deterministic blinded A/B labels; examine both directions, whole-image context and needed crops. Review distinct physical owners, false instances, repeats, granularity and localization separately. High-IoU or same-label overlaps are candidate correspondences, not identity truth. Both policies' unmatched rows remain eligible for review. No scene census, owner-bank admission, GT editing or Co-DETR dependency. Repeated identical boxes may be visually grouped with multiplicity retained. Fixed review budget may end in uncertain physical net benefit. Record confirmed gained/lost owner correspondence and unresolved candidate groups; do not force a numeric physical total when identity/extent is unresolved. Weighted review summaries remain separate from known-reference counts and reflect unresolved limits and selection probabilities. Two bounded workers may review disjoint images; root checks decision-bearing evidence without recursive review.

## Execution budget, acceptance and stop

At most72 planned batch executions (64 fresh,8 separate diagnostic),3084 forwards each; hard250000 total including bounded mechanical repair,10 allocated GPU-hours and2 elapsed runtime hours. Use8 GPUs for independent fixed shards, load each model once. Raw outputs, active shadow traces, source/config/tensor hashes, exit/forward/GPU-cost receipts survive worker return. At most one bounded repair of an affected stage, no automatic scientific extension. Freeze producer/panel at launch. Independent saved-output reduction, exact old replay, non-coordinate identity and no parameter mutation are technical gates. Technical-invalid is unanswered, not a scientific null.

The decision is whether the fixed policy offers plausible extensible benefit worth further work: known coverage gain must be considered with every incumbent loss, normal/border damage, new catastrophic debt and bidirectional physical review. No arbitrary posthoc promotion threshold or masking of damaging strata. Near-zero/wide uncertainty closes as inconclusive. Stop after128 paired outputs, separate diagnostics,32-image review and candidate result; no training, sweep, follow-up donor/KV/embedding experiment or automatic successor. Lead independently accepts.

## User amendment: selective physical review

After inference completed and while review was underway, the user explicitly instructed: “Don't view all! Selectively view the images. Tell Fresh cohort to stop aggressively image review”. The exhaustive all-page/all-row implementation of the32-image budget is canceled. Both review workers were interrupted and asked to preserve only existing evidence and return without further viewing. Root will use at most a few decision-bearing spot checks, not finish the census. The preselected32-image manifest is preserved as provenance; an incompletely reviewed selective subset cannot support the planned weighted representative physical-FN estimate. Report actual reviewed coverage, directional witnesses and unresolved/unreviewed limits. Known-reference128 metrics and shadow evidence remain unchanged.
