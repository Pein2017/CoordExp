# Does changed history sustain the effect of coordinate normalization?

## Release, predecessor and decision

Unit: `2026-09-18-history-readout-crossover`. Root owns release and acceptance;
916-worker owns execution. Released under the user's September18 overnight
delegation to investigate duplication mechanisms with inference/forward probes.
The existing worker remains Astra-low. Children may only use Luna-max or lower;
at most two useful disjoint execution owners, no descendants or reviewer chain.

The [accepted component experiment](../2026-09-18-readout-common-component/results.md)
shows heterogeneous effects of full normalization, including a strong numerical
donut example and unchanged/worse examples. Neither analytic component generally
accounts for full normalization. Additive margin changes can cross a discrete
decision boundary without a special interaction module. The preceding numerical
history test found weak, nonselective local coordinate response, not a copying
mechanism specific to recurrence.

**From the same mature-model recurrence boundaries, does low recurrence persist
when original readout resumes after a normalized output history, and can full
normalization still change recurrence after an original output history?**

This is a targeted transfer and timing discriminator, not a new algorithm or a
repeat of an undirected pulse sweep. The [old R16 pulse result](../2026-09-17-repetition-history-mechanism/results.md)
already shows failed finite pulses and late normalization on its bird case.
The increment here is the complete two-history by two-readout contrast on the
current mature-model panel, using actual histories from both policies, including
the donut benefit and the adverse/null cases. No claim that timing is unstudied.

The strongest alternatives are continuing raw-readout preference, path-dependent
history effects, and termination/another numerical loop masquerading as relief.
If normalized history relapses under raw readout but remains less recurrent under
full readout, transient entry change is insufficient on that history. If it stays
less recurrent after withdrawal, persistence can be mediated by the altered
history there. The reverse switch tests intervention after established exposure.
Mixed results stay mixed. No outcome proves a universal loop circuit, training
origin, physical novelty, or an abstract coverage ledger.

## Fixed inputs and four cells

Bind the accepted `2026-09-18-readout-common-component` output root, acceptance
receipt, execution plan, selection, source releases and source snapshots. Reuse
its exact mature T/U packages, FP32/SDPA runtime, images, serializer, companions,
prompt/masks, coordinate operator and numerical metrics. T/U is not untie-only.

Use all11 `failure` boundaries, without selecting on normalization benefit. For
each, take the actual original and full-normalization target histories through
exactly4 and8 complete newly generated rows. These are two fixed measurement
points, not a dose search. Freeze all prefix token identities before new model
execution. Report their existing repetition/invalidity; do not assume every raw
history is already looping. Each prefix includes the original source history.

At each cut, rebuild the exact target history and continue under:

| Supplied history | Future original readout | Future full normalization |
|---|---|---|
| Original-policy history | Fresh unchanged-policy control | Late switch |
| Full-normalization history | Withdrawal | Fresh unchanged-policy control |

This is at most88 target continuations, including qualification cells. If an
actual source terminates before a cut, record unsupported, never remove EOS or
force continuation. Do not substitute a convenient nearby cut or new image.
No historical token editing beyond supplying those actual saved histories.
Full normalization is the previously frozen median effective-row-norm operator;
no coefficient fitting, token mask, RP change, sampling or shared/centered arms.

Each release gets the same additional32 complete rows /512 tokens /natural EOS
limit. This explicitly extends beyond some old saved suffixes; fresh controls
receive the same horizon. Target outputs only; companion outputs remain outside
the estimand. Preserve native target positions, masks and incremental history
semantics; correct target-history reconstruction is an admission requirement.

## Outcomes and identification limits

Primary evidence is the per-boundary/cut four-cell table of suffix longest
exact/near run, native-pattern return, alternate recurrence, invalid/malformed
rows, EOS and cap, using the accepted8-bin all-pairs near-row criterion. Keep
counts and stop reasons together: earlier EOS alone is not useful recovery.
Also report the first recurrence after the cut, including a run crossing it by
using up to two final supplied rows; mark supplied versus free rows explicitly.
Never credit supplied rows as new output or recovered owners.

The two decisive contrasts are withdrawal versus continued full normalization
on the SAME normalized history, and late switch versus original continuation
on the SAME original history. Across-history differences also change coverage,
geometry, token length, positions and hidden states; they are conditional path
comparisons, not a clean owner-memory effect or an additive mediation estimate.
Do not pool models/cuts as independent image observations or demand exact token
identity as a scientific success criterion. No annotation/visual review gate,
zero-FN requirement, fresh population evaluation or policy promotion.

Retain raw/full shadow coordinate scores and full-vocabulary winning competitors,
actual head input, EOS/family probability, emitted tokens and row/role positions
on each release. At the first raw/full split on a given prefix, compare the same
head state. Later divergent states must remain separately attributed. Save enough
to recompute numerical metrics and operator decisions; no all-layer KV archive.

## Qualification, bounds and stop

Reuse qualified code; implement only the history-cut/rebuild and two-policy
release delta. Snapshot changed producer bytes before execution. First run one
actual four-cell group, then scale if fresh unchanged-policy continuations match
their saved overlap, target history/positions/masks are correct, raw/full operator
application is reconstructed, and persistence/readback works. Exact replay is a
mechanical check, not physical success. Check all44 unchanged-policy overlaps
when supported, not just the pilot. Keep the existing precision tolerances.
One bounded mechanical correction per affected seam is permitted; preserve failed
attempts and completed cells, never relaunch successful work for uniformity.

Use all8 GPUs across independent model/case work and the established native
batch semantics; expected stress occupancy is not a launch gate. Ceiling:
8 allocated GPU-hours,100000 batch forwards,16GiB tensor artifacts including
qualification/failures. Reuse loaded models where practical. Stop with NEEDS_LEAD
if the measured finite panel cannot fit, runtime identity cannot be preserved,
or a correction would change scientific meaning. No replacement cases or sweep.

Output root:
`/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-18-history-readout-crossover`.
Worker owns this unit's state/results, that output root and a minimal probe under
`probes/training_set_completion/`; preserve predecessor producers and receipts.
Root owns catalog/index/questions, acceptance, and any subsequent experiment.
Return one integrated candidate with CPU reproducer, source/config/prefix hashes,
all outcomes/unsupported cases, costs and ended/live workers. Only the persistent
worker writes `integrated-terminal.json` and appends `INTEGRATED_CANDIDATE`,
`NEEDS_LEAD` or `BLOCKED` to `coordination/events.log`. Child completion is not a
package terminal. Stop after integration: no automatic successor, training,
checkpoint sweep, annotation edits, production changes, Notion or memory writes.
