# Spatial progress gate: accepted technical closeout

Lead-accepted on 2026-09-21 as a technical HOLD. H1 remains untested and
unidentified; this acceptance does not certify a successful model experiment.
The [independent package acceptance](/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-21-spatial-progress-gate/lead-acceptance.json)
owns verification, cost, debt, and closure. The [worker result](results.md) and
[frozen protocol](task-profile.md) remain unchanged.

The frozen six-image, 11-trajectory pool yielded one admitted failure and one
matched control, with ten failure HOLDs. Neither qualification campaign passed:
the first failed before model entry, and the single repair failed with
`KeyError('tokens')` after seven model and seven vision forwards. Both campaigns
are consumed. Zero of eight planned scientific cells are analyzable.

This is an execution failure, not a scientific null. The proposed computation—an
emitted coordinate advancing an enumeration frontier past an unvisited object—has
received no discriminating test here. Lane B's narrower negative result does not
supply positive evidence for H1. The source-boundary lookup and source-trace schema
failures are retained implementation defects; no repair or new qualification is
released by this closeout.

Root independently reproduced reduction-v2 byte-for-byte and verified both
qualification receipts and all ten retained source copies. The superseded v1
clock-based reduction remains preserved. Wall duration is reconstructed from
retained filesystem timestamps, not instrumented execution event times.

Both lanes are closed. No further model call, release, training, or successor is
authorized. Revisiting H1 requires a fresh explicit contract and authorization.
