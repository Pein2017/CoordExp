# Does the first x1 choice mediate the donut readout effect?

## Authority and predecessor

Unit: `2026-09-19-first-coordinate-mediation`. Root releases/accepts one bounded
inference-only continuation under the user's overnight mechanism-investigation
grant; existing916-worker executes as Astra-low. At most two useful Luna-max-or-
lower children, no descendants. Stop after the integrated candidate.

The [direction control](../2026-09-19-readout-direction-control/results.md) shows
that full normalization is not uniquely effective: other fixed directions also
reduce numerical recurrence. That does not rule out shared correction of a few
important logits. Root's POST-HOC inspection found that sign20/22/23/24, under
both mature models on417044, change the first free x1 from0 to1 and have near-run1;
the other four sign vectors retain initial0 and longer runs. The corresponding
association in other scenes is not universal. This outcome-selected mechanistic
follow-up is not a prevalence or held-out efficacy test.

The [earlier selected-row mechanism study](../2026-09-17-successful-row-mechanism/results.md)
already established token-mediated release on another checkpoint/history. This
unit adds a reciprocal choice-by-continuing-readout intervention on the current
donut association, including ALL eight previously frozen sign fields and original
readout. It does not claim single-token intervention is a new concept.

**At the two mature-model417044 failure prefixes, does forcing the first x1 to0
versus1 change subsequent recurrence across original and all eight fixed sign
readouts, separating an early-token mediator from continuing readout effects?**

If original readout plus1 sustains reduced recurrence, this choice is sufficient
for that conditional route. If a helpful sign readout forced to0 relapses, the
early choice is necessary for that particular intervention at that point. If it
still escapes later, the readout has another route; if original+1 relapses while
sign+1 does not, ongoing readout remains consequential. These statements concern
the finite conditional numerical outcome, not physical coverage or a universal
mechanism. Coordinate0 may legitimately describe clipping; it is not forbidden.

## Frozen factorial

Reuse the exact tied and untied-plus-axis417044 ORIGINAL completed-row failure
prefixes, models, images, batch companions, prompt/masks/positions, FP32/SDPA,
serializer and sign vectors from the accepted direction-control package. No new
seed, field, model, image or readout coefficient. No tying-only claim.

Two forced choices {coord0, coord1} crossed with nine readout policies
{original, sign19, sign20, sign21, sign22, sign23, sign24, sign25, sign26} under
both models gives36 fresh target continuations, including qualification and18
concordant controls. Full/reflected are excluded: their effects are already
recorded, and they are not needed for this single-mediator factorial.

All18 saved policy paths share the first five free tokens
[151646,15007,332,151647,151648]. At target offset5, the parser identifies the
first x1. Apply the chosen readout normally, confirm those five emitted tokens
and the x1 role, then override ONLY the chosen emitted token at offset5 with
151670 or151671. Feed that exact token into the next ordinary forward. Preserve
the unmodified score winner as evidence. No cache transplant, input-vector edit,
subsequent forcing, RP change, sampling, EOS suppression or further logit mask.

If the prefix/role does not match, return NEEDS_LEAD; do not move the intervention
to a more convenient location. Every other token is naturally greedy under its
declared fixed readout. Limit each whole target continuation to32 complete rows,
512 target tokens or natural EOS, exactly as the predecessor; the controlled
token is included in that horizon. No forced completion after EOS/malformed text.

## Outcomes, controls and interpretation

Primary recurrence outcomes use ONLY fully free rows after the first completed
row. Report first-row geometry/validity separately because one coordinate was
controlled. Also retain the whole-continuation metrics for predecessor replay,
explicitly labeled as including the intervened row. Supplied or controlled rows
receive no owner-recovery credit. Use the existing numerical exact/near-run,
native/alternate return, invalidity, malformed, EOS, length and cap accounting;
near rows use the description-matched all-pairs8-bin criterion.

Report each policy's0-versus1 contrast under each model, with all outcomes and
termination differences, not a weighted success score. The four association-
positive fields and four others are all retained; no seed selection. Keep the
raw choice and actual forced choice, full target tokens, final head input and
raw/applied coordinate logits before and after the intervention and throughout
the bounded continuation. Same-choice different-readout and same-readout
different-choice contrasts answer different questions.

The eighteen concordant controls force the token that the source policy already
selected: original and sign19/21/25/26 use0, sign20/22/23/24 use1. They must match
their complete accepted saved continuations. The opposite-choice cells are
counterfactuals; do not require them to match any successful reference. Prove
that only offset5 bypasses argmax and every later emitted token uses the declared
operator. Same token before the fork must retain the same native conditioning;
input/output embedding weights and cache execution remain unchanged.

## Execution and stop

Reuse the qualified direction runtime and parser; implement only one explicit
token override with metadata and suffix accounting. Snapshot producer bytes.
Qualify a four-cell untied group {original,sign20} by {0,1}, counted within36,
covering both a concordant and a nonmax override per policy. Verify native
controls, exact injection/consumption, masks/positions, all other greedy decisions,
finite counters and durable readback before scaleout. One CPU corruption check
must detect a wrong forced position/token. Preserve the previous tolerances.

Use all8 GPUs across independent cells with existing batch semantics. Ceiling:
4 allocated GPU-hours,50000 batch forwards,8GiB tensors including qualification
and failed attempts. One bounded mechanical correction per affected seam is
allowed; preserve failed evidence and reuse completed cells. Return NEEDS_LEAD
for a semantic conflict or measured budget overrun. No additional bins, rows,
seeds, images, parameter search, training or checkpoint sweeps.

Output root:
`/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-19-first-coordinate-mediation`.
Worker owns this unit state/results, output root and minimal probe delta. Root
owns broad routers, interpretation and acceptance. Integrate all36 cells, CPU
reproducer, hashes, costs and job closure; only the persistent worker writes
`integrated-terminal.json` and appends `INTEGRATED_CANDIDATE`, `NEEDS_LEAD` or
`BLOCKED` to `coordination/events.log`. Then stop. No automatic successor,
annotation/visual-review expansion, production changes, Notion or memory writes.
