# Where does the recurrent coordinate margin arise in the forward pass?

## Decision and predecessor

Root releases one bounded forward-only mechanism unit under the user's current
request to continue and overnight research delegation. Existing916-worker remains
Astra-low; children may be Luna-max or below, at most two useful disjoint owners,
no descendants. Root owns interpretation and acceptance.

The [accepted score-support ablation](../2026-09-19-coordinate-pair-readout/results.md)
shows sparse0/1 score changes can remove numerical recurrence on selected donut
routes. Root's saved-state census finds only two changed argmax decisions on tied
pair01 (first two x1:0→1 and0→30), and only one on untied pair01/only0. At tied's
second gate, unit-direction scoring prefers30 over0 while the larger0 output-row
norm reverses their raw ordering. Full norm scoring instead selects23, so this
selected-pair accounting is not a full-winner claim. At the first0-versus1 gate,
directional preference still favors0 after equal norms. Untied5586 needs seven
changed decisions across x1/y1 and remains outside this donor microscope.

**Question:** In these exact recurrent and low-recurrence donor states, how do
final output-row norms and the accumulated attention/MLP residual updates produce
the observed coordinate margins? Does the saved path distinction concentrate in
a particular forward component, or remain distributed after exact accounting?

This localizes the sources of a score, not a faulty circuit. It is not a new
repair test, layer-patching search, training-origin diagnosis, owner-ledger test,
population estimate or physical-coverage claim. The strongest alternative is
ordinary history-dependent distributed computation plus a static readout length
advantage, without a special repetition module.

## Fixed eight states

The root freezes exact releases, tensor/readout hashes and prefixes in
`coordination/selected-states.json` under the output root. All are x1 predictions
on image417044, at identical per-model native position when comparing offset15:

| Model | Source policy | Free-token offset | Role in comparison |
|---|---|---:|---|
| tied | original | 5 | first decision |
| tied | original | 15 | next decision on native recurrent history |
| tied | pair01 | 15 | next decision after changed first row |
| tied | pair01 | 25 | subsequent unmodified-argmax progression |
| untied | original | 5 | first decision |
| untied | original | 15 | native recurrent-history next decision |
| untied | pair01 | 15 | history after first0→1 route |
| untied | only0 | 15 | history after first0→13 route |

Histories are actual source prefixes. Their other first-row coordinates can differ;
comparisons do not isolate the initial x1 token. Do not mix later states from
incompatible trajectories or call them healthy physical-owner controls. T/U is a
mature package contrast, not an untie-only comparison.

## Minimal execution and accounting

No new free rollout. Replay the eight native prefixes under the exact established
batch, companions, masks, positions, model, image and FP32/SDPA execution. Source
continuation tokens are supplied solely to reach the saved states. Reuse existing
replay/instrumentation and consult qwen3-vl-execution for current module seams.
Do not switch attention backends for convenience. At each state compare an
uninstrumented replay with an observational-hook replay: maximum16 prefix replays,
qualification included. Replay need only reach the selected offset.

Capture only the target position: input residual, each decoder block's actual
attention residual contribution, MLP residual contribution, any additional
residual contribution actually present, pre-final-normalization residual,
normalizer parameters/epsilon/scale, final head input and effective output rows.
Hooks must observe without mutating outputs or cache. Keep selected token logits,
full-vocabulary top competitors, and all1000 coordinate scores. No complete KV,
all-position hidden-state archive, attention-map sweep or gradients.

First establish algebraic closure from actual runtime tensors. For an ordinary
final RMSNorm and linear effective output head, the actual final-state scale and
gain give a fixed linear direction for a selected logit margin. Project the input
residual and each actual residual update onto that direction; their sum must
reconstruct the final raw logit margin. Account explicitly for any actual bias,
extra residual branch or effective adapter term. Do not assume a seam merely from
its module name. If this exact factorization is unsupported, return NEEDS_LEAD;
never silently replace it with an approximate logit lens.

For every state retain pair comparisons0 against the frozen competitors from
selected-states.json, plus complete-coordinate rankings. Decompose the final raw
pair margin symmetrically into row-norm and unit-direction terms using the same
saved representation; report the centering/convention, not causal percentages.
Also project onto the common-row-norm comparison direction. The final RMS scale
used for a state's contributions stays that state's actual scale. Intermediate
cumulative projections are accounting views, not early-exit predictions.

Main deliverable: an eight-state table showing raw winners/margins, equal-norm
rankings, row norms, input contribution, attention and MLP contribution per layer,
reconstruction residual and the donor-history difference. Preserve full signed
contributions rather than selecting only large favorable entries. Report whether
the direction/length split varies across the first and second gates. Large
contributions and attention involvement are candidates, not demonstrated causal
necessity. Do not patch a selected layer in this unit.

## Qualification, acceptance and stop

Qualify the tied pair01 offset15 state first, counted in eight. Instrumented and
uninstrumented same-entry scores and winners must be identical. Reproduced source
head/logit agreement must use the established FP32 absolute ceiling2e-4 and exact
raw winner; report actual maximum errors and fail closed if exceeded. Preserve
native batch/target identity and unpadded prefixes. All eight must meet these
checks before interpretation. Source snapshots precede model execution.

Use FP64 CPU accounting. Residual and projected-score reconstruction tolerance
is2e-4 absolute, with measured numerical residuals retained. If normal floating
point accumulation exceeds this frozen tolerance, return evidence to root rather
than relaxing it. A CPU altered-contribution check must invalidate reconstruction.
This is an observation-only forward seam; no masking, training or new generation
policy is introduced. One bounded mechanical repair per affected seam is allowed,
with failures preserved and completed valid states reused.

Use eight GPUs for eight independent states after qualification; reuse the pilot
state and avoid duplicating it merely for occupancy. Ceiling2 allocated GPU-hours,
10000 batch forwards and4GiB tensors including qualification/failed attempts. Stop
at all eight states and the integrated candidate; no new state, image, checkpoint,
coefficient, layer patch, training, annotation or visual-review work.

## Ownership and direct return

Output root:
`/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-19-coordinate-margin-provenance`.
Worker owns this unit state/results, its output root and minimal task-local probe.
Root owns broad routers, scientific decisions and acceptance. Preserve prior units.

Return one integrated candidate with CPU reconstruction, source hashes, actual
costs and job/child closure. The user explicitly disabled wake-me-up for this round.
Do not register a monitor. After integration send a compact DIRECT completion
message to root thread01a0a3d5-dc45-7693-8467-4801aa7190df through the available
same-host App Server route, with result/terminal paths and technical/scientific
status. Save a direct-delivery receipt. If root is idle use turn/start; if active
use supported turn/steer; resume only if not loaded, with no settings overrides.
Read current status first. A send timeout is unknown delivery: reconcile before
retry, never duplicate blindly. Use the known Unix-socket transport from
lead-worker's worker_turn.py as reference; its lead-to-worker tier-checked CLI
must not be invoked with swapped identities. Direct transport failure does not
invalidate the science: preserve the candidate and report the exact delivery gap.
Stop after this package, with no automatic successor, Notion or memory writes.
