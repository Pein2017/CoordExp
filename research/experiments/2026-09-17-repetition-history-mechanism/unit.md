# Repetition history and withdrawal mechanism

## Active contract
From the frozen R16 DoRA adapter plus original paired coordinate embeddings, do fixed-length earlier-history replacements and bounded output-norm pulses change conditional recurrence and useful post-withdrawal continuation under original native greedy, physical-identity and retention accounting?

## Predecessor and distinction
Fresh128 established structural improvements under continuous norm equalization, not population physical recall or promotion. Row-branch showed one supplied bird route releasing credible free owners and one clock route immediately stopping; novelty and spatial advancement remain confounded. Displacement follow-up closed at admission-HOLD. Sep11 image/history crossing showed selected history maintenance and model-dependent continuation; random-order training also repeats, so sorted order is not necessary. Recent training altered language DoRA, with coordinate input/output rows frozen. Distinct visual response regimes do not establish distinct circuits.

This phase measures relative earlier exposure/placement with final rows fixed, then entry/persistence/return under finite pulses. A-for-B replacement removes B while adding A; probabilities do not isolate facilitation from removed inhibition. Supplied routes and sampling are not natural deployment or training evidence.

## Finite staged budget (before execution)
A: at most3 existing reviewed cases, four histories H_base || X || Y || A || B with X,Y in {A,B}, and native-prefix reference; same-class/equal-token-length A/B. At most one already-supported alternate A expression on one informative case. At most24 full batch executions and2048 additional forward calls for candidate scores/paired mechanics.
B: at most3 saved recurrence/rescue cases; original/full-norm controls reused or checked at native entry; pre-entry one-row pulse, established one-row pulse, established four-row pulse, late sustained norm. Replay each pulse's exact tokens under original readout then release. At most32 batch executions and2048 further score/repair forwards.
Reserve:8 batch executions for concrete mechanical repairs only. Total<=64 native bs4 greedy executions at cap3084 plus4096 score forwards =201472 calls; <=12 allocated GPU-hours, <=6 elapsed execution hours. Stop each rollout at EOS or original total cap, no dose extensions.
C optional sampling is authorized but not launched or assigned budget in this initial freeze. It requires a finite stage supplement before outputs (temperatures0.1/0.3/0.7, fixed seeds and repeats); omit if not decision-changing. No adaptive best-of.

Exact cases/anchors/prefixes/stage conditions and model/config/source hashes are frozen in panel.json and stage-specific immutable manifests before GPU work. A failed anchor holds that contrast only. First production-shaped paired measurement precedes expansion. Visual ablations are optional, not an admission gate; none scheduled initially.

## Runtime and credit
Reuse native original heterogeneous bs4 groups, left padding, image preprocessing/grid and MRoPE, FP32/SDPA, RP1, cap3084. Recompute multimodal execution after changed prefixes; never carry stale image KV. No parameter updates, labels, architecture, norm coefficient changes, new dataset or generic scheduler. Normalization uses existing effective base+delta readout factors, all1000 coordinates only; EOS remains unrestricted.

Separate complete-row likelihood, earliest common-prefix competing-token margins, A/B/C/EOS competition and free decisions. Count supplied/pulse owner union symmetrically out of autonomous post-release gains; preserve credible native-future owners. Known IoU matching is not physical identity; unmatched is UNKNOWN. Review only decision-bearing anchors/changed owner clusters. Report return, different anomaly, EOS and useful coverage separately.

## Evidence and closure
Retain complete tokens/text/row boundaries/stops, exact prefix/model/data/bank/runtime identities, costs, winner changes, sparse full-vocabulary logits and final normalized head inputs at declared seams; targeted coordinate/competitor scores along windows. No full KV/attention archive promised. Independent saved-output reduction and knowledge check precede candidate closeout. Cross-study lead owns final acceptance/global synthesis and coordination/; this task owns unit/implementation/measurement artifacts. No Notion/project-memory changes. No automatic successor.
