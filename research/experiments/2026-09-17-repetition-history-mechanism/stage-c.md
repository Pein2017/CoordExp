# Frozen optional sampling pulse (before sampling outputs)

## Decision and trigger
Deterministic norm timing on bird309264 changed1–3 decisions per finite pulse yet recovered zero known owners and capped; full-time norm recovered3 known owners and ended. Test whether a brief stochastic route change at the SAME established natural prefix can produce useful continuation after withdrawal. This estimates selected-prefix sensitivity under these samplers, not a population rate, identity mechanism or deployment policy.

## Fixed panel
Original R16/paired embeddings/image/prompt and native heterogeneous bs4 group fresh-18; original first54 action tokens (six complete rows), then target-only categorical sampling at temperature0.1,0.3,0.7, each with seeds19,20,21,22,23,24,25,26.24 trajectories total; greedy comparator reuses hash-verified native trajectory. Same original total3084 cap and native companions. RP1, no top-k/top-p truncation (k0,p1), no dedup constraints, no norm transform.

Sample until one complete serialized row (invalid geometry still counts), EOS, or32 sampled tokens, whichever first. A stray closer is not a complete row; preserve malformed draws. Then original unrestricted greedy resumes; never continue after EOS. The32-token bound is an artifact/runtime ceiling, not an adaptive dose. No longer pulses or additional seeds/temperatures follow.

Implement target-only `torch.multinomial(softmax(raw_logits.float()/T),1)` with a dedicated seeded CUDA generator at the native logits-processor seam, selecting that draw via target-only logit forcing. The HF runner remains the original greedy/cache path; the external categorical pulse and every actual transform are explicit. Positive temperature scaling does not change argmax order apart from numerical ties; stochastic selection changes choices. Identical seed labels across temperatures are paired RNG initializations, not identical draws or independent image trials.

## Evidence and accounting
Save every draw, temperature, seed, RNG before/after state, raw/scaled winner, selected-token probability, full raw logits/final head inputs during <=32 sampled steps, actual tokens/text/stops, original companion identity and withdrawal boundary. No KV/attention sweep. Supplied and sampled owner identities receive no autonomous post-release gain credit. Use the union across all24 pulse owner IDs symmetrically for primary known-owner accounting; retain per-trajectory pulse/full/post-release sets separately. Physical candidates remain UNKNOWN/HOLD unless bounded evidence supports them. Review only decision-bearing new/retention conflicts.

Report each trajectory, recurrence/other anomaly/EOS/useful-owner recovery and retention/debt jointly, then descriptive rates per temperature with Wilson95% intervals over8 seeds. Do not select best-of or score the union as single-trajectory quality. Stopping without free coverage is not recovery.

## Cost and stop
24 native batch executions, each<=3084 forwards (<=74016), no additional GPU qualification arm: first admitted sampled cell is a counted cell. Retain failures. CPU seam checks before launch and independent saved-output checks after. Reallocate unused earlier-stage capacity: fixed A/B execution incl alternate-expression and replay controls totals23 batches, plus C24 =47, leaving17 under the original64 ceiling; total call/GPU/time ceilings in unit.md remain unchanged. No training, parameter edits, new images, visual census, adaptive search, or automatic successor.

Credit boundary clarification before launch: a row started during the sampled pulse remains supplied for credit even if the32-token ceiling causes its completion after withdrawal. A fully autonomous row must begin after withdrawal; any material ambiguous crossing fragment stays HOLD.

Reported rate definitions: cap/EOS are native stop outcomes; exact-A return means a post-release literal row equals the accepted A template; alternate-template recurrence means a post-release exact-row run reaches at least3 (a token/box proxy, not physical identity); known-gain candidate means at least one post-release known identity beyond the prefix and symmetric sampled-owner union. Invalid geometry and malformed output are separate event rates. These overlapping descriptors do not form exclusive categories and none alone proves physical recovery. Wilson intervals describe seed variation at this fixed image/prefix only.
