# Wrapper-history factorial

Unit `2026-09-17-wrapper-history-factorial`; question history-repetition-stopping. One root-authorized inference-only package. Root owns scientific acceptance and any successor; this worker owns execution and candidate integration.

## Contract and settled boundary

From the accepted R16/image309264/F history, does replacing contextual K/V of the identical later wrapper change action67 and useful unrestricted continuation, conditional on the existing current-state/supplied-row factors, under exact native/self/restoration and versioned-positive scoring gates?

The [accepted historical-row result](../2026-09-17-history-rereading-mechanism/results.md) establishes margin movement without recovery and a retained-current-cache late effect removed by original-cache rebuild. The [earlier successful-row result](../2026-09-17-successful-row-mechanism/results.md) establishes emitted-choice sufficiency for a different rescue. Neither explains the full origin of repetition. This successor completes the same factorial with a measured but unpatched contextual source; it does not search more layers or populations.

## Fixed factors and runtime

F recipient: original54-token natural bird prehistory plus observed failed row F, followed by common wrapper. Same R16/base/original paired embeddings, image/prompt/native heterogeneous bs4 order/left padding/MRoPE, FP32 SDPA, original greedy RP1/EOS and total3084 action cap. All immutable bindings inherit the accepted panels. No annotation-image substitution.

At action67 (x1 prediction), current input is action66 box_start. Factors are R: S residual output after zero-based layer13; H: S paired post-RoPE K/native V for generated actions54..62; W: S paired K/V for actions63..65 (object_ref_start, bird, object_ref_end). W never includes current action66. H and W substitutions occur only at this decision in zero-based layers14..20 and restore every historical entry after each attention call. Native current-position K/V may change and persist. No key-only, value-only or attention-weight patch.

Reuse W-off cells native-F/R-only/H-only/R+H with exact accepted hashes. Execute W, R+W, H+W and R+H+W. New full native-F and self-F (R/H/W all copied from F) qualify the derivative execution and all target/companion sequences; source-grounded checks reject shifted wrapper bounds, wrong role/source and accidental inclusion of66. Reuse accepted S/F donor captures, whose source/position and input identities are exact. A minimal derivative preserves previous producer bytes and retains an explicit diff; no generic orchestration layer.

R+H+W is an expected donor-restoration mechanics control: layer20 state must reproduce native S; final head/logits must reproduce the accepted R20 S-to-F intervention, not necessarily native S. Bound discrepancies by exact native/self controls and relevant margin. A failure is technical/unresolved until source coverage/positions are checked, never a scientific null; no layer search. Full suffix is measured independently because additional current K/V14..20 can differ from R20-only.

## Outcomes and finite conditional work

Report all eight factorial margins (coord131 minus coord0), ranks, raw winner/top2 margin and EOS separately, actual layer13/14/17/20/head states, retained current K/V, full tokens, recurrence, malformed/invalid/strict and literal repeats, UNKNOWN and EOS/cap. Vector distances are descriptive, not causal percentages.

Use the frozen preceding annotation-snapshot-v1 working export and accepted class-agnostic one-to-one IoU.5 scorer. Bird denominator14; all five export versions retained, no new labels or ignore masks. Primary starts after the complete intervention-generated row (normally action72). Exclude the union of supplied/intervention-generated current positive IDs symmetrically across all eight primary factorial cells; preserve full/prefix/supplied/free, per-condition identities and empty retention denominators. Old bank10 scores remain separate. Unmatched/removed annotations are UNKNOWN, not physical negatives; do not inherit seven-owner credit. No broad review gate.

Before outputs, specificity priority is W, then R+W, then H+W. A non-restoration condition qualifies only if its immediate raw winner changes relative to its paired W-off cell, primary current-positive free coverage increases by at least one with no lost primary baseline ID, and invalid/malformed/strict-valid-repeat/literal-repeat counts do not exceed the paired control. UNKNOWN is reported, not treated as FP. One qualified condition receives exactly one failed-F2 wrapper donor comparison: change only W donor to preselected seed26 F2, keeping selected R/H settings and F recipient. F2 source row is [49,455,100,568]; saved wrapper text already matches. At most68 new native capture forwards qualify its actual positions and tensors. Record displacement/norm mismatch; this is not a norm-matched null. Failed admission or null does not permit another donor.

For changed immediate prefixes, compare against an existing exact original-cache rebuild only when model/input/prompt/forced-token/runtime identities bind exactly. Otherwise permit at most three additional full rebuild executions total (within the package cap), deduplicated by exact first68 target tokens. Priority: selected informative non-restoration condition; all-source restoration if distinct; other improved primary route. All full suffix equivalence is checked from saved raw tokens, not inferred from logits. If only an unchanged immediate token has a decision-bearing later drift, at most one same-token rebuild may be used/reused. Original-prefix controls can be reused once exact native parity is established; do not rerun identical rebuilds. A specificity route shares a rebuild if its first68 tokens are identical, otherwise it fits the same three-execution ceiling or remains explicitly untested for that secondary cache separation.

If only all-source restoration succeeds, or partial effects are non-winning/unhelpful, close as mixed-state dependence. No automatic token-group/layer drilling. Strongest alternative throughout is generic donor compatibility rather than selective native source use or an owner ledger.

## Budget, artifacts and stop

Primary: six full batches (two controls, four W-on); optional failed-W donor one full plus68 short forwards; up to three deduplicated additional rebuilds. Hard ceiling10 full attempts plus256 short forwards,31096 total forwards,4 allocated GPU-hours,2 execution hours,3GiB tensors. Failed attempts count and reduce remaining capacity. Use independent GPUs without changing batch conditions; idle devices do not create arms.

Retain raw outputs, source/cache-position/restoration tensors and receipts, selected downstream states/logits, original/config/model/readout/input/annotation identities, scorer replay, producer snapshots/diff, exact commands/exits/cost and terminal closure under `/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-17-wrapper-history-factorial/`. Root alone owns coordination/ and global frontier. No training, new temperature/RP, architecture, publication, Notion, project-memory, old-source mutation, platform changes or broad image review. Cleanup only demonstrably disposable own intermediates with path/byte accounting; none is planned.

Image417044 (donut) is the first named future cross-case confirmation candidate per user steering, not part of this experiment. Bird was chosen for stable S/F causal replay, not representativeness. Final result must state whether evidence yields a precise cross-case prediction; root decides any test. Return a stable candidate after finite admitted branches and independent saved-output reconstruction, then stop.
