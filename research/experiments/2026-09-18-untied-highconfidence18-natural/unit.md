# Mature tied versus untied-plus-axis: natural recurrence and coverage

From the two mature step-2444 models, does fixed output-only coordinate-norm equalization change natural recurrence, known-owner coverage, geometry validity and stopping on the same 18 refined scenes and existing 128-image sentinel panel?

## Authorization and ownership

User authorization on 2026-09-18: draft multiple research units and delegate parallel/sequential execution to existing worker `01a0a81a-9e32-7db1-bd07-86fa601f4276`, using eight GPUs. Root `01a0a3d5-dc45-7693-8467-4801aa7190df` owns scientific decisions, acceptance and conditional release. Apply research-flow, lead-worker and native-agent-team-guidance. Execution cwd is `/data/CoordExp/.worktrees/research-probes`. No optimizer update or new training is authorized.

This unit owns the shared input/runtime packet for itself and the separately scoped [active-readout](../2026-09-18-untied-active-readout-geometry/unit.md), [gradient-path](../2026-09-18-untied-gradient-path-accounting/unit.md) and [conditional-feedback](../2026-09-18-untied-recurrence-feedback/unit.md) units. Each has its own outcome; failure of an optional diagnostic does not block valid natural generation. The feedback unit is not released by this assignment.

## Predecessor and changed question

The [accepted fresh128 intervention](../2026-09-17-readout-norm-fresh128/results.md) changed R16 known matches578 to592 and strict near-repeat rows604 to40, predominantly on unhealthy cases; physical population improvement was not established. The [native donut branch](../2026-09-18-native-coordinate-branch-completion/results.md) found a better-likelihood realization of a repeated owner, not a retained higher-likelihood new-owner row. Those packages remain closed.

New CPU evidence: untied output coord999/0 retain norm ranks1/2, whereas untied input ranks28/234. The new model also has axis loss. This is a behavioral comparison of complete training packages plus a within-model readout intervention, NOT a controlled causal effect of untied training. New-versus-old differences can reflect axis loss, optimization and runtime history. Output peaks alone need not cause recurrence; legal frame boundaries are not errors.

## Shared immutable inputs

- Base: `/data/Qwen3-VL/model_cache/models/Qwen/Qwen3-VL-2B-Instruct-coordexp-natural-adjacent`.
- Tied checkpoint: `/data/CoordExp/outputs/research/eight-coordinate-bbox-supervision/2026-08-05-closeout/artifacts/training/four-coordinate-xy/checkpoints/step-2444`; adapter SHA256 `49aa206cb43ebc61bf0413e6de6d81cea725549c71d38b596826fda5f523b5da`, shared delta SHA256 `a41cbb2fd05e3f6b7ad43f28f9fc5ce973477812435acf8d79d9b2b61f19e2f2`.
- Untied-plus-axis checkpoint: `/data/CoordExp/outputs/infra_base/train/qwen3-vl-2b-geo-sorted-xy-untied-axis001-ebs24-4epoch/checkpoints/step-2444`; inference manifest SHA256 `ffc3ac18edb5d284e8cad905326eddda13fd4506c90e099cb40b5f85fe1353b5`, two-delta safetensors SHA256 `0b85d4c5261914bc883867e29ae2cd9f25ded2667cc4ab8b32ef6182b618fa2f`. Both input/output deltas are required. This run used CE plus0.01 axis-validity hinge, four epochs/EBS24 and fresh language DoRA.
- Human13: `/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-08-05-static-dynamic-owner-interface-crossover/inputs/human-refined-13.geo_sorted_xy.coord.jsonl`, SHA256 `5c6cc95965c6dd24d7f61f09a0c56edb71eb5a9a05664fa7d26269718f741f23`;13 val images392 positives, ordered IDs1584,2299,2685,4134,5001,6040,7511,10707,13348,13923,14038,14439,16228.
- Refined5 immutable copy: `/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-17-history-rereading-mechanism/human-evaluation/annotation-snapshot-v1/working.norm.jsonl`, SHA256 `1d8d7c6d63e982f2d5060fa96a7cbc825c0314246276b181aaefff9ee80fed85`; train IDs7116,309264,351017,417044,477415 with5,14,49,63,47 positives. It matches current `/data/CoordExp/public_data/coco/rescale_32_1024_bbox/label_studio_refinement_4/working.norm.jsonl` at registration.
- Human13 and refined5 are disjoint:18 scenes570 positive records. Human13 uses coord-token strings; refined5 uses integer norm1000 XYXY. Convert losslessly to a new runtime view, preserve original object IDs/categories/extents, sort stably x1/y1, resolve images to verified absolute paths. Bind image hashes/dimensions. Negative annotation IDs mean new positive objects. Neither source declares exhaustive negative/ignore regions.
- Sentinel: ONLY `cohort=fresh` cases from `/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-17-readout-norm-fresh128/panel.json`, SHA256 `1521feb4b4a138f4bc2460059a40c3ac77c65a931f0b6b40308b57f2382afba1`. Reuse the frozen128 identities, original annotation records, image bindings and32 groups. Exclude its four diagnostic groups. This previously reviewed panel is not new held-out evidence. Deduplicate any overlap by split/image identity, preserving separate reporting membership and refined annotation authority.

The verified overlap is train309264, in sentinel group `fresh-18`: the sentinel has10 original positives while refined5 has14. Generate this image once per condition in its frozen sentinel group. Keep both annotation views: refined18 uses14 and the original sentinel report uses10; do not silently replace its919-positive denominator. The unique scene union is145. If a pooled unique-scene report is useful, declare the refined-bank override explicitly (1479 positives), alongside both original strata; do not sum duplicated memberships. Append the remaining17 refined scenes to the existing sentinel grouping in the declared Human13/refined5 order.

Never reconstruct refined labels from broad train.jsonl/val.jsonl: those rows contain only83/236 original objects rather than178/392. No source annotations are edited. The output root is `/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-18-untied-highconfidence18-natural`.

## Four conditions and runtime

Run `tied-original`, `tied-normalized`, `untied-axis-original`, `untied-axis-normalized`. For each model, normalized applies at EVERY decoding step to ALL1000 coordinate logits: coefficient = FP64 median of effective FP32 coordinate-row norms divided by each effective row norm, then cast results to original logit dtype. This is the existing policy definition; coefficients are recomputed once for each bound head, never tuned. Non-coordinate logits including EOS remain bitwise unchanged; their relative probability may change. Input embeddings are never normalized by this policy.

Use one qualified native Hugging Face FP32/SDPA route, original empty-prefix prompt, no resize, four-coordinate XYXY, desc-first geo_sorted_xy, temperature0/top_p1/RP1, max_new_tokens3084 and natural EOS. Inherit the exact template bytes from the frozen sentinel panel; no prompt change, mask, NMS, forced EOS, sampling, beam search or refresh. Freeze effective batch groups/left padding/companions before generation; four images per group, short final group allowed, same groups in all four conditions. Existing serialized config batch_size is not proof of effective native grouping.

Real-entry qualification: load both checkpoints with correct independent/shared delta semantics, verify effective E/U rows against payloads; for untied input and output use independent forward-sensitivity witnesses or fresh narrowly scoped checks. Verify no-op hooks/identity coefficients preserve logits and native tokens; verify normalization matches explicit effective-row rescaling and leaves non-coordinate logits unchanged. Produce and read back one shared group per model/policy before broad generation. Freeze code/config/payload/input digests and numerical tolerances before rollout; no silent loader fallback or batch/precision switching. Existing coordexp-infras untied loader is the implementation reference; existing research probe wrappers may assume tied weights. Reuse or minimally adapt a local probe; do not refactor production infrastructure.

## Observations, review and admissible claims

All18 refined images and the sentinel panel are evaluated regardless of apparent benefit. Report Human13, refined5 and sentinel separately before pooled summaries. Report source/attempted/valid/analyzable counts, errors and skipped records explicitly. Primary known-owner metric is class-agnostic one-to-one IoU>=0.5 matching, with identities/gains/losses and FN; keep same-category and IoU0.8 as secondary extent-sensitive diagnostics. A changed known match is not automatically physical discovery/loss.

Report exact valid repeated rows, existing strict near-repeat proxy, malformed/invalid rows, literal invalid repeats, endpoint occupancy by coordinate role, unique known matches, output length/EOS/cap and first repeat-candidate row. Pair counts are not repeated-owner counts. Physical recurrence requires a same-owner witness; crowded overlaps and different extents may remain HOLD. Annotation-unmatched remains UNKNOWN, never automatic FP. Primary evidence is native behavior; forced or replayed rows are separate.

For refined18, inspect first recurrence candidates and decision-changing coverage changes using full-image bbox overlays plus local context; at most one bounded pass, up to4 candidate events per image,72 events total. Do not exhaustively enumerate raw proposals or hold all numerical results pending visual completion. Unresolved events remain HOLD. Sentinel gets saved-output proxy/known coverage reports; no new population-wide physical precision claim. Report healthy/unhealthy baseline strata and legal-border owner retention. Use paired image-level intervals for sentinel known-match differences (seed19,10000 bootstrap resamples), not independent-token intervals. Freeze definitions before outcome inspection.

## Shared artifacts and bounds

Retain per-image original output tokens/text, role/row positions, parse/scoring primitives, image/annotation bindings, EOS/cap and runtime receipts. Use one authoritative trajectory/event table. Reusable forward capture should retain coordinate logits, full-vocabulary logsumexp/top competitors and final head inputs only at selected events; the readout unit owns detailed tensor selection. Do not dump all-layer full KV/attention for all tokens. Record actual forward counts, active/padded token work, vision passes, GPU-seconds, wall time, peak memory, tensor bytes and job exit state.

Planned natural outputs:4 times the145-scene union,580 image outputs; at most148 native batches plus up to8 qualification batches. Cap500000 batch-forward calls and40 allocated GPU-hours for this unit, including qualification/failed attempts; max32GiB retained tensors (raw text/metrics excluded). First pilot records throughput/memory and projected total. Stop affected work if bounds cannot hold; ask root rather than silently expanding or changing precision. One bounded mechanical correction per failing seam, unchanged scientific contrast, preserve failed attempts. No quality-driven retries/seeds.

Use all8 GPUs after admission, initially2 per condition where useful; as trajectories become available,6 can generate while1 captures readout and1 measures gradients. Shard independent batches; do not replicate full data on every GPU or occupy cards without useful work. Existing random stress occupancy is expected; launch directly and react to concrete failures. The companion readout and gradient units each cap6 GPU-hours, so the three released units total at most52 allocated GPU-hours. Feedback spend requires a separate root release; no automatic use of remaining budget.

## Dependency, delegation and stop

Worker owns implementation/runtime/results for these three released packages. It may use at most2 simultaneous native implementation workers on disjoint readout/gradient surfaces after freezing the shared loader/input seam; explicitly bound those children to one layer, no further delegation. Follow lead-worker Astra-low execution routing unless root changes it. GPU producers are sharded processes, not extra agent coordinators. Worker integrates; root independently accepts. Root owns index/catalog and final claims.

Natural generation is the priority. Static readout preparation and canonical gradient setup may proceed in parallel; gradient GPU work waits only for relevant loader/forward qualification. Detailed readout capture consumes valid native events as available. A diagnostic HOLD does not block unaffected generation. Return each stable candidate with exact commands, hashes, CPU-recomputable summaries, claim limits and live-job status. After the three released units complete or hit their bounded stops, return the integrated candidate and stop. No successor, conditional-feedback model call, training, staged-checkpoint sweep, cross-model head swap, full causal layer scan, label change, Notion, memory write, commit or destructive cleanup.
