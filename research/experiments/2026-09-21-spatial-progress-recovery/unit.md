# Lane A recovery: worker orchestration with Luna-max

Status: user-authorized continuation; candidate acceptance belongs to the lead.
Date: 2026-09-21. This is execution recovery of the frozen Lane A experiment,
not a new scientific discriminator or a controlled comparison of model families.

## Authorization and objective

The user explicitly rejected having the main worker take over implementation:
“我想测试一下 worker 的`编排`能力; 要求它继续提示luna-max subagent来完成lane A的执行”.
Complete the existing eight primary Lane A cells through worker-to-Luna-max
coaching, and retain enough evidence to learn which prompts, feedback, and
verification caught or failed to catch implementation mistakes.

The earlier accepted technical HOLD and exhausted attempts remain historical
facts. This new authorization supersedes their prohibition on further Lane A
repair/qualification for this continuation only. It does not rewrite those
receipts or reopen Lane B. The original scientific contrast, admission,
candidates, conditions, estimands, identity rules and claim limits remain frozen
in the [predecessor profile](../2026-09-21-spatial-progress-gate/task-profile.md).
The [accepted closeout](../2026-09-21-spatial-progress-gate/lead-results.md) remains
unchanged. H1 is still untested until qualified evidence exists.

## Pair and responsibility

- Lead: 01a0c1f3-dbef-7b63-b2da-8dc7072cea8d, Astra/xhigh.
- Existing worker: 01a0ba19-8004-7400-b8af-98ce9b9df0cd, verified Sol/xhigh;
  preserve its current settings and user override.
- Cwd: /data/CoordExp/.worktrees/research-probes.
- You, the worker, own task decomposition, the child prompt, corrective feedback,
  independent verification, job coordination, and one integrated candidate.
- One Luna execution owner at a time, explicitly gpt-5.6-luna with
  reasoning_effort=max. Live model/list confirms this combination is available.
  Reuse the original Lane A child if its actual model/effort and context fit;
  otherwise create one fresh Luna-max child and record why. Verify the actual
  spawn/session binding; a role label or instruction to think harder is not proof.
- Luna owns implementation, bug-fix tests, real-entry qualification, finite model
  execution, and reducers. The main worker must not silently write its patches,
  substitute a stronger implementation agent, or complete its failed code task.
  Inspecting code, reproducing faults, checking results, and giving targeted
  feedback are appropriate orchestration. You choose how to brief and coach it.
- No child delegation, extra review agents, second sidebar task, or separate
  scheduler. Parallel GPU jobs are allowed within the single execution owner.

## Owned surfaces and immutable inputs

Worker/child may change probes/training_set_completion/spatial_progress_gate/
and add candidate results and orchestration notes in this recovery unit. All new
run artifacts belong under the matching 2026-09-21-spatial-progress-recovery
output root. Lead owns this protocol, state, frontier, questions, and catalog.
Do not change Lane B, either predecessor research record, old run directories,
old acceptance receipts, frozen admission, annotations, or unrelated dirty work.

The shared admission remains:
/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-21-spatial-progress-gate/selection/shared-admission.json
SHA256: 8863a4eb6d00ed9cb27cf8dd41e32429e7081e3bf363907b23bf4a7a0ba1d7d8.
It admits one failure and one matched control, eight unique primary cells;
ten other failure trajectories remain HOLD. No re-admission or replacement.

The checkout now contains unrelated shared-helper/storage refactors. Inspect
current dependencies, preserve their bytes and ownership, and use maintained
imports. Bind new runs to the code actually executed; never claim source parity
from an old filename. Follow docs/OUTPUT_STORAGE_POLICY.md: source captures use
src.artifacts.source_provenance.preserve_source outside outputs. Do not execute
archived code or add source/Markdown files to outputs. If a necessary shared fix
cannot stay within the Lane A adapter, report the exact conflict before editing
that other owner's surface. Do not repair the unrelated migration.

## Known failure evidence supplied to this trial

1. qualification-01 looked for the boundary in the source panel instead of the
   frozen shared panel and failed with zero forwards.
2. qualification-repair-01 completed seven forwards, then KeyError('tokens').
   The lead has already reproduced this on CPU with zero scoring calls:
   _trace_parity reads actual['tokens'], but _actual_row returns parser metadata
   containing start/end offsets, description, coordinates and validity, not tokens.
3. The existing selfcheck passed while missing this real interface. Do not treat
   a passing helper selfcheck as proof that qualification can finish and persist.

These are GIVEN diagnostic hints, not discoveries attributable to Luna or to
this continuation's orchestration. No implementation patch is supplied. Further
errors and the corrective prompting are part of the observation, not a reason to
hide attempts or abandon the original task automatically.

## Repair and execution boundary

Retire the demonstrated faults using real frozen data and a CPU failure/pass
check at the affected caller boundary. Check the full path through qualification,
row identity, parity, persistence and saved reduction before scaleout. Preserve
actual/owner/candidate distinctions at the control; do not assume actual=A.
Reuse existing scorers and the finite frozen experiment; do not redesign infra.

Within the resource limits below, ordinary engineering failures authorize
continued diagnosis, Luna coaching, repair and requalification. There is no
renewed original-plus-one repair counter. Record each real attempt and all cost;
do not relabel failures as preflight, silently restart, or change scientific
conditions after seeing outcomes. If a fault repeats, the worker must change the
diagnostic or feedback strategy rather than send the same retry instruction.

Once qualified, complete all eight primary cells. Reuse qualifying scientific
cells if they satisfy the same frozen evidence contract; do not repeat successful
cells just under a production label. Save complete-row, x1, conditional y1 and
joint-corner scores, crossing versus sham/noncrossed effects, source/runtime
bindings and deterministic reductions. No secondary free-release cells are
included in this primary recovery assignment. Return directional findings as
candidate measurements; the lead owns H1 interpretation and acceptance.

## Resources and stop rules

All eight GPUs remain available. Use independent ready cells in parallel, with
one live invocation per cell and no padding arms just to occupy devices. Do not
wait for the expected stress workload to become idle; react to actual conflicts.

Keep the original cumulative ceilings, including the closed package and every
new failure: 8 allocated GPU-hours, 100000 model forwards, and 16 GiB retained
artifacts. Starting historical charge: 167 model + 167 vision forwards and
533.4304532557726 GPU-seconds. Record this separately from continuation cost.
This continuation has a fresh two-hour wall allowance from successful dispatch;
the dispatch receipt binds the exact start/deadline. The old wall marker is
historical, not a reason to refuse this newly authorized run or silently reset it.
CPU repair iterations have no artificial attempt quota within that allowance.

Continue routine work without per-fix permission. Stop dependent work only for
an actual semantic/identity conflict, unavailable required model tier, a concrete
external blocker, or a resource boundary; return evidence and the smallest
resolution. A time/budget stop is unfinished execution, not a scientific null.
No training, cohort expansion, condition search, Lane B replay, publication,
commit, self-acceptance, or successor is authorized.

## Orchestration evidence and candidate handoff

Keep one concise orchestration-notes.md in this unit, linking exact initial and
corrective prompts, actual child model/effort/context choice, candidate diffs,
failed/passing commands, attempts, feedback and outcome. Retain delivered prompt
text in ordinary research records or JSON artifacts rather than screenshots or
a new logging framework. Distinguish child findings, supplied lead hints, worker
diagnosis/coaching, and any direct worker implementation (which is a deviation).
Record available agent usage and wall/GPU costs; unavailable counters stay
explicitly unavailable and do not block scientific completion.

Conclude with what this single assisted recovery does and does not tell us about
prompting and capability: which interface was missed, which feedback changed the
next action, whether Luna completed it, and what the worker verified. Do not claim
a general model ranking or an unaided success from this one trace.

Return one immutable candidate manifest binding scientific outputs and this
orchestration record, changed paths, fresh acceptance commands, all attempts/cost,
and terminal jobs/child status. Send decision-bearing checkpoints only: real
scope/resource conflicts, first qualified production evidence, and stable candidate.
No worker self-acceptance or new research direction. The lead will independently
verify the candidate when resumed; do not assume an automatic lead wake is armed.
