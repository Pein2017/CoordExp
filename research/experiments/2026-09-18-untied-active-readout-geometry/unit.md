# Untied active readout geometry

## Authority and status

Authorized analysis unit; model forwards require the native-trajectory gate below.
Lead `01a0a3d5-dc45-7693-8467-4801aa7190df` owns scientific admission and acceptance.
Worker `01a0a81a-9e32-7db1-bd07-86fa601f4276` executes the bounded package.
The shared source of runtime, checkpoint, cohort, and decode identities is
[`2026-09-18-untied-highconfidence18-natural`](../2026-09-18-untied-highconfidence18-natural/unit.md).
No parameter updates, checkpoint-stage probes, or automatic successor are authorized here.

## Question and contrast

At naturally reached coordinate decisions, which readout terms explain endpoint
or repeated-region preference: row magnitude, state alignment, or a near-linear
coordinate-logit profile whose maximum falls at a boundary?
Compare the mature tied step 2444 and new untied-plus-axis-loss step 2444 packages.
This is a package comparison, not an untie-only causal experiment.
The new output retains coordinate 999/0 norm ranks 1/2 while its input does not;
verify the source receipt before using that observation in interpretation.
Tying is therefore not required for the observed output norm peaks to form.
An endpoint coordinate can nevertheless be physically correct, including donut x1=0.

Primary evidence is exact decision-margin accounting on native states.
The strongest alternative is that apparent weight geometry is inactive at those
states, and contextual alignment or competition with noncoordinate tokens dominates.
Static spectra and cross-model plots cannot decide this alternative alone.

## Inputs and frozen event panel

Inherit the main unit's 18 high-confidence images/570 positive owners and deduplicated
previous 128-image sentinel cohort; the sentinel is not a new held-out sample.
Reuse valid native outputs and their exact prefixes; do not generate another cohort.
Choose the first 12 qualifying images in the main unit's frozen source order.
Qualification requires a natural recurrence candidate under either primary model;
retain both primary models on those images, including a healthy counterpart.
Before instrumentation, freeze an event receipt with image/model identities,
prefix token hashes, row/token indices, recurrence evidence and physical/HOLD status.
Per model/image select at most three recurrence events: onset, next recurrence,
and last observed recurrence; deduplicate coincident events.
Additionally select at most one earlier healthy row when available in that trajectory.
An image with no qualifying event contributes only its admitted healthy counterpart.
Do not promote a geometry-only duplicate hint to confirmed physical recurrence.
If fewer than 12 images qualify, use fewer; if none qualify, close with static evidence.
No outcome-based replacement, new images, or automatic panel enlargement.

## Measurements

1. Audit effective input/output rows, deltas, final normalization gain and hashes.
   Reuse saved CPU weights; distinguish base structure from learned updates.
2. At each event retain raw full-vocabulary logits, pre/post-final-norm states,
   coordinate role, selected token, rank, and margins to specified competitors.
   Preserve object-start, row-end and EOS competition where those decisions occur.
3. Reconstruct coordinate logits as norm times angular alignment, and reconstruct
   final normalization with its learned per-dimension gain, not only its RMS scalar.
4. Fit the fixed affine basis `[1, k/999]` to each 1000-bin native logit curve.
   Save intercept, slope, residual and second differences; reconstruct endpoint
   versus best-interior margins from slope and residual contributions exactly.
   Smoothness/low rank alone does not imply endpoint selection.
5. If the main unit admits a fixed output-norm diagnostic, recompute it on these
   same states and label this an immediate readout counterfactual. A natural
   norm-treated trajectory is a separate history, never a matched hidden state.
6. Centered spectra, adjacent-row statistics and DCT may describe static structure;
   keep the coordinate-common component for full-vocabulary probability accounting.
   Variance explained does not establish operational rank or a semantic circuit.

## Instrumentation and real-entry gate

Freeze at most ten semantic token positions per selected row before forwards.
Use the existing native loader, batch companions, padding, positions and precision.
Replay exact prefixes; preserve all ambiguous earlier rows as actually emitted.
Require unmodified replay's chosen tokens and decision margins to agree with the
saved native entry within the main unit's measured numerical envelope.
Report reconstruction error; unresolved near ties cannot support a rank-change claim.
Capture per-layer residual, attention-output and MLP-output vectors only at the
selected positions. Attention summaries may aggregate existing prefix regions;
do not retain full attention matrices or treat attention weight as causal credit.
Layer descriptions are observational; no layer patches, scans or ablation sweeps.
All causal language is limited to explicitly performed readout interventions.

## Cost, artifacts and acceptance

Static CPU work may proceed while the main unit runs; native-state forwards wait
for its valid trajectory receipt and the frozen event-selection receipt.
Hard limits: 2000 batch forwards, 6 allocated GPU-hours, 16 GiB retained tensors.
Count failed/retried calls and all model/vision passes; preserve actual batch identity.
Stop at the first limit and report a partial candidate without expanding the panel.
Retain event manifest, source hashes, token/state/logit evidence, compact layer
summaries, reconstruction script/output, cost ledger and explicit omissions.
Independent saved-output reconstruction must reproduce claimed margins and counts.
The lead accepts descriptive results separately from any explanatory inference.
Stop after the fixed panel, candidate records and worker/process closure.
No training, cross-model head/input swaps, label edits, broad tensor collection,
new normalization policy, Notion/memory writes, or automatic feedback-unit release.
