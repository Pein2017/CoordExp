# GPT-Web handoff: CoordExp local source recovery

Use this as a bounded writeback packet for the existing Notion ontology. Do not create a new ontology or promote any research route.

Primary manifest: `/data/CoordExp/research/investigations/2026-08-26-notion-local-source-recovery/manifest.md`.

## Claims that can be restored or modified

1. **Tight-outline visual steering**
   - Restore exact committed source, checkpoint path, held-out slices, decode, evaluator, controls, and metrics from the Step-0/Step-1 PVCI notes.
   - Rewrite as teacher-prefix structured-boundary steering with strong geometry-following behavior.
   - Add a provenance warning: referenced checkpoint, input JSONL, and primary Step-0/Step-1 artifact roots are absent locally; the committed result note is the surviving evidence surface.
   - Keep Status `Inconclusive` or equivalent bounded status; do not promote to broadly reproduced.

2. **Multi-rollout sampling**
   - Restore the exact three step-4887 checkpoints, human-refined12/346-owner panel, K=16 sampler, RP1.0, evaluator, artifact root, and counts.
   - Safe wording: “Sampling exposes additional strict-matched owner support beyond one native greedy trajectory on the recovered 12-image panel.”
   - Do not say that union owners are jointly compilable.

3. **Ordering**
   - Link the eight-coordinate closeout and July-20 matched prefix-order screen.
   - Preserve `x->y` as a preferred future candidate and potential production direction, not a clean causal promotion.
   - Record final recovery answer: **PARTIAL**.

4. **Address/Commit**
   - Split retrospective commit-state readout from future causal use.
   - Retrospective readout can cite frozen A3 step-2445 P0: 2,046 matched observations, retrieval@1 75.27%, and controls.
   - Future-use page must cite the separate 320-image proposal-bridge own-prefix panel and its HOLD/negative result.

5. **Evaluation contract**
   - Add stable source links for parser, EOS/native greedy, artifact/provenance binding, category normalization, official evaluator, and research matcher.
   - State explicitly that official COCO and research F1/owner matching are separate evaluator families.

## Claims that must remain Inconclusive

- tight-outline as a physical-owner identity selector;
- tight-outline as proof of a future free-running route;
- ordering as a clean ordering-only training gain;
- owner-commit readout as proof of causal future consumption;
- a deployable or safe proposal bridge;
- industrial polygon/line historical success;
- any annotation-richness causal explanation.

## Claims that should be split

1. Tight-outline:
   - A: structured boundary cue steers row/geometry — bounded positive;
   - B: cue identifies physical owner — unsupported;
   - C: future free-running policy uses same route — unsupported.

2. Ordering:
   - within-checkpoint earlier-row-order intervention — bounded positive;
   - sorted-vs-random training effect — partial/confounded;
   - `x->y` versus `y->x` training effect — directional/confounded.

3. Address/Commit:
   - retrospective decodability/readout — bounded positive;
   - causal future use — separate negative/HOLD route.

4. Evaluation:
   - official closed-vocabulary COCO evaluation;
   - research one-to-one owner/F1 matching;
   - human-adjudicated unknown/unsupported taxonomy.

## Sources unavailable in the current local estate

- PVCI Step-0/Step-1 primary artifact roots, step-484 checkpoint, and recorded val200 input JSONL. Exact paths survive in committed notes, but the files do not.
- June-04 sorted/random no-newline smoke artifact root. The committed diagnostic note survives.
- Endogenous commit-write-read primary artifact root. The committed result note survives.
- Any metric-bearing industrial polygon/line artifact, checkpoint, data slice, evaluator, or result commit. Treat this as locally unrecoverable unless an external disk, remote node, or user-supplied archive is introduced.

## Canonical Path corrections

- Architecture Address/Commit is incomplete if it points only to `permanent-owner-bridge`; add the historical `owner-commit-binding` source for P0.
- Causal Evaluation should link current `src/inference`, `src/eval`, and `src/vis` sources in addition to `PROJECT_CONTEXT.md` and research-probes.
- The Image2299 statement that 2026-08-25 units are uncommitted is stale: they are committed at `60a0b25a12861785cb319c580fcba7ef7fc02471`. New 2026-08-26 set-compilation changes are currently uncommitted and must not be conflated with the older units.
- The human13 worktree exists, but it is not evidence for the historical industrial polygon/line claim.

## Ordering answer

**PARTIAL**.

There is matched within-checkpoint prefix-order intervention evidence. There is no truly matched training comparison that changes only `x->y` versus `y->x`, and no publication-grade sorted-versus-random retraining comparison.

## Writeback stop rule

Stop after updating provenance, splitting over-broad claims, and preserving Inconclusive/HOLD status. Do not launch training, probes, or a Notion ontology redesign from this packet.
