# Residual-pair admission: completed, preservation failed

Status: **lead-accepted bounded result; both residuals fail actual-A owner
preservation**. See [results](residual-results.md). Exactly two new continuations
completed; no fitting, backfill, order change, or further model work follows.
Owner: `/root/astra_xhigh_composable_learning`. Root personally viewed and
admitted wine-glass667769 and donut1083042 as distinct physical omissions after
A. This is a NEW residual-pair contract; the original redundant-pair result,
eight raw branches, and closed three-fit proposal remain unchanged.

Root independently verified the exact packet and15 tests before assigning
physical GPU0, then inspected the two raw/terminal/reduced results and replayed
the four-owner geometry check before accepting this disposition. The
[launch record](residual-launch.md) owns command/log/exit identities. GPU0 was
released after completion; GPU1 was not used by this launch.

## Frozen question and conditions

From unchanged Stable50, does a literal residual B supplied at exact P+A add
its owner while preserving the ACTUAL P+A owner set and output burden?

| Image | A at original P | New residual B at P+A | Accepted P+A owner denominator | Direction |
|---|---|---|---:|---|
|351017|person478719|wine glass667769, Source seed2026090602 row16|6|B lies right of A|
|417044|donut1082918|donut1083042, Source seed2026090601 row10|9|B lies left of A|

The donut backward-x transition is a separate descriptive stratum. Two images
cannot identify spatial order as the cause of any success or failure.
Hypothetical later learning conditions are explicitly A|P and B|P+A, fixed
per-record across singleton/joint arms. Singleton-B would test conditional
ability, not autonomous access to A. **No fitting is authorized here.**

## Exactly two NEW calls, with accepted diagonals reused

One P+A+B full-row continuation per image; no new natural or P+A diagonal.
The original P+A raw rows are hash-bound and freshly replayed through the
native parser and scorer before packet readiness. Their actual intervention,
prompt, image plan, full action tokens, owner sets and burden must match.

Model loading, input materialization, policy construction and the native
`generate_continuations` call reuse the already verified acquisition executor.
The only new dispatch supplies the frozen P+A prefix and new B row and selects
the stricter residual consumer. Exact loaded model/numerics identity is compared
with `acquisition-v1/loaded-model.json` BEFORE generation; exact prompt/media/grid
checks remain before each call. A physical GPU reassignment changes placement,
not these semantic identities. No named execution-seam change currently requires
another scientific diagonal. Any concrete identity failure stops before an
additional call rather than silently adding one.

Original prompt/media, fp32 SDPA, unmerged Stable50, RP1 greedy, top_p1, top_k0,
no model-default decode, native EOS, full3084 action budget. The P+A prefix uses
18tokens for351017 and19 for417044; each new B row is10tokens. Thus free budgets
are3056 and3055, not3084 additional tokens after the supplied prefix.

## Decision-owning consumer

Each new raw record retains prefix/forced/free tokens, text, native parsed rows,
raw row starts, EOS/cap and exact call association. The native reducer requires:

1. The supplied new B row itself is matched to B at IoU50, not a later row
   merely recovering B under a different assignment.
2. Every actual P+A-matched IoU50 owner is retained, including A and its freely
   generated successors. Stable50's original sole person is NOT the denominator.
3. No increase in annotation-relative FP, strict repeats, parser drops,
   geometry-invalid output or other malformed output; natural EOS.

Report gained/lost owner IDs and TP/FP/F1 at IoU50/60/80, raw starts, and the full
burden ledger. Higher-threshold movement is separate from the IoU50 admission
predicate. Supplied A/B rows are intervention credit, not autonomous discovery.

Stop after both fixed new residual continuations and cold reduction, regardless
of sign. No alternative B, order, seed, prefix, image, sample expansion, retries
to obtain a positive outcome, or automatic three fits. Technical failures remain
technical and preserve the original logs/rows/exit status.

## Exact packet and CPU acceptance

Raw lane root:
`/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-12-parallel-owner-research/composition/`.

- Candidate source `residual-candidates-v1/candidates.json`, SHA256
  `a39233b68b2fc40011c697559c12fb958421a4bb982d4682368217f18a0bd114`.
- Executable input `residual-preparation-v1/input.json`, SHA256
  `a16a26d28b8221ccbe1d9e4d84a16eed691229d55bd5fa9181425b9e0dc92ead`.
- `residual-preparation-v1/summary.json`: exact roles, baseline sets and bounds.
- `residual-preparation-v1/readiness.json`: CPU verification and rejected
  counterexamples. Fifteen focused tests pass, including real native parser/
  matcher fixtures that accept preserved successors and reject incumbent loss.
  A pre-implementation run had8 new-interface failures while the original5
  tests stayed green. Full RED/GREEN logs and shell exits are in lane root.
- Full-packet mutation checks reject an extra diagonal, changed P+A, rebound
  baseline, altered residual-row tokens, and decoder drift.
- The exact first-wave source/tests were preserved under
  `acquisition-v1/code-snapshot/` before the owned module was extended. Old raw
  artifacts and their receipts are unchanged; no shared trainer was edited.

CPU verification:

```bash
python -m pytest -q probes/parallel_owner_research/tests/test_composition.py
python -m probes.parallel_owner_research.composition verify-residual \
  --input /data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-12-parallel-owner-research/composition/residual-preparation-v1/input.json
```

The root-granted launch used `acquire` with the frozen input above, an absent
lane-owned output directory, and explicit `--physical-gpu 0` equal to the single
`CUDA_VISIBLE_DEVICES` value. It reused the two accepted P+A baselines without
rerunning those scientific cells.

## Model-work bounds and estimates

Exactly1 model load,2 continuations and2 image forwards; at most6111 free tokens
and6113 model forwards. Thirty-two GiB peak CUDA allocation and3600s invocation
watchdog are diagnostic execution bounds, not portfolio spending ceilings.
The accepted same-route load previously peaked at9,955,875,328 CUDA bytes.
The completed residual invocation stayed within the frozen bounds; measured
resources and complete evidence are recorded in [results](residual-results.md)
and its linked machine receipt. No model work occurred during CPU readiness.
