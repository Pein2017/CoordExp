# S K10-H20 Natural Crossover Tasks

- [x] Freeze this independent S unit, its source-evidence hashes, exact
  K10∩H20 tuple, four-cell estimands, and no-training boundary.
- [x] Materialize and validate the deterministic three-event, three-shard CPU
  plan from the v3 evidence, source manifest/census/original plan, and gate.
- [x] Implement the write-once pre-GPU sealer with absent-root, exact-device,
  code/test, runtime, and CPU evidence/probe checks, plus per-shard runtime
  identity rehashing that permits only exact sibling shard roots.
- [x] Integrate the fresh `C00/C10/C01/C11` runner and finalizer with the
  shared actuator seam while preserving natural pre-opener admission and
  no-cache execution; the resulting preGPU-v1 fixed tree was technically
  sealed, but that seal is not accepted launch authority.
- [x] Repair the owned successor contract after independent Sol review found
  that preGPU-v1 omitted `test_natural_boundary_attention_actuators.py` from
  the sealed test set and left stale integration-pending status text.
- [x] Land and verify the finalizer lane's separate P1 repair so unqualified
  events cannot retain or publish a non-null tau result.
- [x] Seal a new preGPU-v2 receipt over the repaired five-test fixed tree and
  the finalizer repair, then obtain independent Sol re-review. The receipt is
  sealed and prelaunch-valid; independent review accepted that exact
  prelaunch tree, but the subsequent runtime attempt rejected it before model
  load on all three shards.
- [x] Record the failed launch boundary exactly: ordinary nohup `logs-v1`
  files were zero bytes and their exact process progress is unrecoverable;
  tmux `logs-v2` contains three 97-byte pre-model technical failures on
  `h0_root` raw SHA-256; deterministic gating and absent `execution-v1` /
  `evidence-v1` prove that neither path produced scientific evidence.
- [x] Repair only the runner's directory-inventory final ordering so it hashes
  entries by canonical `relative_path`, including nested prefix siblings, in
  parity with the sealer.
- [x] Seal preGPU-v3 over the minimal hash-order successor; Opus/max confirmed
  the cause/fix and the receipt is prelaunch-valid. Map the successor only as
  `logs-v3 -> execution-v2/evidence-v2`.
- [x] Obtain independent Sol acceptance of the exact preGPU-v3 tree; review
  returned PASS with P0/P1/P2 all zero.
- [x] Record the preGPU-v3 runtime rejection: `logs-v3` contains three
  pre-endpoint manifest/plan identity failures of exactly 84/85/85 bytes,
  while `execution-v2` and `evidence-v2` remain absent. Treat it as technical
  invalidity only.
- [x] Record Fable P0's operator-neutral repair boundary: compare the exact
  nine-field plan projection against a canonical projection recomputed from
  the full manifest event, preserve plan-v1, and retain the full event for the
  live executor.
- [x] Repair the production-shaped manifest-to-plan projection and add an
  exact CPU source-preflight path through live `_validate_inputs` with
  `_load_once` forbidden. Retain the old-runner red regression and the
  successor green tests for all three shards, nested hashes/owners/order,
  required history tokens, output absence, and bare/partial/full CLI modes.
- [x] Close the downstream P0 by extending source preflight through the real
  `_load_cpu_contract`, complete 11-event `_preflight_full_runtime_cohort`,
  and all three selected ledger-history/natural-context/attention-factory/K14
  geometry seams. Build K01/K10/H20/C11 on CPU, retain all-layer consumption
  as required/unattested and K01 parity as unassessed, and prove no model,
  backend session, GPU, `_load_once`, or output-root creation occurred.
- [x] Add a canonical receipt-independent preseal source-preflight API and CLI
  that binds the exact plan, all ten source paths, current code/tests, and two
  absent reserved roots before the v4 receipt exists. Keep receipt-bound
  three-shard source preflights as post-seal acceptance evidence.
- [x] Seal and obtain the first independent acceptance of preGPU-v4 over the
  repaired fixed tree and exact source-preflight evidence. Its raw/self hashes
  are `93672aa1983da7b55865c9b505dac1c22dfe1f36e3bb9a316ef741e128ff8f67` /
  `62eaafc1a453df96a4031c09447c85d70b1aa9bbd20ca2cbc1280168297917d7`, and
  its exclusive historical mapping is `logs-v5 -> execution-v3/evidence-v3`.
- [x] Record the v4 transport and runtime boundary: `logs-v4` are zero-byte
  transport artifacts; every actual `logs-v5` shard failed identically after
  model load with `GateTechnicalInvalid: S gate K10 scalar release failed: C11
  callback requires scalar input_ids`. The immutable, consumed `execution-v3`
  contains only `failure.json`/`failure.stderr` and no scientific result.
- [x] Complete the user-authorized operator-neutral v5 C11 keyword gate
  dispatch repair; preserve S as primary, the no-training boundary, and the
  fixed four-cell estimand.
- [x] Complete and verify the v5 finalizer production-schema/admission
  semantics, including `opener_generated_by_model` iff the first token is the
  opener and native STOP/invalid first-token outcomes as `row_started=false`,
  not technical invalidity. The `gt:2299:29` C11 cell exercises exactly this
  path: first token `291`, `row_started=false`, `mechanically_valid=true`.
- [x] Run the new quiescent conclusion-critical tests and retain their fresh,
  canonical passing receipt; no v5 test or code state is launch authority yet.
- [x] Seal fresh preGPU-v5 over the repaired fixed tree with fresh absent
  `execution-v4` and `evidence-v4` roots. Map only `logs-v6 ->
  execution-v4/evidence-v4`; do not reuse preGPU-v4 or `execution-v3`. Sealed
  at raw `7e4adff6e272dfaad8ad5bb5656c9fc0baf7f2992d3561e37edfb613991e57ec`
  and self `c349258554a2e308c5a4242157eb59cbb6793cdc7476c2a3a65719b845ae4571`.
- [x] Obtain the second independent fixed-tree acceptance of preGPU-v5. Both
  the Fable preseal and postseal reviews returned `PASS` with zero P0 and zero
  P1.
- [x] Launch the one fixed three-shard no-training run only after the v5 gate
  opens, then execute all four cells on all three selected events exactly once
  under the exclusive `logs-v6 -> execution-v4/evidence-v4` mapping. All three
  shards completed on physical devices `0`, `1`, and `7`.
- [x] Finalize full endpoint vectors and componentwise descriptive tau/charged
  utility; preserve valid unmatched outcomes. One CPU-only post-execution
  finalization successor was authorized and consumed for exactly two binding
  slots without changing any endpoint semantics.
- [x] Qualify or hold the source-specific crossover only from the complete
  four-cell endpoint contract; do not infer training or production claims.
  Outcome: `unqualified`; formal tau and both utility horizons are null, not
  zero, because no event has qualified endpoints in all four cells.
- [x] Publish a bounded result and immutable receipts without modifying old
  units or writing external formal roots before the user-owned launch step.
  Published as [results.md](results.md) and [review.md](review.md).

## Post-execution closeout

- [x] Record the consumption of preGPU-v5, `execution-v4`, and `evidence-v4`,
  and close the launch gate. Old `logs-v1`..`logs-v5` and `execution-v3`
  failures remain immutable technical history.
- [x] Record that K10 target release `3/3` is a deterministic
  cross-implementation replication of the selection condition, with `9/9`
  reproduction of the source `K01`/`K10`/`H20` endpoint vectors, and is not
  efficacy or a base rate.
- [x] Record the `gt:2299:29` C11 admission suppression as a single-event
  admission interaction, not a checkpoint-level history route.
- [x] Define endpoint field semantics explicitly, including that `duplicates`
  counts strict physical-owner identity repeats only, so the byte-identical
  `gt:16228:15` C11 row pair correctly reports `duplicates = 0`.
- [x] Document the runtime-identity provenance seam: the embedded
  `gpu_used=false` / `model_loaded=false` block is the sealed CPU-only preGPU
  identity and is not executed-GPU provenance. No artifact rewritten.
- [x] Record the retrospective design and power failure: the plan made a
  non-null aggregate tau structurally impossible because the source baseline
  and K10 unmatched counts were already known. No retry.
- [x] Reconcile unit frontmatter, launch gate, tasks, experiment index,
  compass, the parent 2026-08-06 owners, and project continuity memory.
- [x] Obtain the independent post-remediation documentation re-review. Fable
  returned `PASS` with `P0 = 0`, `P1 = 0`, `P2 = 1`; the one wording finding
  was corrected after review. The documentation state is **certified**.
- [x] Correct the Fable P2: scope the "small box" reading of the unmatched
  K10-family rows to `gt:13348:14` and `gt:16228:15` only, since the
  `gt:2299:29` unmatched boxes are not small.

## Closed decision records

These are settled decisions, not open work.

- [x] **Training route — `HOLD`.** No route proposed or recommended; there is
  no qualified estimand a training objective could target.
- [x] **A3 secondary contrast — `DO NOT RUN`.** S's `row_contract` has
  `commit_token_id: null`, so no commit-wrapper-specific attribution question
  exists.
- [x] **P4 — `DO NOT RUN`.** `authorize_P4 = false` in plan, receipt, and
  evidence; `no_p4 = true` in the frozen execution policy.
- [x] **No sweep, retry, re-selection, or new crossover.** `no_sweep = true` is
  frozen in plan-v1 and preGPU-v5; more cells of this shape cannot qualify
  while the control is unmatched.
- [x] **Owner-matching and grounding validity probe — routed, not authorized.**
  The dominant `unmatched` outcome routes there as the next discriminator on
  existing artifacts. It is a later user-owned decision; it was **not**
  authorized and **not** executed by this unit.
