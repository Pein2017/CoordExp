---
title: S K10-H20 Natural Crossover
description: A fixed no-training S geo_sorted_xy crossover of static K10 routing and nondegenerate H20 history at exact natural boundaries.
type: investigation
role: research-unit
authority: user-approved-successor-within-prior-conditional-phase5
architecture_promotion_status: not_promoted
training_promotion_status: not_promoted
implementation_status: complete_within_unit
unit_id: 2026-08-07-s-k10-h20-natural-crossover
topic: qwen3-vl-dense-enumeration
status: complete
evidence_status: complete_source_specific_unqualified
result: results.md
review: review.md
updated: 2026-08-07
---

# S K10-H20 Natural Crossover

## Authority and boundary

This is one fresh, independent S unit authorized by the user's prior
conditional Phase 5 decision and the explicit approval to do all in-scope
work. It supersedes only the old `no_2x2` proposal for this new unit. The
2026-08-06 natural-boundary artifacts and the 2026-08-05 static-dynamic
artifacts remain immutable source evidence; neither is reopened, repaired, or
rewritten here. The old `no_2x2` plan is a lineage input only and is not an
execution design for this unit.

The sole substrate is S four-coordinate `geo_sorted_xy`, step 2444. This unit
does not compare checkpoints, attribute a result to `<|commit|>`, InfoNCE, or
another training change, and makes no training, architecture, wrapper, token,
decoder, production, or promotion claim.

## Owning question

At the exact S natural pre-opener boundary, does a fresh static-by-history
intervention (`K10 ∧ H20`) have a source-specific endpoint beyond either
factor alone on the three events where the prior v3 evidence independently
qualified both factors? The answer is descriptive unless every cell has a
mechanically valid, source-specific endpoint. A valid unmatched row is a
scientific outcome and is never silently converted to technical invalidity.

## Frozen source evidence and selection

The formal source is the complete v3 evidence and receipt:

| Artifact | Raw SHA-256 | Semantic self SHA-256 |
| --- | --- | --- |
| `s-k-n-h-evidence-native-fn-supersession-v3/evidence.json` | `77f90bc48f8126ee1b071767308db603e1b64c0d19a4ca3e5ad8906cfc837598` | `e04cb63540f9e3a1b27b938ada14f694f6b7208564adcaf2c82656915c62bb95` |
| `s-k-n-h-evidence-native-fn-supersession-v3/evidence.receipt.json` | `fa11800ec7ad832646162f77c9e5a6df71d2e9ca951144e40b2fd060df46bf8e` | `80aa70f46899474ee1f8646c02d9c061babfac9bd58534f4cfa1d75aa9df12fd` |

The planner recomputes, rather than trusts a copied list, the intersection of
`K10.target_strict_release == true` and
`H20.factor_qualified == true` with
`H20.degenerate_grammar_disruption == false`. It fails closed unless the
ordered result is exactly:

1. `gt:2299:29` (manifest index 2, image 2299);
2. `gt:13348:14` (manifest index 5, image 13348); and
3. `gt:16228:15` (manifest index 8, image 16228).

For every selected event the plan binds the source manifest event hash, exact
natural prefix/history hash, and geometry hash. The complete source manifest,
census-v3, original execution plan, and frozen gate result are also bound by
raw and semantic hashes. Any tuple, order, qualification, event, prefix,
geometry, or source-lineage tamper rejects materialization.

## Four-cell design

The fixed cell order is `C00, C10, C01, C11`, repeated identically for each
event in one shard:

| Cell | Definition | Baseline |
| --- | --- | --- |
| `C00` | Explicit all-allowed causal K01-style no-op; hidden K00 full-vocabulary parity is a technical control only. | K01 |
| `C10` | Fresh K10 static image-key routing operator. | C00 |
| `C01` | Fresh H20 history operator. | C00 |
| `C11` | Fresh simultaneous K10∧H20 operator. | C00 |

Every cell records the complete endpoint vector: natural admission mode and
opener provenance, row count and terminal reason, strict physical owner IDs,
target release, uncovered-owner gains, covered repeats, duplicates, malformed
and invalid rows, unmatched rows, native STOP, mechanical validity, and
scientific-unmatched disposition. Componentwise intervention-minus-own-
baseline `tau` and gained-minus-lost charged utility are descriptive only.
The crossover is source-specific-unqualified whenever any cell lacks a
qualified source-specific endpoint; that outcome is expected to be reported,
not repaired by reselection.

## Fixed execution contract

- exact admission mode: `pre_opener_natural`;
- `opener_injected=false`; `opener_generated_by_model=true` if and only if the
  first generated token is the opener;
- a native STOP or invalid first token is an allowed scientific outcome: set
  `row_started=false` and retain its native terminal reason. It is not
  technical invalidity merely because no opener was generated;
- `use_cache=false`;
- `max_rows=3`, `max_row_tokens=256`;
- exactly three shards, one event per shard, on physical devices 0, 1, and 7;
- no event reorder, reselection, outcome-adaptive selection, sweep, A3, P4,
  2×2 reuse, training, or production launch.

A valid, independently accepted pre-GPU receipt would confer only this one
fixed three-shard no-training launch. It does not authorize external formal
roots until the integrated runner and finalizer exist and pass the sealer's
exact code/test/evidence checks. No root is created by plan materialization or
CPU sealing.

## Historical authority after preGPU-v4

This section is retained verbatim as immutable technical lineage. It is
superseded by the completion state recorded below.

PreGPU-v4 was sealed with raw SHA-256
`93672aa1983da7b55865c9b505dac1c22dfe1f36e3bb9a316ef741e128ff8f67` and
semantic self SHA-256
`62eaafc1a453df96a4031c09447c85d70b1aa9bbd20ca2cbc1280168297917d7`. Its
exclusive historical launch mapping was `logs-v5` to
`execution-v3/evidence-v3`. The `logs-v4` files were zero-byte transport
artifacts. The actual `logs-v5` attempt failed identically on every shard
after model load with
`GateTechnicalInvalid: S gate K10 scalar release failed: C11 callback requires scalar input_ids`.
The immutable, consumed `execution-v3` contains only per-shard `failure.json`
and `failure.stderr`; it contains no scientific result or endpoint.

The user-authorized v5 successor is operator-neutral and limited to repairing
C11 keyword gate dispatch and finalizer production-schema/admission semantics.
It preserves S as the primary substrate, the no-training boundary, the fixed
four-cell estimand, and all claim limits. The gate remains `CLOSED` pending
new quiescent tests, a fresh preGPU-v5 seal, and a second independent
acceptance. The exclusive fresh mapping is `logs-v6` to
`execution-v4/evidence-v4`; both fresh roots are currently absent. No v5 PASS
or launch authority exists.

## Completion state after preGPU-v5

This section supersedes the historical text above. It does not rewrite it.

The v5 successor repair completed, preGPU-v5 was sealed, two independent
reviews returned `PASS` with zero P0 and zero P1, and the one authorized fixed
three-shard no-training launch executed exactly once under the exclusive
`logs-v6` to `execution-v4/evidence-v4` mapping on physical devices `0`, `1`,
and `7`. PreGPU-v5 and both fresh roots are now consumed and completed.

| Artifact | Raw SHA-256 | Semantic self SHA-256 |
| --- | --- | --- |
| `pre-gpu-receipt-v5/pre-gpu-receipt.json` | `7e4adff6e272dfaad8ad5bb5656c9fc0baf7f2992d3561e37edfb613991e57ec` | `c349258554a2e308c5a4242157eb59cbb6793cdc7476c2a3a65719b845ae4571` |
| `finalization-receipt-v1/finalization-receipt.json` | `71b305dab1b25f838ba65433902cafe91e5aaa26ed80fbab792f607b1257c15e` | `c0244fb8e35e5fa4e277972085e5dc273ac6a7cda539d96ee64ce966fe5ee3ed` |
| `evidence-v4/evidence.json` | `29407f7cddd632999e3f720e9982fc4a52a31eea5398181f2f9d07dd28947d90` | `1dfa4144d094aa06f35c1cc3fb7f29b2db10efc55367f65b96c7244695d990f3` |
| `evidence-v4/evidence.receipt.json` | `9fc2fbb155bd3158f26d9085072d74e5c48b3c6dfe7496ebb4774296f359c398` | `db5649f7a9628f19b0445511d6e138be31f8dc8c04567080a81de0f230d08a85` |

Formal root:
`/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-08-07-s-k10-h20-natural-crossover/evidence-v4`

One post-execution CPU-only finalization successor was authorized and consumed
under [finalization-successor-authority.md](finalization-successor-authority.md).
It advanced exactly the two permitted binding slots, the finalizer and its
test, and changed no endpoint, parser, owner-match, denominator, event, cell,
tau, utility, operator, prompt, token, wrapper, checkpoint, or runtime
trajectory semantics.

### Disposition

The evidence is artifact-valid and the source-specific crossover is
`unqualified`. Formal aggregate `tau` and both utility horizons are **null,
not zero**, because no event has qualified endpoints in all four cells. This is
conditional case-level selected-three-event evidence over `3` events and `3`
images; it is not checkpoint-level crossover prevalence or effect.

K10 target release `3/3` is a deterministic cross-implementation replication of
the selection condition, not efficacy and not a base rate; the fresh
implementation reproduced all nine source `K01`/`K10`/`H20` endpoint vectors,
`9/9`. C11 reproduces K10's target release at `2/3` and is endpoint-vector-
identical to C10 there; at `gt:2299:29` it suppresses natural admission, with
first token `291` and the opener at rank 4 with log-probability `-6.34561`, so
no row starts. That is a single-event admission interaction, not a
checkpoint-level history route.

The complete endpoint tables, endpoint field semantics, the retrospective
design and power failure, the runtime-identity provenance seam, the strongest
alternative, and the decision table are owned by [results.md](results.md). The
independent review ledger is owned by [review.md](review.md): Fable preseal and
postseal both `PASS` with zero P0 and P1, the formal scientific audit `PASS` on
evidence integrity with an initial documentation `HOLD` at `P1 = 7` / `P2 = 5`,
and the Fable post-remediation re-review `PASS` at `P0 = 0` / `P1 = 0` /
`P2 = 1` with that one wording finding corrected. Documentation for this unit
is **certified**; the formal scientific disposition is unchanged.

The upstream 2026-08-06 support-completion lineage was separately audited
`PASS` (`P0 = 0`, `P1 = 2`, `P2 = 5`). That is provenance for the frozen source
cohort only; it enters no endpoint, contrast, `tau`, or utility here.

### Post-completion boundary

No retry, re-selection, repair launch, new crossover, or sweep is authorized.
Training remains `HOLD` with no proposed route. A3 is `DO NOT RUN`: S's
`row_contract` has `commit_token_id: null`, so no commit-wrapper-specific
attribution question exists. P4 is `DO NOT RUN`. No architecture, objective,
decoder, wrapper, token, or checkpoint promotion follows from this unit.
