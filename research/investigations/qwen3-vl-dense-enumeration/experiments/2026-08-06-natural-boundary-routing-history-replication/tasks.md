# Natural-Boundary Routing and History Replication Tasks

- [x] Freeze independent authority, substrates, all-owner denominator,
  pre/post-opener estimands, unmatched semantics, finite operators, and stop
  rules without modifying the old unit.
- [x] Obtain independent B1-B15 semantic contract audit before implementation.
- [x] Publish a superseding prior-evidence semantic audit and receipt, and in
  the same change narrow the live compass and prior experiment-index entry to
  post-opener conditional scope.
- [x] Implement and test a superseding endpoint finalizer that requires
  `admission_mode`, preserves mechanically valid unmatched outcomes, and emits
  qualified source-specific versus descriptive charged/neutral utilities
  without rewriting old bundles.
- [x] Materialize, test, and seal the initial 784-row CPU-only admission census.
- [x] Implement and test the unseeded natural-boundary release path and endpoint
  receipts.
  - Red test: a model-generated opener yields measured `row_entry` and complete
    segment scores; a misaligned token/log-prob array raises technical invalidity
    rather than returning `not_measured`.
- [x] Implement and test the fixed-dose `K14T/K14B` soft-bias controls.
- [x] Implement and test the all-layer H00/H10/H20 history mask and per-layer
  consumption attestation.
- [x] Implement and test the support planner's missing-native-FN scope while
  reusing the complete frozen 172-TP calibration.
- [x] Materialize and seal the CPU-only 200-context S support-completion plan
  with exact owner/prefix/row/candidate hashes and eight-shard work accounting.
- [x] Run the installed-Qwen CPU mask probes, including float-additive 4D mask
  pass-through and all-layer consumer attestation; silent coercion is invalid.
- [x] Materialize and seal the pre-GPU unit/evidence/runner/finalizer/test hash
  receipt that every runtime identity must repeat.
- [x] Run S `gt:5001:15` first under the new static, N, and unconditional H gate
  contract; close at the frozen bounded technical HOLD rather than treating a
  failed gate as scientific evidence.
  - Attempt v1 is a sealed technical invalidity: K01 full-vocabulary no-op
    drift `0.00011730194` exceeded the frozen `1e-4` tolerance.
  - Attempt v2 used the one exact repair and passed through the model-facing
    computation but failed terminal canonicalization because diagnostic
    `FixedDoseScoreBias` callback metadata is not JSON serializable. No arm
    artifact was persisted. The exact-repair allowance is exhausted; support,
    cohort, and A3 execution remain held.
- [x] Execute and merge the sealed 200-context support plan only after the S
  gate passes mechanically; never rebuild it from gate outcomes. Eight shards
  executed with exact assignment and exact plan order after gate v3;
  `support-merge-v6` is authoritative and census-v3 binds it by path and hash.
  Independently audited `PASS`.
- [x] Receipt realized scalar-forward counts separately from planned work.
  Realized equals expected in all eight shards, totalling `77,428`, with
  `resumed = 0` and `failures = 0`; realized and expected are distinct receipt
  fields.
- [x] Instrument measured support-shard wall time. **Closed incomplete without
  rerun.** Wall time is receipted only for shard-6 (`9,736 s`, from the v5
  supervisor); the other seven shards report `measured_wall_time_seconds: null`
  and the sealed plan receipt was never back-annotated. This is
  non-decision-bearing instrumentation hardening: it changes no count, no
  admission, and no scientific disposition, so per the user's instruction not
  to pursue non-discriminative hardening it is closed as-is rather than rerun.
- [x] Run the same static and natural-history core on every newly admitted S
  event. The sealed K/N/H cohort covers all `11` case events over `8` images.
- [x] Resolve the conditional A3 matched secondary contrast. **Decision: DO NOT
  RUN.** The downstream crossover raised no commit-wrapper-specific question,
  and S's `row_contract` has `commit_token_id: null`, so the triggering
  condition is not met. This conditional is closed, not pending.
- [x] Expand H across the S cohort only when the frozen gate receipt opens.
  H00/H10/H20 ran on all `11` events; only H20 is factor-qualified
  (`9/11`, `2/11` grammar-disruptive), while H10 and N10/N20 are `0/11`.
- [x] Run an S 2×2 only if its frozen static and dynamic qualification gates
  open. Both opened for the `K10`-positive, `H20`-qualified, non-degenerate
  intersection; the conditional crossover ran as the separate
  [2026-08-07 unit](../2026-08-07-s-k10-h20-natural-crossover/results.md) under
  the user's prior conditional Phase 5 decision and returned
  source-specific `unqualified`.
- [x] Aggregate immutable failure evidence; publish `results.md`; add the bounded HOLD to
  the experiment index/compass and update project continuity memory.
- [x] Obtain independent hash/identity/semantic audit; do not stage, commit, or
  push unless separately requested.

## Serialization-only successor

- [x] Record the user's explicit successor exception without modifying the
  frozen unit or old v1/v2 roots.
- [x] Remove non-JSON Python score-bias objects only from persisted callback
  metadata and retain the real K14 factory-to-result persistence regression.
- [x] Run the full focused suite and installed-Qwen CPU probes; seal pre-GPU v4
  against a fresh immutable gate-v3 root.
- [x] Obtain independent exact-diff and launch review:
  `PASS_FOR_ONE_S_GATE_V3` with no P0/P1.
- [x] Run and mechanically validate S gate v3. Completed on `gt:5001:15` across
  all fifteen arms with result self SHA-256
  `9719f2efe77f399ca66985659d902e33087be79bfdd29762643c6045cbed00fc`.
- [x] If gate v3 opens, execute the sealed support plan and continue only along
  the frozen decision-bearing S gates. The K/N/H cohort executed over eight
  shards and was sealed as the formal v3 evidence.

## Closeout

- [x] Seal and publish the K/N/H cohort evidence: `11` events, `8` images,
  raw `77f90bc48f8126ee1b071767308db603e1b64c0d19a4ca3e5ad8906cfc837598`,
  self `e04cb63540f9e3a1b27b938ada14f694f6b7208564adcaf2c82656915c62bb95`.
- [x] Record the qualified static direction as oracle routing and target
  transcription sufficiency only, with `training_claim_status: hold`.
- [x] Record K14 finite salience as `HOLD` at `1` qualifying event and `1`
  image.
- [x] Record that no native STOP occurred in any arm of any event.
- [x] Reconcile the result, review, launch gate, unit, tasks, experiment index,
  compass, and project continuity memory with the completed state and with the
  downstream 2026-08-07 crossover unit.
- [x] Obtain the independent post-remediation documentation re-review covering
  this unit and its downstream consumer. Fable returned `PASS`; the
  documentation state of both units is **certified**.
- [x] Audit the support-completion lineage end to end. Independent read-only
  audit returned `PASS` with `P0 = 0`, `P1 = 2`, `P2 = 5`: `200` frozen
  contexts, eight shards with exact assignment and order, realized `= expected
  = 77,428`, `resumed = 0`, `failures = 0`, `support-merge-v6` authoritative,
  `220` records = `200 + 20` retained, `14` verified native-FN, `11` admitted
  over `8` images. Findings and remediation are recorded in
  [review.md](review.md).
- [x] Adjudicate the shard-6 retry accounting. Lead adjudication recorded in
  [results.md](results.md) and [launch-gate.md](launch-gate.md): the v3 and v4
  shard-6 deaths are unattested transport/process deaths, not established
  operator or run-root mechanical invalidities, so no exact scientific repair
  allowance was consumed by the v5 supervised rerun and the lineage remains
  interpretable.
