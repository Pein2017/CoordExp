---
title: Final radius-versus-direction disambiguation
description: One fixed current-state factorial on the already observed Human13 prefixes, then close the interpretability branch.
type: investigation
role: research-unit
authority: non_normative_research
architecture_promotion_status: not_promoted
implementation_status: authorized_separately
unit_id: 2026-09-08-logit-lens-radius-direction
topic: qwen3-vl-dense-enumeration
status: complete
evidence_status: verified
updated: 2026-09-08
---

## Final-round question and stop

Completed within the frozen bound. See [results](results.md) and
[lead acceptance](lead-acceptance.json). The interpretability branch is closed;
no automatic successor is authorized.

On the exact preceding Human13 generated-prefix anchors, does current-state
direction replacement reproduce full donor-state effects independently of
residual-radius replacement at blocks 24 and27?

The user authorized one more round and then closeout. This unit is that final
round, not permission to start conditional natural-continuation, unseen-image,
or training experiments. No new image, prefix generation, layer search, model,
framework dependency, training, architecture change, commit or publication.
Stop after this factorial and synthesis, or its technical/resource limit,
even if a further hypothesis remains attractive.

Lead owns the scientific question, records, independent reduction and final
acceptance. `dynamics_runner` owns the new experiment-local runner/tests and
one live GPU invocation. The existing two helper runners are read-only,
hash-bound dependencies; their previous receipts and failed artifacts remain.

## Frozen identities and population

Same worktree, Source step-2444, Human13 magnitude-CE overfit, shared base/head/
norm/embedding delta, FP32/SDPA, tokenizer, image processing and exact prefixes
as [the accepted causal-transfer study](../2026-09-08-logit-lens-causal-transfer/unit.md).
Use original overfit-generated trajectories, not new sampling or GT substitutes.

- Image2299: the original 415-token trajectory, first/middle/last rows' 12
  coordinate sites, from the initial paired-lens `run-v2`.
- Other12 Human13 images: existing causal-transfer `stage-b-v1` trajectories,
  middle completed row's four sites per image.
- Total 13 images, 60 population sites, 59 previously distinct-coordinate
  endpoint sites; Image14439 equal-token site remains recorded and excluded
  only from the undefined normalized transfer ratio. No reselection.
- Fix endpoint a=overfit native top-one and s=Source native top-one from prior
  baseline records. Fresh native logits/IDs must reproduce these anchors.
- Record exact prior receipt, trajectory, prompt/media, model/adapter, helper
  and new runner hashes in the new execution receipt.
- Output root:
  `/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-08-logit-lens-radius-direction`.

## Four corners of one intervention

At 1-based decoder outputs 24 and27, write current state h=r*u. For each
donor/receiver direction, compare:

| Arm | State supplied at the current prediction position |
| --- | --- |
| Native/self | receiver radius times receiver direction |
| Radius-only | donor radius times receiver direction |
| Direction-only | receiver radius times donor direction |
| Full-current | donor radius times donor direction |

Run one position per forward, preserving all other residual positions and the
receiver's downstream layers. No cache, future-site graft, full-prefix graft,
token alteration, visual alteration or decoding intervention. Reuse captured
post-vision inputs only after real native-model/text-replay parity passes.

Strongest alternative to a state-content interpretation: changing the radius
alters later residual-to-update balance even when direction is unchanged.
Conversely, donor direction may carry transferable endpoint preference while
radius changes confidence or interacts with the suffix. Neither result alone
establishes a semantic concept or natural owner-coverage mechanism.

## Evidence and checks

Retain the preceding final margin `m=logit(a)-logit(s)`, native-gap-normalized
unclipped R, full-vocabulary donor/receiver/third-token outcomes, and raw
probabilities. Keep directions separate, images equally weighted, and expose
raw eligible site counts. The factorial interaction is
`m_full - m_radius - m_direction + m_native`; do not add it as an extra term to
an already complete symmetric two-factor allocation.

Frozen mechanical tolerances: atol=2e-4, rtol=2e-4 for native/self/prior-anchor
parity and intended norm/direction preservation. Require nonzero finite norms,
exact recipient state identity before patch and no off-target changes. Tests
must distinguish the radius and direction formulas. Preserve failed evidence;
do not enlarge tolerances or reinterpret execution errors as scientific nulls.

At block28, direct final norm/head projection is an algebraic control:
radius-only should preserve receiver logits and direction-only should reproduce
donor logits within the declared tolerance. This is not a scientific finding
or an additional layer search. Native/self check details must be durable.

Full-current is freshly replayed as an anchor to the prior current-only
experiment. Match the former prefix, endpoint and intervention surface; a
decision-bearing discrepancy blocks interpretation until resolved, not silently
relabelled as a new result.

## Resource bound and completion

GPU0 initially, one process, at most20 minutes GPU execution and two technical
attempts. Two resident models are allowed below48 GiB allocated. Existing
trajectory replay avoids all new generation. Use bare `python` under the
current shell environment wrapper. Record native/text/patch/image-forward
counts, wall time, RSS, GPU peaks, payload size and code/input identities.

One authoritative execution receipt with per-image evidence; lead independently
recomputes raw factorial effects, donor choices and acceptance hashes, then
writes the closing result and refreshes direction-local routers once. A
derived-evaluation-only defect may be repaired against retained raw data with
a versioned receipt, without rerunning valid GPU execution. No automatic
successor follows any outcome.
