---
title: Can verified useful native entrances become naturally selected after local CE?
description: One bounded two-entrance Source-started training probe with a frozen cold natural-output and development-regression check.
type: investigation
role: research-unit
authority: non_normative_research
architecture_promotion_status: not_promoted
implementation_status: authorized
unit_id: 2026-09-10-native-entrance-ce-feasibility
topic: qwen3-vl-dense-enumeration
status: complete
evidence_status: verified
updated: 2026-09-10
---

# Authorized question and independent lanes

Starting from original Source step2444, can supervision of the two verified
useful x1 entrances make a saved model select them naturally and recover their
owners without merely trading away other objects? The user accepted this
point-learning probe and asked for fast parallel execution on2026-09-10.

Root owns the scientific contract, final interpretation and this record.
One Astra owner implements training; a second independently prepares and runs
the cold evaluation. They have disjoint code/output ownership and exchange one
completed-checkpoint receipt. This is not two optimization arms or two writers
on shared code. No Luna, nested delegation, new architecture, QP, new stochastic
bank, GT edit, confirmation read, Notion writeback or Git publication.

# Frozen intervention and control

- Initialization is original Source, not round1. The no-update control is its
  retained natural output and the matching existing entry readout.
- Reuse only the two admitted cases and exact Source entrance token prefixes
  from2026-09-10-verified-branch-update-bridge: image368/person2022537 at action
  index97 targets x1=455; image7116/boat181378 at index22 targets x1=291.
- Preserve their annotation-backed moderate-confidence visual qualifications.
  Prefixes are context; other rows, unmatched predictions and EOS receive no
  new positive or negative supervision in this objective.
- Train only the same588 language DoRA A/B/magnitude tensors used by Source256
  learning, with their existing shape/scalar-count checks. Base, vision,
  projector, original selected-token embeddings and output head stay frozen;
  no merge or per-image inference-time payload.
- Loss is the equal-image average negative log-probability of the ONE target
  x1 at each exact prefix. All other replay positions have zero loss weight.
  Use native differentiable multimodal replay, FP32/SDPA, patch linearization,
  unmerged DoRA and eval-mode deterministic forward semantics.
- Optimizer: fresh AdamW, lr1e-5, betas(0.9,0.999), eps1e-8, weight_decay0,
  global gradient-norm clipping1.0, no scheduler or optimizer continuation.
- At each update, accumulate both target losses before one optimizer step;
  then re-read both actual full-vocabulary target-vs-best-other margins.
  Log target ranks/probabilities, loss, raw/clipped gradient norm, trainable
  parameter movement and counters. Do not infer effective dose from LR alone.

# Training stop, save and cold-read barrier

Stop at the first update where BOTH fixed-prefix margins are at least0.1,
or after32 updates, whichever comes first. Freeze this checkpoint before any
development outcomes. The margin threshold is a training stop/diagnostic,
not the scientific acceptance criterion. If32 steps do not cross, evaluate
that finite terminal candidate and state the remaining dose limitation.

Save a standard unmerged adapter to an absent output path. Publish the completed
checkpoint receipt only after tensor/config files and final scores are sealed.
It binds original Source/embedding identity, exact two prefixes/targets,
trainable tensor layout, actual optimizer count and stop reason. No best-checkpoint
selection from natural or development results, retries, LR sweep or automatic
extension. A technical nonfinite/save failure is not a scientific null.

# Frozen natural evaluation and interpretation

The evaluator prepares its manifest BEFORE viewing candidate outcomes:

1. Both training images from their original raw full-image prompts, without
   forced rows, location hints or repaired prefixes.
2. A16-image guard sampled deterministically from the existing Source dev128
   natural baseline, not from confirmation. Rank image IDs by SHA256 of the
   string `native-entrance-ce-dev16-v1:<numeric image id>` and take the first16.
   No filtering by model outcome, caps, prediction count or quality. Exclude
   only a training-image identity collision (fail and report it rather than
   replacing a case). These are historically used development images not
   trained by THIS update, not an untouched generalization test.

Reuse retained no-update Source outcomes with exact prompt/media/GT and policy
bindings. Load the SAVED candidate in a fresh evaluator model process; first
verify both fixed-prefix margins against the trainer's final scores, then
perform one complete greedy read over all18 images using T0/top-p1/RP1,
the same3084 action cap, FP32/SDPA and native token/parser semantics.

Report the two training images and16 guard images separately: target A recovery,
old owner gains/losses/retention at IoU50/60/80, category-consistent global
TP/FP/FN/F1, strict later pixel-IoU>.95 repeats, parser drops, EOS/caps and
complete token length. Unknown unmatched predictions are annotation-relative,
not hallucinations. Keep every output, including malformed or capped cases.

Reconcile whether the trained natural output reaches the frozen entrance
prefix; list earlier token differences and resulting covered-owner differences.
Exact-prefix failure alone does not prove semantic-state absence. If a fixed
entrance crosses while natural target recovery fails, distinguish the observed
access/row/suffix outcomes without claiming a unique hidden mechanism.

No joint owner/F1/burden benefit means no promotion. Even a clean two-image
success with a nonregressing16-image guard is bounded feasibility, not a general
training method or production checkpoint. Do not replace owner outcomes with
loss/margin success or silently impose a new global zero-loss guarantee.

# Execution budget and minimal technical checks

- Trainer: GPU0 only, one load, at most32 updates/64 differentiable target
  forwards and64 post-update score forwards plus at most4 initial/smoke scores.
  Cumulative model time at most900 seconds, including loading/save. No DDP.
- Evaluator: GPU1 only, one fresh candidate load, two fixed-prefix score
  forwards and18 natural continuations. At most55512 generated action tokens.
  Model time at most1800 seconds including loading and finalization.
- Combined allocation ceiling0.75 GPU-hours. Preparation and evaluation-code
  work may run in parallel with training; evaluation does not start before
  the completed-checkpoint barrier. One owner and live invocation per command,
  terminal/exit logs and event-driven waits, no polling or silent relaunch.
- Before broad updates: one real two-example differentiable step must prove
  last-target alignment, finite nonzero selected-parameter gradients, frozen
  nonadapter surfaces, budget accounting, score publication and reload. Count
  this as update1, not an extra throwaway training arm.
- Fresh candidate reload closes the save/load seam; match adapter bytes and
  both fixed-state final scores before interpreting natural outcomes.
- Caller-facing tests target one-token masking, gradient accumulation across
  the two images, joint-margin stop, occupied outputs, checkpoint completeness,
  deterministic guard selection, token/EOS/cap identity and gained/lost accounting.

# Artifacts and stop boundary

Root:
`/data/CoordExp/outputs/research/qwen3-vl-dense-enumeration/2026-09-10-native-entrance-ce-feasibility`.
Trainer owns `training/` and the new entrance_ce.py/test module. Evaluator owns
`evaluation/` and entrance_ce_eval.py/test module. Root owns acceptance and
records. Existing accepted probe artifacts remain immutable.

Finish after the chosen checkpoint, all18 cold outcomes and proportionate fresh
verification, including negative/mixed results. No larger dataset, extra dose,
second objective, QP or new experiment follows merely because budget remains.

# Completed outcome

The first registered joint margin threshold fires at update23 (both targets
already become actual top1 at update21). The independently cold-loaded adapter
reproduces both final score vectors exactly, and all18 natural outputs finish.
Both training target owners appear at IoU50, but training FP/repetition grows
and the16-image guard regresses. This is mechanically verified local learning
and natural target realization without joint quality improvement; the
checkpoint is not promoted. See[results](results.md). No extra dose, selection,
new loss or model intervention was run after this negative joint outcome.
