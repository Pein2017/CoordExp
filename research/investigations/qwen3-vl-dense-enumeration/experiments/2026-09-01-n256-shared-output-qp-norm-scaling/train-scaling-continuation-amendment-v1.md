# Train-only norm-scaling continuation amendment v1

## Phase reset and authorization

The original target-blind transfer ladder remains terminal at N2. After the
separate positive N4 train-only result and mechanism decomposition, the user
authorized continuous autonomous exploration on 2026-09-02: continue until
the worthwhile questions converge, with documentation updated only at final
closure. The user also retained the earlier ruling that scientific evidence,
not a scaling-explosion resource ceiling, owns the stop.

This amendment opens only the train-only scaling question. It does not reopen
screen-dev, natural decode, transfer, architecture, or population claims.

## Active contract

From the sealed step-2444 N256 semantic capture, does the semantically correct
assignment continue to require lower exact shared output-residual cost than
every one of four matched complete-route derangements as the nested train
prefix doubles beyond N4?

- Anchor: fixed 1,136 grammar rows, margin `0.01`, FP64 separator tolerance
  `1e-5`, FP32 replay threshold `0.00998`, the sealed nested train plan, and
  the already sealed N256 semantic capture.
- Stages: `N in {8, 16, 32, 64, 128, 256}`. N4 is the immutable parent point.
- Contrast at each stage: one semantic exact minimum-Frobenius QP versus
  `requested_K=4`, `unique_K=4` stagewise matched, complete-route,
  fixed-point-free derangements planned before any stage solve.
- Decision-owning metrics: raw Frobenius norm and
  `R^2 / standardized_deficit_energy`. A stage passes only when semantic is
  below every null on both metrics after all five exact certificates pass.
- Difficulty ratio: `q_N = semantic normalized cost / minimum null normalized
  cost`; lower is stronger semantic compression.
- Strongest alternative: the N4 gap is a four-image-specific coordinate and
  category readout geometry that weakens as new images enter.

## Diagnostics

For every completed stage, report images, owners, positions, registered and
active constraints, raw and standardized deficit energy, norm, effective
rank, rank-95, and exact certificate bounds. Replay the preceding semantic
payload on the newly added semantic positions and report how many already
satisfy the frozen margin. These carry-forward and spectral readouts explain
the scaling result but do not override the semantic-versus-null gate.

Decoded row order is irrelevant here and remains arbitrary-valid rather than
sorted. No decoding occurs in this continuation.

## Adaptive progression and stop rule

1. Execute N8 once.
2. Stop scientifically negative at the first exact stage that fails either
   semantic-below-all-null comparison.
3. Otherwise continue doubling until at least N16.
4. Stop `POSITIVE_CONVERGED` once every executed stage from N4 onward passes
   and the two latest stages both have `q_N <= 0.5`. Persistence across N4,
   N8, and N16 then answers the bounded scaling question without spending on
   a larger point that cannot change that conclusion.
5. If the advantage remains positive but the convergence rule is not met,
   continue to the next registered doubling. N256 is terminal regardless of
   outcome.

Each stage has one null plan, one null capture process with one model load and
`4*N` teacher-forced forwards, zero natural decodes, and one five-arm solve
set. A reproducible implementation or numerical fault makes that attempt
technical-invalid; repair the root cause and restart from its immutable parent
without changing the estimand or thresholds. No result-driven null, seed,
stage, margin, or retry selection is allowed.

Resources are observed and reported, not used as a scientific ceiling. An
actual unrecoverable exact-solver or runtime boundary closes the affected stage
as `MECHANICAL_OR_RESOURCE_BOUND`, never as absence of structure.

## Claim boundary

A positive closeout establishes only bounded train-prefix semantic compression
relative to matched route derangements on this checkpoint, fixed row surface,
and nested cohort. It is not held-out transfer, ordinary model learning,
population generalization, a low-rank architecture result, or production
readiness.
