# N4 train-only low-norm amendment v1

## Phase reset and authorization

The original full ladder stopped at N2 because strict target-blind transfer
failed. On 2026-09-02 the user separately authorized: `好的,请进行 N4,
回答一下是否存在低范数结构!`

This amendment does not reopen the failed transfer ladder. It authorizes one
train-only N4 discriminator and no screen-dev access, N8, architecture change,
or transfer claim.

## Active contract

From the sealed N256 semantic capture, does the exact N4 semantic output-head
QP require both lower raw minimum Frobenius norm and lower
`R^2 / standardized_deficit_energy` than every one of four unique matched
complete-route derangements?

- Anchor: the frozen step-2444 Source, fixed 1,136 output rows, margin `0.01`,
  certificate tolerance `2e-5`, first four images of the sealed nested train
  plan, and the already sealed N256 semantic capture.
- Contrast: one semantic N4 solve versus `requested_K=4`, `unique_K=4`
  train-only derangement solves generated by the frozen planner.
- Primary positive gate: all five exact solves certify, and semantic is below
  every null on both Frobenius norm and
  `norm_squared_per_standardized_deficit_energy`.
- Strongest alternative: the N2 gap was a two-image coincidence or only
  reflected easier semantic Source deficits rather than shared low-norm
  structure.
- Claim boundary: a pass is N4 train-prefix evidence for shared low-norm
  programmability on the frozen output-row surface. It is not target-blind
  transfer, N256 scaling, population generalization, or architecture
  promotion.

## Execution and stop rule

Reuse the immutable N256 semantic capture without recapture. Plan all four
nulls before any N4 solve. Use one fresh GPU process and one model load for the
16 null teacher-forced forwards, zero natural decodes, then exact CPU solves.
Screen inputs and GT are neither bound nor opened. Resources are observed with
no scientific ceiling; actual unrecoverable runtime failure remains technical
invalidity rather than a scientific negative.

Stop after the terminal N4 aggregate regardless of outcome. Do not construct
or launch N8 under this amendment.
